- # 嵌入式软件学习




## 一、STM32芯片学习



### 0.工程创建



#### 1.官方文件夹目录介绍

##### 1.1总目录

![image-20240706200605792](./嵌入式软件学习.assets/image-20240706200605792.png)

##### 1.2启动文件

1. **相对地址**：./STM32F10x_StdPeriph_Lib_V3.5.0\Libraries\CMSIS\CM3\DeviceSupport\ST\STM32F10x\startup\arm
2. **文件作用**：**STM32程序是从启动文件开始执行的**![image-20240706203840857](./嵌入式软件学习.assets/image-20240706203840857.png)

![image-20240707154248250](./嵌入式软件学习.assets/image-20240707154248250.png)主要作用

当*使用外部RAM时，需要修改RESET中断，在进入__main前调用FSMC外设，激活外部SRAM，*

```c
; Reset handler//注释
Reset_Handler 	PROC //Reset_Handler程序开始
				EXPORT Reset_Handler[WEAK]
				IMPORT SystemInit//导入  SystemInit函数
				IMPORT __main
// 从外部文件引入声明, 格式：IMPORT 要调用的初始化函数名
// 以下语句仅作演示，使用外部存储器时请去掉注释用的“; ”号，本工程使用内部 SRAM，无需初始化
				; IMPORT FSMC_SRAM_Init//使用外部RAM的时候，需要初始化FSMC这个外设
				 LDR R0, =SystemInit//将 SystemInit函数地址赋值到R0 执行R0 也就是说执行 SystemInit函数，初始化时钟等
				 BLX R0

				; 在__main 之前调用 FSMC_SRAM_Init 进行初始化
//以下语句仅作演示，使用外部存储器时请去掉注释用的“; ”号，本工程使用内部 SRAM，无需初始化
				; LDR R0, =FSMC_SRAM_Init//使用外部RAM的时候，需要初始化FSMC这个外设
				; BLX R0
		
		 		LDR R0, =__main
				 BX R0
		 		ENDP

```



**具体需要根据芯片类型选择启动文件**：

![image-20240706224826161](./嵌入式软件学习.assets/image-20240706224826161.png)









##### 1.3外设寄存器描述文件 和 系统文件

![image-20240706204849029](./嵌入式软件学习.assets/image-20240706204849029.png)

1. ![image-20240706204914794](./嵌入式软件学习.assets/image-20240706204914794.png)外设寄存器描述文件，与51单片机头文件一致，描述STM32与其对应的地址![image-20240706205407718](./嵌入式软件学习.assets/image-20240706205407718.png)
2. ![image-20240706205420675](./嵌入式软件学习.assets/image-20240706205420675.png)配置芯片主频



##### 1.4内核寄存器描述文件

- 相对地址：./STM32F10x_StdPeriph_Lib_V3.5.0\Libraries\CMSIS\CM3\CoreSupport

![image-20240706205839987](./嵌入式软件学习.assets/image-20240706205839987.png)





##### 1.5外设库函数文件

- 相对地址：
  1. 头文件：STM32F10x_StdPeriph_Lib_V3.5.0\Libraries\STM32F10x_StdPeriph_Driver\inc
  2. 源文件：STM32F10x_StdPeriph_Lib_V3.5.0\Libraries\STM32F10x_StdPeriph_Driver\src

<img src="./嵌入式软件学习.assets/image-20240706221009349.png" alt="image-20240706221009349" style="zoom: 80%;" />



##### 1.6库文件包含文件 与 中断文件

相对地址：STM32F10x_StdPeriph_Lib_V3.5.0\Project\STM32F10x_StdPeriph_Template

- conf.h：用于配置**库函数头文件的包含关系** + **用来检查函数参数定义**，所有库函数都需要

- it.h：用来存放中断函数

  



![image-20240706221657094](./嵌入式软件学习.assets/image-20240706221657094.png)











------





#### 2.建立工程



新建工程步骤总结：

![image-20240706225032366](./嵌入式软件学习.assets/image-20240706225032366.png)

<img src="./嵌入式软件学习.assets/image-20240706225228760.png" alt="image-20240706225228760" style="zoom:50%;" />表示使用库函数编译

-  还需要在工程里定义  **STM32F10X_MD **芯片类型。该宏定义关系中断号等由于芯片不同产生的不同宏编译的区别

![image-20250330112516716](./嵌入式软件学习.assets/image-20250330112516716.png)

[STM32标准外设库中USE_STDPERIPH_DRIVER, STM32F10X_MD的含义 - 果壳中的宇宙 - 博客园](https://www.cnblogs.com/TrueElement/archive/2012/09/06/2673208.html)





##### 1.创建新工程

![image-20240706203444253](./嵌入式软件学习.assets/image-20240706203444253.png)

![image-20240706202913042](./嵌入式软件学习.assets/image-20240706202913042.png)

![image-20240706203058872](./嵌入式软件学习.assets/image-20240706203058872.png)

![image-20240706203239065](./嵌入式软件学习.assets/image-20240706203239065.png)

##### 2.移植固件库



![image-20240706204103462](./嵌入式软件学习.assets/image-20240706204103462.png)创建工程后，会自动的创建项目目录和文件。



###### 2.1启动文件移植

![image-20240706204323043](./嵌入式软件学习.assets/image-20240706204323043.png)

###### 2.2外设描述文件与系统文件移植

![image-20240706205607520](./嵌入式软件学习.assets/image-20240706205607520.png)



###### 2.3内核描述文件移植

![image-20240706210027890](./嵌入式软件学习.assets/image-20240706210027890.png)

###### 2.4将上述文件添加到工程



1. ![image-20240706210951005](./嵌入式软件学习.assets/image-20240706210951005.png)修改名称技巧：单击Group，光标离开2S，再次单击Group。

2. ![image-20240706211230168](./嵌入式软件学习.assets/image-20240706211230168.png)添加已存在文件到组内

3. ![image-20240706211425519](./嵌入式软件学习.assets/image-20240706211425519.png)

4. 因为启动文件只需要选择一个，![image-20240706211526552](./嵌入式软件学习.assets/image-20240706211526552.png)，按下Add

5. add ![image-20240706211726219](./嵌入式软件学习.assets/image-20240706211726219.png)

6. add![image-20240706211738409](./嵌入式软件学习.assets/image-20240706211738409.png)

7. 最终添加这几个文件![image-20240706211845086](./嵌入式软件学习.assets/image-20240706211845086.png)

8. 添加头文件路径<img src="./嵌入式软件学习.assets/image-20240706212032060.png" alt="image-20240706212032060" style="zoom:50%;" />![image-20240706212055412](./嵌入式软件学习.assets/image-20240706212055412.png)

   

##### 3.移植用户库

###### 3.1创建用户文件夹

- ![image-20240706212314862](./嵌入式软件学习.assets/image-20240706212314862.png)工程文件夹中，**创建新文件夹User**。



- ![image-20240706212441979](./嵌入式软件学习.assets/image-20240706212441979.png)![image-20240706212508421](./嵌入式软件学习.assets/image-20240706212508421.png)**创建新组**



- ![image-20240706212623925](./嵌入式软件学习.assets/image-20240706212623925.png)<img src="./嵌入式软件学习.assets/image-20240706212813415.png" alt="image-20240706212813415" style="zoom:50%;" />![image-20240706212836765](./嵌入式软件学习.assets/image-20240706212836765.png)           **创建main文件**

  

- 验证工程是否正确，再main文件中编写![image-20240706213219734](./嵌入式软件学习.assets/image-20240706213219734.png)并编译无问题，则工程无问题。

- <img src="./嵌入式软件学习.assets/image-20240706222333294.png" alt="image-20240706222333294" style="zoom:50%;" />移植库函数包含文件、中断文件

- ![image-20240706222458096](./嵌入式软件学习.assets/image-20240706222458096.png)并将文件添加到组内









**使用库函数-宏定义：**

- 在stm32f10x.h中![image-20240706222806789](./嵌入式软件学习.assets/image-20240706222806789.png)有一个宏定义，只有其有效时才会编译conf.h文件。需要在Keil中对其进行宏定义。![image-20240706223011769](./嵌入式软件学习.assets/image-20240706223011769.png)，这样才会包含外设库函数。**此宏定义也是是否使用库函数定义。**

- 添加头文件目录

  



##### 4.移植官方库函数

- ![image-20240706220617082](./嵌入式软件学习.assets/image-20240706220617082.png)创建新文件夹，Library
- 移植外设库函数源文件<img src="./嵌入式软件学习.assets/image-20240706221131165.png" alt="image-20240706221131165" style="zoom:50%;" />
- 移植外设库函数头文件<img src="./嵌入式软件学习.assets/image-20240706221224216.png" alt="image-20240706221224216" style="zoom: 50%;" />
- ![image-20240706221351382](./嵌入式软件学习.assets/image-20240706221351382.png)创建Library组
- ![image-20240706221434208](./嵌入式软件学习.assets/image-20240706221434208.png)将源文件添加到组内





- 添加头文件目录![image-20240706223306600](./嵌入式软件学习.assets/image-20240706223306600.png)





##### 5.调节组的位置和属性





###### 5.1移动组位置

因为库函数和其他配置文件都不需要修改，可以将User文件组单独挪到最下面，方便修改

![image-20240706223601015](./嵌入式软件学习.assets/image-20240706223601015.png)

###### 5.2修改文件属性

启动文件和外设库文件在开发过程中不需要修改，可以锁住文件。

方法：点击文件属性，修改为只读。在Keil中显示有金钥匙![image-20240706224231685](./嵌入式软件学习.assets/image-20240706224231685.png)

![image-20240706224034539](./嵌入式软件学习.assets/image-20240706224034539.png)



#### 3.工程架构

![image-20240706225511100](./嵌入式软件学习.assets/image-20240706225511100.png)

##### 5.1启动文件 - starup_xx

![image-20240706231040063](./嵌入式软件学习.assets/image-20240706231040063.png)

1. 在启动文件中，定义了**复位中断**，在**上电复位**或者**按下了复位键**，执行复位中断。
2. 复位中断只有两个事：<img src="./嵌入式软件学习.assets/image-20240706230009940.png" alt="image-20240706230009940" style="zoom:50%;" />
   - 调用Systeminit函数
   - 调用main

3. 启动文件还定义了其他中断，在满足条件时调用。中断函数的定义是在stm32f10x.h/.c

   



关于上述复位中断函数解析：

![image-20240707154621356](./嵌入式软件学习.assets/image-20240707154621356.png)

![image-20240707154632051](./嵌入式软件学习.assets/image-20240707154632051.png)





##### 5.2系统配置文件 system_xx.h

其中有个函数**Systeminit（）**，也就是复位中断调用的函数。作用是：**设置微控制器启动，初始化嵌入式闪存接口、锁相环、更新系统内核时钟变量**



##### 5.3库函数

<img src="./嵌入式软件学习.assets/image-20240706231105476.png" alt="image-20240706231105476" style="zoom:50%;" />

- ![image-20240706231133982](./嵌入式软件学习.assets/image-20240706231133982.png)此函数是对外设寄存器的描述，也就是宏定义。*且再其中通过宏定义*

  ```c
  #ifdef USE_STDPERIPH_DRIVER
    #include "stm32f10x_conf.h"
  #endif
  
  //1.使用此宏定义，则使用库函数进行编写程序
  //2.不使用此宏定义，则使用寄存器的方式进行编写程序
  ```

  *包含头文件stm32f10x_conf.h(由因为此文件包含了所有外设库函数，所以同时包含所有库函数)*，**在程序编写中，只需调用stm32f10x.h即可包含所有外设库函数**
- ![image-20240706231643976](./嵌入式软件学习.assets/image-20240706231643976.png)头文件包含，调用此文件即可调用所有外设库函数。







#### 4.快速创建工程

1. 创建工程文件夹，并在keil中创建新工程，保存在文件夹中
2. 在工程文件夹中创建User、Start、Libarary文件夹<img src="./嵌入式软件学习.assets/image-20240707101917108.png" alt="image-20240707101917108" style="zoom:50%;" />
3. 移植官方固件库
   - Start：启动文件、内核描述文件、外设寄存器描述文件、系统配置文件![image-20240707102156673](./嵌入式软件学习.assets/image-20240707102156673.png)
   - Libarary:库函数所有的头文件、源文件
   - User：main文件、中断文件、config.h



4. 添加组、调整组位置与属性、添加头文件地址、添加全局宏

   - 添加组<img src="./嵌入式软件学习.assets/image-20240707103716635.png" alt="image-20240707103716635" style="zoom: 67%;" />

   - 组内添加文件、并修改文件属性![image-20240707102940056](./嵌入式软件学习.assets/image-20240707102940056.png)

   - 添加头文件路径

   - 添加全局宏<img src="./嵌入式软件学习.assets/image-20240707103134865.png" alt="image-20240707103134865" style="zoom: 80%;" />、 **STM32F10X_MD**

   - 修改OBJ位置与生成hex文件

     ![image-20241020105847359](./嵌入式软件学习.assets/image-20241020105847359.png)

   

5. 编写程序
   - main添加头文件<img src="./嵌入式软件学习.assets/image-20240707103837618.png" alt="image-20240707103837618" style="zoom:80%;" />
   - 编写程序，VScode打开工程时无法有效跳转库函数，可以定义一下库函数宏	

------



### 1.GPIO外设

GPIO-即通用输入输出口

![image-20240706121010577](./嵌入式软件学习.assets/image-20240706121010577.png)GPIO电路图





#### 1.1输入电路

![image-20240706124620497](./嵌入式软件学习.assets/image-20240706124620497.png)

##### 1.1.1限幅电路

IO输入电压在 VSS-VDD（0-3.3V），当大于VDD时上方二级管道通，当小于VSS（负电压）下方二极管道通。

![image-20240706121512367](./嵌入式软件学习.assets/image-20240706121512367.png)

##### 1.1.2上下拉电阻

输入的默认电平，在无外部输入时，此IO端口的输入状态

- 上拉：上方开关导通
- 下拉：下方开关道通
- 悬空：上下开关都关闭，**此状态非常容易受外部干扰，一般是外部电路有上下拉电阻**

![image-20240706121844190](./嵌入式软件学习.assets/image-20240706121844190.png)

##### 1.1.3施密特触发器

![image-20240706124452774](./嵌入式软件学习.assets/image-20240706124452774.png)

主要是用于进行滤波，**上下两个滤波阈值，大于上限为高电平，小于下限位低电平**；在两个阈值之间为滤波范围，只有**没有高于上限或者低于下限都不会改变状态**。

![image-20240706124101263](./嵌入式软件学习.assets/image-20240706124101263.png)







#### 1.2输出电路



##### 1.2.1输出来源

- IO控制：如图由IO寄存器控制IO输出：
  1. 输出数据寄存器：控制16个IO整体状态，程序上需要使用 |、&的方式操作某一位。
  2. 位设置寄存器、位清除寄存器：两个寄存器，单独对某一位进行置位和清除。
- 外设控制：例如IIC、SPI等外设控制IO端口



![image-20240706125024441](./嵌入式软件学习.assets/image-20240706125024441.png)



##### 1.2.2MOS管---输出模式

- 推挽输出：数据寄存器为1时，上管道通管断开，输出高电平。反之输出低电平。**这种模式有较强的驱动能力。**



- 开漏输出：**数据寄存器为1时，上下管都断开，为高阻态**；输出寄存器为0时，下管导通上管断开，输出低电平。**这种模式，高电平没有驱动能力，有外部电路决定状态，低电平有驱动能力**。

  

  ***开漏输出作用：***

  ***1.IIC协议多级通讯中，在高阻态不干扰其他设备通信*** ，“线与特性”：若有很多个开漏模式引脚连接到一起时，只有当所有引脚都输出高阻态，才由 上拉电阻提供高电平，此高电平的电压为外部上拉电阻所接的电源的电压。若其中一个引 脚为低电平，那线路就相当于短路接地，使得整条线路都为低电平，0伏。 

   ***2.兼容5V设备，外部电路接入上拉5V，当输出高阻态时，外部电路输出5V***



​				

- 关闭输出：当IO配置为输出模式时，MOS管都关闭，IO端口信号由外部决定

  



![image-20240706125545644](./嵌入式软件学习.assets/image-20240706125545644.png)





#### 1.3GPIO模式

![image-20240706130918788](./嵌入式软件学习.assets/image-20240706130918788.png)

- 浮空、上拉、下拉：区别是，外部空闲时的默认电平。
- 模拟输入：没有经过上下拉电阻、没有经过施密特触发器的滤波
- 开漏、推挽：推挽高低电平都有驱动能力；开漏输出高电平没有输出能力，输出高阻态
- **IO输入IO输出：**
  1. **当为IO输入时，关闭IO输出。防止输出干扰输入**
  2. **当为IO输出时，IO输入同时有效，输出的同时也可以读出当前输出的电平。**
  3. **IO数值输入，只有在模拟输入时关闭**
- 复用输出：输出使能，输出速度可配置，可工作在开漏及推挽模式，但是**输出 信号源于其它外设**。



##### 外设GPIO模式选择

当使用其他外设时，不清楚使用GPIO的何种外设，可以查询**参考手册-GPIO部分**

![image-20240727105023414](./嵌入式软件学习.assets/image-20240727105023414.png)





#### 1.4库函数结构体配置



1. GPIO输出速度配置：可以限制输出引脚的最大翻转速度，**为了低功耗和稳定性，一般直接选择最大50MHZ**就行。

2. GPIO模式配置：

   ![image-20240707153448021](./嵌入式软件学习.assets/image-20240707153448021.png)

   

```c

   /*开启LED相关的GPIO外设时钟*/ 
  RCC_APB2PeriphClockCmd( LED1_GPIO_CLK| 
                          LED2_GPIO_CLK| 
                          LED3_GPIO_CLK, ENABLE);  //时钟外设可以 使用“|”，同时使能多个时钟，因为宏是每个BIT位下入寄存器

/*初始化GPIO端口*/
 GPIO_InitTypeDef GPIO_InitStructure; 
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0 |  GPIO_Pin_1 | GPIO_Pin_2;  //可以使用“|”，一次初始化多个端口
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;  //推挽输出
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;  //输出50MHZ
 GPIO_Init(LED1_GPIO_PORT, &GPIO_InitStructure);  //初始化端口
  
/*控制GPIO端口输出*/


GPIO_SetBits(GPIOA, GPIO_Pin_0);  //PA_0 端口拉高 数据设置寄存器
GPIO_ResetBits(GPIOA, GPIO_Pin_0);  //PA_0 端口拉低 数据清除寄存器

		//指定端口拉高拉低
GPIO_WriteBit(GPIOA,GPIO_Pin_0,Bit_RESET) ;
GPIO_WriteBit(GPIOA,GPIO_Pin_0,(BitAction)1) ; //强制转换枚举类型  1/0可以是布尔变量

        //端口组写入
GPIO_Write(GPIOA,0x0001) ; //PA_0拉高，PA_1 - PA_15都为低





```



![image-20240710213241044](./嵌入式软件学习.assets/image-20240710213241044.png)

读取IO输入值：

- GPIO_ReadInputDataBit()-读取某一位的GPIO输入值（**输入**数据寄存器值）
- GPIO_ReadInputData()-读取某一组的GPIO值（**输入**数据寄存器值），16位，例如读取GPIOA的所有端口数据



读取IO输出值：

- GPIO_ReadOutputDataBit()-读取某一位的GPIO输出值（**输出**数据寄存器值），**在输出电平同时可以读取当前GPIO电平**
- GPIO_ReadInputData()-读取某一组的GPIO值（**输出**数据寄存器值）

可以获取某个按键值，并做出反应





#### 1.5GPIO驱动电路



##### 1.5.1LED驱动

推荐使用上面这种电路，因为：大部分单片机采用 **高电平弱驱动，低电平强驱动**

![image-20240706132625203](./嵌入式软件学习.assets/image-20240706132625203.png)



###### 1.5.2驱动大功率

需要使用大功率器件

![image-20240706132930655](./嵌入式软件学习.assets/image-20240706132930655.png)





### 2.外部中断

#### 2.1中断概念

![image-20240718215926469](./嵌入式软件学习.assets/image-20240718215926469.png)

#### 2.3中断执行流程

注意中断嵌套，先执行嵌套中断在执行终端，最后再执行主程序



<img src="./嵌入式软件学习.assets/image-20240718220248858.png" alt="image-20240718220248858" style="zoom:80%;" />



#### 2.4STM32中断



##### NVIC

使用**NVIC**统一管理中断，每个中断通道都有16个可编程优先等级，可对优先等级进行分组，进一步分为**抢占优先级 与 响应优先级**，中断NVIC用来仲裁中断优先等级。

<img src="./嵌入式软件学习.assets/image-20240718221228752.png" alt="image-20240718221228752" style="zoom:80%;" />

###### 抢占优先级和响应优先级

- **响应优先级是用来排队的**， **当两个优先级的抢占优先级一样时**，响应优先级高的，优先被执行
- **抢占优先级是用来中断嵌套的**，当别的中断在运行时，高强占优先级中断可以打断低中断优先级
- **两个优先级一致，按中断号（中断向量表中的号码）排队**，数值小的优先响应
- 固定4位来设定优先级，不同的组有不同的取值范围
- **先比较抢占优先级 ，再比较响应优先级 ，再比较中断号**

![image-20240718221746605](./嵌入式软件学习.assets/image-20240718221746605.png)









##### 中断向量表

一般是在FLASH起始地址，可以配置偏移寄存器将中断向量表进行偏移。，由于中断触发是硬件触发，由硬件在中断向量表中寻找中断函数地址，实现跳转。



#### 2.5外部中断-EXTI



##### 中断概述

- 可以检测：**上升沿、下降沿、双边沿、软件触发**（不需要硬件触发，只需要软件实现语句）
- 支持16个GPIO，但是**不能为同一通道**（例如 PA0/PB0/PC0为同一通道），例如：**不能同时使用PA1 PB1**
- 可以**触发中断**（由NVIC仲裁，执行中断服务函数）、**事件响应**（不经过CPU，触发别的外设动作，属于外设之间的配合）

![image-20240720214726088](./嵌入式软件学习.assets/image-20240720214726088.png)



##### 中断时序过程

![image-20240720220349323](./嵌入式软件学习.assets/image-20240720220349323.png)

###### AFIO

- AFIO主要用于引脚复用功能的选择和重定义
- 在STM32中，AFIO主要是完成两个任务：
  1. **复用功能引脚重定义**：<img src="./嵌入式软件学习.assets/image-20240720221146339.png" alt="image-20240720221146339" style="zoom:50%;" />，外设功能引脚复用
  2. **中断引脚选择**：![image-20240720220808392](./嵌入式软件学习.assets/image-20240720220808392.png)，中断触发引脚选择。由于EXTI只有20个中断线，由AFIO对中断进行选择。



##### EXIO外设框架图



- 可以检测：**上升沿、下降沿、双边沿、软件触发**（不需要硬件触发，只需要软件实现语句）

- 中断、事件可进行屏蔽

  

![image-20240720223127469](./嵌入式软件学习.assets/image-20240720223127469.png)





#### 2.6外部中断程序

##### 初始化配置

- GPIO

- AFIO

  1. ![image-20240727105409518](./嵌入式软件学习.assets/image-20240727105409518.png)清除AFIO外设配置

     ![image-20240727105654712](./嵌入式软件学习.assets/image-20240727105654712.png)

- EXIT

  ```c
  void EXTI_DeInit(void); //清除EXTI的外设寄存器
  void EXTI_Init(EXTI_InitTypeDef* EXTI_InitStruct); //根据外设初始化结构体，进行外设初始化
  void EXTI_StructInit(EXTI_InitTypeDef* EXTI_InitStruct); //对外设初始化结构体进行复位，当需要配置多个EXTI复位上一次配置结构体
  
  
  
  void EXTI_GenerateSWInterrupt(uint32_t EXTI_Line); //软件触发一次中断 ： 选定的中断线触发中断
  
  //建议main函数中使用 查询与清除中断有效标记位
  FlagStatus EXTI_GetFlagStatus(uint32_t EXTI_Line);
  void EXTI_ClearFlag(uint32_t EXTI_Line);
  
  //建议中断程序中使用 查询与清除中断有效标记位
  ITStatus EXTI_GetITStatus(uint32_t EXTI_Line);
  void EXTI_ClearITPendingBit(uint32_t EXTI_Line);
  ```

  

- NVIC

  ```c
  //配置中断分组：1.设置几个响应，几个抢断; 2.整个工程只能选择一种组，只需要配置一次
  void NVIC_PriorityGroupConfig(uint32_t NVIC_PriorityGroup); 
  
  void NVIC_Init(NVIC_InitTypeDef* NVIC_InitStruct); //初始化中断NVIC
  ```

  

<img src="./嵌入式软件学习.assets/image-20240727092318537.png" alt="image-20240727092318537" style="zoom: 33%;" />

实现外部中断代码

```c
#include "stm32f10x.h"                  // Device header
 
void INIT_function(void) ;

int main()
{
	INIT_function() ;

	while (1)
	{
		;
	}
}


void INIT_function()
{	
	//--时钟开启RCC
	//开启GPIO 、AFIO时钟，
	//EXIT不需要开启时钟、NVIC属于内核外设不需要开启时钟（RCC只管内核外的外设）
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE) ;
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE) ;

	//初始化GPIO
	GPIO_InitTypeDef GPIO_initstruct ;
	GPIO_initstruct.GPIO_Mode = GPIO_Mode_IPU ;
	GPIO_initstruct.GPIO_Pin = GPIO_Pin_12  ;
	GPIO_initstruct.GPIO_Speed = GPIO_Speed_50MHz ;
	GPIO_Init(GPIOB ,&GPIO_initstruct) ;


	//初始化AFIO-中断选择器 ,选择那个EXIT通道由那个GPIO接入
	GPIO_EXTILineConfig( GPIO_PortSourceGPIOB,GPIO_PinSource12) ; //选择12号线中断为GPIOB ，  PB12选择为中断输入


	//初始化EXIT外设
	EXTI_InitTypeDef  EXTI_InitStruct ;

	EXTI_InitStruct.EXTI_Line =  EXTI_Line12 ; // 选择第12个线路
	EXTI_InitStruct.EXTI_Mode = EXTI_Mode_Interrupt; //选择中断触发  ： 有事件触发和中断触发
	EXTI_InitStruct.EXTI_Trigger = EXTI_Trigger_Falling; //选择边沿触发，这里选择下降沿触发 ；   有上升沿、下降沿、上下都触发
	EXTI_InitStruct.EXTI_LineCmd  =ENABLE ; //中断使能：开启、关闭
	EXTI_Init(&EXTI_InitStruct);


	//初始化NVIC


	NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2); //中断分组，整个工程只需要配置一次；这里选择组2,4个抢断，4个响应

    
    
	NVIC_InitTypeDef NVIC_InitStruct ;
	NVIC_InitStruct.NVIC_IRQChannel = EXTI15_10_IRQn ; //设置EXIT中断号  这里是EXTI12 选择此IRQ
	NVIC_InitStruct.NVIC_IRQChannelCmd = ENABLE; //使能中断
	NVIC_InitStruct.NVIC_IRQChannelPreemptionPriority = 1; //抢占优先级 1  根据分组来，这里分组2：取值0-3
	NVIC_InitStruct.NVIC_IRQChannelSubPriority = 1  ; //响应优先级 1  根据分组来，这里分组2：取值0-3

 	NVIC_Init(&NVIC_InitStruct);
                      


}



//中断函数名都是固定的，在启动文件的 中断向量表中寻找
void EXTI15_10_IRQHandler(void)
{

	if (EXTI_GetITStatus(EXTI_Line12)==SET) //因为此中断EXTI 10-15都可以进入此中断，需要判断一下是否是外部中断-12
	{



		EXTI_ClearITPendingBit(EXTI_Line12) ; //由于中断无法硬件复位，必须软件进行复位；        在外部中断触发后，中断标记位有效进入中断，如果未进行复位，将会一直进入中断卡死
	}
	

}
```

- EXIT不需要开启时钟、NVIC属于内核外设不需要开启时钟（RCC只管内核外的外设）

- 中断分组，整个工程只需要配置一次

  - 中断配置IQR：有通用的中断，以及每种芯片独立的中断<img src="./嵌入式软件学习.assets/image-20240727123506128.png" alt="image-20240727123506128" style="zoom: 80%;" /><img src="./嵌入式软件学习.assets/image-20240727114815293.png" alt="image-20240727114815293" style="zoom:50%;" />这里F103C8使用此

- 中断函数名是固定的，在启动文件中的**中断向量表**中已进行定义![image-20240727120219158](./嵌入式软件学习.assets/image-20240727120219158.png)

- 由于**中断无法硬件复位，必须软件进行复位；在外部中断触发后，中断标记位有效进入中断，如果未进行复位，将会一直进入中断卡死**

  



### 3.TIM定时器



#### 3.0系统滴答定时器



- **嵌套在NVIC中的，24位递减定时器**，所有cotex-M内核芯片都具有该定时器。

<img src="./嵌入式软件学习.assets/image-20250320195642151.png" alt="image-20250320195642151" style="zoom:80%;" />

![image-20250320195825573](./嵌入式软件学习.assets/image-20250320195825573.png)







- COUNTFLAG：当计数器递减到0，该位置1。软件读取后自动清零
- CLKSOURCE：选择滴答定时器时钟，0-AHB/8  1-AHB
- TICKINT：中断使能位，1-计数为0时，产生中断。  0-不开启中断
- ENABLE： 开启定时器使能

![image-20250320200022472](./嵌入式软件学习.assets/image-20250320200022472.png)



##### 1.通过滴答定时器---实现0.5ms/2ms函数

- CTRL-控制寄存器
- LOAD-重装载寄存器 ， 由于重装载值需要数到零才能置位标记。所以需要在填充值时 - 1 <img src="./嵌入式软件学习.assets/image-20250323154512500.png" alt="image-20250323154512500" style="zoom:50%;" />
- VAL - 获取当前计数器值
- CALIB - 校验补偿值 - 一般不使用

![image-20250320201941013](./嵌入式软件学习.assets/image-20250320201941013.png)



寄存器初始化：

- **调用physys_TickInit( 0xFF FFFF) ;** 将24位滴答定时器装满，并设置重装载寄存器
- 清空当前值
- 设置当前时钟位为AHB时钟 - 72M
- 开启系统滴答定时器

![image-20250320202328204](./嵌入式软件学习.assets/image-20250320202328204.png)



###### 程序设计



这种非阻塞式程序设计，之前出现过一次质量问题。由于中断执行次数太多，导致累计的时间差过大，差了多个0.5ms。这种程序有时序追赶逻辑，当中断不再执行时，会在一段时间内多次执行0.5m函数，追赶时间差。但是软件内部实现认为以及走过了长时间，之前与主板通许一段时间内为接受到主板消息立即停机。主板200ms发送一次，但是由于程序追赶逻辑中，100ms内时间累计已经达到200ms还是未接受到主板消息，直接停机。



- 将24位数值左移8位后，变成32位数值。这样只有前24位在进行数值变化
- **因为定时器减到零后，会重新加载到最大值。而无符号变量通样如此，下溢后也会回到最大值。**巧妙利用溢出来实现精准定时
- 这种好处是：当软件内部产生中断时，时间大于500ms但是只要多次中断导致误差累计到500ms会进行一次时间追赶。这样使得最大误差不超过500ms。

```c
#define GET_TIME()   ((SysTick->VAL) << 8)  //获取当前系统滴答定时器值 ， 并左移8位

#define TIME_1US    (72ul << 8 )        //系统AHB-72M ，计数 72次代表1us
#define TIME_500US  (TIME_1US * 500)
#define TIME_2MS    (TIME_1US * 2000)

unsigned long ul_baseTime = 0 ; 
unsigned long ul_waitTime = 0 ; 
unsigned long flag  = 0 ; 

ul_baseTime = GET_TIME() ; //以当前定时器时钟为基准时间

while(1)
{
    ul_waitTime = ul_baseTime - GET_TIME();  //由于滴答定时器是递减定时器
        
    if(ul_waitTime > TIME_500US) //基准时间 和当前时间差值为0.5ms
    {
        ul_baseTime -= TIME_500US ; //基准时间减少 0.5ms
     
        //执行0.5ms函数
        flag ++ ;
        
    }
    
    if(flag&0X01)  //执行2ms函数1
        if(flag&0X02)  //执行2ms函数2
            if(flag&0X03)  //执行2ms函数3
                if(flag&0X04)  //执行2ms函数4
    
    
    
}


```





##### 2.使用滴答定时器延时



```c
//滴答定时器初始化函数
void delay_init()
{
	SysTick_CLKSourceConfig(SysTick_CLKSource_HCLK_Div8);	//选择外部时钟  HCLK/8
	fac_us=SystemCoreClock/8000000;				 //计数器需要计数值 ，如分配后为9M  也就是计数器计数9次，为1us 
	fac_ms=(u16)fac_us*1000;					//代表每个ms需要的systick时钟数   
}		



//阻塞式延迟-us
void delay_us(u32 nus)
{		
	u32 temp;	    	 
	SysTick->LOAD=nus*fac_us; 					//时间加载	  		 
	SysTick->VAL=0x00;        					//清空计数器
	SysTick->CTRL|=SysTick_CTRL_ENABLE_Msk ;	//开始倒数	  
	do
	{
		temp=SysTick->CTRL;
	}while((temp&0x01)&&!(temp&(1<<16)));		//开启了定时器，且等待时间到达   
	SysTick->CTRL&=~SysTick_CTRL_ENABLE_Msk;	//关闭计数器
	SysTick->VAL =0X00;      					 //清空计数器	 
}
```







#### 3.1计时器介绍



[STM32——TIMER（定时器）篇_stm32定时器-CSDN博客](https://blog.csdn.net/weixin_52706451/article/details/138422575)



![image-20250402205849519](./嵌入式软件学习.assets/image-20250402205849519.png)



![image-20240727211820095](./嵌入式软件学习.assets/image-20240727211820095.png)

- 定时器本质上就是计数器，当外部信号是准确的时钟时，计数也就成了计时
- 时基单元：16位计数器（计数）、预分频器（设定输入时钟脉冲）、自动重装载寄存器（目标值）
- 功能有：**输入捕获、输出比较、定时中断、内外时钟选择、编码器接口、主从触发模式等**

##### 1.定时器分类

1. 不同定时器时钟、功能上都有差别，时钟源也不同。
2. <img src="./嵌入式软件学习.assets/image-20240727213923046.png" alt="image-20240727213923046" style="zoom:67%;" />没有基本定时器

![image-20240727213216742](./嵌入式软件学习.assets/image-20240727213216742.png)



##### 2.基本定时器框图

- 16位定时器，只能向上计数。为内部资源，没有GPIO输入输出。

![image-20240727214835598](./嵌入式软件学习.assets/image-20240727214835598.png)

- 基本定时器的时钟**只能来源于系统时钟**（72MHZ）。

- 预分频器寄存器写0，不分频，72M；写1，二分屏，36MHZ；（**实际分频系数 = PSC + 1**）

- 计时器只能**自增计数（向上计数）**<img src="./嵌入式软件学习.assets/image-20240804151134875.png" alt="image-20240804151134875" style="zoom:80%;" />，在计数过程中不断与自动重装载寄存器比较，当**计数值与自动重装载寄存器相等**时，计数器清零，触发中断（UI）或者事件（U，触发其他外设电路工作）<img src="./嵌入式软件学习.assets/image-20240804151305692.png" alt="image-20240804151305692" style="zoom:50%;" />

  



##### 3.主模式介绍

![image-20240727215510197](./嵌入式软件学习.assets/image-20240727215510197.png)关于**主模式触发DAC（硬件在不受CPU的控制下自动运行）**功能![image-20240727222926066](./嵌入式软件学习.assets/image-20240727222926066.png)：

1. 当需要定时采集ADC时，例如定时采集电池电压，原来采用定时器触发中断，在定时器中断函数进行AD转换。但是频繁进入中断会导致大量占用其他程序与中断时间。
2. 可以采用主模式，由硬件触发DAC转换<img src="./嵌入式软件学习.assets/image-20240727220040760.png" alt="image-20240727220040760" style="zoom: 80%;" />，产生事件映射到TRGO来触发DAC，过程由硬件进行处理不需要软件参与。



##### 通用定时器框图

![image-20240727221003821](./嵌入式软件学习.assets/image-20240727221003821.png)

- 计数模式：向上计数、向下计数、中央对齐计数![image-20240804152142314](./嵌入式软件学习.assets/image-20240804152142314.png)



###### 时钟源输入

- 2.通过**外部时钟引脚输入时钟**，经过滤波、预分频等电路：**1.直接作为计数器时钟  2.为TRGI输入**![image-20250402194132380](./嵌入式软件学习.assets/image-20250402194132380.png)这一路输入：1.可以使用**外部时钟定时**  2.可以用于**外部信号计数（区别于外部中断频繁触发中断**)

- 3.ITRx:来源其他时钟的TRGO输出，用来做级连功能：例如TIM1的产生事件映射到TRGO，接入下一个定时器TIM的ITR2输入接口，用于级连计数。例如下图，TIM2的ITR0来自于TIM1。![image-20250402194703877](./嵌入式软件学习.assets/image-20250402194703877.png)

- 4.TI1P_ED：为CH1的边沿

- 5.CH1/CH2输入

  **总结输入：内部时钟、外部时钟、其他定时器（并联）、CH1边沿输入、CH1/CH2引脚**

  

![image-20240727221618618](./嵌入式软件学习.assets/image-20240727221618618.png)

###### 主从模式

1. 基本定时器：只能DAC
2. 通用定时器：级连定时器、DAC/ADC 

![image-20240727223107035](./嵌入式软件学习.assets/image-20240727223107035.png)

###### 输入捕获、输出比较

![image-20240804153231246](./嵌入式软件学习.assets/image-20240804153231246.png)

输入捕获。输出比较：共用统一寄存器与IO端口，只能一路选择一个功能，不过有4路



##### 高级定时器

![image-20240804153454802](./嵌入式软件学习.assets/image-20240804153454802.png)

###### 与通用继电器差距

![image-20240804153824034](./嵌入式软件学习.assets/image-20240804153824034.png)

1. 重复次数计数器：对输出信号进行分频，多次时基电路进行计数后，才触发中断或者事件，增加计时范围
2. 死区：增加功率器件全部关断时间，防止直通烧毁
3. 互补输出，生成两路互补PWM信号驱动电机
4. 刹车：在外部IO功能。或者内部时钟时效后，直接关断PWM电机输出

##### 定时器总结

- 定时器时钟选择：RCC内部时钟、ETR外部时钟输入 、其他时钟输入 、 CH捕获通道输入 。 
- 外部模式2：由ETR引脚直接输入时钟。
- 外部时钟模式1 ：经过TRGI的选择后再进行输入
- 编码器模式：在使用正交编码器时使用的模式

![image-20240804155931371](./嵌入式软件学习.assets/image-20240804155931371.png)



###### 影子寄存器

带阴影的寄存器都为影子寄存器，例如**预分频寄存器、自动重装载寄存器 、 输入捕获/输出比较寄存器**   ， **影子寄存器为实际起作用的寄存器，当中途发生变化时，需要等本轮更新产生后，再将改变值起作用。**

![image-20250517180002618](./嵌入式软件学习.assets/image-20250517180002618.png)





###### 预分频器时序与影子寄存器

![image-20240804160757270](./嵌入式软件学习.assets/image-20240804160757270.png)



**预分配寄存器开启影子寄存器功能，在本次计数完成后，产生更新标记后，更新预分频寄存器的值**

![image-20240804160714067](./嵌入式软件学习.assets/image-20240804160714067.png)

![image-20240804161453689](./嵌入式软件学习.assets/image-20240804161453689.png)

1. 计数器使能后，计数器开始运行

2. <img src="./嵌入式软件学习.assets/image-20240804160921877.png" alt="image-20240804160921877" style="zoom:80%;" />重装载寄存器为FC，当计数器到达预设值后，**需要下一个时钟到来后，重新复位清零，同时复位后产生更新事件**

3. **预分频计数器：预分频控制寄存器（人为控制写入） 与 影子寄存器（实际起作用）；控制寄存器由人为写入后，需要在本轮计数结束后产生更新标记，影子寄存器进行更新才能实际起作用，防止在计数周期中更新输出频率，一个周期内计数时钟不同，影子寄存器会在下一个计数周期更新输出频率**<img src="./嵌入式软件学习.assets/image-20250402200620427.png" alt="image-20250402200620427" style="zoom:50%;" />

   

###### 计数器时序

- PSC +1 ，是因为设定是这样的，需要+1为实际分频系数
- ARR+1，是因为达到ARR后，需要下一个时钟到来才会产生更新标记

![image-20240804161905140](./嵌入式软件学习.assets/image-20240804161905140.png)



###### ARR开启影子寄存器

- ARPF控制预装寄存器功能，1开启，0-关闭，如下图。在写入重装载寄存后，立即更新，在计数时间到达36后，复位计数产生中断
- 但是有时候有个问题：当前计数器值为36，设置ARR寄存器的值为20，需要先技术到0XFFFF，再溢出重新计数到20生效。

<img src="./嵌入式软件学习.assets/image-20240804162604471.png" alt="image-20240804162604471" style="zoom:80%;" />

- 1-开启，预装寄存器功能。在写入自动重装载寄存器后，需要在下一个周期重新设置，实际起作用的是影子寄存器。

<img src="./嵌入式软件学习.assets/image-20240804162835926.png" alt="image-20240804162835926" style="zoom:80%;" />





#### 3.2定时器中断

##### 中断配置步骤

![image-20240804174548167](./嵌入式软件学习.assets/image-20240804174548167.png)







##### 库函数

- 3.时基单元配置

  ```c
  void TIM_DeInit(TIM_TypeDef* TIMx); //时钟外设寄存器复位
  void TIM_TimeBaseInit(TIM_TypeDef* TIMx, TIM_TimeBaseInitTypeDef* TIM_TimeBaseInitStruct); //时基单元初始化配置函数
  void TIM_TimeBaseStructInit(TIM_TimeBaseInitTypeDef* TIM_TimeBaseInitStruct); //时基结构体复位配置
  ```

  

- 5.运行控制-使能定时器

  ```c
  void TIM_Cmd(TIM_TypeDef* TIMx, FunctionalState NewState); //使能定时器运行，配置好后，使能计数开始工作
  ```

  

- 4.中断输出控制

  ```c
  void TIM_ITConfig(TIM_TypeDef* TIMx, uint16_t TIM_IT, FunctionalState NewState); //配置定时器哪些标记位进入中断
  ```

  

- 2.选择时钟源

- ![image-20240804181106712](./嵌入式软件学习.assets/image-20240804181106712.png)ETR/TIX输入，可以选择两种模式为时钟输入。

- <img src="./嵌入式软件学习.assets/image-20240804180530190.png" alt="image-20240804180530190" style="zoom:80%;" />外部输入电路图

  ```c
  //RCC内部时钟模式 - 配置该定时器的时钟源来源于内部时钟
  void TIM_InternalClockConfig(TIM_TypeDef* TIMx);
  
  //ITRX，其他定时器时钟，用于定时器级连
  void TIM_ITRxExternalClockConfig(TIM_TypeDef* TIMx, uint16_t TIM_InputTriggerSource);
  
  //TIX,来源输入捕获通道CH1/CH2
  void TIM_TIxExternalClockConfig(TIM_TypeDef* TIMx, uint16_t TIM_TIxExternalCLKSource,  
                                  uint16_t TIM_ICPolarity, uint16_t ICFilter);
  
  
   //来源ETR外部GPIO时钟，外部模式1  （ETR有两条模式进入时钟，TIX也是）
  void TIM_ETRClockMode1Config(TIM_TypeDef* TIMx, uint16_t TIM_ExtTRGPrescaler, uint16_t TIM_ExtTRGPolarity,
                               uint16_t ExtTRGFilter);
   //来源ETR外部GPIO时钟，外部模式2
  void TIM_ETRClockMode2Config(TIM_TypeDef* TIMx, uint16_t TIM_ExtTRGPrescaler, 
                               uint16_t TIM_ExtTRGPolarity, uint16_t ExtTRGFilter);
   //来源ETR外部GPIO时钟，使用ETR时，配置ETR预分频器、极性、滤波等
  void TIM_ETRConfig(TIM_TypeDef* TIMx, uint16_t TIM_ExtTRGPrescaler, uint16_t TIM_ExtTRGPolarity,
                     uint16_t ExtTRGFilter);
  ```

  



- 其他使用到的库函数

  ```c
  //预分频器值，TIM_PSCReloadMode写入模式（影子寄存器功能是否开启，在下一计数周期改变预分频器值）
  void TIM_PrescalerConfig(TIM_TypeDef* TIMx, uint16_t Prescaler, uint16_t TIM_PSCReloadMode); 
  
  //计数方式：向上、向下、中央对称
  void TIM_CounterModeConfig(TIM_TypeDef* TIMx, uint16_t TIM_CounterMode);
  
  //重装载寄存器，预装入（影子寄存器功能，是否在下一周期更新ARR）
  void TIM_ARRPreloadConfig(TIM_TypeDef* TIMx, FunctionalState NewState);
  
  //手动给计数器一个值
  void TIM_SetCounter(TIM_TypeDef* TIMx, uint16_t Counter);
  
  //手动给自动重装载寄存器一个值
  void TIM_SetAutoreload(TIM_TypeDef* TIMx, uint16_t Autoreload);
  
  //获取当前计数器值，程序获取当前计数到多少
  uint16_t TIM_GetCounter(TIM_TypeDef* TIMx);
  
  //获取当前预分配器值
  uint16_t TIM_GetPrescaler(TIM_TypeDef* TIMx);
  
  
  //获取和清除标记位，上面两个用于主函数判断，下面两个用于中断函数
  FlagStatus TIM_GetFlagStatus(TIM_TypeDef* TIMx, uint16_t TIM_FLAG);
  void TIM_ClearFlag(TIM_TypeDef* TIMx, uint16_t TIM_FLAG);
  
  ITStatus TIM_GetITStatus(TIM_TypeDef* TIMx, uint16_t TIM_IT);
  void TIM_ClearITPendingBit(TIM_TypeDef* TIMx, uint16_t TIM_IT);
  ```

  



##### 定时器-内部时钟-更新中断程序

- **配置步骤**：开启外设RCC时钟、配置外部时钟源、配置时基单元、配置中断标记位控制使能、配置NVIC、启动定时器

- **配置时基单元**：

  1. TIM_ClockDivision：，这个结构体成员与时基单元无关系。外部时钟滤波采样频率f(时钟由内部时钟分配得到，也就是TIM_ClockDivision进行设置)，与另一个参数TIM_ICInitStructure.TIM_ICFilter（采样点个数N），以f频率进行采样，采集N个点；当N个点全为高电平则为高。

     **f越小，N越大，滤波效果越好，同时信号时延越高**

     ![image-20240804224830777](./嵌入式软件学习.assets/image-20240804224830777.png)

  2. 关于自动重装载、分频器需要 -1：

     - 预分频器寄存器写0，不分频，72M；写1，二分屏，36MHZ；（**实际分频系数 = PSC + 1**）

     - ![image-20240804225410976](./嵌入式软件学习.assets/image-20240804225410976.png)到达重装载寄存器后，需要再来一个脉冲才能触发更新中断，复位计数器；

       **所以程序设计：所需计数值 - 1 （反推重装载寄存器值）**

  3. 初始化时基后，需要清除一下中断，由于初始化中，写入预分频值、重装载计数器值，是程序内直接触发一更新事件，会产生一次更新中断，这里立即将中断清除，防止进入中断进行一次处理

     ```c
       /* Generate an update event to reload the Prescaler and the Repetition counter
          values immediately  通过一次更新事件来立即更新预分频值、重装载计数器值*/ 
       TIMx->EGR = TIM_PSCReloadMode_Immediate;     
     ```

     

     

  - 

```c

#include "timer.h"
#include "stm32f10x.h"                  // Device header

void timer_init(void)
{
    //1.开启RCC外设时钟
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2,ENABLE) ; 
    
	//2.TIM2 - 使用内部RCC时钟 ； 这条可以省略 定时器上电后默认时钟为内部时钟
    TIM_InternalClockConfig(TIM2) ; 

    //3.配置时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseStructInit(&TIM_TimeBaseInitStruct) ;
    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ; //一分频，这个是用于外部时钟滤波采样频率f ,与另一个参数N-采样点配合TIM_ICInitStructure.TIM_ICFilter = 15 （15次）；f越小，N越大，滤波效果越好，同时信号时延越高
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ; //计数模式 - 向上计数
    TIM_TimeBaseInitStruct.TIM_Period =  10000 - 1; //ARR 自动重装寄存器 到达自动重装载寄存器后，需要再等一帧才能进行复位，产生更新时间
    TIM_TimeBaseInitStruct.TIM_Prescaler = 7200 - 1 ;//PSC 预分频器值 0不分频 1-2分屏
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0 ; //重复计数器 --- 只有高级定时器才有
    TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStruct) ;
    TIM_ClearFlag(TIM2,TIM_IT_Update) ;//由于上述初始化中，写入预分频值、重装载计数器值有影子寄存器，程序需要立即对初始化值进行生效，于是程序内直接触发一更新事件，会产生一次更新中断，这里立即将中断清除，防止开启中断后，立即进入中断执行一次。
    
    //4.中断控制，选择中断 ；这里选择使能更新中断，计数到达重装载计数器下一个脉冲到达会产生更新中断
    TIM_ITConfig(TIM2,TIM_IT_Update,ENABLE) ; 
    
    //5.配置NVIC
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2) ; //配置NVIC分组，一个工程只需配置一次
    NVIC_InitTypeDef NVIC_InitStruct ; //中断NVIC配置
    NVIC_InitStruct.NVIC_IRQChannel = TIM2_IRQn; //中断通道，所需配置的中断
    NVIC_InitStruct.NVIC_IRQChannelCmd = ENABLE; //开启中断
    NVIC_InitStruct.NVIC_IRQChannelPreemptionPriority = 2 ; //抢占优先级
    NVIC_InitStruct.NVIC_IRQChannelSubPriority =  2; //响应优先级
    NVIC_Init(&NVIC_InitStruct) ;

	//6.启动定时器
    TIM_Cmd(TIM2,ENABLE) ; 
}


void TIM2_IRQHandler(void)
{
    if (TIM_GetITStatus(TIM2,TIM_IT_Update) == 1) //检测TIM2是否有更新中断
    {
        timer_num_s++ ;

        TIM_ClearITPendingBit(TIM2,TIM_IT_Update) ; //清除中断标记位
    }
    
}

```





##### 定时器-外部时钟-更新中断程序



- TIM_ETRClockMode2Config（）设置外部ETR时钟的采样F与N

![image-20240804230121469](./嵌入式软件学习.assets/image-20240804230121469.png)



```c
//外部ETR引脚定时
void timer_init(void)
{
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2,ENABLE) ; //开启RCC外设时钟

	
    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_IPU;
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_0;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct) ; //PA0-上拉输入

//参数1：定时器 ，参数二：外部时钟预分频数  参数三：极性 参数四：决定采样f与N
    TIM_ETRClockMode2Config(TIM2,TIM_ExtTRGPSC_OFF,TIM_ExtTRGPolarity_NonInverted,0x10) ; //使用外部ETC端口时钟，模式二接口

    //配置时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseStructInit(&TIM_TimeBaseInitStruct) ;

    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ; //一分频，这个是用于外部时钟滤波采样频率f ,与另一个参数N-采样点配合TIM_ICInitStructure.TIM_ICFilter = 15 （15次）；f越小，N越大，滤波效果越好，同时信号时延越高
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ; //计数模式 - 向上计数
    TIM_TimeBaseInitStruct.TIM_Period =  5 - 1; //自动重装寄存器 到达自动重装载寄存器后，需要再等一帧才能进行复位，产生更新时间
    TIM_TimeBaseInitStruct.TIM_Prescaler = 1 - 1 ;//预分频器值 1不分频 2-已分屏
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0 ; //重复计数器 --- 只有高级定时器才有
    TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStruct) ;


    TIM_Cmd(TIM2,ENABLE) ; //启动定时器
}

int Git_times(void)
{
    return TIM_GetCounter(TIM2) ;

}
```









#### 3.3输出比较

- 输出比较就是：计数器CNT与CCR（比较/捕获寄存器，两种功能共用一个寄存器）进行比较，当小于或大于电平进行跳变。
- IC-输入捕获 OC-输出比较   CCR -比较捕获寄存器
- 高级/通用定时器具有该功能，基本定时器没有；
- 高级功能还具有互补输出、死区生成功能。

![image-20240805221332480](./嵌入式软件学习.assets/image-20240805221332480.png)

![image-20240805221056676](./嵌入式软件学习.assets/image-20240805221056676.png)

##### 1.PWM-脉宽调制

分辨率，是指PWM变化的步距 1.1% 1.2%。。变化 与 1% 、2%。。。l两种变化的程度不一样

![image-20240805222054020](./嵌入式软件学习.assets/image-20240805222054020.png)



##### 2.输出比较电路原理

###### 输出模式

- 冻结：**CNT = CCR相等时，REF保持不变。**保持冻结时电平输出，在想暂停PWM输出时使用该模式

- ![image-20240805223108945](./嵌入式软件学习.assets/image-20240805223108945.png)有效电平-高电平  无效电平 - 低电平

  1. 匹配时，置高/低电平--**一次性方波输出**<img src="./嵌入式软件学习.assets/image-20240805223432885.png" alt="image-20240805223432885" style="zoom:50%;" />，黄色为输出（一般很少使用）
  2. 匹配时电平翻转，**产生50%方波**![image-20240805223929574](./嵌入式软件学习.assets/image-20240805223929574.png)如图高电平与低电平持续时间

  - 强制模式<img src="./嵌入式软件学习.assets/image-20240805224207948.png" alt="image-20240805224207948" style="zoom:50%;" />，直接关闭CNT与CCR功能，强制已规定电平输出
  - ![image-20240805224345511](./嵌入式软件学习.assets/image-20240805224345511.png)用于PWM脉宽调制模式，**根据计数方式与比较方式，有4种模式**非常重要！！！！



![image-20240805222507070](./嵌入式软件学习.assets/image-20240805222507070.png)

```c
#define TIM_OCMode_Timing      //冻结
#define TIM_OCMode_Active      //相等时，有效电平
#define TIM_OCMode_Inactive    //相等时，无效电平
#define TIM_OCMode_Toggle      //匹配时电平翻转
#define TIM_OCMode_PWM1        //PWM-1
#define TIM_OCMode_PWM2        //PWM-2

                          ((MODE) == TIM_ForcedAction_Active) ||  //强制有效电平
                          ((MODE) == TIM_ForcedAction_InActive))  //强制无效电平
```





- oc1_ref:表示输出比较电路的参考信号，可以输出到TRGO主模式
- CC1P位 : 选择是否对参考信号进行翻转，0-不翻转直接输出 1-翻转
- CC1E位：使能输出电路
- OC1M【2:0】：配置输出比较的模式
- <img src="./嵌入式软件学习.assets/image-20240805222507070.png" alt="image-20240805222507070" style="zoom: 33%;" />
- 在比较模式中可以配置PWM的极性，在输出前也可以ton过CC1R配置输出极性

![image-20240805230400916](./嵌入式软件学习.assets/image-20240805230400916.png)



- CCR控制占空比、ARR控制频率（**PWM模式1 、向上计数**）
- 这里ARR = 99  CCR = 30
- 产生的PWM参考电压 - REF 经过极性选择、输出使能后，由对应GPIO输出
- 在选择PWM模式时，可以通过不同模式选择输出极性，在产生PWM信号后，还可以对极性再次翻转
- 

![image-20240805224932196](./嵌入式软件学习.assets/image-20240805224932196.png)





- ARR决定周期 、 分辨率（ARR越大分辨率越大）
- CRR决定占空比
- 

![image-20240805225023065](./嵌入式软件学习.assets/image-20240805225023065.png)





##### 3.实际运用



###### 1.舵机

根据输入的PWM占空比，舵机旋转固定角度

![image-20250409215040233](./嵌入式软件学习.assets/image-20250409215040233.png)

###### 2.直流电机

![image-20250409215441419](./嵌入式软件学习.assets/image-20250409215441419.png)

![image-20250409215503381](./嵌入式软件学习.assets/image-20250409215503381.png)





##### 4.库函数

```c
//输出比较配置：OC:output Compare ,有四个通道
void TIM_OC1Init(TIM_TypeDef* TIMx, TIM_OCInitTypeDef* TIM_OCInitStruct);
void TIM_OC2Init(TIM_TypeDef* TIMx, TIM_OCInitTypeDef* TIM_OCInitStruct);
void TIM_OC3Init(TIM_TypeDef* TIMx, TIM_OCInitTypeDef* TIM_OCInitStruct);
void TIM_OC4Init(TIM_TypeDef* TIMx, TIM_OCInitTypeDef* TIM_OCInitStruct);

//给输出比较结构体赋一个初值
void TIM_OCStructInit(TIM_OCInitTypeDef* TIM_OCInitStruct); 

//单独修改CCR寄存器值，更改占空比值
void TIM_SetCompare1(TIM_TypeDef* TIMx, uint16_t Compare1);
void TIM_SetCompare2(TIM_TypeDef* TIMx, uint16_t Compare2);
void TIM_SetCompare3(TIM_TypeDef* TIMx, uint16_t Compare3);
void TIM_SetCompare4(TIM_TypeDef* TIMx, uint16_t Compare4);



//运行过程中，停止输出PWM并强制输出高低电平
void TIM_ForcedOC1Config(TIM_TypeDef* TIMx, uint16_t TIM_ForcedAction);
void TIM_ForcedOC2Config(TIM_TypeDef* TIMx, uint16_t TIM_ForcedAction);
void TIM_ForcedOC3Config(TIM_TypeDef* TIMx, uint16_t TIM_ForcedAction);
void TIM_ForcedOC4Config(TIM_TypeDef* TIMx, uint16_t TIM_ForcedAction);

//配置CCR比较寄存器预装功能，影子寄存器在更新事件时生效
void TIM_OC1PreloadConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPreload);
void TIM_OC2PreloadConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPreload);
void TIM_OC3PreloadConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPreload);
void TIM_OC4PreloadConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPreload);

//单独设置输出通道极性（配置参考电压REF 后端配置的输出极性），在初始化结构体也可以设置输出极性 ，OC4未有带N
void TIM_OC1PolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPolarity);
void TIM_OC1NPolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCNPolarity);
void TIM_OC2PolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPolarity);
void TIM_OC2NPolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCNPolarity);
void TIM_OC3PolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPolarity);
void TIM_OC3NPolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCNPolarity);
void TIM_OC4PolarityConfig(TIM_TypeDef* TIMx, uint16_t TIM_OCPolarity);

//单独设置输出使能参数
void TIM_CCxCmd(TIM_TypeDef* TIMx, uint16_t TIM_Channel, uint16_t TIM_CCx);
void TIM_CCxNCmd(TIM_TypeDef* TIMx, uint16_t TIM_Channel, uint16_t TIM_CCxN);


//单独设置输出比较模式
void TIM_SelectOCxM(TIM_TypeDef* TIMx, uint16_t TIM_Channel, uint16_t TIM_OCMode);



//高级定时器使能主输出：仅高级定时器使用，需要调用此函数使能主输出否则PWM不能进行正常输出 TIM1/TIM8
void TIM_CtrlPWMOutputs(TIM_TypeDef* TIMx, FunctionalState NewState);

```

![image-20250410214650289](./嵌入式软件学习.assets/image-20250410214650289.png)





##### 4.比较输出代码







###### 1.配置步骤：

<img src="./嵌入式软件学习.assets/image-20240811112635795.png" alt="image-20240811112635795" style="zoom: 50%;" />

1. 配置时钟RCC：GPIO、TIM
2. 配置TIM时钟输入选择
3. 配置时基单元
4. 配置输出比较单元：CCR值、输出比较模式、极性选择、输出使能
5. 配置输出GPIO：复用推挽输出模式
6. 使能TIM





###### 2.PWM-呼吸灯程序

1. GPIO复用：在数据手册中查询TIM4-CH1复用在PB6上![image-20240811170332077](./嵌入式软件学习.assets/image-20240811170332077.png)，**如果此时该引脚需要用作GPIO控制或者I2C1_SCL进行控制，则可AFIO进行重映射**![image-20240811170856190](./嵌入式软件学习.assets/image-20240811170856190.png)如图TIM4-CH1映射重映射在PD12上
2. ![image-20240811171418981](./嵌入式软件学习.assets/image-20240811171418981.png)

```c

/*初始化PWM控制*/
void PWM_init(void)
 {
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM4,ENABLE) ; //打开RCC-TIM时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE) ; //打开RCC-gpiob时钟

    TIM_InternalClockConfig(TIM2) ; //选择内部时钟

        //初始化GPIO引脚 PB6-TIM2-CH1
    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AF_PP ; //需要设置复用推挽输出，引脚的控制全才会从GPIO寄存器转移到片上外设
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_6;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOB,&GPIO_InitStruct);


    //打开时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ;
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ;
    TIM_TimeBaseInitStruct.TIM_Period = 1000-1 ; //ARR
    TIM_TimeBaseInitStruct.TIM_Prescaler = 72-1;//PSC
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0;
    TIM_TimeBaseInit(TIM4,&TIM_TimeBaseInitStruct) ;

    //初始化输出比较通道
    TIM_OCInitTypeDef  TIM_OCInitStruct ;
    TIM_OCStructInit(&TIM_OCInitStruct) ; //初始化结构体：因为下方有部分成员未使用到，初始化结构体为局部变量，不初始化会出现随机值
    
    TIM_OCInitStruct.TIM_OCMode = TIM_OCMode_PWM1 ; //输出比较模式：PWM1
    TIM_OCInitStruct.TIM_OCPolarity = TIM_OCPolarity_High ; //输出比较极性：High-高电平为有效电平（极性不翻转） ，Low - 翻转极性
    TIM_OCInitStruct.TIM_OutputState = TIM_OutputState_Enable ; //设置输出使能 ： 使能
    TIM_OCInitStruct.TIM_Pulse = 50; //设置CCR
    // TIM_OCInitStruct.TIM_OCIdleState = ;  这些都是高级定时器设置
    // TIM_OCInitStruct.TIM_OCNIdleState = ;
    // TIM_OCInitStruct.TIM_OCNPolarity = ;
    // TIM_OCInitStruct.TIM_OutputNState = ;
    TIM_OC1Init(TIM4,&TIM_OCInitStruct) ;


    TIM_Cmd(TIM4,ENABLE) ; //启动定时器
}



/*修改占空比*/
void Set_CCR_CH1(uint16_t value)
{
    TIM_SetCompare1(TIM4,value) ;
}



/*main函数，实时修改占空比*/
int main()
{
	PWM_init() ;
	while(1) 
	{
		for (int i = 0; i < 1000; i++)
		{
			Set_CCR_CH1(i) ;
			Delay_ms(10) ;
		}
		for ( int i = 1000; i >=0; i--)
		{
			Set_CCR_CH1(i) ;
			Delay_ms(10) ;
		}		
	}
}


```



###### 3.AFIO重映射配置引脚



**1.开启GPIO重映射使用**

1. 开启AFIO时钟

2. 开启需要的重映射方式

   ```c
   RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE) ; //打开AFIO时钟
   GPIO_PinRemapConfig(GPIO_PartialRemap1_TIM2,ENABLE) ; //使用TIM2部分重映射方式1
   ```

   



![image-20240811194802477](./嵌入式软件学习.assets/image-20240811194802477.png)

在中文参考手册中，可以**看到TIM2有几种重映射方式，对应几种不同的引脚映射方案。**

```c
    @arg GPIO_PartialRemap1_TIM2     //部分重映射1
    @arg GPIO_PartialRemap2_TIM2     //部分重映射2
    @arg GPIO_FullRemap_TIM2         //完全重映射
```



**2.禁用JTAG/SWD功能所占用的引脚**

这几个**GPIO默认是作为软件调试端口使用的，需要用作其他功能。需要在软件内先禁用。**

1. 开启AFIO时钟
2. 开启需要的重映射方式

```c
RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE) ; //打开AFIO时钟
GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable,ENABLE) ; //禁用JTAG、打开SWJ
```



![image-20240811201727211](./嵌入式软件学习.assets/image-20240811201727211.png)默认调试端口运用

![image-20240811201752828](./嵌入式软件学习.assets/image-20240811201752828.png)通过设置AFIO模式禁用部分功能

```c
#define GPIO_Remap_SWJ_NoJTRST    	 /*!< Full SWJ Enabled (JTAG-DP + SW-DP) but without JTRST */     001
#define GPIO_Remap_SWJ_JTAGDisable   /*!< JTAG-DP Disabled and SW-DP Enabled */   010
#define GPIO_Remap_SWJ_Disable       /*!< Full SWJ Disabled (JTAG-DP + SW-DP) */ 100
```









**此实验将原来TIM-CH1的引脚PA0，重映射到引脚PA15进行输出,又因为PA15复用了仿真功能需要对该功能进行关闭**



```c
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2,ENABLE) ; //打开RCC-TIM时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE) ; //打开RCC-gpiob时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE) ; //打开AFIO时钟


    GPIO_PinRemapConfig(GPIO_PartialRemap1_TIM2,ENABLE) ; //使用TIM2部分重映射方式1 将CH1由PA0 映射到 PA15
    GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable,ENABLE) ; //禁用JTAG、打开SWJ  打开PA15





    //TIM_InternalClockConfig(TIM2) ; 

        //初始化GPIO引脚 PB6-TIM2-CH1
    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AF_PP ; //需要设置复用推挽输出，引脚的控制全才会从GPIO寄存器转移到片上外设
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_15;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct);


    //打开时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ;
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ;
    TIM_TimeBaseInitStruct.TIM_Period = 1000-1 ; //ARR
    TIM_TimeBaseInitStruct.TIM_Prescaler = 72-1;//PSC
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0;
    TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStruct) ;

    //初始化输出比较通道
    TIM_OCInitTypeDef  TIM_OCInitStruct ;
    TIM_OCStructInit(&TIM_OCInitStruct) ; //初始化结构体：因为下方有部分成员未使用到，初始化结构体为局部变量，不初始化会出现随机值
    TIM_OCInitStruct.TIM_OCMode = TIM_OCMode_PWM1 ; //输出比较模式：PWM1
    TIM_OCInitStruct.TIM_OCPolarity = TIM_OCPolarity_High ; //输出比较极性：有效电平-高电平
    TIM_OCInitStruct.TIM_OutputState = TIM_OutputState_Enable ; //设置输出使能 ： 使能
    TIM_OCInitStruct.TIM_Pulse = 50; //设置CCR
    // TIM_OCInitStruct.TIM_OCIdleState = ;  这些都是高级定时器设置
    // TIM_OCInitStruct.TIM_OCNIdleState = ;
    // TIM_OCInitStruct.TIM_OCNPolarity = ;
    // TIM_OCInitStruct.TIM_OutputNState = ;
    TIM_OC1Init(TIM2,&TIM_OCInitStruct) ;


    TIM_Cmd(TIM2,ENABLE) ; //启动定时器

```





###### 4.同一个定时器输出多个通道

- 一个定时器有4路比较输出，由于共用CNT、ARR所以频率是一样的
- 由于每一路CCR不同，导致占空比不相同











#### 3.4.输入捕获



##### 1.输入捕获简介

输入捕获，测量输入信号的频率、占空比

- **输出比较：比较CNT与CCR的值，改变电平变化，执行输出动作              **
- **输入捕获：在输入电平跳变时，将CNT值锁存在CCR中**。 类比外部中端是在电平跳变申请中断，这里是在跳变时触发后面硬件电路锁存CNT计数值
- **PWMI**模式，专门用于测量频率和占空比。一路测量频率，一路测量占空比
- **主从触发模式**：实现硬件全自动测量
- 只能测量方波信号，其他波形需要再经过外部电路整形为方波

![image-20240811212734163](./嵌入式软件学习.assets/image-20240811212734163.png)

![image-20240811212349091](./嵌入式软件学习.assets/image-20240811212349091.png)



##### 2.频率测量

测量频率，都是通过测量周期时间，由于没有绝对的计时器，只有计数  或者  通过固定频率进行计数间接进行计时

- **测频法**：一**段时间内周期个数**，可以获得这段时间内的平均周期  t =T/N   ：
  1. 由于本身是**平均数，自带滤波**
  2. 在一段时间进行计数，**更新时间慢**
  3. 用于高频信号测量
- **测周法**：在两个上升沿之间，测量周期时间（由于**没办法直接测量时间，只能通过固定频率计数间接获得周期时间**）
  1. 一个周期即可更新，**更新时间快**
  2. 用于测量低频信号



###### 使用何种方法：

1. 测频法用于高频信号，测周法用于低频信号
2. 两种方法都有计数±1误差。例如测频法，时间到了，但是被测量信号还没跳变，少计一个；测周法，CNT还没记完，被测信号跳变呀就来了，计次在一半  ；**减小误差需要将N扩大**
3. 中界频率：当两种测量方法误差一样，也就是N相同。通过上下两个公式，两个N相等，解出fm



**小于中界频率，使用测频法；大于中界频率，使用测周法**

![image-20240811214147276](./嵌入式软件学习.assets/image-20240811214147276.png)



###### 实现两种方式：

- 测频法：利用外部中断EXIT、定时器外部信号计数外部信号跳变沿。每隔一段时间取跳变沿次数，获取频率。
- 测周法就是下方实现的输入捕获，测量两个上升沿时间获得周期。







##### 3.输入捕获电路

- 输入滤波器&边沿检测器：对外部信号进行**滤波**、**边沿进行检测**（上下边沿检测）

- 可以分开两路，一路直接到自己的CCR，另一路到相邻CCR。**使用PWMI模式，一路捕获上升沿-周期；一路捕获下降沿-占空比** 两个CCR对一个信号进行测量，一个捕获上升沿测量周期，一个捕获下降沿测量占空比。

- 预分频器：对输入信号**进行分频**

- CCR：每来一个边沿触发信号，就会将CNT的值向CCR转运一次。

- 每次CCR锁存一次CNT值，**可以触发一次捕获中断或者捕获事件**。

- 每次捕获后都需将CNT值进行清零，这部分可以交给硬件。**每次捕获后将CNT清零，这个可以交给主从触发模式。**

   

![image-20240812225210335](./嵌入式软件学习.assets/image-20240812225210335.png)





###### 详细电路

- TI1:CH1引脚
- TI1F：滤波后的信号
- fdts :采样滤波器的时钟来源
- ICF[3:0]控制采样频率和采样个数，当出现毛刺和干扰比较大时需要修改该参数
- 边沿检测器检测上升沿还是下降沿，使用CC1P选择极性（上升沿/下降沿），得到TI1FP1
- CC1S：选择信号来源
- ICPS：选择分频系数
- CC1E：使能输入
- <img src="./嵌入式软件学习.assets/image-20250414230512475.png" alt="image-20250414230512475" style="zoom:50%;" />通过这两个信号输入从模式控制器，可以实现**自动化对计数器CNT进行清零**。
- 

![image-20240812230452596](./嵌入式软件学习.assets/image-20240812230452596.png)





上述滤波器：**采样频率越低，采样个数N越大，滤波效果越好**。以采样频率采集信号，当持续N个信号都为1则信号为1

![image-20240812224208110](./嵌入式软件学习.assets/image-20240812224208110.png)



###### 3.1异或门输入

CH1进入有两路信号

- 路线1：**为三相无刷电机服务**，无刷电机有三个霍尔传感器检测转子位置，可以根据转子位置进行换相，定时器可以作为无刷电机的接口定时器驱动换相电路工作
- 路线2：单独一路CH1，异或电路无效。

![image-20240812213549521](./嵌入式软件学习.assets/image-20240812213549521.png)









#### 3.5主从触发模式

- 主模式：**定时器内部信号，映射到TRGO引脚，用于触发其他外设**（主控，控制别人）
- 从模式：**接受其他外设或自身外设信号，用于控制自身定时器运行**（被控）。选择触发源与从模式，当该触发源有效时，会自动的进行从模式。
- 触发源选择：选择指定信号，得到TRGI，通过TRRGI触发其他从模式

![image-20240812230806975](./嵌入式软件学习.assets/image-20240812230806975.png)

###### 从模式清零CNT

- 选择触发源为TI1FP1
- 从模式选择Reset

<img src="./嵌入式软件学习.assets/image-20250414230931524.png" alt="image-20250414230931524" style="zoom:50%;" />





###### 主模式功能选择，使用TRGO触发其他外设

![image-20240812231423601](./嵌入式软件学习.assets/image-20240812231423601.png)

###### 从模式触发源

- 000 - 011其他定时器的主模式输出
- 100 - 111 <img src="./嵌入式软件学习.assets/image-20250414231405652.png" alt="image-20250414231405652" style="zoom:67%;" />

![image-20240812231705480](./嵌入式软件学习.assets/image-20240812231705480.png)



###### 从模式触发功能

![image-20240812231743444](./嵌入式软件学习.assets/image-20240812231743444.png)

```c
/*******************************  主从触发模式 ****************************/
//选择输入触发源TRGI ，从模式触发源
void TIM_SelectInputTrigger(TIM_TypeDef* TIMx, uint16_t TIM_InputTriggerSource);
//选择输出触发源TRGO，选择主模式输出触发源
void TIM_SelectOutputTrigger(TIM_TypeDef* TIMx, uint16_t TIM_TRGOSource);
//选择从模式
void TIM_SelectSlaveMode(TIM_TypeDef* TIMx, uint16_t TIM_SlaveMode);
```











#### 3.6输入捕获实现代码



##### 0.库函数配置

```c
 //IC输入捕初始化配置，输出比较OC分多个函数，IC使用一个函数配置多个通道，方便多通道复用,结果体内也许选择所配置的通道
void TIM_ICInit(TIM_TypeDef* TIMx, TIM_ICInitTypeDef* TIM_ICInitStruct);

//PWMI模式初始化，可以快速配置两个通道。该模式用于获取信号的周期、频率 
void TIM_PWMIConfig(TIM_TypeDef* TIMx, TIM_ICInitTypeDef* TIM_ICInitStruct);


//配置输入通道的分频器系数
void TIM_SetIC1Prescaler(TIM_TypeDef* TIMx, uint16_t TIM_ICPSC);
void TIM_SetIC2Prescaler(TIM_TypeDef* TIMx, uint16_t TIM_ICPSC);
void TIM_SetIC3Prescaler(TIM_TypeDef* TIMx, uint16_t TIM_ICPSC);
void TIM_SetIC4Prescaler(TIM_TypeDef* TIMx, uint16_t TIM_ICPSC);

//分别读取四个通道的CCR ：输出捕获模式下使用TIM_SetCompare1（）写；输入捕获读TIM_GetCapture1（）
uint16_t TIM_GetCapture1(TIM_TypeDef* TIMx);
uint16_t TIM_GetCapture2(TIM_TypeDef* TIMx);
uint16_t TIM_GetCapture3(TIM_TypeDef* TIMx);
uint16_t TIM_GetCapture4(TIM_TypeDef* TIMx);


/*******************************  主从触发模式 ****************************/
//选择输入触发源TRGI ，从模式触发源
void TIM_SelectInputTrigger(TIM_TypeDef* TIMx, uint16_t TIM_InputTriggerSource);
//选择输出触发源TRGO，选择主模式输出触发源
void TIM_SelectOutputTrigger(TIM_TypeDef* TIMx, uint16_t TIM_TRGOSource);
//选择从模式，需要执行的具体动作
void TIM_SelectSlaveMode(TIM_TypeDef* TIMx, uint16_t TIM_SlaveMode);
```







##### 1.输入捕获 - 硬件全自动测量周期

- 外部信号经过滤波、边沿检测、分频触发CCR寄存器锁存CNT值

- 当边沿检测到信号，产生TI1FP1，通过从模式自动清零CNT值（在CCR锁存CNT值后）

- **左上角所示原理：在两个上升沿之间，CNT自增；每捕获到一个上升沿，锁存CNT值，再将CNT值进行清零；这样CCR可以实时更新每个周期大小**

- 从模式清零CNT，实现硬件全自动测量周期<img src="./嵌入式软件学习.assets/image-20240812232831802.png" alt="image-20240812232831802" style="zoom:50%;" />，**当需要使用从模式自动清零时，只能使用CH1和CH2**，当**使用其他通道式，只能开启更新中断，并在中断中清除CNT值**，**如果使用CH1与CH2，软件主从模式实现自动更新无需中断，需要数据的时候获取CCR寄存器的值即可获取周期。**但是使用CH3或者CH4时，无法使用主从模式自动对CNT进行清零，需要在中断中进行清零，这样对系统资源消耗过大，可以使用记频法利用定时器外部输入获取上升沿个数。程序每隔固定时间获取上升沿个数即可获取周期，消耗系统资源比较小。

- <img src="./嵌入式软件学习.assets/image-20250414232951774.png" alt="image-20250414232951774" style="zoom:50%;" />实现思路：每个上上升沿将CNT值锁存在CCR寄存器中，再将CNT计数零。第二次CCR中的值就是两个上升沿的事件。**CH1/2采用主从模式自动清零，CH3/4需要采用更新中断，在中断中人为清零**

  

![image-20240812232047874](./嵌入式软件学习.assets/image-20240812232047874.png)



###### 代码：

- 初始化GPIO、TIM4时钟
- GPIO初始化输入模式。**外设使用GPIO不需要复用，只需要配置输入模式**
- 配置时基单元，让CNT在时基单元在时钟的驱动下计数
- 配置输入捕获单元：滤波器、极性、分频器、CCR
- 配置从模式触发：T1FP1复位CNT
- 开启定时器



TIM2产生100HZ频率 ，TIM4输入捕获所产生的方波频率

```c
//main:
uint32_t f_ccr = 0 ;
int main()
{
	PWM_init() ;
	Pwm_IC_Init() ;

	Set_Pcl_TIM2(7200) ;
	while(1) 
	{
		f_ccr = Get_Ic1_f() ;

	}

}





//PWM-输出比较：复用TIM2-CH1-PA15用作PWM输出
void PWM_init(void)
{
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2,ENABLE) ; //打开RCC-TIM时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE) ; //打开RCC-gpiob时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE) ; //打开AFIO时钟


    GPIO_PinRemapConfig(GPIO_PartialRemap1_TIM2,ENABLE) ; //使用TIM2部分重映射方式1 将CH1由PA0 映射到 PA15
    GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable,ENABLE) ; //禁用JTAG、打开SWJ  打开PA15





    //初始化GPIO引脚 PA15-TIM2-CH1
    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AF_PP ; //需要设置复用推挽输出，引脚的控制全才会从GPIO寄存器转移到片上外设
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_15;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct);


    //打开时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ;
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ;
    TIM_TimeBaseInitStruct.TIM_Period = 100-1 ; //ARR
    TIM_TimeBaseInitStruct.TIM_Prescaler = 72-1;//PSC
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0;
    TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStruct) ;

    //初始化输出比较通道
    TIM_OCInitTypeDef  TIM_OCInitStruct ;
    TIM_OCStructInit(&TIM_OCInitStruct) ; //初始化结构体：因为下方有部分成员未使用到，初始化结构体为局部变量，不初始化会出现随机值
    TIM_OCInitStruct.TIM_OCMode = TIM_OCMode_PWM1 ; //输出比较模式：PWM1
    TIM_OCInitStruct.TIM_OCPolarity = TIM_OCPolarity_High ; //输出比较极性：有效电平-高电平
    TIM_OCInitStruct.TIM_OutputState = TIM_OutputState_Enable ; //设置输出使能 ： 使能
    TIM_OCInitStruct.TIM_Pulse = 50; //设置CCR
    // TIM_OCInitStruct.TIM_OCIdleState = ;  这些都是高级定时器设置
    // TIM_OCInitStruct.TIM_OCNIdleState = ;
    // TIM_OCInitStruct.TIM_OCNPolarity = ;
    // TIM_OCInitStruct.TIM_OutputNState = ;
    TIM_OC1Init(TIM2,&TIM_OCInitStruct) ;

    TIM_Cmd(TIM2,ENABLE) ; //启动定时器
 
}
void Set_CCR_CH1(uint16_t value)
{
    TIM_SetCompare1(TIM2,value) ; //修改CCR-占空比
}
void Set_Pcl_TIM2(int16_t pclnum)
{
    TIM_PrescalerConfig(TIM2,pclnum,TIM_PSCReloadMode_Update) ; //修改Pcl - 频率
}





//输入捕获 ： 使用TIM4-CH1-PB6-进行输入捕获
void Pwm_IC_Init(void)
{
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM4,ENABLE) ; //打开RCC-TIM时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE) ; //打开RCC-gpiob时钟


    //初始化GPIO引脚 PA15-TIM2-CH1
    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_IN_FLOATING ; //输入捕获等外设，使用GPIO输入时不需要进行复用，正常配置输入
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_6;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOB,&GPIO_InitStruct);


    //打开时基单元
    TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStruct ;
    TIM_TimeBaseInitStruct.TIM_ClockDivision = TIM_CKD_DIV1 ;
    TIM_TimeBaseInitStruct.TIM_CounterMode = TIM_CounterMode_Up ;
    TIM_TimeBaseInitStruct.TIM_Period = 65530-1 ;   //ARR 最好设置较大一些 防止所测量的信号周期过长，还未完成计数就立即清零
    TIM_TimeBaseInitStruct.TIM_Prescaler = 72-1;//PSC
    TIM_TimeBaseInitStruct.TIM_RepetitionCounter = 0;
    TIM_TimeBaseInit(TIM4,&TIM_TimeBaseInitStruct) ;

    //初始化输入捕获单元
    TIM_ICInitTypeDef TIM_ICInitStruct ;
    TIM_ICInitStruct.TIM_Channel = TIM_Channel_1; //通道1 - CH1
    TIM_ICInitStruct.TIM_ICFilter = 0x5; //输入信号滤波器 有毛刺和噪声可以提高此值
    TIM_ICInitStruct.TIM_ICPolarity = TIM_ICPolarity_Rising; //上升沿触发  边沿选择：上升沿、下降沿、边沿（两种都触发）
    TIM_ICInitStruct.TIM_ICPrescaler = TIM_ICPSC_DIV1; //不分频-每次触发都有效   分频系数：2分频-两次边沿信号触发、4分频-四次边沿信号触发、8分频-八次边沿信号触发
    TIM_ICInitStruct.TIM_ICSelection =TIM_ICSelection_DirectTI ; //数据通道选择 ： 直连通道 、交叉通道 、TRC
    TIM_ICInit(TIM4,&TIM_ICInitStruct) ;

    //配置从模式：触发源、触发模式
    TIM_SelectInputTrigger(TIM4,TIM_TS_TI1FP1) ; //定时器从模式触发源选择 : 这里选择TIM4-TI1FP1
    TIM_SelectSlaveMode(TIM4,TIM_SlaveMode_Reset) ; //复位从模式，当所选择的触发源有效是对CNT进行复位

    TIM_Cmd(TIM4,ENABLE) ;

}

uint32_t Get_Ic1_f(void)
{
    
    return  (1000*1000 /TIM_GetCapture1(TIM4)) ;
}
```













##### 2.输入捕获 - PWMI模式测量周期和占空比

- CCR1用于捕获上升沿-周期，CCR2用于捕获下降沿-占空比
- 右上角所示：在上升沿时，锁存CCR清零CNT，在CNT自增过程中，自动锁存下降沿时CNT值。<img src="./嵌入式软件学习.assets/image-20250414233425255.png" alt="image-20250414233425255" style="zoom:67%;" />

![image-20240812233450171](./嵌入式软件学习.assets/image-20240812233450171.png)

```c
TIM_PWMIConfig(TIM4,&TIM_ICInitStruct) ; //只需配置CH1/CH2中某一路输入，另一路自动相反配置 ，简化另一路配置
/*
1.只能用于CH1、CH2捕获周期和占空比，不可用于CH3/CH4
2.只需配置CH1或CH2中一路，另一路自动相反配置。例如当CH1配置为：上升沿、直连，自动将CH2配置：下降沿、交叉通道
*/


```













其他代码与上面测量周期代码一直，需要增加一路CH2配置下降沿测量，测量占空比。

```c
    //初始化输入捕获单元
    TIM_ICInitTypeDef TIM_ICInitStruct ;
    TIM_ICInitStruct.TIM_Channel = TIM_Channel_1; //通道1 - CH1
    TIM_ICInitStruct.TIM_ICFilter = 0x5; //输入信号滤波器 有毛刺和噪声可以提高此值
    TIM_ICInitStruct.TIM_ICPolarity = TIM_ICPolarity_Rising; //上升沿触发  边沿选择：上升沿、下降沿、边沿（两种都触发）
    TIM_ICInitStruct.TIM_ICPrescaler = TIM_ICPSC_DIV1; //不分频-每次触发都有效  
    TIM_ICInitStruct.TIM_ICSelection =TIM_ICSelection_DirectTI ; //数据通道选择 ： 直连通道 、交叉通道 、TRC

    TIM_PWMIConfig(TIM4,&TIM_ICInitStruct) ; //只需配置CH1/CH2中某一路输入，该库函数对另一路自动相反配置 ，简化另一路配置





//计算占空比 CCR2/CCR1  CCR2高电平持续时间，CCR1周期
uint32_t Get_Duty(void)
{
    return  ( TIM_GetCapture2(TIM4)*100/TIM_GetCapture1(TIM4)) ;
}
```









#### 3.7定时器--编码器接口

- **接受正交信号，自动计算编码器位置、方向、速度**
- 高级定时器和通用定时器都带一个编码器接口
- AB相两路信号输入，接入CH1、CH2。 CH3/CH4无此功能

![image-20240814215539137](./嵌入式软件学习.assets/image-20240814215539137.png)









##### 1.正交编码器

- 正交编码器：输入两路信号**相位相差90度**，**方波频率代表速度**，可以一路**信号的提前和滞后来判断方向**
- 两路信号抗噪声
- 编码器执行逻辑：测量一路信号的**上升沿个数--速度** ，当**A相上升沿时的B相电平 -- 方向**

![image-20240814215651743](./嵌入式软件学习.assets/image-20240814215651743.png)



##### 2.编码器接口电路

连接AB相两路信号进入CH1/CH2通道，进入编码器接口，编码器接口根据正反转控制CNT的加减

![image-20240814221847729](./嵌入式软件学习.assets/image-20240814221847729.png)



##### 3.编码器接口基本结构

- 信号经过CH1/CH2进入编码器接口
- 编码器接口控制CNT加减，可以自增或者自减，相当一个带方向的外部时钟，正转加反向减。
- 编码器模式用的是CH1/CH2引脚
- **这里ARR设置65535，反转通过补码特性获得负数。** 当翻转时，为65534，将此值转化直接复制给有符号 short类型，值转换为 -1 

![image-20240814222033720](./嵌入式软件学习.assets/image-20240814222033720.png)



![ image-20250518145443605](./嵌入式软件学习.assets/image-20250518145443605.png)







- 正交编码器，可以根据**两路相位来判断方向，根据一路频率来判断速度**
- 计数逻辑：对两路的**每个边沿都进行计数（相比一路精度 *2**），![image-20250518150311877](./嵌入式软件学习.assets/image-20250518150311877.png)可以发现，一**路固定变压另一路正反转电平不一致，用于判断方向**

![image-20250518145952990](./嵌入式软件学习.assets/image-20250518145952990.png)





##### 4.工作模式

下方假设A相连接TI1FP1、B相连接TI2FP2

- 有三种模式：**A相边沿计数 、B相边沿计数、AB相边沿都计数    （正转CNT++,翻转CNT--）：**     
  1. 单项计数模式---A相计数：![image-20240814223418760](./嵌入式软件学习.assets/image-20240814223418760.png)![image-20240814223438277](./嵌入式软件学习.assets/image-20240814223438277.png)，根据A相边沿信号时的B相电平状态判断正反转，从而实现CNT加减
  2. B相同理
  3. **AB相边沿都计数，实现细分倍频更准确：**![image-20240814223707872](./嵌入式软件学习.assets/image-20240814223707872.png)![image-20240814223803626](./嵌入式软件学习.assets/image-20240814223803626.png)，根据AB相边沿都进行对方电平判断，从而实现CNT加减，分辨率更高。

![image-20240814222506766](./嵌入式软件学习.assets/image-20240814222506766.png)

<img src="./嵌入式软件学习.assets/image-20240814222712964.png" alt="image-20240814222712964" style="zoom:80%;" />



###### 实例：



**正交信号抗噪声**![image-20240814224319572](./嵌入式软件学习.assets/image-20240814224319572.png)，*当一相不变，另一相由于干扰信号来回跳动，导致CNT加减来回波动，上升沿加下降沿减，总体计数未变化。并不会影响总计数值*





###### 正向不翻转

![image-20240814223925610](./嵌入式软件学习.assets/image-20240814223925610.png)



###### 反向翻转

- 在外部电路接错的情况下。软件改变方向
- **配置上升压不反相。下降沿进行反向**，在下图需要将A相电平进行翻转查表

![image-20240814225249974](./嵌入式软件学习.assets/image-20240814225249974.png)

![image-20240814224856680](./嵌入式软件学习.assets/image-20240814224856680.png)



![image-20240814225507557](./嵌入式软件学习.assets/image-20240814225507557.png)







##### 5.实验代码

初始化CNT为0，正转时++每次加4，反向时--减到0后反向溢出为65535，经过int16_t解析后为-1.

```c
void Encoder_Init(void)
{
	/*开启时钟*/
	RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);			//开启TIM3的时钟
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);			//开启GPIOA的时钟
	
	/*GPIO初始化*/
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6 | GPIO_Pin_7; //CH1/CH2
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOA, &GPIO_InitStructure);							//将PA6和PA7引脚初始化为上拉输入
	
	/*时基单元初始化*/
	TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				//定义结构体变量
	TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;     //时钟分频，选择不分频，此参数用于配置滤波器时钟，不影响时基单元功能
	TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up; //计数器模式，选择向上计数 无作用
	TIM_TimeBaseInitStructure.TIM_Period = 65536 - 1;               //计数周期，即ARR的值 ，这里为65535，方便反向计数
	TIM_TimeBaseInitStructure.TIM_Prescaler = 1 - 1;                //预分频器，即PSC的值，不分频
	TIM_TimeBaseInitStructure.TIM_RepetitionCounter = 0;            //重复计数器，高级定时器才会用到
	TIM_TimeBaseInit(TIM3, &TIM_TimeBaseInitStructure);             //将结构体变量交给TIM_TimeBaseInit，配置TIM3的时基单元
	
	/*输入捕获初始化，输入捕获 只有边沿检测 和  极性选择使用了*/
	TIM_ICInitTypeDef TIM_ICInitStructure;							//定义结构体变量
	TIM_ICStructInit(&TIM_ICInitStructure);							//结构体初始化，若结构体没有完整赋值
																	//则最好执行此函数，给结构体所有成员都赋一个默认值
																	//避免结构体初值不确定的问题
    
    
    
	TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;				//选择配置定时器通道1
	TIM_ICInitStructure.TIM_ICFilter = 0xF;							//输入滤波器参数，可以过滤信号抖动
	TIM_ICInit(TIM3, &TIM_ICInitStructure);							//将结构体变量交给TIM_ICInit，配置TIM3的输入捕获通道
    
	TIM_ICInitStructure.TIM_Channel = TIM_Channel_2;				//选择配置定时器通道2
	TIM_ICInitStructure.TIM_ICFilter = 0xF;							//输入滤波器参数，可以过滤信号抖动
	TIM_ICInit(TIM3, &TIM_ICInitStructure);							//将结构体变量交给TIM_ICInit，配置TIM3的输入捕获通道
	
	/*编码器接口配置 1.所配置的定时器   2.棉麻器模式这里选择两个通道都计数  3.IC1极性  4.IC2极性*/
    
    //此函数必须在输入捕获初始化之后进行，否则输入捕获的配置会覆盖此函数的部分配置，注意此时参数的Rising和Falling已经不代表上升沿和下降沿了，而是代表是否反相。配置的上面输入捕获的极性选择，高不翻转低翻转。 这两个参数当编码器接反时进行配置。
    //需要反向，只需要任意一路翻转
	TIM_EncoderInterfaceConfig(TIM3, TIM_EncoderMode_TI12, TIM_ICPolarity_Rising, TIM_ICPolarity_Rising);
																
																	
																	
	
	/*TIM使能*/
	TIM_Cmd(TIM3, ENABLE);			//使能TIM3，定时器开始运行
}



/**
  * 函    数：获取编码器的增量值
  * 参    数：无
  * 返 回 值：自上此调用此函数后，编码器的增量值
  每隔固定时间获取，CNT除以时间就是速度，获取的间隔时间需要小一点防止CNT溢出
  */
int16_t Encoder_Get(void)
{
	/*使用Temp变量作为中继，目的是返回CNT后将其清零*/
	int16_t Temp;
	Temp = TIM_GetCounter(TIM3);
	TIM_SetCounter(TIM3, 0);
	return Temp;
}


```































### 4.RCC系统时钟

**RCC时钟分为两部分：时钟来源、时钟分配**

![image-20240804163605532](./嵌入式软件学习.assets/image-20240804163605532.png)



#### 4.1时钟来源

- **HSE**：外部高速时钟 ：一般使用外部晶振（分为有源和无源晶振）
  1. 有源晶振有2个引脚，需要借助于外部的时钟电路(接到mcu内部的震荡电路)才能产生振荡信号，自身无法振荡。<img src="./嵌入式软件学习.assets/image-20250302104331419.png" alt="image-20250302104331419" style="zoom:50%;" />
  2. 有源晶振：只需要电源，就可输出比较好的波形。只需接X1，共地。有源晶振无需MCU起震，直接输入可靠的信号进入MCU<img src="./嵌入式软件学习.assets/image-20250302104520706.png" alt="image-20250302104520706" style="zoom:50%;" />
- **HSE**：内部高速时钟，**内部RC震荡器，受温度影响比较大，有温漂**。**当外部高速时钟故障时，会自动切换到HSI**，在系统初始化时，初步先使用内部高速时钟，再切换外部高速时钟，等外部高速时钟稳定后，再完全切换过去。
- LSI/LSE：内部外部低速时钟





![image-20240804164054272](./嵌入式软件学习.assets/image-20240804164054272.png)

#### 4.2系统时钟输入



![image-20250302105737265](./嵌入式软件学习.assets/image-20250302105737265.png)锁相环，PLLSRC--锁相环时钟源，可以选择HSI或者HSE。选择锁相环倍频系数后对所选时钟进行倍频。

![image-20240804164501101](./嵌入式软件学习.assets/image-20240804164501101.png)

- **在启动文件中的，systeminnit函数中配置：1.先使用内部8MHZ时钟，让芯片启动   2.当外部时钟准备好后，外部8MHZ经过 9 倍频到72HZ  3.系统由内部RC时钟源切换到外部时钟晶振时钟源**
- **CSS时钟安全控制**，**当发现外部时钟问题后，立即切换为内部时钟，防止系统出现错误**，CSS在定时器中PWM刹车中也有作用1，当出现时钟问题后，立即切断电机PWM输出



- SYSCLK系统时钟：外设和总线来源于此时钟。<img src="./嵌入式软件学习.assets/image-20250402203129852.png" alt="image-20250402203129852" style="zoom:50%;" />，系统时钟可以选择：**1.HSI   2.PLL锁相环 3.HSE**
- <img src="./嵌入式软件学习.assets/image-20250402203357293.png" alt="image-20250402203357293" style="zoom:50%;" />锁相环来源
- CSS安全时钟检测：当检测到外外部时钟失效后，立即切换到内部HSI。

![image-20250402202550718](./嵌入式软件学习.assets/image-20250402202550718.png)

![image-20250402203954923](./嵌入式软件学习.assets/image-20250402203954923.png)

![image-20250402204658458](./嵌入式软件学习.assets/image-20250402204658458.png)

#### 4.3时钟输入

系统时钟，进入外设或者总线，进行预分频。



![image-20240804171504248](./嵌入式软件学习.assets/image-20240804171504248.png)

以上时钟配置为固件库默认配置，主频72MHZ、AHB-72MHZ、APB2-72MHZ、ABP1-36MHZ；定时器单独开一路挂在APB1/2上，在固件库默认配置下：所有定时器的时钟输入都为72MHZ









#### 4.4其他时钟

 

##### 1.RCC时钟

时钟源：**HSE、LSE 、LSI**

<img src="./嵌入式软件学习.assets/image-20250302111533158.png" alt="image-20250302111533158" style="zoom:50%;" />





##### 2.独立看门狗

时钟源：LSI

<img src="./嵌入式软件学习.assets/image-20250302111829507.png" alt="image-20250302111829507" style="zoom:50%;" />







#### 4.5MCO内部时钟输出





**将内部时钟输出，可以通过此引脚检测系统时钟是否配置准确**

<img src="./嵌入式软件学习.assets/image-20250302112004622.png" alt="image-20250302112004622" style="zoom: 33%;" />

<img src="./嵌入式软件学习.assets/image-20250302112027692.png" alt="image-20250302112027692" style="zoom:50%;" />

















#### 4.6  SystemInit



##### 函数内部实现



![image-20250323151046683](./嵌入式软件学习.assets/image-20250323151046683.png)CR-0位



```c

  /* Set HSION bit     开启芯片内部HSI高速时钟 ，系统启动后首先依靠内部时钟源而工作的。*/
  RCC->CR |= (uint32_t)0x00000001;


  /* Reset SW, HPRE, PPRE1, PPRE2, ADCPRE and MCO bits */
#ifndef STM32F10X_CL
  RCC->CFGR &= (uint32_t)0xF8FF0000;
#else
  RCC->CFGR &= (uint32_t)0xF0FF0000;
#endif /* STM32F10X_CL */ 


  
  /* Reset HSEON, CSSON and PLLON bits */
  RCC->CR &= (uint32_t)0xFEF6FFFF;

  /* Reset HSEBYP bit */
  RCC->CR &= (uint32_t)0xFFFBFFFF;

  /* Reset PLLSRC, PLLXTPRE, PLLMUL and USBPRE/OTGFSPRE bits */
  RCC->CFGR &= (uint32_t)0xFF80FFFF;

#ifdef STM32F10X_CL
  /* Reset PLL2ON and PLL3ON bits */
  RCC->CR &= (uint32_t)0xEBFFFFFF;

  /* Disable all interrupts and clear pending bits  */
  RCC->CIR = 0x00FF0000;

  /* Reset CFGR2 register */
  RCC->CFGR2 = 0x00000000;
#elif defined (STM32F10X_LD_VL) || defined (STM32F10X_MD_VL) || (defined STM32F10X_HD_VL)
  /* Disable all interrupts and clear pending bits  */
  RCC->CIR = 0x009F0000;

  /* Reset CFGR2 register */
  RCC->CFGR2 = 0x00000000;      
#else
  /* Disable all interrupts and clear pending bits  */
  RCC->CIR = 0x009F0000;
#endif /* STM32F10X_CL */
    
#if defined (STM32F10X_HD) || (defined STM32F10X_XL) || (defined STM32F10X_HD_VL)
  #ifdef DATA_IN_ExtSRAM
    SystemInit_ExtMemCtl(); 
  #endif /* DATA_IN_ExtSRAM */
#endif 

  /* Configure the System clock frequency, HCLK, PCLK2 and PCLK1 prescalers */
  /* Configure the Flash Latency cycles and enable prefetch buffer */
  SetSysClock();

#ifdef VECT_TAB_SRAM
  SCB->VTOR = SRAM_BASE | VECT_TAB_OFFSET; /* Vector Table Relocation in Internal SRAM. */
#else
  SCB->VTOR = FLASH_BASE | VECT_TAB_OFFSET; /* Vector Table Relocation in Internal FLASH. */
#endif 
}
```









##### 系统时钟配置

在最后配置系统时钟频率的函数<img src="./嵌入式软件学习.assets/image-20250302163525759.png" alt="image-20250302163525759"  />





```c
/* #define SYSCLK_FREQ_HSE    HSE_VALUE */
/* #define SYSCLK_FREQ_24MHz  24000000 */ 
/* #define SYSCLK_FREQ_36MHz  36000000 */
/* #define SYSCLK_FREQ_48MHz  48000000 */
/* #define SYSCLK_FREQ_56MHz  56000000 */
#define SYSCLK_FREQ_72MHz  72000000
#endif


static void SetSysClock(void)  //根据上述宏定义执行不同的时钟初始化函数，来配置对应的系统时钟
{
    #ifdef SYSCLK_FREQ_HSE
      SetSysClockToHSE();
    #elif defined SYSCLK_FREQ_24MHz
      SetSysClockTo24();
    #elif defined SYSCLK_FREQ_36MHz
      SetSysClockTo36();
    #elif defined SYSCLK_FREQ_48MHz
      SetSysClockTo48();
    #elif defined SYSCLK_FREQ_56MHz
      SetSysClockTo56();  
    #elif defined SYSCLK_FREQ_72MHz
      SetSysClockTo72();
    #endif
}
```



这里是72M时钟配置函数

<img src="./嵌入式软件学习.assets/image-20250302204321235.png" alt="image-20250302204321235" style="zoom:50%;" />预取址功能与周期



![image-20250302204711962](./嵌入式软件学习.assets/image-20250302204711962.png)

```c
static void SetSysClockTo72(void)
{
  __IO uint32_t StartUpCounter = 0, HSEStatus = 0;
  
  /* SYSCLK, HCLK, PCLK2 and PCLK1 configuration ---------------------------*/    
  /* Enable HSE */    
  RCC->CR |= ((uint32_t)RCC_CR_HSEON); //使能HSE,内部MCU开始起振外部晶振
 
    
    
  /*防止起振失败超时*/
  do
  {
    HSEStatus = RCC->CR & RCC_CR_HSERDY; // 当此位标记位1，表示外部晶振启动完成
    StartUpCounter++;  
  } while( (HSEStatus == 0) &&  ( StartUpCounter != HSE_STARTUP_TIMEOUT) );
    
    
    

  if ((RCC->CR & RCC_CR_HSERDY) != RESET)
  {
    HSEStatus = (uint32_t)0x01;
  }
  else
  {
    HSEStatus = (uint32_t)0x00;
  }  
    
    
    

  if (HSEStatus == (uint32_t)0x01) //HSE启动成功
  {
      
      
    //使能预取址功能 、 取址等待时间 极少修改
      
    /* Enable Prefetch Buffer */
    FLASH->ACR |= FLASH_ACR_PRFTBE;

    /* Flash 2 wait state */
    FLASH->ACR &= (uint32_t)((uint32_t)~FLASH_ACR_LATENCY);
    FLASH->ACR |= (uint32_t)FLASH_ACR_LATENCY_2;    

 
      
      
      
    /* HCLK = SYSCLK   HCLK-AHB时钟 = 系统时钟 = 72M 不分频 */
    RCC->CFGR |= (uint32_t)RCC_CFGR_HPRE_DIV1;
      
    /* PCLK2 = HCLK     PCLK = AHB时钟 = 72M  不分频*/
    RCC->CFGR |= (uint32_t)RCC_CFGR_PPRE2_DIV1;
    
    /* PCLK1  = HCLK / 2  2分频-36M   */
    RCC->CFGR |= (uint32_t)RCC_CFGR_PPRE1_DIV2;


    /*  PLL configuration: PLLCLK = HSE * 9 = 72 MHz   配置锁相环配置--72M    RCC_CFGR_PLLXTPRE-HSE不分频   RCC_CFGR_PLLSRC--锁相环时钟选择HSE    PLL--9倍频 */
    RCC->CFGR &= (uint32_t)((uint32_t)~(RCC_CFGR_PLLSRC | RCC_CFGR_PLLXTPRE |
                                        RCC_CFGR_PLLMULL));
    RCC->CFGR |= (uint32_t)(RCC_CFGR_PLLSRC_HSE | RCC_CFGR_PLLMULL9);
      
    /* Enable PLL   使能锁相环*/
    RCC->CR |= RCC_CR_PLLON;

      
      
    /* Wait till PLL is ready   等待锁相环完全开启*/
    while((RCC->CR & RCC_CR_PLLRDY) == 0)
    {
    }
    
      
    /* Select PLL as system clock source    选择系统时钟来源  选择PLL时钟为系统时钟*/
    RCC->CFGR &= (uint32_t)((uint32_t)~(RCC_CFGR_SW));
    RCC->CFGR |= (uint32_t)RCC_CFGR_SW_PLL;    

      
      
    /* Wait till PLL is used as system clock source     等待PLL时钟切换完成*/
    while ((RCC->CFGR & (uint32_t)RCC_CFGR_SWS) != (uint32_t)0x08)
    {
    }
      
      
  }
  else
  { /* If HSE fails to start-up, the application will have wrong clock 
         configuration. User can add here some code to deal with this error */
  }
}
```









### 5.ADC



#### 5.1ADC简介

- **精度**：12位  	**类型**：逐次逼近型 	**转换时间**：1us-ad转换频率为1MHZ，ADC转换需要一定时间
- 16个-GPIO测量外部电压        2个内部信号源：内部温度传感器、内部参考电压（**1.2V基准电压**，这个基准电压是不随外部电压变化的，）-
- **1.2V基准电压**：1. 之前国民技术芯片出现AD采样不准确，通过判断1.2V采样是否准确，来判断其他ADC采样口是否采样准确   2.当外部供电不为3.3v时，可以通过1.2V进行校准，例如ADC采集1.2V发现偏低，ADC口可能都产生干扰了，根据差值对其他ADC口进行补偿
- **模拟看门狗**：判断ADC值高于或低于阈值，产生中断

![image-20240817211513334](./嵌入式软件学习.assets/image-20240817211513334.png)







 





##### 5.1STM32-ADC电路



###### 1.ADC芯片-ADC0809

**8位ADC转换芯片，支持8路通道。通过地址选择器，选择通道。再使用DAC由最高位向最低位比较确认SAR的每一位是0是1，逐次逼近输入电压。**

- 通道选择电路：3-8译码器，通过地址IO输入选择通道。因为ADC转换时间比较快，通过复用多路信号，使用一个ADC模块节约成本。分时复用

- ADC模块工作原理<img src="./嵌入式软件学习.assets/image-20240817215213409.png" alt="image-20240817215213409" style="zoom: 33%;" />：在选择外部通道后，逐次**逼近寄存器（采用二分法)**控制**DAC模块**，根**据基准电压**输出**对应电压**与外部输入电压进行比较。将最后比较相近的电压输出到外部的缓存器

- **逐次逼近-二分法：**12位ADC-**由高向第位**确定每一位是0/1，

  ​		如下图：当需要确定32对应的电压值，SAR首先将BIT7位置1，在DAC上输出电平，经过比较器高与置1，低于置0，判断BIT7为零为1；在BIT6判断为0为1（128）以上步骤从最高位到最低位每位进行比较，逐次逼近，确定每位上是0是1。
  
- 比较完成后，将SAR的值输出就是输入电压的近似值

![image-20240817215941076](./嵌入式软件学习.assets/image-20240817215941076.png)

![image-20240817214530037](./嵌入式软件学习.assets/image-20240817214530037.png)







###### 2.STM32-ADC内部电路图

- ADC输入：16路GPIO外部模拟信号输入；2路内部输入：温度、参考电压
- 模拟多路开关：相比上述ADC器件，**此模拟开关可以同时选择多路同时输入**。**规则通道可以最多同时采集16路通道电压**，**注入通道最多同时采集4路通道**。相比上述ADC器件是报菜名一道一道做菜。STM32这个是写菜单给ADC，ADC快速作完：1.规则通道，一道一道输出出来（只有一个寄存器） 2.注入通道一起输出出来（4个寄存器，一个通道一个）
- 规则通道：**只有一个数据寄存器**。当有多个通道需要转换时，需要进行DMA进行搬运，否则后面转换数据会对前面数据进行覆盖
- 注入通道：**有4个数据寄存器**，每个通道一个寄存器，不用担心数据覆盖

![image-20240817221604524](./嵌入式软件学习.assets/image-20240817221604524.png)





**外部START信号**

触发ADC采样信号

- 注入组、规则组触发源单独配置
- 触发信号源：**软件内部、有定时器主模式、外部GPIO、定时器通道等**
- 当需要定时采集ADC值，可以使用定时器主模式，纯硬件实现定时触发ADC采集，不需要每次进入定时器中断采集ADC数据。设置某个定时器更新事件到TRGO，ADC的启动转换信号为该TRGO，周期性转换。

![image-20240817223407306](./嵌入式软件学习.assets/image-20240817223407306.png)



**参考电压**

Vref±为参考电压，Vdda/Vssa为供电引脚，在芯片内部进行了连线 0-3.3V

![image-20240817223950309](./嵌入式软件学习.assets/image-20240817223950309.png)



**输入时钟**

由APB2时钟进行分频得到

<img src="./嵌入式软件学习.assets/image-20250513220215265.png" alt="image-20250513220215265" style="zoom:50%;" />

![image-20250513220243828](./嵌入式软件学习.assets/image-20250513220243828.png)



**DMA请求**

DMA硬件触发源，触发ADC转换

![image-20250513220408942](./嵌入式软件学习.assets/image-20250513220408942.png)



**逻辑图**

- 模拟看门狗：需要**设定看门通道**、**数据上下阈值**，当超过阈值置位标记位AWD，并产生中断

- 标记位

  | 标记位 | 事件                         |
  | ------ | ---------------------------- |
  | AWD    | 模拟看门狗触发               |
  | JEOC   | 注入事件转换完成             |
  | EOC    | **注入和规则通道都**转换完成 |

  

![image-20240817224559069](./嵌入式软件学习.assets/image-20240817224559069.png)







###### 3.输入通道

- **ADC的外部输入16个通道是固定的**
- 需要配置为模拟输入

如图：

![image-20240817230102980](./嵌入式软件学习.assets/image-20240817230102980.png)



###### 4.双ADC模式-待补充

可以使用两组ADC，配置成：1.双ADC模式-两个ADC同步工作	2.交叉模式-两个ADC交叉采样同一组电路，提高采样频率，交叉采样一个AD口









###### 5.规则模式-转换模式

- 在序列中可以任意指定ADC通道，使用扫描时需要指定通道数目
- 扫描模式：1.在所有序列转换完成才会置标记位 2.ADC转换非常快，需要快速将数据搬运。综上所以需要：**搭配DMA**这样每转换一个序列都可快速数据搬运

| 模式--点动/自动（单次次/连续） | 扫描方式（单通道/多通道） |
| ------------------------------ | ------------------------- |
| 单次                           | 非扫描 -- 单通道          |
| 连续                           | 扫描 -- 多通道            |



- **单次转换，非扫描模式**<img src="./嵌入式软件学习.assets/image-20240817230917042.png" alt="image-20240817230917042" style="zoom: 50%;" />点动方式：只转换一次、一个通道。在序列1设定一个通道，转换完毕产生EOC标记或中断；需要再进行一次触发，进行下一次转换。









- **连续转换，非扫描模式**<img src="./嵌入式软件学习.assets/image-20240817231027158.png" alt="image-20240817231027158" style="zoom:50%;" />**连续转换一个通道，需要值时读取数据寄存器即可**











- **单次转换，扫描模式**<img src="./嵌入式软件学习.assets/image-20240817231101560.png" alt="image-20240817231101560" style="zoom:50%;" />，**点动模式：需要指定通道数目，每触发一次扫描转换各个序列中的通道，此时需要借助DMA实时搬运数据，所有数据转换完成后产生EOC标记或中断**









- **连续转换，扫描模式**<img src="./嵌入式软件学习.assets/image-20240817231141552.png" alt="image-20240817231141552" style="zoom:50%;" />持续工作，转换多个通道







###### 6.外部ADC触发源

![image-20240817232911601](./嵌入式软件学习.assets/image-20240817232911601.png)



###### 7.数据对齐

- 因为ADC是12位，寄存器是16为，所以有两种对齐方式
- 右对齐：高位补零，**取寄存器直接为ADC数值**，一般使用这种方法
- 左对齐：地位补零，数据会扩大16备，**常用于缩减分辨率，取高八位用于计算与判断**

![image-20240817232936617](./嵌入式软件学习.assets/image-20240817232936617.png)



###### 8.转换时间

分为两个部分：采样保持 、 量化编码

- **采样时间**：采样、保持步骤，通过电容暂时锁存电压，防止在量化编码阶段电压发生变化。**时间通过软件设置，时间越大越能避免毛刺干扰**
- **量化编码时间**：**12.5个ADC周期**，**stm32为12位ADC，需要12个周期进行12比较，0.5个周期用于其他处理**
- **ADC周期**：由ADC-RCC分频决定，ADCCLK为ADC驱动时钟

![image-20240817233449655](./嵌入式软件学习.assets/image-20240817233449655.png)





###### 9.校准

- 校准一定需要在使能ADC后，ADC_cmd()
- 校准过程是固定的，在初始化后加入固定代码即可

![image-20240817234932509](./嵌入式软件学习.assets/image-20240817234932509.png)







###### 10.ADC时钟

**ADC时钟是经过APB2预分频得到的**。由于ADC**最高只能为14MHZ**，而**APB1为72MHZ所以只能6/8分频为 12MHZ、9MHZ**

<img src="./嵌入式软件学习.assets/image-20240818151331059.png" alt="image-20240818151331059" style="zoom:50%;" />

![image-20240818151713389](./嵌入式软件学习.assets/image-20240818151713389.png)











###### 11.ADC-简化原理图

- 信号输入：16个外部GPIO、2个内部输入：温度、参考电压1.2V
- 模拟开管选择：规则**最多16路、注入最多4路**
- 转换后写入寄存器：**规则1个、注入4个**
- 外部触发：软件、硬件（**定时器**、外部IO等）
- 外部时钟：由APB2分频而来，推动逐次逼近的电路运行
- 中断事件：模拟看门狗、规则和注册都转换完成<img src="./嵌入式软件学习.assets/image-20250513220733086.png" alt="image-20250513220733086" style="zoom:50%;" />
- 软件使能：ADC_cmd()

![image-20240817225322226](./嵌入式软件学习.assets/image-20240817225322226.png)













#### 5.2ADC代码



###### 程序初始化步骤

- 配置时钟：GPIO、ADC、需要配置ADC-CLK分频器（配置ADC时钟频率）
- 配置GPIO外设：模拟输入
- 配置模拟开关：将通道配置在序列中
- 配置ADC准换模块（转换模式（单次/连续）、是否扫描、几个通道、触发源、左右对齐等）
- ADC_cmd开启ADC
- 官方建议 ：开启ADC后，初始化校验ADC



###### 库函数配置

```c
 RCC_ADCCLKConfig() ; //RCC-ADC分频函数

void ADC_Init(ADC_TypeDef* ADCx, ADC_InitTypeDef* ADC_InitStruct);//ADC初始化
void ADC_Cmd(ADC_TypeDef* ADCx, FunctionalState NewState); //ADC使能启动

void ADC_DMACmd(ADC_TypeDef* ADCx, FunctionalState NewState);//ADC-DMA搬运

void ADC_ITConfig(ADC_TypeDef* ADCx, uint16_t ADC_IT, FunctionalState NewState);//中断控制，是否通向NVIC

//ADC校准函数  依次调用即可
void ADC_ResetCalibration(ADC_TypeDef* ADCx);//复位校准
FlagStatus ADC_GetResetCalibrationStatus(ADC_TypeDef* ADCx); //获取复位校准状态
void ADC_StartCalibration(ADC_TypeDef* ADCx);//开始校准
FlagStatus ADC_GetCalibrationStatus(ADC_TypeDef* ADCx); //获取开始校准状态


//软件触发ADC采样
void ADC_SoftwareStartConvCmd(ADC_TypeDef* ADCx, FunctionalState NewState);
//使能外部GPIO触发ADC采样
void ADC_ExternalTrigConvCmd(ADC_TypeDef* ADCx, FunctionalState NewState);


//ADC规则组通道配置 - 给采样序列指定GPIO通道
void ADC_RegularChannelConfig(ADC_TypeDef* ADCx, uint8_t ADC_Channel, uint8_t Rank, uint8_t ADC_SampleTime);


//ADC获取采样值 - 读取ADC数据转换寄存器值
uint16_t ADC_GetConversionValue(ADC_TypeDef* ADCx);
//获取双ADC采样值
uint32_t ADC_GetDualModeConversionValue(void);


//模拟看门狗配置
void ADC_AnalogWatchdogCmd(ADC_TypeDef* ADCx, uint32_t ADC_AnalogWatchdog); //使能模拟看门狗
void ADC_AnalogWatchdogThresholdsConfig(ADC_TypeDef* ADCx, uint16_t HighThreshold, uint16_t LowThreshold); //配置上下限阈值
void ADC_AnalogWatchdogSingleChannelConfig(ADC_TypeDef* ADCx, uint8_t ADC_Channel); //配置需要看门的通道

//开启ADC内部通道：温度、参考电压
void ADC_TempSensorVrefintCmd(FunctionalState NewState);

//ADC-间断模式
void ADC_DiscModeChannelCountConfig(ADC_TypeDef* ADCx, uint8_t Number);
void ADC_DiscModeCmd(ADC_TypeDef* ADCx, FunctionalState NewState);

//获取、清除 标记位
FlagStatus ADC_GetFlagStatus(ADC_TypeDef* ADCx, uint8_t ADC_FLAG);
void ADC_ClearFlag(ADC_TypeDef* ADCx, uint8_t ADC_FLAG);
ITStatus ADC_GetITStatus(ADC_TypeDef* ADCx, uint16_t ADC_IT);
void ADC_ClearITPendingBit(ADC_TypeDef* ADCx, uint16_t ADC_IT);



//注入通道配置 -函数名带 Injected 都为注入通道配置函数
void ADC_AutoInjectedConvCmd(ADC_TypeDef* ADCx, FunctionalState NewState);
void ADC_InjectedDiscModeCmd(ADC_TypeDef* ADCx, FunctionalState NewState);
void ADC_ExternalTrigInjectedConvConfig(ADC_TypeDef* ADCx, uint32_t ADC_ExternalTrigInjecConv);
void ADC_ExternalTrigInjectedConvCmd(ADC_TypeDef* ADCx, FunctionalState NewState);
void ADC_SoftwareStartInjectedConvCmd(ADC_TypeDef* ADCx, FunctionalState NewState);
FlagStatus ADC_GetSoftwareStartInjectedConvCmdStatus(ADC_TypeDef* ADCx);
void ADC_InjectedChannelConfig(ADC_TypeDef* ADCx, uint8_t ADC_Channel, uint8_t Rank, uint8_t ADC_SampleTime);
void ADC_InjectedSequencerLengthConfig(ADC_TypeDef* ADCx, uint8_t Length);
void ADC_SetInjectedOffset(ADC_TypeDef* ADCx, uint8_t ADC_InjectedChannel, uint16_t Offset);
uint16_t ADC_GetInjectedConversionValue(ADC_TypeDef* ADCx, uint8_t ADC_InjectedChannel);



```







###### 单次非扫描

- 由ADC-IN0通道0，对应PA0采集外部电压数据。
- 并将通道0放入规则转换序列1中，采用软件触发、单次非扫描方式

```c
//main.c
unsigned short ADC1_value = 0 ;
unsigned short volt_value = 0 ;
int main()
{
Adc_Init() ;
while (1)
{
    ADC1_value = Get_ADC1_Value() ;
    volt_value = (ADC1_value/4095)*3.3 ;
}

}




//adc.c
void Adc_Init(void)
{
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_ADC1,ENABLE); //使能ADC时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE); //使能GPIOA时钟
    RCC_ADCCLKConfig(RCC_PCLK2_Div6) ; //ADC时钟，这里选择6分频12MHZ。    对APB1（72MHZ）时钟进行分频，由于ADC时钟最大为14MHZ 这里只能6/8分频

    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AIN; //模拟输入，断开GPIO输出控制
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_0;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct) ; //配置通道0 查表IN0为PA0

    //配置模拟开关，配置转换菜单
    ADC_RegularChannelConfig(ADC1,ADC_Channel_0,1,ADC_SampleTime_7Cycles5) ; //配置ADC1的-通道0-放入规则通道序列1位置-采样时间为7周期  ： 采样时间越短，转换越快精度较差； 时间越长转换慢但精度高

    //初始化ADC
    ADC_InitTypeDef ADC_InitStruct;
    ADC_InitStruct.ADC_Mode =ADC_Mode_Independent ;//独立模式   ADC模式：独立模式、双ADC模式（有很多更细分类）
    ADC_InitStruct.ADC_DataAlign = ADC_DataAlign_Right; //右对齐  数据对齐：左对齐、右对齐
    ADC_InitStruct.ADC_ExternalTrigConv = ADC_ExternalTrigConv_None;//不使用外部触发-使用软件内部触发   ADC外部触发源：时钟、外部引脚
    ADC_InitStruct.ADC_ContinuousConvMode = DISABLE; // 单次模式    模式：DISABLE-单次模式-只转换一次   ENABLE-连续模式- 一直转换
    ADC_InitStruct.ADC_ScanConvMode = DISABLE;//非扫描模式     是否开启扫描模式：DISABLE-非扫描   ENABLE-扫描通过此参数
    
    //餐单可以全部填充，但是需要转换几个AD口，通过此参数配置需要转换的个数
    ADC_InitStruct.ADC_NbrOfChannel =1; //序列中需要转换ADC个数  个数：1-16只有工作在扫描模式生效，非扫描模式下该参数无效
    ADC_Init(ADC1,&ADC_InitStruct) ;

    ADC_Cmd(ADC1,ENABLE) ;

    //校准一定在ADC使能后进行，否则一直初始不了卡在初始化
    ADC_ResetCalibration(ADC1); //复位校准
    while (ADC_GetResetCalibrationStatus(ADC1) == SET); //获取复位状态标记位是否一种为1，是则一直等待硬件复位完成         复位校准由软件置位，硬件完成校准后由硬件进行清零
    ADC_StartCalibration(ADC1); //开始校准
    while (ADC_GetCalibrationStatus(ADC1) == SET) ; //等待校准完成
    


}

unsigned short Get_ADC1_Value(void)
{
    ADC_SoftwareStartConvCmd(ADC1,ENABLE) ;//软件触发ADC1转换，单次非连续模式，每获取一次AD值需要软件触发一次
    while (ADC_GetFlagStatus(ADC1,ADC_FLAG_EOC) == RESET) ; //判断规则和注册组是否转换完成 没有数据则一直等待

    return  ADC_GetConversionValue(ADC1) ; //这里数据寄存器中描述：读取数据寄存器后会自动清除标记位EOC

}
```







###### 连续非扫描模式

单通道连续非扫描模式，在初始化后软件触发一次，硬件自动一直进行ADC转换放入寄存器中

```c

void Adc_Init(void)
{
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_ADC1,ENABLE); //使能ADC时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE); //使能GPIOA时钟
    RCC_ADCCLKConfig(RCC_PCLK2_Div6) ; //ADC时钟，这里选择6分频12MHZ。    对APB1（72MHZ）时钟进行分频，由于ADC时钟最大为14MHZ 这里只能6/8分频

    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AIN; //模拟输入，断开GPIO输出控制
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_0;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct) ; //配置通道0 查表IN0为PA0

    //配置模拟开关
    ADC_RegularChannelConfig(ADC1,ADC_Channel_0,1,ADC_SampleTime_7Cycles5) ; //配置ADC1的-通道0-放入规则通道序列1位置-采样时间为7周期  ： 采样时间越短，转换越快精度较差； 时间越长转换慢但精度高

    //初始化ADC
    ADC_InitTypeDef ADC_InitStruct;
    ADC_InitStruct.ADC_Mode =ADC_Mode_Independent ;//独立模式   ADC模式：独立模式、双ADC模式（有很多更细分类）
    ADC_InitStruct.ADC_DataAlign = ADC_DataAlign_Right; //右对齐  数据对齐：左对齐、右对齐
    ADC_InitStruct.ADC_ExternalTrigConv = ADC_ExternalTrigConv_None;//不使用外部触发-使用软件内部触发   ADC外部触发源：时钟、外部引脚
    ADC_InitStruct.ADC_ContinuousConvMode = ENABLE; // 连续模式    模式：DISABLE-单次模式-只转换一次   ENABLE-连续模式- 一直转换
    ADC_InitStruct.ADC_ScanConvMode = DISABLE;//非扫描模式     是否开启扫描模式：DISABLE-非扫描   ENABLE-扫描
    ADC_InitStruct.ADC_NbrOfChannel =1; //序列中需要转换ADC个数  个数：1-16只有工作在扫描模式生效，非扫描模式下该参数无效
    ADC_Init(ADC1,&ADC_InitStruct) ;

    ADC_Cmd(ADC1,ENABLE) ;

    ADC_ResetCalibration(ADC1); //复位校准
    while (ADC_GetResetCalibrationStatus(ADC1) == SET); //获取复位状态标记位是否一种为1，是则一直等待硬件复位完成         复位校准由软件置位，硬件完成校准后由硬件进行清零
    ADC_StartCalibration(ADC1); //开始校准
    while (ADC_GetCalibrationStatus(ADC1) == SET) ; //等待校准完成


    ADC_SoftwareStartConvCmd(ADC1,ENABLE) ;//初始化后，软件触发一次ADC1转换，后续连续进行ADC转换
    


} 

unsigned short Get_ADC1_Value(void)
{
    
    return  ADC_GetConversionValue(ADC1) ; //这里数据寄存器中描述：读取数据寄存器后会自动清除标记位EOC

}

```















### 6.DMA



#### 6.1DMA简介

- 直接存储器存取（DMA）：可以直接访问：SRAM、FLASH、寄存器
- 外设寄存器->存储器（SRAM） 、 **存储器（SRAM、FLASH）->存储器（SRAM）**
- **存储器->存储器 ：使用软件触发**           **外设寄存器->存储器 ：使用硬件触发**（每个DMA触发源不同，需要区分不同的DMA通道，在stm32H7中无需这样区分）
- 

![image-20240818204330268](./嵌入式软件学习.assets/image-20240818204330268.png)







##### 1.存储器映像

- 程序是从零地址开始运行，通过BOOT0/B00T1跳帽选择地址映射，零地址映射到0x800 0000从flash开始运行，零地址映射到0x1fff f000从系统bootloader开始运行，零地址从0x2000 0000开始运行系统从RAM开始运行
- 程序flash、sram运行过程变量与外设寄存器；寄存器可以看做一个普通SRAM，CPU可以读写。但寄存器每一位后面可以控制外设，寄存器是CPU与外设桥梁
- 

![image-20240818204943805](./嵌入式软件学习.assets/image-20240818204943805.png)



##### 2.DMA框图

- DMA1-有7个通道，DMA2-5个通道 。**复用一个DMA总线，使用DMA仲裁器**在DMA同时需要搬运数据时，根据优先级选择一个通道优先搬运
- CPU可以通过AHB，访问DMA寄存器来配置DMA
- **访问存储器使用内部软件触发**，**读写外部寄存器需要外设DMA请求**
- **FLASH，在CPU或者DMA直接访问时只读（只能作为源地址）**，只有在使用FLASH外设时进行擦写

![image-20240818211811836](./嵌入式软件学习.assets/image-20240818211811836.png)







##### 3.DMA框图

- **外设站点、存储器站点**：只是命名区分相当于两个点，任意设置是falsh、ram、寄存器地址
- **方向**：规定数据搬运方向 ：**外设寄存器到存储器** 、**存储器到外设寄存器**、**存储器到存储器（FLASH只读，只能作为源地址）**
- **站点配置**：789
  
  1. 起始地址：设定两点位置
  2. 数据宽度：字节-8位、半字-16位、字-32位
  3. 地址是否自增：搬运完成后，设定的起始地址是否自增。**外设寄存器不可配置，会自增到其他寄存器**
  
- **传输计数器**：

  1. 自减计数器，用于设定传输次数。
  2. **当计数清零，起始地址复位到初始值，准备新一轮传输**    
  3.  **修改计数器值：必须DMA_cmd禁能后，再修改**

  

- **自动重装载器**：计数器清零后，自动重装。**相当于开启了连续模式**。

  

- **M2M**:

  - 1 - 软件触发：**一般用于存储器到储存器** 与ADC外设软件触发不同，这里是连续不断触发DMA最快速度搬运完数据，将传输计数器清零。**不可与自动重装载器一起用**，不然一直传输无法停止
  - 0 - 硬件触发：外设触发

- **传输条件：开关使能、触发有效、计数器非零**

- 计数器一定要将DMA关闭后才能修改

![image-20240818213907227](./嵌入式软件学习.assets/image-20240818213907227.png)





##### 4.DMA请求

- 由DMA_cmd()使能

- 可由硬件、软件触发DMA

- **硬件触发：1.不同通道的硬件触发源不同     2.每个硬件触发源都进入或门，任一触发源有效，触发DMA（每个通道有且只有一个硬件触发信号有效不然会出现问题）**

- ![image-20240825205454440](./嵌入式软件学习.assets/image-20240825205454440.png)在使用DMA通道时，需要开启外设触发源到DMA的型号

  如上图有：定时器通道、ADC通道；假如开启定时器 **ADC_DMACmd（）；**，**用于开启DMA硬件触发信号源**
  
  ```c
      USART_DMACmd(USART1,USART_DMAReq_Rx ,ENABLE) ; //开启DMA接受的硬件触发源
      USART_DMACmd(USART1,USART_DMAReq_Tx ,ENABLE) ; //开启DMA发送的硬件触发源
  ```
  
  

![image-20240819211956681](./嵌入式软件学习.assets/image-20240819211956681.png)

![image-20250330095428927](./嵌入式软件学习.assets/image-20250330095428927.png)

##### 5.DMA传输传输大小

- **传输位数相等，正常传输**
- **小传大，高位补零**
- **大传小，舍弃高位**

![image-20240819212825077](./嵌入式软件学习.assets/image-20240819212825077.png)



##### 6.DMA + ADC

DMA与ADC常一起使用，因为连续转换中数据会被覆盖，加入DMA后可以分时复用DR寄存器。

![image-20250329225216411](./嵌入式软件学习.assets/image-20250329225216411.png)



#### 6.2DMA程序设计

##### 库函数备注

```c
//配置传输计数器 -需要DMA_cmd禁能修改计数器，修改后再使能
void DMA_SetCurrDataCounter(DMA_Channel_TypeDef* DMAy_Channelx, uint16_t DataNumber); 
//获取传输计数器值
uint16_t DMA_GetCurrDataCounter(DMA_Channel_TypeDef* DMAy_Channelx);




//配置结构体初始化
void DMA_DeInit(DMA_Channel_TypeDef* DMAy_Channelx);
void DMA_Init(DMA_Channel_TypeDef* DMAy_Channelx, DMA_InitTypeDef* DMA_InitStruct);
void DMA_StructInit(DMA_InitTypeDef* DMA_InitStruct); 

//DMA外设使能
void DMA_Cmd(DMA_Channel_TypeDef* DMAy_Channelx, FunctionalState NewState);

//开启中断、半传输中断、全传输中断
void DMA_ITConfig(DMA_Channel_TypeDef* DMAy_Channelx, uint32_t DMA_IT, FunctionalState NewState);

//获取、清除标记位
FlagStatus DMA_GetFlagStatus(uint32_t DMAy_FLAG);
void DMA_ClearFlag(uint32_t DMAy_FLAG);
ITStatus DMA_GetITStatus(uint32_t DMAy_IT);
void DMA_ClearITPendingBit(uint32_t DMAy_IT);

```



##### 数据搬运

存储器到存储器

![image-20240819214350732](./嵌入式软件学习.assets/image-20240819214350732.png)



该实验：将一个数组内数据搬运到另一个数据   

过程注意：外设站点和存储器站点，使能自增的配置不一样：DMA_PeripheralInc_Enable 、DMA_MemoryInc_Enable（使用不同的寄存器配置）

```c


uint8_t arr_a[8] = {1,2,3,1,5,6,7,8};
uint8_t arr_b[8] = {0};
int main()
{
Init_Dma((uint32_t)arr_a,(uint32_t)arr_b,8) ;
while (1)
{
}
}



void Init_Dma(uint32_t addr_a,uint32_t  addr_b,uint16_t dma_num)
{
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_DMA1,ENABLE) ;


    DMA_InitTypeDef DMA_InitStruct;
    //外设站点
    DMA_InitStruct.DMA_PeripheralBaseAddr = addr_a;//外设站点基地址
    DMA_InitStruct.DMA_PeripheralDataSize = DMA_PeripheralDataSize_Byte ; //一字节  搬运数据宽度：字节、半字、字
    DMA_InitStruct.DMA_PeripheralInc = DMA_PeripheralInc_Enable;//自增  外设站点是否自增

    //存储器站点
    DMA_InitStruct.DMA_MemoryBaseAddr = addr_b;//储存器站点基地址
    DMA_InitStruct.DMA_MemoryDataSize = DMA_PeripheralDataSize_Byte;//一字节  搬运数据宽度：字节、半字、字
    DMA_InitStruct.DMA_MemoryInc = DMA_MemoryInc_Enable;//自增  储存器站点是否自增!!! 这里注意外设站点与源地址的自增使能Enable不同

    //目的地DMA_DIR_PeripheralDST  原地址DMA_DIR_PeripheralSRC
    DMA_InitStruct.DMA_DIR = DMA_DIR_PeripheralSRC ;//源地址      外设地址是源地址还是目的地址：     
    
    DMA_InitStruct.DMA_BufferSize = dma_num; //传输次数     传输计数器次数0-65535
    
    DMA_InitStruct.DMA_M2M = DMA_M2M_Enable; //M2M模式-存储器到存储器-软件触发        DMA_M2M_Enable-软件触发  DMA_M2M_Disable-硬件触发     
    DMA_InitStruct.DMA_Mode = DMA_Mode_Normal; //正常模式，不使用重装计数器   DMA_Mode_Normal-正常模式不使用重装    DMA_Mode_Circular-循环模式使用重装  注意：M2M模式不可使用重装，否则一直无法停止
    DMA_InitStruct.DMA_Priority = DMA_Priority_Medium; //低优先级    DMA通道优先级：有四个优先级 HIHG-LOW

    DMA_Init(DMA1_Channel2,&DMA_InitStruct); //DMA1_Channel1 配置DMA1-cha1

    DMA_Cmd(DMA1_Channel2,ENABLE) ;//使能DMA1-通道1  立即进行搬运
}
```







##### 软件内触发多次转运

基于上面数据搬运程序进行修改，当被搬运数值发生改变时，**软件触发DMA再次搬运：重新对自减计数器复制，前提需要先关闭DMA通道**

```c

uint8_t arr_a[8] = {1,2,3,4,5,6,7,8};
uint8_t arr_b[8] = {0};
int main()
{
Init_Dma((uint32_t)arr_a,(uint32_t)arr_b,8) ;

for (int8_t i = 0; i < 8; i++)
{
    arr_a[i] = 8 - i;
}

Enabledma(8); //触发在此转运
while (1)
{
}
}


void Enabledma(uint16_t size)
{
    //转运三大条件 ： 自减计数器、DMA_cmd、触发源（软件/硬件外设）
    DMA_Cmd(DMA1_Channel2,DISABLE) ; //在配置自减计数器时，需要先禁能DMA！！！
    DMA_SetCurrDataCounter(DMA1_Channel2,size) ; //赋值自减计数器
    DMA_Cmd(DMA1_Channel2,ENABLE) ; 
    while (DMA_GetFlagStatus(DMA1_FLAG_TC2) == RESET); //获取DMA1-CH2通道转换完成标记位  等待DMA转换完成
    DMA_ClearFlag(DMA1_FLAG_TC2) ;  //清除转换完成标记位
}
```









##### ADC扫描模式 + DMA

规则连续扫描模式，不断往ADC_DR寄存器中填值，同时触发DMA搬运数据，搬运完成后计数器清零，自动重装载寄存器重新填值

![image-20240819214500815](./嵌入式软件学习.assets/image-20240819214500815.png)





软件触发ADC工作在连续扫描模式，每转换一个序列ADC自动触发DMA进行搬运到RAM数组中

- 时钟配置：ADC、DMA、ADC分频、GPIO
- GPIO配置：模拟输入
- ADC配置-模拟开关：配置序列
- ADC配置-初始化ADC：连续扫描模式，连续扫描3个序列
- DMA配置：源地址ADC-DR寄存器，目的地址RAM数组，循环工作模式、外部硬件触发；注意DMA硬件触发源是固定的
- 使能：DMA、ADC、ADC-DMA硬件触发信号
- 校准ADC：一定在使能ADC后
- 软件触发ADC

```c
uint16_t Adc_data[3] ={0} ; //用于存储数据

void Adc_Init(void)
{
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_ADC1,ENABLE); //使能ADC时钟
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE); //使能GPIOA时钟
    RCC_ADCCLKConfig(RCC_PCLK2_Div6) ; //ADC时钟，这里选择6分频12MHZ。    对APB1（72MHZ）时钟进行分频，由于ADC时钟最大为14MHZ 这里只能6/8分频
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_DMA1,ENABLE) ; //DMA-时钟配置

    GPIO_InitTypeDef GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AIN; //模拟输入，断开GPIO输出控制
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_0 | GPIO_Pin_1 | GPIO_Pin_2; 
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct) ; //同时配置PA0-2 //IN0-IN2同时配置三个通道


    //配置模拟开关 - 将三个通道放入序列中
    ADC_RegularChannelConfig(ADC1,ADC_Channel_0,1,ADC_SampleTime_7Cycles5) ; //配置ADC1的-通道0-放入规则通道序列1位置-采样时间为7周期  ： 采样时间越短，转换越快精度较差； 时间越长转换慢但精度高
    ADC_RegularChannelConfig(ADC1,ADC_Channel_1,2,ADC_SampleTime_7Cycles5) ; 
    ADC_RegularChannelConfig(ADC1,ADC_Channel_2,3,ADC_SampleTime_7Cycles5) ; 



    //初始化ADC
    ADC_InitTypeDef ADC_InitStruct;
    ADC_InitStruct.ADC_Mode =ADC_Mode_Independent ;//独立模式   ADC模式：独立模式、双ADC模式（有很多更细分类）
    ADC_InitStruct.ADC_DataAlign = ADC_DataAlign_Right; //右对齐  数据对齐：左对齐、右对齐
    ADC_InitStruct.ADC_ExternalTrigConv = ADC_ExternalTrigConv_None;//不使用外部触发-使用软件内部触发   ADC外部触发源：时钟、外部引脚
    ADC_InitStruct.ADC_ContinuousConvMode = ENABLE; // 连续模式    模式：DISABLE-单次模式-只转换一次   ENABLE-连续模式- 一直转换
    ADC_InitStruct.ADC_ScanConvMode = ENABLE;//扫描模式     是否开启扫描模式：DISABLE-非扫描   ENABLE-扫描
    ADC_InitStruct.ADC_NbrOfChannel =3; //序列中需要转换ADC个数  个数：1-16只有工作在扫描模式生效，非扫描模式下该参数无效
    ADC_Init(ADC1,&ADC_InitStruct) ;






    /*DMA配置：
    1.源地址为寄存器地址，目的地址为RAM数组；使用外部触发、循环模式  
    2.需要根据规定的外部硬件触发源选择DMA通道
    */
    DMA_InitTypeDef DMA_InitStruct;
    //外设站点
    DMA_InitStruct.DMA_PeripheralBaseAddr = (uint32_t) (&(ADC1->DR));//外设站点基地址 ：ADC数据规则通道数据寄存器
    DMA_InitStruct.DMA_PeripheralDataSize = DMA_PeripheralDataSize_HalfWord ; //半字-寄存器为16位  搬运数据宽度：字节、半字、字
    DMA_InitStruct.DMA_PeripheralInc = DMA_PeripheralInc_Disable;//不自增  ，寄存器地址不进行自增

    //存储器站点
    DMA_InitStruct.DMA_MemoryBaseAddr = (uint32_t) Adc_data;//储存器站点基地址
    DMA_InitStruct.DMA_MemoryDataSize = DMA_MemoryDataSize_HalfWord;//半字  搬运数据宽度：字节、半字、字
    DMA_InitStruct.DMA_MemoryInc = DMA_MemoryInc_Enable;//自增  储存器站点是否自增!!! 这里注意外设站点与源地址的自增使能Enable不同

    DMA_InitStruct.DMA_DIR = DMA_DIR_PeripheralSRC ;//源地址      外设地址是源地址还是目的地址：目的地DMA_DIR_PeripheralDST  原地址DMA_DIR_PeripheralSRC
    DMA_InitStruct.DMA_BufferSize = 3; //传输3次-重新搬运     传输计数器次数0-65535
    
    DMA_InitStruct.DMA_M2M = DMA_M2M_Disable; //外部硬件触发       DMA_M2M_Enable-软件触发  DMA_M2M_Disable-硬件触发     
    DMA_InitStruct.DMA_Mode = DMA_Mode_Circular; //循环模式，使用重装计数器            DMA_Mode_Normal-正常模式不使用重装    DMA_Mode_Circular-循环模式使用重装  注意：M2M模式不可使用重装，否则一直无法停止
    DMA_InitStruct.DMA_Priority = DMA_Priority_Medium; //中优先级    DMA通道优先级：有四个优先级 HIHG-LOW
    DMA_Init(DMA1_Channel1,&DMA_InitStruct); //这里只能是DMA1-CH1，参考手册中定死了，每个DMA搬运外部硬件触发源



    DMA_Cmd(DMA1_Channel1,ENABLE) ;//使能DMA1-通道1  
    ADC_DMACmd(ADC1,ENABLE); //开启ADC-DMA的硬件触发信号
    ADC_Cmd(ADC1,ENABLE) ;//开启ADC

    //ADC校准-一定需要在ADC使能后进行初始化
    ADC_ResetCalibration(ADC1); //复位校准
    while (ADC_GetResetCalibrationStatus(ADC1) == SET); //获取复位状态标记位是否一种为1，是则一直等待硬件复位完成         复位校准由软件置位，硬件完成校准后由硬件进行清零
    ADC_StartCalibration(ADC1); //开始校准
    while (ADC_GetCalibrationStatus(ADC1) == SET) ; //等待校准完成

    ADC_SoftwareStartConvCmd(ADC1,ENABLE) ;//初始化后，软件触发一次ADC1转换，ADC工作在连续模式，ADC控制DMA搬运

}

```







### 7.存储器



#### 7.1存储器映射



- 每个地址的映射，ARM将每个地址进行编制，SOC厂商根据规定的地址分配。
- 主要是使用三个Block，每个Block-512M
- Block0 --- 用于Flash ，**存储程序 0x0800 0000** ；            **bootloader区 0x1FFF F000**                **选项字节 0X1FFF F800 大小为16个字节**
- Block1 --- 用于SRAM ， **RAM取 0x2000 0000** 
- Block2 --- 用于片上外设，**片上外设 0x4000 0000** 
- Block7 --- 用于片上外设，**内核外设 0xE000 0000** 

**![image-20250217203931540](./嵌入式软件学习.assets/image-20250217203931540.png)**

![image-20250217204233163](./嵌入式软件学习.assets/image-20250217204233163.png)



##### 外设映射

外设地址根据总线分类,AHB/APB1为高速总线，APB2为低速总线。

**寄存器地址 = 总线地址(APB1/APB2/AHP1)+外设偏移地址(相对总线地址) + 具体寄存器偏移地址（相对外设基地址）**



![image-20250217205802790](./嵌入式软件学习.assets/image-20250217205802790.png)





![image-20250217210833955](./嵌入式软件学习.assets/image-20250217210833955.png)



![image-20250217211348099](./嵌入式软件学习.assets/image-20250217211348099.png)

![image-20250217211504977](./嵌入式软件学习.assets/image-20250217211504977.png)



#### 7.1片内FLASH

在操作FLASH过程中，CPU不执行主函数和中断，所以在汇川技术会对需要存储的值进行停机存储，也就是**性能不进行中断发波时存数据。也使用EEP存储数据。**





##### 7.1简介

![image-20250511155451587](./嵌入式软件学习.assets/image-20250511155451587.png)





- <img src="./嵌入式软件学习.assets/image-20240826221438836.png" alt="image-20240826221438836" style="zoom:50%;" />flash包含：**选项字节（配置主程序存储flash读写保护）、系统存储器bootloader、程序flash区** 
- 闪存外设可修改：**选项字节（配置主程序存储flash读写保护）、程序存储区； **bootloader不可进行修改**
- IAP：用户自己编写的bootloader程序，简称flash-boot。可以通过任意接口进行程序升级。
- ICP：JTAG/SWD/bootloder  直接更新存储器的全部内容

![image-20240826221349229](./嵌入式软件学习.assets/image-20240826221349229.png)



**下表需要根据不同的芯片，查找专门的官方PDF-闪存编辑手册**，不同芯片内存大小不一样，页数和页大小也不一样

- 读flash：直接使用指针去地址值即可   

- 写flash：需要使用闪存接口-FLASH外设 操作 FLAH存储介质进行擦除 

- boot区-2K    用户选择字节-16个字节

  

![image-20240826223159682](./嵌入式软件学习.assets/image-20240826223159682.png)



每页的起始地址结尾都是 000 、400 、800 、C00

![image-20250517124632919](./嵌入式软件学习.assets/image-20250517124632919.png)







##### 7.2基本结构图



- 不可擦除BOOT区
- 通过控制选项字节配置 ， 来配置程序存储区的读写保护



![image-20240826224009668](./嵌入式软件学习.assets/image-20240826224009668.png)



##### 7.2加解锁

- 需要在操作FLASH之前解锁，操作完后再加锁
- 芯片复位上电后，默认是上锁的，上锁会锁住：FPEC-键值寄存器 、FLASH-CR控制寄存器
- 需要先写入K1，再写入K2，错误操作序列会直接锁住芯片，无法解锁，需要重新复位。
- 加锁需要操作：**CR寄存器的LOCK位，这样可以再次解锁**。如果是通过操作错误键值来加锁会直接锁住FLASH

![image-20240826224925872](./嵌入式软件学习.assets/image-20240826224925872.png) 





```c

//解锁
void FLASH_Unlock(void)
{

  FLASH->KEYR = FLASH_KEY1;
  FLASH->KEYR = FLASH_KEY2;

}


//加锁
void FLASH_Lock(void)
{
  FLASH->CR |= CR_LOCK_Set;
}
```





##### 7.3使用指针访问存储器

```c
//读     使用 __IO 防止编译器优化
uint16_t DATA = * ( (__IO uint16_t *)  0x800 0000  ) //将该地址以uint16_t的方式读出
uint8_t DATA = * ( (__IO uint8_t *)  0x800 0000  ) //将该地址以uint8_t的方式读出
    
    
    
//写  需要进行一系列步骤后，对地址进行操作
   * ( (__IO uint8_t *)  0x800 0000  ) = 0x1234 ; 
```

![image-20240826225946474](./嵌入式软件学习.assets/image-20240826225946474.png)



##### 7.4程序编程过程

- **写之前需要先对操作页进行擦除，除非写入的值为0**
- 如果写入地址写保护（配置选项字节），不允许操作









###### 1.全擦除

- 程序存储器flash所有内容都进行擦除
- 库函数直接**进行解锁K1K2**，再将**MER/STRT位置1**后外设开始进行工作，循环判断BSY是否为0，擦除完成
- MER = 1设置全擦除
- STRT = 1 启动外设

![image-20240827223938590](./嵌入式软件学习.assets/image-20240827223938590.png)





- 库函数全擦除没有解锁和加锁操作，需要用户配置

```c
	status = FLASH_WaitForLastOperation(EraseTimeout); //等待忙
  if(status == FLASH_COMPLETE)
  {
     FLASH->CR |= CR_MER_Set;  //设置MER位 - 全擦除
     FLASH->CR |= CR_STRT_Set; //启动外设工作
    
    status = FLASH_WaitForLastOperation(EraseTimeout);

    FLASH->CR &= CR_MER_Reset;//清除MER位，防止影响后续其他操作
```









###### 2.页擦除

- 库函数直接**进行解锁K1K2**，再将**MER/STRT位置1,AR设置擦除页首地址**后外设开始进行工作，循环判断BSY是否为0，擦除完成
- PER = 1 设置为页擦除
- AR = 需要擦除页首地址
- STRT = 1 启动外设

![image-20240827224409535](./嵌入式软件学习.assets/image-20240827224409535.png)



库函数页擦除没有解锁和加锁操作，需要用户配置

```c

//指定页的起始地址，擦除该页
FLASH_Status FLASH_ErasePage(uint32_t Page_Address)
{


  status = FLASH_WaitForLastOperation(EraseTimeout); //等待忙
  
  if(status == FLASH_COMPLETE) //芯片空闲
  { 
    FLASH->CR|= CR_PER_Set; //PER-设置页擦除
    FLASH->AR = Page_Address; //写入起始地址
    FLASH->CR|= CR_STRT_Set; //启动外设
   
    status = FLASH_WaitForLastOperation(EraseTimeout);
    FLASH->CR &= CR_PER_Reset;//清除PER位，防止影响后续其他操作
  }
  return status;
}


```









###### 3.写过程

- 首先进行**解锁K1K2,置PG为1（写模式），在对应地址下写入半字（16位），等待外设完成写入。**
- **因为写入的最小单位是半字，如果是写入字可以操作两次半字写入，如果是写入字节可以读取一页放入RAM中，在RAM中对整页数据进行字节操作操作，在循化调用下方函数，将修改好的整页数据写入FLASH中**
- 在写入数据时，flash会判断是否进行了擦除，如果未进行擦除就写入数据，写入会失败。



![image-20240827225131835](./嵌入式软件学习.assets/image-20240827225131835.png)



```c
  //写入半字
status = FLASH_WaitForLastOperation(ProgramTimeout);
  if(status == FLASH_COMPLETE)
  {
    FLASH->CR |= CR_PG_Set; 	//PG位置1
  
    *(__IO uint16_t*)Address = Data; //在规定地址写入数据
      
    status = FLASH_WaitForLastOperation(ProgramTimeout);
    

    FLASH->CR &= CR_PG_Reset;
  } 



//写入字 先写低16位再写高16位
 if(status == FLASH_COMPLETE)
  {

    FLASH->CR |= CR_PG_Set; //PG写模式
  
    *(__IO uint16_t*)Address = (uint16_t)Data; //写入低16位

    status = FLASH_WaitForLastOperation(ProgramTimeout);
 
    if(status == FLASH_COMPLETE)
    {

      tmp = Address + 2; //往后移两个字节 大端模式 高位在高字节

      *(__IO uint16_t*) tmp = Data >> 16;
    

      status = FLASH_WaitForLastOperation(ProgramTimeout);
        

      FLASH->CR &= CR_PG_Reset;
    }
    else
    {
      FLASH->CR &= CR_PG_Reset;
    }
  }   


//写入字节
FLASH_Status FLASH_ProgramOptionByteData(uint32_t Address, uint8_t Data)
{
  FLASH_Status status = FLASH_COMPLETE;
  /* Check the parameters */
  assert_param(IS_OB_DATA_ADDRESS(Address));
  status = FLASH_WaitForLastOperation(ProgramTimeout);

  if(status == FLASH_COMPLETE)
  {
    /* Authorize the small information block programming */
    FLASH->OPTKEYR = FLASH_KEY1;
    FLASH->OPTKEYR = FLASH_KEY2;
    /* Enables the Option Bytes Programming operation */
    FLASH->CR |= CR_OPTPG_Set; 
    *(__IO uint16_t*)Address = Data;
    
    /* Wait for last operation to be completed */
    status = FLASH_WaitForLastOperation(ProgramTimeout);
    if(status != FLASH_TIMEOUT)
    {
      /* if the program operation is completed, disable the OPTPG Bit */
      FLASH->CR &= CR_OPTPG_Reset;
    }
  }
  /* Return the Option Byte Data Program Status */
  return status;
}
```



##### 7.5选项字节

- xxx/nxxx:在写入xxx寄存器时，需要在nxxx寄存器中写入反码，这个由硬件自动操作。
- RDP与nRPD：在RDP写入值，需要在**nRPD写入该值的反码**；**反码会硬件自动写入**
- RDP：当为0X000000A5时，FLASH解除读保护，可以被读取。当RDP不是0x0000 00A5时，FLASH区不可被读取。
- WRP0/1/2/3：每一位配置每一页的**写保护**  **不同容量的芯片不一样，小容量只用wrp0,部分大容量最后一位直接写保护一大片区域**

![image-20240827225954494](./嵌入式软件学习.assets/image-20240827225954494.png)



###### 1.选项字节---擦除

- 判断外设是否在忙
- 解锁flash-K1K2  FPEC寄存器写入两个键值
- 解锁选项字节小锁-OPTWRE中的K1K2两把锁，需要按顺序写入
- OPTER = 1 ，表示模式为：擦除选项字节
- STRT = 1 ，启动外设进行擦除
- 等待BSY擦除完成

![image-20240827231108427](./嵌入式软件学习.assets/image-20240827231108427.png)



```c
  //省略： 解除FLASH写入锁 FPEC

status = FLASH_WaitForLastOperation(EraseTimeout);
  if(status == FLASH_COMPLETE)
  {
    FLASH->OPTKEYR = FLASH_KEY1; //解小锁
    FLASH->OPTKEYR = FLASH_KEY2;
    
    FLASH->CR |= CR_OPTER_Set; //设置OPTER位
    FLASH->CR |= CR_STRT_Set; //启动外设

    status = FLASH_WaitForLastOperation(EraseTimeout);
  }
```





###### 2.选项字节---写入

- 判断外设是否在忙
- 解锁flash-K1K2
- 解锁选项字节小锁-OPTWRE中的K1K2两把锁，需要按顺序写入
- OPTPG = 1 ，表示模式为：写入选项字节
- 指针操作写入值
- 等待BSY擦除完成



![image-20240827231231178](./嵌入式软件学习.assets/image-20240827231231178.png)





##### 7.6电子签名

1. **电子签名也就是STM32的唯一ID号，放在bootloader区**
2. bootloader区含有：**bootloader、芯片ID号、程序flash大小**



![image-20240827231753499](./嵌入式软件学习.assets/image-20240827231753499.png)



##### 7.7库函数备注

```c
//解锁     #define FLASH_KEY1    ((uint32_t)0x45670123)           #define FLASH_KEY2  ((uint32_t)0xCDEF89AB)
void FLASH_Unlock(void) {  FLASH->KEYR = FLASH_KEY1;  FLASH->KEYR = FLASH_KEY2; } //向K1、K2依次写入值

//上锁
void FLASH_Unlock(void) //LOCK位设置为1

//FLASH操作函数返回值
typedef enum
{ 
  FLASH_BUSY = 1, //芯片当前忙
  FLASH_ERROR_PG, //编程错误
  FLASH_ERROR_WRP, //写保护错误
  FLASH_COMPLETE, //函数执行无问题，执行完成
  FLASH_TIMEOUT //等待超时
}FLASH_Status;

//页擦除
FLASH_Status FLASH_ErasePage(uint32_t Page_Address); //参数为需要擦除页首地址

//全擦除
FLASH_Status FLASH_EraseAllPages(void);

//擦除选项字节
FLASH_Status FLASH_EraseOptionBytes(void);


//写入
FLASH_Status FLASH_ProgramWord(uint32_t Address, uint32_t Data); //写入字
FLASH_Status FLASH_ProgramHalfWord(uint32_t Address, uint16_t Data);//写入半字
FLASH_Status FLASH_ProgramOptionByteData(uint32_t Address, uint8_t Data); //写入字节



FLASH_Status FLASH_EnableWriteProtection(uint32_t FLASH_Pages);//写保护
FLASH_Status FLASH_ReadOutProtection(FunctionalState NewState);//读保护
FLASH_Status FLASH_UserOptionByteConfig(uint16_t OB_IWDG, uint16_t OB_STOP, uint16_t OB_STDBY);//选项字配置
```









#### 7.3片内SRAM编程



**1.优点：**

- 不希望修改flash中内容，可以在RAM中运行程序
- 下载与运行速度都非常快，无需和flash一样进行擦除
- 当FLASH被锁定时，使用SAM软件进行解锁
- 



**2.缺点：**

- 片内RAM空间相对较小，片外RAM扩展时钟太慢还没有内部FLASH快
- 掉电易丢失



![image-20250318182255316](./嵌入式软件学习.assets/image-20250318182255316.png)



- 可以使用SRAM软件对FLASH进行解锁，FLASH进行了写保护

![image-20250318183238010](./嵌入式软件学习.assets/image-20250318183238010.png)



![image-20250318184633888](./嵌入式软件学习.assets/image-20250318184633888.png)



##### 1.启动地址映射

通过上电时，对boot引脚的配置来映射程序启动地址



![image-20250318190245574](./嵌入式软件学习.assets/image-20250318190245574.png)









![image-20250318190422365](./嵌入式软件学习.assets/image-20250318190422365.png)







##### 2.同工程创建副本RAM调试

- **这样相当于原工程副本，文件保持与原工程一致，但是可以配置不同的工程配置**

![image-20250318192652776](./嵌入式软件学习.assets/image-20250318192652776.png)



切换不同的调试工程

![image-20250318192926174](./嵌入式软件学习.assets/image-20250318192926174.png)



##### 3.重新配置sct文件

**重新修改工程的加载域和运行域**

原来RAM空间位0x10000 各分一半给加载域和执行域

![image-20250318193742680](./嵌入式软件学习.assets/image-20250318193742680.png)





##### 4.配置中断向量表地址

**在systeminnit函数中，最后会在寄存器中配置中断向量表位置。正常使用FLASH，时无需定义，这里使用SRAM需要定义宏**

![image-20250318195013609](./嵌入式软件学习.assets/image-20250318195013609.png)

##### 5.配置下载设置

修改下载地址



![image-20250318194633996](./嵌入式软件学习.assets/image-20250318194633996.png)





##### 6.总结

1. 创建同名工程
2. 配置SCT文件，设置加载域和执行域都在RAM区
3. 设置systeminit函数最后的宏定义，将中断向量表位置修改到寄存器中 （**sct文件只是将中断向量表存放在指定位置，但是芯片寻址函数需要在寄存器的指定地址，需要在寄存器中指定中断相量表的地址**）
4. 短接BOOT调帽，两个BOOT引脚都选择高电平。，从RAM0x2000 0000启动。调整JTAG下载地址，下载程序。



### 8.看门狗WGD

- 看门狗分为：**独立看门狗。窗口看门狗**。在计数结束后，触发一次软件复位
- **喂狗本质就是重复计数器**，递减计数器，在计数器达到零前为进行喂狗，更新时间直接触发程序重启。
- 独立看门狗：**独立---使用独立的内部 LSI 时钟（内部低速时钟）**，对时间要求低。**系统自带的LSI时钟本身误差比较大**
- 窗口看门狗：需要在**精确的窗口时间内**进行喂狗，使用的是ABP1。喂狗不能块了也不能慢。



![image-20240902221920295](./嵌入式软件学习.assets/image-20240902221920295.png)





#### 8.1独立看门狗

##### 8.1.1独立看门狗电路框图



- 独立看门狗时钟来源于LSI --- 内部低速时钟，不受外部时钟与内部高速时钟影响。
- PR寄存器分频，最高256分频。
- 12位递减计数器，首先需要在重装载寄存器设定计数值，在递减计数器减到零前，通过向键寄存器写入特定值，触发重装载寄存器值填充到递减计数器。（喂狗）

![image-20240902223239341](./嵌入式软件学习.assets/image-20240902223239341.png)



##### 8.1.2键寄存器

- 控制寄存器，控制电路运行
- 避免之前的在某一位置1，控制某一功能；使用特定的值用于开启功能，避免干扰
- 写保护：同时保护另外两个配置寄存器：PR/RLR![image-20250517102231529](./嵌入式软件学习.assets/image-20250517102231529.png)





- 0XCCCC：启用独立看门狗
- 0XAAAA：喂狗
- 当干扰写入非键值，对PR/RLR进行写保护，需要写入0X5555来解除写保护。

![image-20240902223355532](./嵌入式软件学习.assets/image-20240902223355532.png)





##### 8.1.3超时时间配置

递减计数器值配置、分频系数配置

![image-20240902224441563](./嵌入式软件学习.assets/image-20240902224441563.png)

##### 8.1.4独立看门狗程序

###### 1.库函数备注



**独立看门狗使用的是芯片内部RSL时钟，在软件开启独立看门狗后，芯片会强制开启RSL时钟**

![image-20250517114131630](./嵌入式软件学习.assets/image-20250517114131630.png)



```c
//寄存器写使能函数：使能-在键寄存器中写入0x5555    禁能-键寄存器写入0x0000（这里可以是任意值，不过库函数固定为0x0000）
void IWDG_WriteAccessCmd(uint16_t IWDG_WriteAccess); 

//写预分频器 -写入PR寄存器
void IWDG_SetPrescaler(uint8_t IWDG_Prescaler);

//写重装值 - 写入RLR寄存器
void IWDG_SetReload(uint16_t Reload);

//重新装载寄存器-喂狗 ：在键寄存器写入0xAAAA,触发将RLR寄存器值写入自减寄存器中
void IWDG_ReloadCounter(void);

//独立看门狗使能 ： 键寄存器写入 0xCCCC
void IWDG_Enable(void);

//获取独立看门狗状态
FlagStatus IWDG_GetFlagStatus(uint16_t IWDG_FLAG);

//这里是RCC获取标记位函数，辅助代码编写
/*
以下是各个时钟是否准备完成标记位：

 *     @arg RCC_FLAG_HSIRDY: HSI oscillator clock ready
  *     @arg RCC_FLAG_HSERDY: HSE oscillator clock ready
  *     @arg RCC_FLAG_PLLRDY: PLL clock ready
  *     @arg RCC_FLAG_LSERDY: LSE oscillator clock ready
  *     @arg RCC_FLAG_LSIRDY: LSI oscillator clock ready

以下为查看复位标记状态位 ---- 以何种方式进行复位的

  *     @arg RCC_FLAG_PINRST: Pin reset   复位键复位--复位引脚
  *     @arg RCC_FLAG_PORRST: POR/PDR reset  上电复位、掉电复位
  *     @arg RCC_FLAG_SFTRST: Software reset  软件触发复位
  *     @arg RCC_FLAG_IWDGRST: Independent Watchdog reset  独立看门狗复位
  *     @arg RCC_FLAG_WWDGRST: Window Watchdog reset  窗口看门狗复位
  *     @arg RCC_FLAG_LPWRRST: Low Power reset  低功耗复位

*/
FlagStatus RCC_GetFlagStatus(uint8_t RCC_FLAG);
void RCC_ClearFlag(void);//清除标记位 RCC标记位一旦置位只能软件进行清除，否则标记一直存在
```



###### 2.实现代码



```c
int main()
{

flag = RCC_GetFlagStatus(RCC_FLAG_IWDGRST); //获取是否上次复位是由独立看门狗引起复位
 RCC_ClearFlag();//清除状态位，RCC标记位一旦置位只能软件进行清除，否则标记一直存在


//解除PR/RLR(分频与重装载寄存器)写保护  :库函数往键寄存器写入 0x5555---解除写保护
IWDG_WriteAccessCmd(IWDG_WriteAccess_Enable) ;

/*
1.设置分频时间、计数器值      这里设置1S复位时间，尽量使用分频系数较小，这样计算时间为小数后写入就近整数误差小

2.公式   T = 0.025ms * 分频系数 *（计数值 + 1）

3. 0.025 * 16 *  x = 1000   ===> x  = 2500

*/
IWDG_SetPrescaler(IWDG_Prescaler_16); //16分频
IWDG_SetReload(2500-1); //2499
IWDG_Enable() ; //启动看门狗 库函数往键寄存器写入 0XCCCC  ，这里另外操作了键寄存器写入非0x5555,启动写保护

IWDG_ReloadCounter();//进入循环函数之前，先进行一次喂狗更精准   向键寄存器写入0XAAAA
while(1)
{
    for (int i = 0; i < 600; i++)
    {
        for (int i1 = 0; i1 < 600; i1++)
        {
            IWDG_ReloadCounter(); //喂狗
        }
        
    }
    



}	

```







#### 8.2窗口看门狗



##### 8.2.1窗口看门狗电路框图

- 与独立看门狗不同：喂狗直接写入CR寄存器（也是递减寄存器）
- CR寄存器为7位递减寄存器，最高位（T6）为溢出标记位，当最高位由1变为0，溢出复位。
- CR（递减计数器）可以判断计数器T6是否为1（将T6视作标记位） 或者     计数器值是否大于0x04 （将T0-T6看为一个整体）
- CFR:设定最早喂狗时间<img src="./嵌入式软件学习.assets/image-20240902230437555.png" alt="image-20240902230437555" style="zoom:80%;" />，当写入C寄存器时，判断当前是不是提前喂狗（CR  大于 CFR），进行复位
- 当T6由1变为0，![image-20250517105153162](./嵌入式软件学习.assets/image-20250517105153162.png)取反后，复位信号有效。 这里表示时间溢出后，复位
- CR寄存器最高位WDGA为 看门狗使能标记位 ![image-20250517105432083](./嵌入式软件学习.assets/image-20250517105432083.png)，与门控制产生的复位信号是否起作用。

![image-20240902230105952](./嵌入式软件学习.assets/image-20240902230105952.png)





##### 8.2.2窗口看门狗工作特性

- T[0,6]：**等于0x40-产生早期唤醒中断**（在复位前，死前中断，**可以做一些操作：保存重要参数等**）；**小于0x40（等于0x3f,最高位由1变为0）产生复位**

- 早于设定值喂狗也会产生复位

- 早于或晚于窗口时间喂狗，也会产生复位

  

![image-20240903214110227](./嵌入式软件学习.assets/image-20240903214110227.png)



##### 8.2.3超时时间配置

- 当APB1时钟进入看门狗分频器前，有**固定的4096**进行分频![image-20240903215358683](./嵌入式软件学习.assets/image-20240903215358683.png)

- | 分频系数 | WDGTB设定值 |
  | -------- | ----------- |
  | 1        | 0           |
  | 2        | 1           |
  | 4        | 2           |
  | 8        | 3           |





**超时时间** = **1/36M**  *  **4096**(固定分频) * **设定分频**（WDGTB）* **递减寄存器值**（***T0 - T5***）

![image-20240903215213176](./嵌入式软件学习.assets/image-20240903215213176.png)





##### 8.1.4窗口看门狗程序



###### 1.库函数备注

![image-20250517120147659](./嵌入式软件学习.assets/image-20250517120147659.png)

```c
void WWDG_DeInit(void); //初始化配置结构体
void WWDG_SetPrescaler(uint32_t WWDG_Prescaler); //写入预分频器系数
void WWDG_SetWindowValue(uint8_t WindowValue); //写入窗口值

void WWDG_EnableIT(void); //使能中断 --- 在即将溢出前的中断 ---- 死前中断

void WWDG_SetCounter(uint8_t Counter); //喂狗 ，直接操作CR寄存器
void WWDG_Enable(uint8_t Counter); //使能窗口看门狗，需要在使能的时候喂一下狗。 即使看门狗被禁止，递减计数器依然运行，需要在初始化前重新喂一次狗


FlagStatus WWDG_GetFlagStatus(void); //获取窗口看门狗状态位
void WWDG_ClearFlag(void);//清除窗口看门狗的状态位
```



###### 2.实现代码



```c
RCC_APB1PeriphClockCmd(RCC_APB1Periph_WWDG,ENABLE); //开启窗口看门狗时钟
flag = RCC_GetFlagStatus(RCC_FLAG_WWDGRST); //获取是否上次复位是由  窗口看门狗引起复位
 RCC_ClearFlag();//清除状态位，RCC标记位一旦置位只能软件进行清除，否则标记一直存在
/* 这里想窗口看门时间为 30ms - 50ms

T1 = 50ms  = 1/36M * 4096 * 8(分频系数) * x   ====>求得 x=54.931~55

T2 = 。。。。 ===》 y = 32.958 ~  33

*/ 
WWDG_SetPrescaler(WWDG_Prescaler_8); //8分频 -  在进入窗口看门狗之前芯片默认对APB1进行4096分频

WWDG_SetWindowValue(21|0x40) ;//设置窗口值  最早喂狗时间 30ms    递减计数器--此时计数器已经减去30ms时间，窗口时间应该为20ms值为  21

WWDG_Enable( (55-1) | 0x40); //使能窗口看门狗，并在初始化时同时进行喂狗  55-1 是 T0-T5的计数值    0x40是T6的标志位值
 
while (1)
{   Delay_ms(31) ;
    WWDG_SetCounter( (55-1) | 0x40 ) ; //喂狗 放在最后 防止离初始化函数太近导致提前喂狗导致复位
}
```









#### 8.3两种看门狗的差别

两个看门狗，在开启后无法再关闭。

![image-20240903215514165](./嵌入式软件学习.assets/image-20240903215514165.png)







### 9.串口通讯

#### 9.1介绍

![image-20240905224416448](./嵌入式软件学习.assets/image-20240905224416448.png)

##### 9.1.1串口硬件电路

- 需要接GND、TX、RX ---保证正常通讯 ；当需要对子芯片进行供电时，可以增加3.3V
- 当只需要从设备A - >设备B 单线传输 ，只需要一根线
- 当电平标准不一致，需要加入电平转换芯片 （232/485）

![image-20240905224723364](./嵌入式软件学习.assets/image-20240905224723364.png)



##### 9.1.2电平标准

- **TTL、232、485代表电平标准，在软件层面都是一致的。当需要使用时加入响应的转换芯片即可相互转换**
- 

![image-20240905225316673](./嵌入式软件学习.assets/image-20240905225316673.png)

![image-20240905230746207](./嵌入式软件学习.assets/image-20240905230746207.png)

![image-20240905230805768](./嵌入式软件学习.assets/image-20240905230805768.png)







###### 485电平标准

- **采用双绞线、差分信号**
- **两根线、半双工、多点式通讯（非TTL/232串口的点对点）**
- 最远可达1200m

![image-20240905230518657](./嵌入式软件学习.assets/image-20240905230518657.png)



**485单芯片实现两个差分线实现半双工**

- 使用一个引脚控制RS485芯片的DE引脚，实现发送还是接受模式。
- 

![image-20250511175854366](./嵌入式软件学习.assets/image-20250511175854366.png)

![image-20250511180127262](./嵌入式软件学习.assets/image-20250511180127262.png)

![image-20250512202713034](./嵌入式软件学习.assets/image-20250512202713034.png)

![image-20250512202732159](./嵌入式软件学习.assets/image-20250512202732159.png)











###### 232电平标准

抗干扰，通讯速率下降，在15m左右

![image-20240905230245260](./嵌入式软件学习.assets/image-20240905230245260.png)





##### 9.1.3串口参数及时序

- 波特率：每秒传输BIT位的个数，双方约定波特率采集数据

- ![image-20240907160721833](./嵌入式软件学习.assets/image-20240907160721833.png)

- **起始结束**：数据线**空闲时为高电平**，当需要发送数据前发送**起始位（固定为低电平，打破空闲高电平)**，发送数据位完成后，发送**停止位（默认高电平，为下一帧数据做准备）**，**停止位可以设置0.5、1、1.5、2位**

- **数据位（低位先行）**：**一般为8位**，如果**开启奇偶校验功能，一共9位增加一位奇偶校验位**，**发送数据低位先行（上图发送数据为0x07）**先将地位数据发送出去

- ![image-20240907161922493](./嵌入式软件学习.assets/image-20240907161922493.png)

- 奇偶校验方式：**外设可选择-无校验/奇校验/偶校验**。例如奇校验就是 ：0010 1110 当“1”的数量为偶数时，就补1 ；为偶数时补零

  0110 1000	0 	奇校验

  0110  0101       1	奇校验

​	1011 1000	0	偶校验 

​	1011 1001	1	偶校验 

![image-20240907160500361](./嵌入式软件学习.assets/image-20240907160500361.png)



##### 9.1.4串口通讯实际波形

**起始位低电平、数据位-低位先行、结束位高电平**

- ![image-20240907163152387](./嵌入式软件学习.assets/image-20240907163152387.png)0x55
- ![image-20240907163212990](./嵌入式软件学习.assets/image-20240907163212990.png)0xAA
- ![image-20240907163537546](./嵌入式软件学习.assets/image-20240907163537546.png)当设置为偶校验时：0x55,校验位为0
- <img src="./嵌入式软件学习.assets/image-20240907163817714.png" alt="image-20240907163817714" style="zoom:80%;" />两位停止位，两帧数据分隔时间更长

![image-20240907162952996](./嵌入式软件学习.assets/image-20240907162952996.png)



#### 9.2USART外设

- 串口分为：**USART**（同步串口，**多一个时钟线向外输出时钟**）、**UART**（异步串口）
- stm32外设：当时设置外设配置后，只需向外设数据寄存器内写入数据，外设会根据设定的配置自动组包，物理层发送出去
- **数据位-8/9位（包含校验位） 、 停止位-0.5/1/1.5/2 、校验方式-无校验/奇校验/偶校验**
- 硬件流控制-在串口接受设备缓存与处理速度较慢，**独立设置一条IO控制线，当接受设备准备好后拉高IO控制线，STM32开始写入**，防止数据覆盖丢失
- STM32外设资源：**USART1**（**APB2-72M**）、**USART2**（APB2-36M)、**USART3**（APB2-36M)
- 使用FIFO触发，每次触发一次读或写中断操作一个字节数据（8位），当有大量数据时，频繁进入中断。可以编写串口FIFO，当读取8个字节刚好一帧数据触发一次FIFO中断进行读取或者写入，大大减少中断次数。
- 使用DLE中断 + 环形队列的设计方式

![image-20240907171929570](./嵌入式软件学习.assets/image-20240907171929570.png)

##### 9.2.1串口外设框图

- **串口寄存器DR在上层抽象成一个寄存器，物理上是有两个寄存器的**（读寄存器和写寄存器，两个寄存器物理地址一致）；**当我们读取DR寄存器时，实际上读取RDR（只读寄存器），当我们写入DR寄存器时。写入TDR寄存器（只写寄存器）**

- **写**：当TDR数据，被放入发送移位寄存器时，**会产生TXE标记位（发送寄存器空，该标记有效可以将新的数据装载在发送寄存器，但此时可能不会立即发送出去，需等待发送数据寄存器清空）**。等待发送移位寄存器，一位一位将数据从低位发送，**低位先行**。发送完成后再从TDR拿数据

- **读**：外设读取外部数据，放在最高位，一位一位向后移。当读取完成后，放入RDR寄存器，**产生RXNE标记位（接受寄存器非空**，接受数据寄存器有数据）

  **读写过程双缓存机制，只要发送数据寄存器为空，写入数据保证移位寄存器发送完数据立即从数据寄存器内拿数据。同理接受一样。**

![image-20240907173502144](./嵌入式软件学习.assets/image-20240907173502144.png)





##### 9.2.2USART框图

- 时钟经过波特率发射器，产生新的驱动时钟，驱动移位寄存器工作。
- 对于上层操作只操作DR寄存器，读写DR数据寄存器（底层会细分具体读-RDR，写-TDR）
- 当TDR写寄存器的数据写入移位寄存器---产生TXE标记位（发送寄存器空）
- 当接受移位寄存器接受数据后，写入RDR-----产生RXNE标记位（接受寄存器非空）

![image-20240907175845606](./嵌入式软件学习.assets/image-20240907175845606.png)



##### 9.2.3数据帧

- 可以设置 **8、9位字长**
- 设置**有无校验位**
- 一般配置：8位有效载荷 + 无校验  、9位有效载荷 + 校验

根据上面两种配置，可以产生4种搭配：

- 一般都是（保证**有效数据载荷为8位**）：**9位字长+有校验位 、8位字长 + 无校验**

![image-20240907203527490](./嵌入式软件学习.assets/image-20240907203527490.png)9位字长，可以设置停止位

![image-20240907203612945](./嵌入式软件学习.assets/image-20240907203612945.png)8位字长，可以设置停止位





##### 9.2.4停止位

停止位配置：0.5/1/1.5/2 ，用来配置停止位时间，**高速串口需要更长的停止位**来区分前后两段帧

![image-20240907204137550](./嵌入式软件学习.assets/image-20240907204137550.png)





##### 9.2.5数据采样

- 需要保证**波特率一致**
- 保证**采样在数据中间** -对其实帧进行侦查（**16倍采样频率**，在8910次为数据中心，后续数据采样以此为起点）。确保**采样频率频率符合波特率，且每次采样位置在数据的中间时刻**
- 噪声判断，噪声明显时，标记位置位NE。



###### 1.起始位侦测

- 当输入电路检测到**起始帧，进行采样对齐，就开始以波特率的采样频率进行采样**
- **在检测起始帧时，对齐采样中心**。后续按照波特率进行采样，即可在中间时刻进行采样。只要第一位对齐，后续都是对齐。
- 当检测有效后，**以8910为采样点（数据中心点）**，后续数据按照波特率进行采样都是在数据中心进行采样了（**以起始位数据中心偏移一位数据时间**）。



**检测过程：**输入电路以波特率的**16倍进行采样**，当**检测到下降沿**后，进行16次采样，在**357中进行采样、8910中进行采样，当两次采样3个数据中至少有2个0，认为起始位有效**。当两次采样**中有1**，**认为有噪声，噪声标记位NE有效**

![image-20240907210343838](./嵌入式软件学习.assets/image-20240907210343838.png)





###### 2.数据位采样

- 在起始中对齐了数据中心，后续数据采样**只需要采集8910三个位置的电压（采集数据中心）**
- **当全为1，高电平**；    **全为0，低电平**    ； **2:1---以三次采样中，两次以上的数据为所采集电平（两次为1 就为1）**

8910![image-20240907210622458](./嵌入式软件学习.assets/image-20240907210622458.png)





##### 9.2.6波特率发生器

- 产生的频率为 16倍的波特率**（用于采样**）     16 * 波特率 = f / div
- 这个寄存器配置，由库函数自动配置，无需我们计算，只需给出所需的波特率。

![image-20240907211332134](./嵌入式软件学习.assets/image-20240907211332134.png)





#### 9.3代码编写

##### 库函数

```c
//配置串口结构体
void USART_DeInit(USART_TypeDef* USARTx);
void USART_Init(USART_TypeDef* USARTx, USART_InitTypeDef* USART_InitStruct);
void USART_StructInit(USART_InitTypeDef* USART_InitStruct);
//发送函数
void USART_SendData(USART_TypeDef* USARTx, uint16_t Data);
//接受函数
uint16_t USART_ReceiveData(USART_TypeDef* USARTx);

//中断标记位
FlagStatus USART_GetFlagStatus(USART_TypeDef* USARTx, uint16_t USART_FLAG);
void USART_ClearFlag(USART_TypeDef* USARTx, uint16_t USART_FLAG);
ITStatus USART_GetITStatus(USART_TypeDef* USARTx, uint16_t USART_IT);
void USART_ClearITPendingBit(USART_TypeDef* USARTx, uint16_t USART_IT);



//使能串口
void USART_Cmd(USART_TypeDef* USARTx, FunctionalState NewState);

//串口中断配置
void USART_ITConfig(USART_TypeDef* USARTx, uint16_t USART_IT, FunctionalState NewState);

//开启串口 + DMA模式
void USART_DMACmd(USART_TypeDef* USARTx, uint16_t USART_DMAReq, FunctionalState NewState);


```



##### 代码编写



###### GPIO配置

在数据手册中，有关于复用其他外设时，GPIO需要配置的模式

![image-20241111231940338](./嵌入式软件学习.assets/image-20241111231940338.png)

###### 硬件流控

 可以有一路IO信号控制是否发送和

<img src="./嵌入式软件学习.assets/image-20241111232857265.png" alt="image-20241111232857265" style="zoom:50%;" />

![image-20241111232918223](./嵌入式软件学习.assets/image-20241111232918223.png)





##### 重定向Printf函数

**原理：**print函数底层一直调用fputc函数，重新修改fputch函数定向到用户程序，这样将原来用于电脑窗口显示重定向到串口输出

###### 关于C语言标准库与MicroLIb

![image-20241113000320128](./嵌入式软件学习.assets/image-20241113000320128.png)当需要使用到C语言标准库时，需要将Keil中的MicroLIB选项勾选。

![image-20241113000436051](./嵌入式软件学习.assets/image-20241113000436051.png)

```c
#include <stdio.h> //需要包含官方库 用于调用printf

//重写printf 1.首先加入库#include <stdio.h>  2.在重定向函数 fputc(int ch , FILE*f)
int fputc(int ch ,FILE*f) //print函数底层是一只调用fputc函数，重新修改fputch函数定向到用户程序
{
    Serial_SendByte(ch);
    return ch ;
   
}





//将数据从串口发送出去
void Serial_String(char *String)
{
    char *p = String ;
    while (*p != '\0')
    {
       Serial_SendByte(*p++) ;   
    }

}
void Serial_SendByte(unsigned short data)
{
    while (USART_GetFlagStatus(USART1,USART_FLAG_TXE) == RESET) ;//等待TXE标记位置1，数据发送寄存器空。该标记位由硬件置1，当写入数据时自动清零。
    USART_SendData(USART1,data); //USARTx->DR = (Data & (uint16_t)0x01FF) 该库函数，将数据低9位（串口发送数据最多支持9位有效载荷）以外的数据清零，将数据赋值给DR寄存器       
}

```





##### 标记位读取（轮询方式）

通过轮询读取TXE、RXNE标记位，获取串口数据寄存器状态，读取或者写入数据。这种方式需要轮询查询，效率低。



###### 串口初始化

```c
void Serial_Init(void)
{
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_USART1,ENABLE);
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE); //Uart1 对应PA9/PA10

    GPIO_InitTypeDef  GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode =  GPIO_Mode_AF_PP; //TX - 复用推挽输出
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_9;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct);

    GPIO_InitStruct.GPIO_Mode =  GPIO_Mode_IPU; //RX - 上拉输入（总线空闲时为高电平）
    GPIO_InitStruct.GPIO_Pin = GPIO_Pin_10;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA,&GPIO_InitStruct);

    //串口初始化外设
    USART_InitTypeDef USART_InitStruct;
    USART_InitStruct.USART_BaudRate = 9600 ; //直接写入所需配置的波特率 ，由库函数自动配置BRR寄存器
    USART_InitStruct.USART_HardwareFlowControl = USART_HardwareFlowControl_None; //不使用硬件流控方法
    USART_InitStruct.USART_Mode = USART_Mode_Rx|USART_Mode_Tx; //串口同时配置为接受与发送
    USART_InitStruct.USART_Parity = USART_Parity_No ; //无校验位    USART_Parity_Even：偶校验  USART_Parity_Odd：奇校验
    USART_InitStruct.USART_StopBits = USART_StopBits_1; //一位停止位
    USART_InitStruct.USART_WordLength = USART_WordLength_8b; //8位字长    一般选择8b+无校验 9b+校验位
    USART_Init(USART1,&USART_InitStruct);


    USART_Cmd(USART1,ENABLE); //开启串口外设
}
```



###### 发送接受

```c
//等到发送寄存器空，写入数据
void Serial_SendByte(unsigned short data)
{
    while (USART_GetFlagStatus(USART1,USART_FLAG_TXE) == RESET) ;//等待TXE标记位置1，数据发送寄存器空。该标记位由硬件置1，当写入数据时自动清零。
    USART_SendData(USART1,data); //USARTx->DR = (Data & (uint16_t)0x01FF) 该库函数，将数据低9位（串口发送数据最多支持9位有效载荷）以外的数据清	零，将数据赋值给DR寄存器       
}

//发送数组
void Serial_SendArr(uint8_t *arr , uint8_t length)
{
    uint8_t *p = arr ;
    while (length)
    {
        length--;
        Serial_SendByte(*p++) ; 
    }   
}

//发送字符串
void Serial_String(char *String)
{
    char *p = String ;
    while (*p != '\0')
    {
       Serial_SendByte(*p++) ;   
    }
}
```





##### 中断方式

- 将TXE、RXNE映射到串口中断，在串口中断中，判断那个标记位有效来判断是读是写。
- 由于频繁触发中断，可以使用串口自带的FIFO，这样可以设置8个字节深度的FIFO，每接受到8个数据触发一次FIFO中断，读取一次数据。

由于DSP28035芯片中。串口不支持DMA模式，改为串口FIFO中断模式，减少中断频率。





##### 串口 + DMA中断方式

[超详细！新手必看STM32-DMA配置与实验（串口使用DMA发送数据）_stm32 dma-CSDN博客](https://blog.csdn.net/qq_48361010/article/details/134240753)

[DMA详解以及在串口数据处理上的应用_串口dma-CSDN博客](https://blog.csdn.net/qq_44852376/article/details/131256571?ops_request_misc=%7B%22request%5Fid%22%3A%225ab0b7eae3be7ff25d677f864f4141fd%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=5ab0b7eae3be7ff25d677f864f4141fd&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~baidu_landing_v2~default-7-131256571-null-null.142^v102^pc_search_result_base9&utm_term=stm32串口DMA&spm=1018.2226.3001.4187)



- DMA中断优先级  > 串口中断优先级

  









###### 1.DMA发送



USART_DMACmd**命令开启CR3寄存器的DMAT/DMAR位**，实际是开启<img src="./嵌入式软件学习.assets/image-20250329231644273.png" alt="image-20250329231644273" style="zoom: 33%;" />对应**DMA通道的硬件触发源**，当对应的硬件源触发源有效（**由手册得知通过TXE/RXNE标记位触发**）后，DMA被启动传输。



- USART_DMACmd函数将**CR3**位置位

  ```c
  void USART_DMACmd(USART_TypeDef* USARTx, uint16_t USART_DMAReq, FunctionalState NewState)
  {
  
    if (NewState != DISABLE)
    {
      USARTx->CR3 |= USART_DMAReq;
    }
    else
    {
      USARTx->CR3 &= (uint16_t)~USART_DMAReq;
    }
  }
  ```

  ![image-20250511163525706](./嵌入式软件学习.assets/image-20250511163525706.png)

- 调用下列函数将DMA的硬件触发源启动。<img src="./嵌入式软件学习.assets/image-20250329231644273.png" alt="image-20250329231644273" style="zoom: 80%;" />

- ![image-20250511163429580](./嵌入式软件学习.assets/image-20250511163429580.png)

- ![image-20250511163452069](./嵌入式软件学习.assets/image-20250511163452069.png)

  









- 串口DMA发送流程，**图中TXE、TC都是DMA自动通过SR标记位实现，并不是中断实现**。所以不需要开启中断。
- 发送时，通过DMA-**TCIF全传输完成标记位**判断**DMA是否传输完成**
- 通过串口SR的 **TC发送完成标记位，判断串口发送完成**
- DMA传输完成，不代表数据都发送出去了，还需要移位寄存器将数据移位出去，需要串口**TC标记位有效后，才能判断所有数据传输完成**

![image-20250329231304645](./嵌入式软件学习.assets/image-20250329231304645.png)



- 如图在TXE是，将第一个数据F1搬运到DR寄存器中。
- 此时数据1进入移位寄存器，DR寄存器空产生TXE，第二个数据搬运到DR寄存器。
- 当数据1发送完成，数据2转移到移位寄存器，此时产生TXE，数据3搬运到DR寄存器。搬运完成产生TCIF标记位
- 但是此时数据2还在移位寄存器发送，数据3还在DR寄存器中待发送。**需要串口产生TC中断（也可以是标记位，不过中断反应更快），判断此此数据发送完成**。再进行下一步操作。

![image-20250329231319313](./嵌入式软件学习.assets/image-20250329231319313.png)





###### 2.接收



- RXNE有效立即触发DMA传输，接受完成数据与触发DMA传输类似同步进行
- 可以利用IDLE中断来判断本次传输是否完成



![image-20250330114916850](./嵌入式软件学习.assets/image-20250330114916850.png)



![image-20250330114927883](./嵌入式软件学习.assets/image-20250330114927883.png)



###### 3.代码实现

- IDLE标记位是硬件自动清除，需要先读SR再读DR寄存器
- 移植到h7等有cache的芯片，要注意DMA地址需要使用指令从内存中更新，不然会造成从cache中获取之前数据，但是内存dma搬运数据已更新。
- ![image-20250512204406352](./嵌入式软件学习.assets/image-20250512204406352.png)

```c
#include "phy_uart.h"

/**
 * @brief 该串口设计框架
 * 
 * 1.发送：使用点触式发送，DMA只工作一次。当触发写入时，先将数据放放入环形队列中，防止程序多个地方（或者多次）触发写操作。在0.5ms函数中轮询判断上一次数据是否发送完成
 *          。发送通过DMA依次将数组内容搬运到DR寄存器中，等待TC中断则这次发送完成。
 * 
 * 2.接收：使用DMA循环模式，串口与DMA不停止工作，一直等待数据。利用DMA的传输半中断 与传输全中断将数据不停地搬运到BUFF中。当出现IDLE中断时判断该帧结束。
 *          将数据放入环形队列中，等待主程序获取数据。
 * 
 * 
 * 
 */



void Phy_UartInit(void)
{

    RCC_APB2PeriphClockCmd(RCC_APB2Periph_USART1,ENABLE) ;
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE) ;
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_DMA1,ENABLE) ;


    //GPIO初始化
    GPIO_InitTypeDef  GPIO_InitStruct ;
    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AF_PP; //TX引脚 推挽输出
    GPIO_InitStruct.GPIO_Pin = UART_TX_GPIO;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz; 
    GPIO_Init(GPIOA , &GPIO_InitStruct ) ;

    GPIO_InitStruct.GPIO_Mode = GPIO_Mode_IPU; //RX引脚 上拉输入（因为串口默认上拉高电平）
    GPIO_InitStruct.GPIO_Pin = UART_RX_GPIO;
    GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz; 
    GPIO_Init(GPIOA , &GPIO_InitStruct ) ;  
    
    
    //串口初始化
    USART_InitTypeDef  USART_InitStruct ;
    USART_InitStruct.USART_BaudRate = UART_BUAD; //波特率
    USART_InitStruct.USART_HardwareFlowControl = USART_HardwareFlowControl_None; //无硬件流控制
    USART_InitStruct.USART_Mode = USART_Mode_Rx | USART_Mode_Tx ; //接受与发送都开启
    USART_InitStruct.USART_Parity = USART_Parity_No; //无校验
    USART_InitStruct.USART_StopBits = USART_StopBits_1; //1位停止位
    USART_InitStruct.USART_WordLength = USART_WordLength_8b; //8位有效载荷

    USART_Init(USART1,&USART_InitStruct) ;


    //DMA初始化 --- TX 通道4
    DMA_InitTypeDef  DMA_InitStruct ;


    DMA_InitStruct.DMA_PeripheralBaseAddr = st_phyuart_tx.buff;  //源地址 -- 发送数组 - 自增
    DMA_InitStruct.DMA_PeripheralDataSize = DMA_PeripheralDataSize_Byte; //数据宽度 字节
    DMA_InitStruct.DMA_PeripheralInc = DMA_PeripheralInc_Enable; //自增


    DMA_InitStruct.DMA_MemoryBaseAddr = &(USART1->DR);  //目的地址 数据寄存器 -- 不自增
    DMA_InitStruct.DMA_MemoryDataSize = DMA_PeripheralDataSize_Byte;
    DMA_InitStruct.DMA_MemoryInc = DMA_MemoryInc_Disable;


    DMA_InitStruct.DMA_BufferSize = st_phyuart_tx.len; //传输次数
    DMA_InitStruct.DMA_Mode = DMA_Mode_Normal; //正常模式，发送一次就不发送了，不用重装。  点触式
    DMA_InitStruct.DMA_Priority = DMA_Priority_Medium;
    DMA_InitStruct.DMA_DIR = DMA_DIR_PeripheralSRC; //外设作为源地址
    DMA_InitStruct.DMA_M2M = DMA_M2M_Disable; //不使用软件触发 --- 该模式一般用于寄存器之间的数据传输

    DMA_Init(TX_DMA_CH,&DMA_InitStruct) ;


    //DMA初始化 --- RX 通道5


    DMA_InitStruct.DMA_PeripheralBaseAddr = &(USART1->DR);  //源地址 -- 数据寄存器 - 不自增
    DMA_InitStruct.DMA_PeripheralDataSize = DMA_PeripheralDataSize_Byte;
    DMA_InitStruct.DMA_PeripheralInc = DMA_PeripheralInc_Disable;


    DMA_InitStruct.DMA_MemoryBaseAddr = st_phyuart_rx.buff;  //目的地址 接受数组 -- 自增
    DMA_InitStruct.DMA_MemoryDataSize = DMA_PeripheralDataSize_Byte;
    DMA_InitStruct.DMA_MemoryInc = DMA_MemoryInc_Enable;


    DMA_InitStruct.DMA_BufferSize = st_phyuart_rx.len; //传输次数
    DMA_InitStruct.DMA_Mode = DMA_Mode_Circular; //循环模式，重装载次数
    DMA_InitStruct.DMA_Priority = DMA_Priority_Medium;
    DMA_InitStruct.DMA_DIR = DMA_DIR_PeripheralSRC; //外设作为源地址
    DMA_InitStruct.DMA_M2M = DMA_M2M_Disable; //不使用软件触发 --- 该模式一般用于寄存器之间的数据传输

    DMA_Init(RX_DMA_CH,&DMA_InitStruct) ;




    //中断配置
    NVIC_InitTypeDef NVIC_InitStructure;

	NVIC_InitStructure.NVIC_IRQChannel = DMA1_Channel4_IRQn;				                //NVIC通道设置
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = DMA1_CH4_NVIC_ID ;				//抢占优先级
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;						                //子优先级
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;							                //IRQ通道使能
	NVIC_Init(&NVIC_InitStructure);	

    NVIC_InitStructure.NVIC_IRQChannel = DMA1_Channel5_IRQn;				                //NVIC通道设置
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = DMA1_CH5_NVIC_ID ;				//抢占优先级
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;						                //子优先级
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;							                //IRQ通道使能
	NVIC_Init(&NVIC_InitStructure);	


    NVIC_InitStructure.NVIC_IRQChannel = USART1_IRQn;				                        //NVIC通道设置
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = UART_NVIC_ID ;				    //抢占优先级
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;						                //子优先级
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;							                //IRQ通道使能
	NVIC_Init(&NVIC_InitStructure);	


    //具体中断配置
    USART_ClearFlag(USART1,USART_FLAG_TC) ; //清除标记位

    USART_ITConfig( USART1, USART_IT_IDLE, ENABLE); //中断空闲中断
    USART_ITConfig( USART1, USART_IT_TC, ENABLE); //发送完成


    DMA_ITConfig(RX_DMA_CH,DMA_IT_TC,ENABLE) ; //接受传输完成
    DMA_ITConfig(RX_DMA_CH,DMA_IT_HT,ENABLE) ; //接受一半完成





    USART_DMACmd(USART1,USART_DMAReq_Rx ,ENABLE) ; //开启DMA通达的硬件触发源
    USART_DMACmd(USART1,USART_DMAReq_Tx ,ENABLE) ;




    //使能命令
    DMA_Cmd(TX_DMA_CH,DISABLE) ;//使能DMA1-通道4   发送关闭 --- 点触式发送
    DMA_Cmd(RX_DMA_CH,ENABLE) ;//使能DMA1-通道5    接受开启 --- 循环持续运行

    USART_Cmd(USART1,ENABLE) ; //开启串口

    Tx_Flag = TR_INIT ; //传输标记初始化
    Rx_Flag = TR_INIT ;






}


//物理层发送接口
BOOL Phy_Uart_WriteData(St_PhyUart  temp_st_PhyUart)
{
    BOOL ret =  Phy_Push_Fifo(&Tx_Fifo , temp_st_PhyUart) ;
    return ret ;
}

//o.5ms循环中循环将数据发送出去
void Phy_Uart_TxData(void)
{
    St_PhyUart  temp_st_PhyUart ;

    if (Tx_Flag != TR_ING)  //串口不在发送过程中
    {
        if (Phy_Pop_Fifo(&Tx_Fifo, &temp_st_PhyUart) == TRUE)
        {
            st_phyuart_tx.len = temp_st_PhyUart.len ;
            for (uint16_t i = 0; i < st_phyuart_tx.len; i++)
            {
                st_phyuart_tx.buff[i]  =  temp_st_PhyUart.buff[i] ;
            }
        
            DMA_Cmd(TX_DMA_CH,DISABLE) ; //在修改重装载值前，需要将DMA关闭
            DMA_SetCurrDataCounter(TX_DMA_CH, st_phyuart_tx.len) ; //设定传输次数
            DMA_Cmd(TX_DMA_CH,ENABLE) ; //在开启DMA传输
            Tx_Flag = TR_ING ;
        }
    }
}




//物理层接受接口
BOOL Phy_Uart_ReadData(St_PhyUart  *temp_st_PhyUart)
{
    BOOL ret = Phy_Pop_Fifo(&Rx_Fifo,temp_st_PhyUart);
    return ret ;
    
}



void USART1_IRQHandler(void)
{
    
    if (USART_GetITStatus(USART1,USART_IT_TC) == SET) //根据串口DMA工作原理，当TC标记有效后，窗口数据完全发送到总线上
    {
        
        Tx_Flag =  TR_OK; //发送完成
        DMA_Cmd(TX_DMA_CH,DISABLE) ; //关闭串口
        USART_ClearITPendingBit(USART1,USART_IT_TC) ; //清除中标记

    }


    if (USART_GetITStatus(USART1,USART_IT_IDLE) == SET)
    {
        uint16_t temp = 0 ;
        Phy_UartGetDmaData() ; 
        Rx_Flag = TR_OK ;//接受完成
        Phy_Push_Fifo(&Rx_Fifo,st_phyuart_rx_dma); //将拼凑好的一帧数据放入队列中

        st_phyuart_rx_dma.len = 0 ; //清除搬运数组
        memset(st_phyuart_rx_dma.buff,0,UART_MAX_BUFLEN) ;

        temp = USART1->SR ; //IDLE标记位通过先读取SR、再读取DR寄存器由硬件清除
        temp = USART1->DR ;
 
    }

}

void DMA1_Channel4_IRQHandler(void)
{
    Rx_Flag = TR_ING ;//接受过程中
    Phy_UartGetDmaData() ;
}


void DMA1_Channel5_IRQHandler(void)
{
    Rx_Flag = TR_ING ;//接受过程中
    Phy_UartGetDmaData() ;
}



//拼凑DMA数据
void Phy_UartGetDmaData(void)
{
    static unsigned short temp_posbak ;
    unsigned short temp_pos = 0 ;
    unsigned short temp_len = 0 ;
    
    temp_pos  =  st_phyuart_rx.len - DMA_GetCurrDataCounter(RX_DMA_CH); //获取距离数据顶部的位置


    //前后位置变化，证明有数据变化，因为开启了半传输中断、全传输中断。不可能数据重新传输了一个来回回到之前的位置，期间一定产生中断。
    //这里也有可能：刚好数据接受完成触发全传输中断，等待一会又触发IDLE中断
    if (temp_pos != temp_posbak)
    {
            /*
             *
             * [   0   ]
             * [   1   ] <- old_pos |------------------------------------|
             * [   2   ]            |                                    |
             * [   3   ]            | Single block (len = pos - old_pos) |
             * [   4   ]            |                                    |
             * [   5   ]            |------------------------------------|
             * [   6   ] <- pos
             * [   7   ]
             * [ N - 1 ]
             */

        if (temp_pos >temp_posbak) //数据在上一位置的下方
        {
            temp_len = temp_pos - temp_posbak ; //增加的数据长度
            Phy_DmabuffAdd(st_phyuart_rx.buff+temp_posbak,temp_len) ; 
        }
        else
        {
        /*    [   0   ]            |---------------------------------|
            * [   1   ]            | Second block (len = pos)        |
            * [   2   ]            |---------------------------------|
            * [   3   ] <- pos
            * [   4   ] <- old_pos |---------------------------------|
            * [   5   ]            |                                 |
            * [   6   ]            | First block (len = N - old_pos) |
            * [   7   ]            |                                 |
            * [ N - 1 ]            |---------------------------------|
            * 数据在上一次数据上方 ， */

            Phy_DmabuffAdd(st_phyuart_rx.buff+temp_posbak,(st_phyuart_rx.len - temp_posbak)) ;  // (N - old_pos)
            Phy_DmabuffAdd(st_phyuart_rx.buff,temp_pos);  // (len = pos)
            
        }
        
        
    }
    
    temp_posbak = temp_pos ;
}


static Phy_DmabuffAdd(uint8_t * buff, unsigned short len  )
{
    for (unsigned short  i = 0; i < len; i++)
    {
        st_phyuart_rx_dma.buff[st_phyuart_rx_dma.len + i ] =  buff[i]  ;
        
    }
    st_phyuart_rx_dma.len += len ;


}





//环形队列操作
BOOL Phy_Push_Fifo( ST_PHY_FIFO  * q, St_PhyUart msg)
{
    St_PhyUart * temp_msg ; 
    if ((q->num ) && (q->get == q->put) ) //FIFO满
    {
       return FALSE ;
    }

    temp_msg = &(q->fifo[q->put]) ;

    temp_msg->len = msg.len ;

    for (uint16_t i = 0; i <  msg.len ; i++)
    {
        temp_msg->buff[i] = msg.buff[i] ;
    }
    

    

    q->put = (q->put + 1) % PHY_FIFO_MAX ;
    q->num ++ ;

    return TRUE ; 
   

}


BOOL Phy_Pop_Fifo(ST_PHY_FIFO  * q, St_PhyUart * st_data)
{
    if ((q->num  == 0) && (q->get == q->put) ) //FIFO空
    {
       return FALSE ;
    }

    st_data->len = (*q).fifo[q->get ].len ;
    for (uint16_t i = 0; i < st_data->len; i++)
    {
        st_data->buff[i] =  (*q).fifo[q->get].buff[i] ;
    }

    if (q->num > 0 ) q->num -- ;
    q->get  = (q->get + 1 ) % PHY_FIFO_MAX ;

    return TRUE ;


}

```







#### 9.4关于串口的一些实现思想

1. 最简单的是轮询的方式，从DR数据寄存器中读取和写入数据。 实时性较低，数据容易丢失。
2. 使用RXEN 与 TXN两个中断实现数据读写 ，频繁中断对其他数据处理不友好。
3. 使用FIFO中断。降低中断频率，等FIFO满后再取数据。之前出现干扰帧，4级FIFO导致缓存内一直有一个数据，需要定时清空FIFO。在DSP芯片不支持串口DMA可以使用这种方式
4. 串口 + DMA + IDLE中断   ：DMA一直搬运，串口空闲后，产生IDLE。IDLE中断中，关闭DMA，重新开启一次DMA。这种方式需要重新开启一次DMA且DMA搬运次数需要提前知道数据量的大小，防止数据丢失。
5. 串口 + DMA 中断。 DMA工作在循环模式，不需要约定数据量的大小，有数据就搬运。只需最后产生idle中断后提示软件进行处理。

[STM32---UART使用DMA数据传输_uart dma-CSDN博客](https://blog.csdn.net/li391402/article/details/117559727?spm=1001.2014.3001.5506)

[UART 串口空闲中断+DMA传输半完成、完成中断_uart dma-CSDN博客](https://blog.csdn.net/toyoutoyou001/article/details/143077429?spm=1001.2014.3001.5506)

![image-20250329113800177](./嵌入式软件学习.assets/image-20250329113800177.png)







### 10.IIC通讯



#### 10.1简介

- 同步半双工通讯，一般使用一主多从
- 由于其物理接口简单且占用线路少，I2C被广泛应用于嵌入式系统和电子设备中，方便连接各种低速外设，例如传感器、存储器、时钟芯片等。同时，I2C支持多种速度等级。
- 标准IIC速度 - **100KHZ**
- 快速IIC速度 - **400KHZ**
- SDA/SCL**上拉电阻为4.7K**
- SDA/SCL 都为开漏输出，输出同时可以输入。

![image-20250306215645710](./嵌入式软件学习.assets/image-20250306215645710.png)



#### 10.2硬件电路

- SDA： 空闲状态下，主机可以**主动**发起对SDA的控制， **从机**：只有在主机需要从机发送数据时 和 从机应答数据时控制数据线。
- SCL：时钟线，只有主机可以控制



- 为防止电路时序错误，例如设备1输出1，设备2输出0，导致道通有电流产生，出现短路。输出高电平由上拉电平决定，设备输出高阻态不控制总线。
- **硬件“线与”现象**，**当任意设备拉低电路，总线都处于低电平。只有当所有设备都输出高电平时，才输出高电平**。可以实现多主机下的时钟同步和总线仲裁。
- 由于IIC硬件结构：**开漏输出+上拉电阻结构，导致低电平到高电平变化时，需要一定时间<img src="./嵌入式软件学习.assets/image-20250313193350537.png" alt="image-20250313193350537" style="zoom: 80%;" />，这导致IIC速度受限制**。

![image-20250306215854303](./嵌入式软件学习.assets/image-20250306215854303.png)





**总线设备内部驱动电路:**

输出时mos管道通，输出低电平。输出高电平时关闭，高阻态弱上拉。

![image-20250306220453457](./嵌入式软件学习.assets/image-20250306220453457.png)





#### 10.3软件信号解析



##### 1.起始与终止信号

- 起始信号：SCL高电平时，将SDA拉低，**再将SCL拉低（除了起始与终止信号SCL都是低电平开始，方便与其他信号衔接）**
- 终止信号：将SCL信号拉高后，将SDA拉高
- 起始与终止条件都是由主机产生。

![image-20250308143749834](./嵌入式软件学习.assets/image-20250308143749834.png)





##### 2.数据位

- 只有在**SCL低电平时改变SDA**，**在SCL高电平时SDA不允许改变，此时读取数据。高电平为1，低电平为0**
- **数据是高位先行**，与串口区分，串口是低位先行
- 主机在操作完总线后，读取从机数据时，需要释放总线（SDA/SCL处于高电平），保持时钟线变化，从机回复数据。
- 在传输过程中的任意时刻，主机可以暂停时钟线的变化，此时总线也暂停。主机可以处理其他动作，随时在此恢复数据传输。
- **在实际工程中，一般读写需要尽早，因为不知道周期和持续时间。例如在硬件IIC中，SCL下降沿就开始写，SCL上升沿就开始读**

![image-20250310192232927](./嵌入式软件学习.assets/image-20250310192232927.png)





##### 3.应答位

- **需要在发送一个字节后，再产生一个应答位。0-应答 1-非应答**
- 例如：主机在发送完成一个字节后，释放总线SDA，SDA自动回拉到高电平。此时从机接受完成一个字节数据后，检测到此时SCL为低电平，将SDA拉低，产生应答。主机在下个高电平时读取此时数据位为低电平。如果此时没有从机应答，SDA自动会拉到高电平，主机读取为高电平。

![image-20250310193625668](./嵌入式软件学习.assets/image-20250310193625668.png)





##### 4.设备地址

- 设备地址一般是**7位或者10位，其中7位使用的比较多**。
- 一般设备地址的最后几位，由电路编程。例如 1010 XXX ,7位地址最后3位，由3个IO引脚定义，由电路确定地址。



##### 5.指定地址写

[IIC 通信协议详解_i2c协议-CSDN博客](https://blog.csdn.net/Teminator_/article/details/141224886?spm=1001.2101.3001.6650.1&utm_medium=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~Ctr-1-141224886-blog-93789579.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~Ctr-1-141224886-blog-93789579.235^v43^pc_blog_bottom_relevance_base3)



- **指定设备，指定设备寄存器，写入数据**
- 第一位 **7位地址 + 1位读写位（0-写，1-读 主机视角）**

![image-20250310200236759](./嵌入式软件学习.assets/image-20250310200236759.png)



![image-20250310195942875](./嵌入式软件学习.assets/image-20250310195942875.png)

**具体步骤**

1. 开始信号。

2. 发送 I2C 设备地址，每个 I2C 器件都有一个设备地址，通过发送具体的设备地址来决定访问哪个 I2C 器件。这是一个 8 位的数据，其中高 7 位是设备地址，最后 1 位是读写位（为 1 的话表示这是一个读操作，为 0 的话表示这是一个写操作）。
3. 读写控制位，因为是向 I2C 从设备发送数据，因此是写信号 0。
4. 从机发送的 ACK 应答信号。
5. 发送要写入数据的**寄存器地址**。
6. 从机发送的 ACK 应答信号。
7. 发送要写入**寄存器的数据**。
8. 从机发送的 ACK 应答信号。
9. 停止信号。



##### 6.指定地址读

<img src="./嵌入式软件学习.assets/image-20250310201724366.png" alt="image-20250310201724366" style="zoom:50%;" />在写入设备地址时，**主机写入帧：设备地址+写位+寄存器地址+写入数据**。这里**从机地址寄存器**读取主机写入的**寄存器地址**，并指向线性表。将从机的写入数据写入指定的线性表中。**可以先使用写操作偏移寄存器地址，再切换读，这样实现指定地址读**



`I2C` 单字节读时序比写时序要复杂一点，读时序分为四个步骤：

- 第一步是发送设备地址，**写**
- 第二步是发送要读取的寄存器地址，**这里相当于给上述地址寄存器赋值，后续读也将从该地址开始**
- 第三步重新发送设备地址，**读**
- 第四步 从器件输出要读取的寄存器值



**具体步骤：**

1. 主机发送起始信号。

2. 主机发送要读取的 I2C 从设备地址。

3. 从机发送的 ACK 应答信号。

4. 主机发送设备寄存器地址+读写控制位，因为是向 I2C 从设备发送数据，因此是写信号 0。

5. 从机发送的 ACK 应答信号。

6. **重新发送 START 信号。**  **因为要重新改变读写方式，需要重新开启新的一帧**

7. 重新发送要读取的 I2C 从设备地址。

8. 主机发送设备寄存器地址+读写控制位，读写控制位，这里是读信号 1，表示接下来是从 I2C 从设备里面读取数据。

9. 从机发送的 ACK 应答信号。

10. 从 I2C 器件里面读取到的数据。

11. 主机发出 NACK 信号，表示读取完成，不需要从机再发送 ACK 信号了。

12. 主机发出 STOP 信号，停止 I2C 通信。

    

![image-20250310203024556](./嵌入式软件学习.assets/image-20250310203024556.png)





##### 7.总结



![image-20250310203051211](./嵌入式软件学习.assets/image-20250310203051211.png)

![image-20250310203104665](./嵌入式软件学习.assets/image-20250310203104665.png)







#### 10.4软件模拟IIC协议

##### 1.端口初始化





- SDA/SCL都配置为**开漏输出**，因为**GPIO模式中有讲到IO输出同时也可以读取输入**，所以主机想输入时，SCL保持时钟活动，SDA输出1-开漏输出，并在SCL高电平期间读取SDA状态。

```c
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
	
	GPIO_InitTypeDef GPIO_InitStructure;
 	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_OD;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8;
 	GPIO_Init(GPIOB, &GPIO_InitStructure);
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_9;
 	GPIO_Init(GPIOB, &GPIO_InitStructure);
	
	OLED_W_SCL(1);
	OLED_W_SDA(1);
```





##### 2.引脚操作宏定义

SDA/SCL引脚拉低拉高

```c
/*引脚配置*/
#define OLED_W_SCL(x)		GPIO_WriteBit(GPIOB, GPIO_Pin_8, (BitAction)(x))
#define OLED_W_SDA(x)		GPIO_WriteBit(GPIOB, GPIO_Pin_9, (BitAction)(x))
```







##### 3.基本信号类型



**为了使各个信号彼此衔接，除了起始信号，所有【操作信号最后SCL都为低电平**







**起始信号**

<img src="./嵌入式软件学习.assets/image-20250311200442917.png" alt="image-20250311200442917" style="zoom:80%;" />起始信号

<img src="./嵌入式软件学习.assets/image-20250311200514456.png" alt="image-20250311200514456" style="zoom:80%;" />上一数据结束后马上跟上一个起始信号

```c

/*
1.由于需要兼容起始条件，以及上一帧数据发送完成后紧接着一个起始条件。
2.先将SDA拉高，再拉高SCL
3.在SCL高电平期间拉低SDA --- 起始信号
4.将SCL拉低 
*/
void OLED_I2C_Start(void)
{
	OLED_W_SDA(1);
	OLED_W_SCL(1);
	OLED_W_SDA(0);
	OLED_W_SCL(0);
}
```





**终止条件**

```c
void OLED_I2C_Stop(void)
{
	OLED_W_SDA(0); //由于起始信号无法知道SDA开始状态，因为需要产生上升沿，需要先将SDA拉低
	OLED_W_SCL(1);
	OLED_W_SDA(1);
}

```





**写入一个字节**

```c

void MyI2C_SendByte(uint8_t Byte)
{
	uint8_t i;
    
    //由于SCL为0，先变化SDA。再拉高SCL供从机读取，再拉低SCL便于下次操作SDL
	for (i = 0; i < 8; i ++)				
	{
		MyI2C_W_SDA(Byte & (0x80 >> i));	//高位先行
		MyI2C_W_SCL(1);						//释放SCL，从机在SCL高电平期间读取SDA
		MyI2C_W_SCL(0);						//拉低SCL，主机开始发送下一位数据
	}
    MyI2C_W_SDA(1) ;  //释放SDA
    OLED_W_SCL(1);	//额外的一个时钟，不处理应答信号。
    //这里也可以对应答信号进行处理，当SCL高电平期间可以获取从机的应答位
	OLED_W_SCL(0);
}
```





**读取一个字节**

```c
uint8_t MyI2C_ReceiveByte(void)
{
	uint8_t i, Byte = 0x00;				
	MyI2C_W_SDA(1);							//接收前，主机先确保释放SDA，避免干扰从机的数据发送
	for (i = 0; i < 8; i ++)				
	{
		MyI2C_W_SCL(1);						//释放SCL，主机机在SCL高电平期间读取SDA
		if (MyI2C_R_SDA() == 1){Byte |= (0x80 >> i);}	//读取SDA数据，并存储到Byte变量,当SDA为1时，置变量指定位为1，当SDA为0时，不做处理，指定位为默认的初值0
		MyI2C_W_SCL(0);						//拉低SCL，从机在SCL低电平期间写入SDA
	}
     MyI2C_W_SDA(0);
     OLED_W_SCL(1);	//给从机应答信号
	OLED_W_SCL(0);//释放SCL
	return Byte;							//返回接收到的一个字节数据
}
```



**应答**

上述设计都是带应答的，也可以将应答单独处理

```c
void MyI2C_SendAck(uint8_t AckBit)
{
	MyI2C_W_SDA(AckBit);					//主机把应答位数据放到SDA线
	MyI2C_W_SCL(1);							//释放SCL，从机在SCL高电平期间，读取应答位
	MyI2C_W_SCL(0);							//拉低SCL，开始下一个时序模块
}


uint8_t MyI2C_ReceiveAck(void)
{
	uint8_t AckBit;							//定义应答位变量
	MyI2C_W_SDA(1);							//接收前，主机先确保释放SDA，避免干扰从机的数据发送
	MyI2C_W_SCL(1);							//释放SCL，主机机在SCL高电平期间读取SDA
	AckBit = MyI2C_R_SDA();					//将应答位存储到变量里
	MyI2C_W_SCL(0);							//拉低SCL，开始下一个时序模块
	return AckBit;							//返回定义应答位变量
}

```



##### 



#### 10.5硬件IIC外设

![image-20250311204704401](./嵌入式软件学习.assets/image-20250311204704401.png)



- 和串口收发过程类似，不过串口是全双工有两组移位和数据寄存器。这里只有一组

- **发送**：当时程序向DR寄存器写入数据，外设会自动将DR寄存器数据搬运到移位寄存器，并置位TXE发送寄存器空标记位。实现数据发送无缝衔接

- **接受**： 当数据移位寄存接受到数据，会自动将数据搬运到DR寄存器，并置位RXNE接受寄存器非空。

  

![image-20250311210048523](./嵌入式软件学习.assets/image-20250311210048523.png)



![image-20250311210947217](./嵌入式软件学习.assets/image-20250311210947217.png)





##### 1.发送寄存器时序

- EV代表事件，也就是寄存器变化，但是有时候伴随多个寄存器变化，事件代表寄存器集合
- EV8状态：移位寄存器非空，数据寄存器空。此时可以将下次所需要发送数据放入数据寄存器中。

![image-20250311211752826](./嵌入式软件学习.assets/image-20250311211752826.png)

![image-20250311211918721](./嵌入式软件学习.assets/image-20250311211918721.png)



![image-20250311212011910](./嵌入式软件学习.assets/image-20250311212011910.png)







##### 2.接受时序

![image-20250311212403384](./嵌入式软件学习.assets/image-20250311212403384.png)







##### 3.IIC库函数编程



#### 

**常用库函数函数**

```c
void I2C_GenerateSTART(I2C_TypeDef* I2Cx, FunctionalState NewState);  //产生起始信号
void I2C_GenerateSTOP(I2C_TypeDef* I2Cx, FunctionalState NewState); //产生终止信号

void I2C_AcknowledgeConfig(I2C_TypeDef* I2Cx, FunctionalState NewState);//收到一个字节后，是否给从机应答位


void I2C_SendData(I2C_TypeDef* I2Cx, uint8_t Data); //写入数据到数据寄存器DR
uint8_t I2C_ReceiveData(I2C_TypeDef* I2Cx); //读取数据寄存器DR数据
void I2C_Send7bitAddress(I2C_TypeDef* I2Cx, uint8_t Address, uint8_t I2C_Direction); //发送7地址数据接口 ，底层调用的I2C_SendData函数，不过封装了一个最低位设置读写位
```





**初始化硬件IIC外设**



硬件IIC，已经规定外设引脚

![image-20250313184446981](./嵌入式软件学习.assets/image-20250313184446981.png)

- 软件IIC使用 **开漏输出**
- 硬件IIC引脚配置 **复用开漏输出**，复用AF - 是将引脚的控制全移交给外设。 开漏因为IIC-硬件设计





```c
	/*开启时钟*/
	RCC_APB1PeriphClockCmd(RCC_APB1Periph_I2C2, ENABLE);		//开启I2C2的时钟
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);		//开启GPIOB的时钟
	
	/*GPIO初始化*/
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_OD; //复用开漏输出，AF-开漏-将引脚控制权交给外设
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_10 | GPIO_Pin_11;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOB, &GPIO_InitStructure);					//将PB10和PB11引脚初始化为复用开漏输出
	
	/*I2C初始化*/
	I2C_InitTypeDef I2C_InitStructure;						//定义结构体变量
	I2C_InitStructure.I2C_Mode = I2C_Mode_I2C;				//模式，选择为I2C模式 其他为SMBus模式配置
	I2C_InitStructure.I2C_ClockSpeed = 50000;				//时钟速度，选择为50KHz  ，最高支持400KHZ频率


	/*
	1.这个参数只有速度高于100KHZ生效，低于100KHZ都是1:1方波
	2.有两个参数选择 ： I2C_DutyCycle_16_9 	16:9		I2C_DutyCycle_2   2:1    低电平：高电平
	3.在高速传输时，要保证低电平时间长一些，保证数据变化时间
	*/
	I2C_InitStructure.I2C_DutyCycle = I2C_DutyCycle_2;		//时钟占空比，选择Tlow/Thigh = 2


	I2C_InitStructure.I2C_Ack = I2C_Ack_Enable;				//应答，选择使能   主机是否应答
	I2C_InitStructure.I2C_AcknowledgedAddress = I2C_AcknowledgedAddress_7bit;	//应答地址，选择7位，从机模式下才有效
	I2C_InitStructure.I2C_OwnAddress1 = 0x00;				//自身地址，从机模式下才有效
	I2C_Init(I2C2, &I2C_InitStructure);						//将结构体变量交给I2C_Init，配置I2C2
	
	/*I2C使能*/
	I2C_Cmd(I2C2, ENABLE);									//使能I2C2，开始运行
```





**库函数写数据**

- 软件引脚模拟IIC，是阻塞式的IO，会判断引脚是否置位
- 硬件IIC是非阻塞式的，例如发送起始信号后，需要确认标记位是否发送成功，也就是上述逻辑中的事件。

```c
void MPU6050_WriteReg(uint8_t RegAddress, uint8_t Data)
{
	I2C_GenerateSTART(I2C2, ENABLE);										//硬件I2C生成起始条件
	MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_MODE_SELECT);					//等待EV5时间
	
	I2C_Send7bitAddress(I2C2, MPU6050_ADDRESS, I2C_Direction_Transmitter);	//硬件I2C发送从机地址，方向为发送
	MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_TRANSMITTER_MODE_SELECTED);	//等待EV6
	
	I2C_SendData(I2C2, RegAddress);											//硬件I2C发送寄存器地址
	MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_TRANSMITTING);			//等待EV8
	
	I2C_SendData(I2C2, Data);												//硬件I2C发送数据
	MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_TRANSMITTED);				//等待EV8_2
	
	I2C_GenerateSTOP(I2C2, ENABLE);											//硬件I2C生成终止条件
}

```







### 11.SPI通讯



#### 11.1硬件电路

**相比IIC**：

- 硬件开销更大，需要4线
- 速度更快
- 

- 同步全双工时序

![image-20250313193756578](./嵌入式软件学习.assets/image-20250313193756578.png)



- SCL、MOSI、MISO都连在一起，单独每一个从设备引出一个片选引脚CS进行控制，可以配合38译码器节约引脚资源。
- 当片选引脚为**低电平有效（选中该芯片）**，**高电平无效（未选择）**。
- 从机规定，当其**SS-片选引脚高电平-无效时，MISO引脚配置为高阻态**。（从机未被选中时，输出高阻态）这样**防止多个从机同时输出，高低电平造成短路**。
- 输出引脚配置为：推挽输出 。输入配置：浮空输入或者上拉输入

![image-20250313194325149](./嵌入式软件学习.assets/image-20250313194325149.png)





#### 11.2SPI移位时序



- SPI在**第一个边沿，数据左移（高位先行），将数据放在数据线**上。在**第二个边沿，将数据线上的数据读取放入移位寄存器**中。
- 数据置换的效果。发送一个数据并接受一个数据，当只想发送或者接受时，也采用这种方式。只发送时，置换回的数据不读取。接受时，使用0x00或者0xff去置换。
- 通过一个字节的置换，实现了主从机的数据交换。
- 主机提供时序，保证主从机时序一致。

![image-20250313200539576](./嵌入式软件学习.assets/image-20250313200539576.png)



<img src="./嵌入式软件学习.assets/image-20250313201051653.png" alt="image-20250313201051653" style="zoom:50%;" />第一个边沿，将数据左移，数据放入数据线上。



<img src="./嵌入式软件学习.assets/image-20250313201226612.png" alt="image-20250313201226612" style="zoom:50%;" />第二个边沿，将数据线上的数据，读取放入移位寄存器中。







#### 11.3信号时序



##### 1、起始信号

- 在SPI**通讯期间，需要一直保持CS引脚为低电平**。 当**未选中时需要保持高电平**

<img src="./嵌入式软件学习.assets/image-20250313201847649.png" alt="image-20250313201847649" style="zoom:50%;" />



##### 2、数据交换

根据两个寄存器 

| CPOL     | CS空闲时刻，SCLK状态：1-高电平  0-低电平 |
| -------- | ---------------------------------------- |
| **CPHA** | **确定采样与输出信号的边沿**             |

![image-20250313205202150](./嵌入式软件学习.assets/image-20250313205202150.png)

###### 模式0 - 实际运用的比较多

**模式零使用最多**



- 由于第一个边沿需要移入数据，所以在CS下降沿时，从机主机就开始将数据移出。
- 



![image-20250313205910727](./嵌入式软件学习.assets/image-20250313205910727.png)



###### 模式1

![image-20250313204212609](./嵌入式软件学习.assets/image-20250313204212609.png)









###### 模式2



![image-20250313205944258](./嵌入式软件学习.assets/image-20250313205944258.png)







###### 模式3



![image-20250313205956612](./嵌入式软件学习.assets/image-20250313205956612.png)







###### 一般使用方法

- IIC采用读写寄存器的方式，不同寄存器有不同功能
- SPI采用指令集的方式，发送不同命令来控制从机运行

![image-20250514204438324](./嵌入式软件学习.assets/image-20250514204438324.png)

#### 11.4软件模拟SPI



##### 1.SPI端口配置

- MOSI、CS、CLK --- 配置为 推挽输出 
- MISO --- 配置为上拉输入



##### 2.将引脚变换封装函数



```c
void MySPI_W_SS(uint8_t BitValue)
{
	GPIO_WriteBit(GPIOA, GPIO_Pin_4, (BitAction)BitValue);		//根据BitValue，设置SS引脚的电平
}

/**
  * 函    数：SPI写SCK引脚电平
  * 参    数：BitValue 协议层传入的当前需要写入SCK的电平，范围0~1
  * 返 回 值：无
  * 注意事项：此函数需要用户实现内容，当BitValue为0时，需要置SCK为低电平，当BitValue为1时，需要置SCK为高电平
  */
void MySPI_W_SCK(uint8_t BitValue)
{
	GPIO_WriteBit(GPIOA, GPIO_Pin_5, (BitAction)BitValue);		//根据BitValue，设置SCK引脚的电平
}

/**
  * 函    数：SPI写MOSI引脚电平
  * 参    数：BitValue 协议层传入的当前需要写入MOSI的电平，范围0~0xFF
  * 返 回 值：无
  * 注意事项：此函数需要用户实现内容，当BitValue为0时，需要置MOSI为低电平，当BitValue非0时，需要置MOSI为高电平
  */
void MySPI_W_MOSI(uint8_t BitValue)
{
	GPIO_WriteBit(GPIOA, GPIO_Pin_7, (BitAction)BitValue);		//根据BitValue，设置MOSI引脚的电平，BitValue要实现非0即1的特性
}

/**
  * 函    数：I2C读MISO引脚电平
  * 参    数：无
  * 返 回 值：协议层需要得到的当前MISO的电平，范围0~1
  * 注意事项：此函数需要用户实现内容，当前MISO为低电平时，返回0，当前MISO为高电平时，返回1
  */
uint8_t MySPI_R_MISO(void)
{
	return GPIO_ReadInputDataBit(GPIOA, GPIO_Pin_6);			//读取MISO电平并返回
}
```





##### 3.交换一字节数据

这里演示模式0

```c
	/*在初始化时，设置默认电平*/
	MySPI_W_SS(1);											//SS默认高电平
	MySPI_W_SCK(0);											//SCK默认低电平



/**
  * 函    数：SPI交换传输一个字节，使用SPI模式0
  * 参    数：ByteSend 要发送的一个字节
  * 返 回 值：接收的一个字节
  */
uint8_t MySPI_SwapByte(uint8_t ByteSend)
{
	uint8_t i, ByteReceive = 0x00;					//定义接收的数据，并赋初值0x00，此处必须赋初值0x00，后面会用到
	
	for (i = 0; i < 8; i ++)						//循环8次，依次交换每一位数据
	{
		MySPI_W_MOSI(ByteSend & (0x80 >> i));		//使用掩码的方式取出ByteSend的指定一位数据并写入到MOSI线
		MySPI_W_SCK(1);								//拉高SCK，上升沿移出数据
		if (MySPI_R_MISO() == 1){ByteReceive |= (0x80 >> i);}	//读取MISO数据，并存储到Byte变量
																//当MISO为1时，置变量指定位为1，当MISO为0时，不做处理，指定位为默认的初值0
		MySPI_W_SCK(0);								//拉低SCK，下降沿移入数据
	}
	
	return ByteReceive;								//返回接收到的一个字节数据
}





//优化移位逻辑
uint8_t MySPI_SwapByte(uint8_t ByteSend)
{				
	for (i = 0; i < 8; i ++)						
	{
		MySPI_W_MOSI(ByteSend & (0x80 >> i));
        ByteSend<<= 1 ;  //向左移位1
		MySPI_W_SCK(1);							
		if (MySPI_R_MISO() == 1){ByteReceive |= 0x01;}	 											
		MySPI_W_SCK(0);							
	}
	return ByteReceive;								
}

```





#### 11.5硬件SPI

- SPI1挂载在APB2 - 72M ； SPI2挂载在APB1 - 36M
- 8位与16位数据帧，每一次数据交互位数。**一般是一个字节**
- 可选择高位或者低位先行，一般为高位先行。    **IIC/SPI都是高位先行，串口是低位先行**。
- PCLK --- 指的是外设时钟。APB1-72MHZ       APB2-36MHZ

![image-20250315144231239](./嵌入式软件学习.assets/image-20250315144231239.png)





##### 1.硬件架构

- LSBIRST位  --- 控制低位先行还是高位先行
- 由于SPI在发送同时接受数据，由于数据交换的设计原理，接受与移位寄存器共用。但是分开接受数数据寄存器与发送数据寄存器。
- 通过TXE 与 RXNE配合可以实现稳定的数据流

![image-20250315145302661](./嵌入式软件学习.assets/image-20250315145302661.png)



##### 2.对比IIC、UART、SPI 软件发送与接收设计异同

- SPI是同步全双工交换数据，可以**同时接受与发送**。所以共用一个移位寄存器，分开发送与接受数据缓存寄存器
- IIC是半双工，**同一时刻只能发送或者接受**。所以只设计一个移位寄存器与数据缓存寄存器
- <img src="./嵌入式软件学习.assets/image-20250315150041867.png" alt="image-20250315150041867" style="zoom:80%;" />
- UART：异步全双工，可以异步接受与发送，所以设计两组<img src="./嵌入式软件学习.assets/image-20250315150340715.png" alt="image-20250315150340715" style="zoom:80%;" />



![image-20250315151146514](./嵌入式软件学习.assets/image-20250315151146514.png)









##### 3.硬件传输时序



###### 1.连续传输，可以一直进行数据交互。但是接受与发送非同时。



1. 发送空标记位有效，将数据0XF1写入WDR，数据被立即转入数据移位寄存器，TXE有效，但是此时上一个数据还在发送
2. TXE有效后，紧接着放入数据0XF2到WDR寄存器中
3. 当移位寄存器发送完成后，将WDR中的数据0xF2放入移位寄存器中，并将TXE置位
4. 将数据0xF3写入WDR
5. 此时等待0xF2移位完成，0XF3写入移位寄存器，TXE有效。当时此时只需要写入3个字节数据。
6. 等待0xF3移位完成BSY为0，表示数据发送完成

![image-20250315151924375](./嵌入式软件学习.assets/image-20250315151924375.png)







- 在发送同时接受数据，当一帧数据接受完成后，数据被放入RDR数据寄存器中，RXNE置位。当软件读取到该位有效后，需要将数据读走。读取RDR同时会自动将RXNE复位。

![image-20250315152925635](./嵌入式软件学习.assets/image-20250315152925635.png)





###### 2.非连续传输数据，发送同时接受一帧数据，保持同步。但是有时间上的浪费，时序不连贯。





1. 当检测到 TXE=1 ,BSY = 0 表示移位寄存器与WDR为0 。 此时写入第一个数据0XF1
2. 等待BSY为0，表示上一帧数据发送完成。**此时同步读取RDR寄存器**。
3. 读取完成数据后，在写入下一个数据

**重复上述步骤，可以在发送数据后，同时接受一帧数据。**

![image-20250315153824694](./嵌入式软件学习.assets/image-20250315153824694.png)



**非连续相比连续传输，帧之间会有一段等待时间。**影响传输效率

![image-20250315154347663](./嵌入式软件学习.assets/image-20250315154347663.png)













###### 3.库函数硬件外设编程



**端口初始化**

- CS片选引脚，可以选择**外设绑定的硬件-配置为复用推挽输出**；可以选择**任意通用GPIO --- 配置为推挽输出**。
- MOSI/CLK，**复用推挽输出，将GPIO控制权交给外设**
- MISO，**上拉输入。由于输入数据可以同时进入GPIO与其他外设，没有复用一说**





**外设初始化**

```c
	/*SPI初始化*/
	SPI_InitTypeDef SPI_InitStructure;						//定义结构体变量
	SPI_InitStructure.SPI_Mode = SPI_Mode_Master;			//模式，选择为SPI主模式 ---主机

//可选：单线发送、单线接受、双线全双工、双线只接受
	SPI_InitStructure.SPI_Direction = SPI_Direction_2Lines_FullDuplex;	//方向，选择2线全双工 --- 不同线制

	SPI_InitStructure.SPI_DataSize = SPI_DataSize_8b;		//数据宽度，一次传输位数选择为8位

	SPI_InitStructure.SPI_FirstBit = SPI_FirstBit_MSB;		//先行位，选择高位先行

	SPI_InitStructure.SPI_BaudRatePrescaler = SPI_BaudRatePrescaler_128;	//波特率分频，选择128分频 SPI1挂载在APB2-72M，这里选择128分频。也就是SPI-SCL时钟为 72M / 128 M

//下面两个参数选择 --- SPI 上下边沿采样与CLK空闲状态
	SPI_InitStructure.SPI_CPOL = SPI_CPOL_Low;				//SPI极性，选择低极性
	SPI_InitStructure.SPI_CPHA = SPI_CPHA_1Edge;			//SPI相位，选择第一个时钟边沿采样，极性和相位决定选择SPI模式0

	SPI_InitStructure.SPI_NSS = SPI_NSS_Soft;				//NSS-CS片选引脚，这里选择由软件控制---自由选择任意GPIO为CS引脚；也可以选择硬件控制，固定外设GPIO
	SPI_InitStructure.SPI_CRCPolynomial = 7;				//CRC多项式，暂时用不到，给默认值7
	SPI_Init(SPI1, &SPI_InitStructure);						//将结构体变量交给SPI_Init，配置SPI1
	

	/*SPI使能*/
	SPI_Cmd(SPI1, ENABLE);									//使能SPI1，开始运行
	
	/*设置默认电平*/
	MySPI_W_SS(1);											//SS默认高电平
```





**非连续读写数据**



```c
uint8_t MySPI_SwapByte(uint8_t ByteSend)
{
	while (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_TXE) != SET);	//等待发送数据寄存器空
	
	SPI_I2S_SendData(SPI1, ByteSend);								//写入数据到发送数据寄存器，开始产生时序
	
	while (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_RXNE) != SET);	//等待接收数据寄存器非空,此时写表示发送完成
	
	return SPI_I2S_ReceiveData(SPI1);								//读取接收到的数据并返回
}
```













### 13.485通讯

#### 485与CAN的区别

- 485：一主多从，一个主机控制多个从机，距离更远
- CAN：多节点式，速度更快

![image-20241112225508176](./嵌入式软件学习.assets/image-20241112225508176.png)



### 12.CAN通讯



#### 1.CAN简介

![image-20250211204831967](./嵌入式软件学习.assets/image-20250211204831967.png)



#### 2.CAN硬件接线

- 高速CAN：125K-1M，<40m ,采用闭环网络，并联120欧终端电阻，**终端电阻作用：当无设备操作时-默认CAN_H与CAN_L差值为0（默认1  ），设备输出1时，无需操作总线，默认为1**
- 低速CAN：10k - 125K , <1km ,采用开环网络，串联2.2K欧终端电阻
- 终端电阻作用：防止回波反射（尤其是高频信号、远距离传输）
- CAN设备都需通过CAN收发器挂载在CAN总线上（TX输出1，不对总线进行操作，TX输出0，将总线拉开形成电势差）

![image-20250211205915901](./嵌入式软件学习.assets/image-20250211205915901.png)



#### 3.CAN的电平标准

- 高速CAN默认电压差为0，低速CAN默认两线不相等，高速CAN恢复显性电平速度更快，速率更快。低速CAN显性与隐性电平差异较大，长距离传输时，即使有压降也可以区分显隐性





![image-20250211210944758](./嵌入式软件学习.assets/image-20250211210944758.png)







#### 4.CAN总线帧格式

![image-20250211213133478](./嵌入式软件学习.assets/image-20250211213133478.png)





- 帧起始SOF，用于**通知各个节点有数据传输和同步**
- ID帧：越小优先级越高，标准帧11位扩展帧29位
- RTR：用于区分数据帧还是遥控帧
- IDE：用于区分标准帧还是扩展帧
- SRR![image-20250211215043452](./嵌入式软件学习.assets/image-20250211215043452.png)
- r0:保留位
- DLC：数据长度（字节数，4-代表后方4个字节），指定后方有效载荷长度
- ACK：发送方释放总线，监听总线状态。如果有接受导数据，发送显性电平，发送方接受到后确认消息被接受，此标记为用于数据重传等功能

![image-20250211215723194](./嵌入式软件学习.assets/image-20250211215723194.png)



![image-20250211213956692](./嵌入式软件学习.assets/image-20250211213956692.png)

![image-20250211214021075](./嵌入式软件学习.assets/image-20250211214021075.png)

![image-20250211214040133](./嵌入式软件学习.assets/image-20250211214040133.png)



#### 5.位填充

- 规则：发送出现5个连续电平后，自动追加一个相反电平补充位。接受方按照相同规则解析并剔除填充位，恢复原始数据。
- 作用：
  1. 波形经常变换，可以进行波特率再同步
  2. 将错误帧和过载帧区分
  3. 避免长时间发送1或0，导致总线不变化，其他设备误认为总线空闲。





#### 6.位同步  





##### 6.1硬同步

- SS2：同步段，如果跳变沿出现在SS段，认为时序同步
- PTS段：传播时间段，用于物理传输延时
- PBS1、PBS2段：通过调节两段长度，灵活调节采样点位置



![image-20250212193546213](./嵌入式软件学习.assets/image-20250212193546213.png)

![image-20250212193807649](./嵌入式软件学习.assets/image-20250212193807649.png)





##### 6.2硬同步





- 帧起始SOF段用于硬件同步，所有设备中都有右下角的“秒表”，发送方在SS段发送数据产生跳变沿，其他接受方在接受到下降沿时立马将指针指向SS段。保存接收方与发送方时序同步，接收方在数据采样点进行采样。
- 只在SOF下降沿生效



![image-20250212195720535](./嵌入式软件学习.assets/image-20250212195720535.png)





##### 6.3再同步（重新同步）

- 当硬同步后，由于各个设备之间的**时钟有误差，当一帧数据长时间传输时，误差会累积非常大，导致采样点偏移，所以需要再同步**。---所有设备无法做到绝对精准，时钟由于各种原因产生误差。
- 每个数据边沿都进行再同步
- SJW--**-调整步长最大长度值**，增加或减少的Tq时间，根据误差自动调制，并非固定SJW长度。**防止由于噪声干扰过大补偿，造成补偿过度。如果设备时钟误差比较大，可以将SJW调大一点，风险是可能补偿过度**
- 当接受方提前了，延长PBS1的长度，保证下一帧SS段对齐
- 当接受方延时了，减少上一拍PBS2，保证下一帧对齐



![image-20250212200933096](./嵌入式软件学习.assets/image-20250212200933096.png)





#### 7.总线仲裁





##### 7.1先占先得

- **已有设备操作总线其他设备不可再操作** ， 不运行抢占
- **需要等待总线空闲一段时间(连续11个隐性电平)后才能再发送数据，正常活跃时由于位填充的原因这种情况不存在**

![image-20250212202559832](./嵌入式软件学习.assets/image-20250212202559832.png)







##### 7.2非破坏性仲裁



- ID号小 --- 优先级高    ID号大----优先级低
- 线与机制、回读机制
- 在其他设备发送时，各个设备都在等待总线空闲，一旦空闲后一段时间，所有设备需要发送都会操作总线并回读发送数据。
- 所有段都会执行位填充，包括仲裁段，但是不会影响到仲裁
- 相同ID号时，数据帧大于遥控帧、标准数据帧大于扩展数据帧

![image-20250212203325488](./嵌入式软件学习.assets/image-20250212203325488.png)









#### 8.CAN外设

![image-20250215170112930](./嵌入式软件学习.assets/image-20250215170112930.png)

![image-20250215170214261](./嵌入式软件学习.assets/image-20250215170214261.png)





- 发送：将配置好的CAN帧写入邮箱，CAN控制器根据总线状态自动写入数据到总线。邮箱---类似与缓存，使用多个缓存，防止总线一直非空闲而导致数据丢失，当总线空闲时，会根据配置选择其中一个邮箱发送
- 接受：当CAN接收器接受到总线数据后，会依次通过过滤器（只要一个过滤器满足即可，无需在通过后续的过滤器），每个过滤器可以绑定FIFI0或FIFO1，这样可以实现对消息的分组（可以根据报文优先级等人为分组）



![image-20250215170919042](./嵌入式软件学习.assets/image-20250215170919042.png)





##### 发送接受寄存器配置位

![image-20250217190934889](./嵌入式软件学习.assets/image-20250217190934889.png)



















































### 14.232通讯





### 15.通讯总结

#### 1.归纳

![image-20240905223049783](./嵌入式软件学习.assets/image-20240905223049783.png)

##### 1.1通讯方式

- 全双工：一般都有两个数据线，**同一时间可以接受和发送数据** ：SPI 、USART
- 半双工：**可以发送或接受数据，单不能同一时间，分时复用**
- 单工：只能发送或者接受

##### 1.2异步与同步

当发送数据或接受数据时，需要知道何时进行采样。**有时钟线的通讯方式可以根据时钟进行采样，没有时钟线的通讯方式需要约定通讯速率**。由于异步需要约定速率，对于时间要求比较高，无法通过软件模拟。而同步时钟一致，当时钟停止时（例如一方进入中断），通讯双方同时禁止在同一时刻，时钟恢复后，再次恢复通讯，可以由软件模拟时钟进行通讯对硬件外设依赖较少。

同步（有时钟线）：IIC、SPI

异步（无时钟线需要约定通讯速率）：串口、CAN、USB

##### 1.3串口与CAN 

- 同为异步传输，双方约定通讯协议
- 都可以通过差分信号传输 485、CAN 
- 都需要进行时钟同步，起始位同步、再同步





### 16.程序升级方式

- ISP：系统编程。这种编程方式允许在板级上直接进行。支持ISP的芯片内部通常固化了一段boot程序，用于引导升级过程。例如STM32的boot串口下载软件
- ICP：意为在电路编程。这种编程方式的特点是在芯片实际工作时，对其进行编程操作。例如使用J-Link、ST-Link、e-Link32等工具进行的编程操作
- IAP：它是指在程序运行过程中进行编程，如升级程序或更新固件。IAP允许用户程序在运行时对User Flash的特定区域进行烧写，从而实现在产品发布后，通过预留的通信接口（如UART串口、ETH以太网、I2C、SPI等）轻松更新和升级固件程序。









#### 1.ISP

- 一般BOOT0/1都接GND，程序直接从GND运行
- STM32的系统存储器一般只串口ISP

![image-20250217195958679](./嵌入式软件学习.assets/image-20250217195958679.png)









#### 2.IAP

![image-20250316105744971](./嵌入式软件学习.assets/image-20250316105744971.png) 	

![image-20250316194323121](./嵌入式软件学习.assets/image-20250316194323121.png)

**简单复述IAP实现过程**

- 在开发工程时，APP与boot是分开开发的，此时注意两端程序在FLASH中的存放地址。
- boot放入0x800 0000 下，大小为SIZE1。APP放在 0x 800 0000 + SIZE1 地址下，大小为单片机Flash大小 - SIZE1（**注意在main哈数开头，偏移中断向量表位置**） 。<img src="./嵌入式软件学习.assets/image-20250316191311054.png" alt="image-20250316191311054" style="zoom:50%;" />
- 由于烧录需要使用BIN文件，直接存放进FLASH中。需要生成BIN文件<img src="./嵌入式软件学习.assets/image-20250316191422622.png" alt="image-20250316191422622" style="zoom:50%;" />
- 创建一个RAM数组，大小为FLASH一页大小。通过上位机将BIN文件拆包发送，数组每接受到1K数据就触发FLASH写入。
- flash写入时，先解锁，再循环将数据写入。由于最小支持半字写入，需要将两个字节拼接写入。
- 当全部数据写入完成后可以进行PC指针跳转指令
- 关闭所有中断，并复位外设。 初始化MSP栈顶指针，随后PC指针指向APP复位函数。





汇川技术设计思路：



- 出厂后升级：将APP的hex文件与BOOT的HEX文件打包为一个BIN文件，当同时接受到两道指令，两个指令时间间隔小于1S---防止干扰。当机器正在运行时，返回不允许升级命令。当未在运行时，从APP跳入BOOT区，在BOOT区中：上位机与boot区进行数据交互，将BIN文件分段打包给BOOT，boot将软件写入flash区后，跳转到APP区
- 厂区升级：通过芯片厂商的ISP，先写入一个RAM-BOOT，RAMboot再和上位机通讯，将传输的数据进行解密将FLASH-boot烧录。随后flashboot再和上位机进行通讯烧录APP
- RAMboort，不破坏flash中的内容、下载速度非常快、运行非常快。由于FLASH被写保护了，通过RAM-BOOT进行解锁





**保护与补充思路：**

- 将BIN打包分段传输，需要加入校验机制
- 在flash备份上一次代码，本次升级失败或者运行中程序损毁，可以跳转到备用程序进行运行。
- 有版本和设备码的校验，验证软件和硬件匹配
- **boot软件使用内部时钟HSI，APP使用外部高速时钟HSE **原因：1.**运行boot当外部晶振改变，容易出现超频导致软件运行不正常**  **2.PLL锁相环修改一次后不允许修改，HSE使用时进入系统总线需要倍频。当BOOT区修改了PLL后，APP不允许修改，会出现时钟错误**
- 写入FLASH后再读出来校验一下，判断是否写入正确



#### STM32的IAP技术

##### 基于CAN总线的STM32F103 BootLoader设计

![Snipaste_2022-01-18_10-42-37](./嵌入式软件学习.assets/Snipaste_2022-01-18_10-42-37.png)

##### 1 不同的程序下载方式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           

目前，单片机的程序烧录方式可以分为三种：ICP，ISP，IAP。

###### 1.1 **ICP**：In-Circuit Programming

在电路中编程。使用厂家配套的软件或仿真器进行程序烧录，目前主流的有JTAG接口和SWD接口，常用的烧录工具为J-Link、ST-Link等。

![image-20220117153051661](./嵌入式软件学习.assets/image-20220117153051661.png)

在程序开发阶段，通常在连接下载器的情况下直接使用编程软件进行程序下载调试。

在MDK软件中可以选择不同的下载器。

![image-20220118104735482](./嵌入式软件学习.assets/image-20220118104735482.png)

##### 1.2 **ISP**：In-System Programing

在系统中编程。以STM32为例，其内置了一段Bootloader程序，可以通过更改BOOT引脚电平来运行这段程序，再通过ISP编程工具将程序下载进去。下载完毕之后，再更改BOOT至正常状态，使得MCU运行所下载的程序。

![image-20220117155513080](./嵌入式软件学习.assets/image-20220117155513080.png)

正点原子的STM32开发板中专门设计了一个单片机自动复位及设置Boot引脚电平状态的电路，便于程序下载。通过RTS与DTR引脚，配置boot引脚。

![image-20220118110342687](./嵌入式软件学习.assets/image-20220118110342687.png)

##### 1.3 **IAP：**In-Application  Programming

在应用中编程。IAP可以使用微控制器支持的任一种通信接口（如I/O端口、USB、CAN、UART、I2C、SPI等）下载程序或数据到FLASH中。IAP允许用户在程序运行时重新烧写FLASH中的内容。但需要注意，IAP要求至少有一部分程序（Bootloader）已经使用ICP或ISP烧到FLASH中。

![image-20220117162528633](./嵌入式软件学习.assets/image-20220117162528633.png)

无论是ICP技术还是ISP技术，都需要连接下载线，设置跳线帽等操作。一般来说，产品的电路板都会密封在外壳中，在这时若要使用ICP或ISP的方式对程序进行更新，则必然要拆装外壳，如果产品的数量比较多，将花费很多不必要的时间。

采用IAP编程技术，可以在一定程度上避免上述的情况。一般情况下，产品的外壳都会留有通信接口，若能通过这种通信方式对程序进行升级，则可以省去拆装的麻烦。在此基础上，若引入远距离或无线数据传输方案，更可以实现远程编程或无线编程。

![image-20220118110603659](./嵌入式软件学习.assets/image-20220118110603659.png)

![image-20220118105230821](./嵌入式软件学习.assets/image-20220118105230821.png)

**某种微控制器支持IAP技术的首要前提是其必须是基于可重复编程闪存的微控制器。**

STM32微控制器带有可编程的内置闪存，同时STM32拥有在数量上和种类上都非常丰富的外设通信接口，因此在STM32上实现IAP技术是**完全可行**的。 

#### 2 IAP方案设计

##### 2.1 IAP简介

IAP技术的核心在于BootLoader程序的设计，这段程序预先烧录在单片机中，正常的APP程序可以使用BootLoader程序中的IAP功能写入，也可以两部分代码一起写入，以后需要程序更新时通过IAP进行代码更新。每次板卡上电都会首先执行BootLoader程序，在程序内判断进行固件升级还是跳转到正常的APP程序。

![image-20220118140633570](./嵌入式软件学习.assets/image-20220118140633570.png)

是否进行固件升级的判断可以从硬件和软件两个方面进行考虑。

1. 硬件实现：通过拨码开关、跳线帽等方式设定单片机某一引脚电平状态，程序通过读取引脚电平判断是否需要升级。此种方式需要接触板卡进行操作，当板卡被封闭在外壳中或安装于不便于操作位置时很难实现。
2. 软件实现-1：软件内设定一标志位（变量），通过判断标志位状态判断是否需要升级。该标志位状态掉电不能改变，故需要存储在外部EEPROM或单片机内部FLASH中。若存储在外部EEPROM，则需要增加额外的电路；若存储在单片机内部FLASH，由于FLASH每次写入都需要擦除一整页，会造成资源浪费。
3. 软件实现-2：单片机每次上电首先进入BootLoader程序，在BootLoader中等待一定时间，若上位机软件在该时间段内发起通讯，则停留在BootLoader程序中等待固件升级；若该时间段内无通讯，则跳转到正常的APP程序。该方式每次上电都要等待一定时间，需要考虑是否可以接收。
4. 在软件运行过程中，检测是否有升级指令。当机器未运行时，进入对应的bootloard区域

**在IAP过程中，单片机通过特定的通讯方式从上位机软件接收程序数据，并执行FLASH擦写操作对APP部分的程序进行更新。**



###### 1.bin文件产生

IAP过程中传输的**数据文件一般为后缀名为bin**的文件，该文件内容与正常烧录进FLASH中的数据内容一致，便于程序升级。**但是MDK软件并不能直接生成bin文件，需要进行一些配置。**

![image-20220118223915522](./嵌入式软件学习.assets/image-20220118223915522.png)

```
fromelf.exe  --bin -o ..\OBJ\TIMER.bin ..\OBJ\TIMER.axf  //“TIMER”需要改成自己程序的名字
```

由于我把fromelf.exe所在目录添加到了环境变量，所以可以直接这样写。如未添加环境变量，则需要写清楚详细路径，该文件一般在MDK软件的安装目录下。

![image-20220118224112848](./嵌入式软件学习.assets/image-20220118224112848.png)

配置完成重新编译之后即可生成bin文件（OBJ目录）。

##### 2.2 FLASH空间划分

BootLoader程序和APP 程序存放在 STM32 FLASH 的不同地址范围，一般从最低地址区开始存放 BootLoader，紧跟其后的就是 APP 程序。在FLASH足够大的情况下，还可以分配多个APP程序区域，便于恢复默认程序或者执行多段功能不同的程序。

<img src="./嵌入式软件学习.assets/image-20250316171607043.png" alt="image-20250316171607043" style="zoom:80%;" />如图放入两端APP软件。在APP1程序出现故障，可以自动将APP2软件复制到APP1中



##### 2.2.1 不同型号STM32 FLASH大小

在进行FLASH空间划分之前，首先需要了解一下不同型号STM32单片机的FLASH大小。

![stm32命名](./嵌入式软件学习.assets/stm32命名.png)

对于不同容量的STM32F1系列产品，其FLASH页大小是不同的，具体的容量划分规则如下：

- **小容量产品**：FLASH容量在**16K至32K**字节之间的STM32F101xx、STM32F102xx和STM32F103xx微控制器。
- **中容量产品**：FLASH容量在**64K至128K**字节之间的STM32F101xx、STM32F102xx和STM32F103xx微控制器。
- **大容量产品**：FLASH容量在**256K至512K**字 节之间的STM32F101xx和STM32F103xx微控制器。

对于**小容量和中容量的产品，其页大小为1K，对于大容量产品，其页大小为2K**。

![image-20220118215938662](./嵌入式软件学习.assets/image-20220118215938662.png)

![image-20220118215956812](./嵌入式软件学习.assets/image-20220118215956812.png)

![image-20220118220016412](./嵌入式软件学习.assets/image-20220118220016412.png)

##### 2.2.2 查看程序占用空间大小

在进行FLASH空间划分时，必须知道编写的程序占用FLASH空间大小。用MDK软件进行工程编译之后会生成一个.map文件，在该文件末尾可找到程序需要占用的FLASH空间。

![image-20220118220839216](./嵌入式软件学习.assets/image-20220118220839216.png)

![image-20220118220950845](./嵌入式软件学习.assets/image-20220118220950845.png)

在实际设计过程中，主要是确定BootLoader程序占用空间，便于确定APP程序的起始地址。

在这时候可以先编写部分BootLoader程序，再通过map文件查看当前占用空间，从而预估BootLoader程序最终会占用的空间大小。

在实际空间分配过程中，可以稍微大一点，以便于后续对BootLoader的功能扩展。

> 如果对MDK的编译过程以及文件类型感兴趣，可以阅读《[野火EmbedFire]《STM32库开发实战指南——基于野火霸道开发板》—20211109》第46章的内容。
>
> 视频链接：https://www.bilibili.com/video/BV1Ss411M75x?spm_id_from=333.999.0.0

![image-20220118221554622](./嵌入式软件学习.assets/image-20220118221554622.png)



###### boot空间设置

在MDK软件配置项中，可以对程序的起始位置以及大小进行设置。

![image-20220118221916201](./嵌入式软件学习.assets/image-20220118221916201.png)

对于BootLoader程序来说，只需要设置其Size，该值可根据刚才map文件中的值进行预估。

##### 2.2.3 APP程序中需要做的配置项

BootLoader程序按照正常的程序编写即可。而APP程序由于其下载位置与默认程序下载位置不同，故需要做一些特殊的配置。

首先是APP 程序起始地址设置

![image-20220118222804656](./嵌入式软件学习.assets/image-20220118222804656.png)

起始位置即去除BootLoader程序之后剩余空间的首地址。一般设定为某一页的首地址，因为FLASH写入之前必须进行页擦除。

Size = FLASH原始大小 - 偏移量（0x4000），我使用的是STM32F103C8T6，FLASH大小为64K。



###### 中断向量表偏移

[STM32中断向量表的偏移量设置方法_stm32 中断向量表偏移-CSDN博客](https://blog.csdn.net/qq_36958104/article/details/108842629?spm=1001.2101.3001.6650.7&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-7-108842629-blog-131209282.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-7-108842629-blog-131209282.235^v43^pc_blog_bottom_relevance_base3)





在系统启动的时候，会首先调用 SystemInit 函数初始化时钟系统，同时SystemInit 还完成了中断向量表的设置，我们可以打开 SystemInit 函数，看看函数体的结尾处有 这样几行代码：

```c
#ifdef VECT_TAB_SRAM
SCB->VTOR = SRAM_BASE | VECT_TAB_OFFSET;
/* Vector Table Relocation in Internal SRAM. */
#else
SCB->VTOR = FLASH_BASE | VECT_TAB_OFFSET;
/* Vector Table Relocation in Internal FLASH. */
#endif
```

![image-20250316174507256](./嵌入式软件学习.assets/image-20250316174507256.png)



接着设置**中断向量表的偏移量，在主函数起始位置添加**：

```c
SCB->VTOR = FLASH_BASE | 0x4000; //0x4000即BootLoader大小（偏移量）
```

![image-20220118223450681](./嵌入式软件学习.assets/image-20220118223450681.png)











##### 2.3 IAP程序设计 

###### 2.3.1 IAP（BootLoader）编程关键技术

1. 通信
2. Flash擦写
3. APP跳转



###### 通信需要考虑的问题

1. 选用何种通信方式：串口、CAN、以太网......
2. 通信协议：数据分发、帧头帧尾校验
3. 配套上位机



###### Flash写入流程

1 解锁

```c
void FLASH_Unlock(void)				//解锁
```

2 擦除

```c
//页擦除
FLASH_Status FLASH_ErasePage(uint32_t Page_Address);

//整片擦除
FLASH_Status FLASH_EraseAllPages(void);
```

3 写入

```c
//半字写入 --- 最小的FLASH操作单位是半字，当接受上位机的数据需要拼接成半字写入
FLASH_Status FLASH_ProgramHalfWord(uint32_t Address, uint16_t Data);

//全字写入
FLASH_Status FLASH_ProgramWord(uint32_t Address, uint32_t Data);
```

4 上锁

```C
void FLASH_Lock(void);
```



接收多少数据写入一次？

可以根据**FLASH一页大小例如1K，先创建一个1K大小的数组接受来自上位机的一段数据放在RAM区，再将RAM数据写入FLASH中**。

> FLASH写入之前必须进行页擦除
>
> STM32F103 FLASH 页大小：1k 或 2k
>
> 接收满1页大小的数据写入一次

```c
#define APP_LEN_MAX 10*1024
u8 APP_RX_BUF[APP_LEN_MAX] = {0};  //接收缓冲,最大5kb，每次接受1K数据

//通过外部接口，接受上位机BIN数据
if(APPLength < APP_LEN_MAX)
{
    for(i = 0; i <RxMessage.DLC; i++)
    {
        APP_RX_BUF[APPLength] = RxMessage.Data[i];
        APPLength++;
    }
}

//WriteAddr:应用程序的起始地址，必须为某扇区的起始地址
//pBuffer:应用程序CODE.
//NumToWrite:应用程序大小(字节).

//接受完成一页数据后，将RAM数据写入到FLASH中。
void iap_write_appbin(u32 WriteAddr,u8 *pBuffer,u32 NumToWrite)
{
	u32 i = 0;
	u16 temp;

    if (WriteAddr < STM32_FLASH_BASE || (WriteAddr >= (STM32_FLASH_BASE + 1024 * STM32_FLASH_SIZE)))
    {
        printf("地址越界!\r\n");
        return;   
    }
          
    if(WriteAddr % 1024) //1k整除
    {
        printf("地址非FLASH扇区首地址!\r\n");
        return;
    }

    /*开始写入*/
    
    FLASH_Unlock(); //解锁
    
    while(1)
    {
        if(NumToWrite == 0)//上一个扇区刚好完整写完
        {
            break;
        }
        else if(NumToWrite < STM_SECTOR_SIZE)//剩余要写入的内容不到一个扇区
        {
            FLASH_ErasePage(WriteAddr); //擦除这个扇区 
            
            for(i = 0; i < NumToWrite; i += 2)
            {
                temp  = (u16)pBuffer[i + 1] << 8;
                temp |= (u16)pBuffer[i];
                FLASH_ProgramHalfWord(WriteAddr, temp);
                WriteAddr += 2;
            }
            break; //写入结束，退出while
        }
        else
        {
            FLASH_ErasePage(WriteAddr); //擦除这个扇区
            
            //写入整个扇区
            for(i = 0; i < STM_SECTOR_SIZE; i += 2)
            {
                temp  = (u16)pBuffer[i + 1] << 8;
                temp |= (u16)pBuffer[i];
                FLASH_ProgramHalfWord(WriteAddr, temp);
                WriteAddr += 2;
            }
           
            pBuffer += STM_SECTOR_SIZE;
            NumToWrite -= STM_SECTOR_SIZE;
        }
    }
    
    
    FLASH_Lock(); //上锁
}

```



##### APP跳转



```c

//传入程序起始地址，例如： iap_load_app（0x08005000） ；

void (*iapfun)(void);      // 函数指针，用于指向复位函数运行

void iap_load_app(u32 appxaddr)
{
    int i = 0;
    
    /*检查栈顶地址是否合法 ： 起始地址对应中断向量表为栈顶指针。这里对传入的地址转换为指针后，取出栈顶指针。
    由于该芯片RAM区大小为128K - 地址0 - 127k ( 0x0 0000 - 0X1 FFFF),只要超过这个地址，在&2FFE0000时，FFE这之间一定会有1出现
    
    */
	if(((*(volatile uint32_t *)appxaddr)&0x2FFE0000)==0x20000000)	
	{ 


        
         /* 关闭全局中断 */
        __set_PRIMASK(1); 
                 
        /* 关闭滴答定时器，复位到默认值 */
        SysTick->CTRL = 0;
        SysTick->LOAD = 0;
        SysTick->VAL = 0;
        
        /* 设置所有时钟到默认状态 */
        RCC_DeInit();
        
        /* 关闭所有中断，清除所有中断挂起标志 */  
        for (i = 0; i < 8; i++)
        {
            NVIC->ICER[i]=0xFFFFFFFF;
            NVIC->ICPR[i]=0xFFFFFFFF;
        }
        
        /* 使能全局中断 */ 
        __set_PRIMASK(0);
        
        /* 在RTOS工程，这条语句很重要，设置为特权级模式，使用MSP指针 */
        __set_CONTROL(0);
        
        
        
        
        
         //将传入的地址偏移4个字节后，取出Reset_Handler的地址并赋值给函数指针。首地址是MSP，地址+4是复位中断服务程序地址
		iapfun= (void (*)(void))   *(volatile uint32_t *)(appxaddr+4);  
        
        
        //初始化APP堆栈指针(用户代码区的第一个字用于存放栈顶地址)
        MSR_MSP(*(volatile uint32_t *))appxaddr);
        
		iapfun();	//跳转到APP的Reset_Handler函数执行。
        
        /* 跳转成功的话，不会执行到这里，用户可以在这里添加代码 */
        while (1)
        {

        }
	}
}		
```

##### 2.3.2 IAP编程实战

需要准备的设备及材料

![image-20220121143949107](./嵌入式软件学习.assets/image-20220121143949107.png)









USB-CAN适配器

![image-20220121144424114](./嵌入式软件学习.assets/image-20220121144424114.png)

https://item.taobao.com/item.htm?spm=a1z10.1-c-s.w5003-22815269889.2.43d17a62SzFCrG&id=15148465486&scene=taobao_shop



USB转TTL CH340模块

![image-20220121144728633](./嵌入式软件学习.assets/image-20220121144728633.png)

https://detail.tmall.com/item.htm?spm=a230r.1.14.47.29c45370i9NVFF&id=41323941056&ns=1&abbucket=1&skuId=3944185979947



周立功CANPro调试软件（可直接发送bin文件）

![image-20220121144900193](./嵌入式软件学习.assets/image-20220121144900193.png)

https://www.zlg.cn/index.php/can/down/down/id/42.html



创芯科技原厂调试软件

![image-20220121144928431](./嵌入式软件学习.assets/image-20220121144928431.png)





串口调试助手

![image-20220121144840029](./嵌入式软件学习.assets/image-20220121144840029.png)



**参照正点原子的串口IAP实验例程编写基于CAN总线的BootLoader程序**

相较于原子的IAP例程，主要做以下几点工作：

1. 原子例程所使用的芯片为STM32F103ZET6，更改目标芯片为STM32F103C8T6，并修改FLASH容量宏定义以及重新划分FLASH空间；
2. 添加CAN底层驱动代码至工程；
3. 逻辑代码修改。

![image-20220121151201114](./嵌入式软件学习.assets/image-20220121151201114.png)

##### 2.3.3 代码解析





##### 2.5 IAP实验验证





##### 2.6 不足与展望

目前数据为一次性整包传输，接收到的数据存储在数组里，占用单片机RAM空间，而单片机的RAM是有限的。

以STM32F103C8T6为例，RAM大小仅为20KB。若APP程序所需大于这个值，则此方法不能使用。

另，直接传输数据包的方式可靠性低，如果数据传输过程中出错无法判断。

为解决上述问题，需考虑将**数据分包传输，并增加数据校验**。



目前使用调试助手的方式仅能实现整个文件自动传输，若要采用数据分包+校验的方式，需定制开发上位机软件。



单片机与上位机需制定一套稳定可靠的通信协议。



















































### 17.STM32启动过程



#### 17.1启动模式

- 堆栈指针MSP 与 程序计数器指针PC 从0x0000 0000 / 0x0000 0004获取 复位中断的地址
- 厂商可以把初始地址映射到其他地址，以区分不同芯片的启动模式（F1/F4/F7/H7启动方式不一样  ）

![image-20250220202606365](./嵌入式软件学习.assets/image-20250220202606365.png)





##### 17.1.1-STM32-F1、F4启动方式

**在系统复位后，SYSCLK第4个上升沿后，BOOT引脚的值将被存储。**也就是在复位后的第4个上升沿后，boot引脚可以用于其他功能。boot启动模式引脚值已被锁存

![image-20250220203304586](./嵌入式软件学习.assets/image-20250220203304586.png)

- 主闪存存储器 - APP
- 系统存储器 - 根据产品类型区分![image-20250220203952281](./嵌入式软件学习.assets/image-20250220203952281.png)一般是使用串口下载，互联性可以ton过多个窗口、CAN、USB等

![image-20250220203718257](./嵌入式软件学习.assets/image-20250220203718257.png)





##### 17.1.2-STM32-F7启动方式



- **在系统复位后，SYSCLK第4个上升沿后，BOOT引脚的值将被存储。**

- **在F7中只有一个BOOT引脚**，根据高低电平选择用户软件还是ISP，再通过寄存器配置起始地址

  

![image-20250220204825539](./嵌入式软件学习.assets/image-20250220204825539.png)







**17.1.2-STM32-H7启动方式**

![image-20250220210732166](./嵌入式软件学习.assets/image-20250220210732166.png)





#### 17.2启动文件.s





##### 1.系统上电后启动

- MSP、PC从0x0000 0000 /0x0000 0004 经过boot选择后由硬件电路在 0x0000 0000 /0x0000 0004 写入需要映射的地址 重映射到内部flash程序首地址0x0800 0000，由于程序的前端存储的是中断向量表，**直接执行中断向量表的内容，中断表第一个就是栈顶指针，直接复制给MSP指针。中断表第二个指针是复位中断函数地址。**![image-20250220213731709](./嵌入式软件学习.assets/image-20250220213731709.png)[【stm32】stm32深入思考(1) 之 系统启动流程 main之前的过程，__main作用，start.s作用_taotao830-腾讯云开发者社区](https://tencentcloud.csdn.net/67629aa1f3b8a55e4e98a34e.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzQwNjYyNDcxLCJpYXQiOjE3NDAwNTc2NzEsInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.ZsQIolh8BFfdd9Yt4h4L_WL0QC8Pz8LUGMpCasr5Of0&spm=1001.2101.3001.6650.7&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-7-105329451-blog-115255840.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-7-105329451-blog-115255840.235^v43^pc_blog_bottom_relevance_base3)

  

- PC取地址后 进入复位中断服务函数，开始初始化



[STM32从上电开始启动过程详解(上电-＞分散加载-＞main函数)_stm32上电过程-CSDN博客](https://blog.csdn.net/hanchaoman/article/details/143775793)

![image-20250220212813839](./嵌入式软件学习.assets/image-20250220212813839.png)

![image-20250220213557983](./嵌入式软件学习.assets/image-20250220213557983.png)



![image-20250227214453721](./嵌入式软件学习.assets/image-20250227214453721.png)

##### 2.启动文件

- 调用C库的__main() ，由 此调用用户自己的main函数


![image-20250220211859200](./嵌入式软件学习.assets/image-20250220211859200.png)





##### 3.复位中断函数

![image-20250225200234398](./嵌入式软件学习.assets/image-20250225200234398.png)

```c
; Reset handler   // ;表示注释
Reset_Handler    PROC //PROC、ENDP成对使用，类似与{ } 表示程序范围
                 EXPORT  Reset_Handler             [WEAK] //EXPORT - 全局属性   WEAK-弱定义（外部定义同名函数，优先使用外部）
    
     IMPORT  __main
     IMPORT  SystemInit //声明外部函数 类似与extern
    
                 LDR     R0, =SystemInit //执行系统初始化函数，外部没有此函数会报错
                 BLX     R0
    
    
                 LDR     R0, =__main //执行__main C库函数，再由C库函数跳转到main函数
                 BX      R0
                 ENDP
```





##### 4.堆栈简介



- **栈-由编译器自动分配和释放，存放形参、局部变量等。当函数局部变量较多时，函数嵌套关系复杂，需要加大栈的大小，否则会溢出**  莫名其妙的错误， 并进入了 HardFault 的时候，你就要考虑下是不是栈空间不够大。
- **堆-程序员分配和释放，不调用malloc等将不会使用到堆**
- 堆是由低地址向高地址生长 ，栈是由高地址向低地址生长

![image-20250225201633392](./嵌入式软件学习.assets/image-20250225201633392.png)



```c
Stack_Size      EQU     0x00000400   //这里定义栈为 1KB

                AREA    STACK, NOINIT, READWRITE, ALIGN=3
Stack_Mem       SPACE   Stack_Size
__initial_sp


; <h> Heap Configuration
;   <o>  Heap Size (in Bytes) <0x0-0xFFFFFFFF:8>
; </h>

Heap_Size       EQU     0x00000200  //这里定义堆为512B

                AREA    HEAP, NOINIT, READWRITE, ALIGN=3
__heap_base
Heap_Mem        SPACE   Heap_Size
__heap_limit

                PRESERVE8
                THUMB
```

![image-20250225213443756](./嵌入式软件学习.assets/image-20250225213443756.png)

![image-20250225213425310](./嵌入式软件学习.assets/image-20250225213425310.png)



![image-20250225213622296](./嵌入式软件学习.assets/image-20250225213622296.png)



- **堆处于物理低地址，栈初始时栈指针指向在高地址，就出现堆和栈在内存空间中连续，所以当堆空间被用完，那么堆指针自然地指向栈区，栈空间用完栈指针就会指向堆区。**

![image-20250303214809549](./嵌入式软件学习.assets/image-20250303214809549.png)





###### 关于堆栈的溢出检测

[STM32嵌入式系统中的堆栈溢出问题与解决方案_stm32堆栈溢出-CSDN博客](https://blog.csdn.net/2301_80825409/article/details/145950607?spm=1001.2014.3001.5506)

[STM32 堆栈溢出检测_stm32怎么看堆栈溢出-CSDN博客](https://blog.csdn.net/weixin_37381614/article/details/118973039?spm=1001.2014.3001.5506)

[STM32的Code/RO/RW/ZI区、Flash/Ram的占用情况、堆栈大小Stack_Size/Heap_Size的设置_stm32 program size-CSDN博客](https://blog.csdn.net/qq_31073871/article/details/102569983?spm=1001.2014.3001.5506)



![image-20250401194629161](./嵌入式软件学习.assets/image-20250401194629161.png)



FLASH区： **CODE**（代码） + **RO**（常量） + **RW**（具有初始值的全局变量、静态变量）

RAM区 ：**RW** （具有初始值的全局变量、静态变量）+ **ZI** （无初始值的全局变量、静态变量）+ **栈** （局部变量、参数）+ **堆**（用户自由分配）



- __disable_irq();关闭进入中断函数，但是中断标记位不清
- __enable_irq();开启中断函数响应，之前没响应的立马响应。

![image-20250401195353512](./嵌入式软件学习.assets/image-20250401195353512.png)

- 正常情况下堆栈是连续存放在一起的，因此只需要这三个变量就可以确定堆和栈的起始位置和大小，但对于有多段不连续的内存的单片机来说这三个值无法确定堆的大小，因此还需要修改.s启动文件，增加一个**__stack_base变量**，并导出变量由C语言调用，修改后的.s启动文件如下

![image-20250401195219242](./嵌入式软件学习.assets/image-20250401195219242.png)



```c
extern int __initial_sp;
extern int __stack_base;
extern int __heap_base;
extern int __heap_limit;

const int s_magic = 0x43218765;
const int h_magic = 0x56781234;
 
int stack_set_guard(void)
{
		__disable_irq();
	
		int* msp = (int *)__get_MSP();
		int* base = &__stack_base;
 
		if(msp < base) {
				__enable_irq();
				return -1;
		}
				
	
		for( ; base != msp; base++)
				*base = s_magic;
		
		__enable_irq();
		
		return (uint32_t)msp - (uint32_t)&__stack_base;
}
 
int stack_detect_guard(void)
{
		__disable_irq();
	
		int* msp = (int *)__get_MSP();
		int* base = &__stack_base;
 
		if(msp < base || *base != s_magic) {
				__enable_irq();
				return -1;
		}
	
		for( ; base != msp; base++) {
				if(*base != s_magic)
						break;
		}
		
		__enable_irq();
		
		return (uint32_t)base - (uint32_t)&__stack_base;
}



int main(void)
{
	int ret = stack_set_guard();
	
    /* 初始化 */
    ...
 
    /* 调用一些函数 */
    ...
 
    /* 检测单个函数栈使用量 */
    ret = stack_set_guard();
    xxx_func();
    ret = stack_detect_guard() - ret;
 
    while (1)
    {
        /* 调用一些函数 */
        ...
    	ret = stack_detect_guard();
	    printf("stack_detect_guard %d\r\n", ret);
    }
 
}



```





##### 5.中断向量表

- **中断向量表被放置在代码段的最前面**。例如：当我们的程序在 FLASH 运行时，那么向 量表的起始地址是：0x0800 0000。结合图 2.3.2 可以知道，地址 0x0800 0000 存放的是栈顶 地址。DCD：以四字节对齐分配内存，也就是下个地址是0x0800 0004，存放的是Reset_Handler中断函数入口地址。
- 中断向量表也就是函数名，也就是函数地址



```c
__Vectors       //由 __Vectors   和 __Vectors   定义中断向量表
    
    		   DCD     __initial_sp               ; Top of Stack //DCD要求4字节对齐（以下每个地址加4字节），这里执行初始化栈顶指针 0x0800 0000，栈顶指针在初始化栈的时候声明了
                DCD     Reset_Handler              ; Reset Handler //复位中断函数 0x0800 0004 PC指针
                DCD     NMI_Handler                ; NMI Handler
                DCD     HardFault_Handler          ; Hard Fault Handler
                DCD     MemManage_Handler          ; MPU Fault Handler
                DCD     BusFault_Handler           ; Bus Fault Handler
                DCD     UsageFault_Handler         ; Usage Fault Handler
                DCD     0                          ; Reserved
                DCD     0                          ; Reserved
                DCD     0                          ; Reserved
                DCD     0                          ; Reserved
                DCD     SVC_Handler                ; SVCall Handler
                DCD     DebugMon_Handler           ; Debug Monitor Handler
                DCD     0                          ; Reserved
                DCD     PendSV_Handler             ; PendSV Handler
                DCD     SysTick_Handler            ; SysTick Handler

                ; External Interrupts
                DCD     WWDG_IRQHandler            ; Window Watchdog
                DCD     PVD_IRQHandler             ; PVD through EXTI Line detect
                DCD     TAMPER_IRQHandler          ; Tamper
                DCD     RTC_IRQHandler             ; RTC
                DCD     FLASH_IRQHandler           ; Flash
                DCD     RCC_IRQHandler             ; RCC
                DCD     EXTI0_IRQHandler           ; EXTI Line 0
                DCD     EXTI1_IRQHandler           ; EXTI Line 1
                DCD     EXTI2_IRQHandler           ; EXTI Line 2
                DCD     EXTI3_IRQHandler           ; EXTI Line 3
                DCD     EXTI4_IRQHandler           ; EXTI Line 4
                DCD     DMA1_Channel1_IRQHandler   ; DMA1 Channel 1
                DCD     DMA1_Channel2_IRQHandler   ; DMA1 Channel 2
                DCD     DMA1_Channel3_IRQHandler   ; DMA1 Channel 3
                DCD     DMA1_Channel4_IRQHandler   ; DMA1 Channel 4
                DCD     DMA1_Channel5_IRQHandler   ; DMA1 Channel 5
                DCD     DMA1_Channel6_IRQHandler   ; DMA1 Channel 6
                DCD     DMA1_Channel7_IRQHandler   ; DMA1 Channel 7
                DCD     ADC1_2_IRQHandler          ; ADC1_2
                DCD     USB_HP_CAN1_TX_IRQHandler  ; USB High Priority or CAN1 TX
                DCD     USB_LP_CAN1_RX0_IRQHandler ; USB Low  Priority or CAN1 RX0
                DCD     CAN1_RX1_IRQHandler        ; CAN1 RX1
                DCD     CAN1_SCE_IRQHandler        ; CAN1 SCE
                DCD     EXTI9_5_IRQHandler         ; EXTI Line 9..5
                DCD     TIM1_BRK_IRQHandler        ; TIM1 Break
                DCD     TIM1_UP_IRQHandler         ; TIM1 Update
                DCD     TIM1_TRG_COM_IRQHandler    ; TIM1 Trigger and Commutation
                DCD     TIM1_CC_IRQHandler         ; TIM1 Capture Compare
                DCD     TIM2_IRQHandler            ; TIM2
                DCD     TIM3_IRQHandler            ; TIM3
                DCD     TIM4_IRQHandler            ; TIM4
                DCD     I2C1_EV_IRQHandler         ; I2C1 Event
                DCD     I2C1_ER_IRQHandler         ; I2C1 Error
                DCD     I2C2_EV_IRQHandler         ; I2C2 Event
                DCD     I2C2_ER_IRQHandler         ; I2C2 Error
                DCD     SPI1_IRQHandler            ; SPI1
                DCD     SPI2_IRQHandler            ; SPI2
                DCD     USART1_IRQHandler          ; USART1
                DCD     USART2_IRQHandler          ; USART2
                DCD     USART3_IRQHandler          ; USART3
                DCD     EXTI15_10_IRQHandler       ; EXTI Line 15..10
                DCD     RTCAlarm_IRQHandler        ; RTC Alarm through EXTI Line
                DCD     USBWakeUp_IRQHandler       ; USB Wakeup from suspend
__Vectors_End
```







##### 6.__main( )



__main 是编译系统提供的一个函数，负责完成库 函数的初始化和初始化应用程序执行环境，最后自动跳转到 main()。



































































### 18.MAP文件



- .o文件：由.c或者.s产生
- .axf文件：由.O文件链接而成，仿真时用到
- .hex文件，由.axf生成，可执行文件
- .map文件：链接器生成的列表文件，对于分析程序占用情况非常有用

生成路径：**.o  -  .axf  -  .hex** 

![image-20250219194440300](./嵌入式软件学习.assets/image-20250219194440300.png)

![image-20250219195524023](./嵌入式软件学习.assets/image-20250219195524023.png)

![image-20250219200847334](./嵌入式软件学习.assets/image-20250219200847334.png)

![image-20250219195012074](./嵌入式软件学习.assets/image-20250219195012074.png)

**可以双击工程名 --- 打开MAP文件**



##### 1.1map文件的组成



1. 程序段交叉引用关系：函数之间的调用关系 ![image-20250219202411341](./嵌入式软件学习.assets/image-20250219202411341.png)例如红框表示的是：main.c调用delay.c中的delay_ms函数

   

2. 删除映像未使用的程序段 ： 一些**工程内定义的函数、变量但是未被调用或者使用**，编译器自动删除在此体现![image-20250219202712694](./嵌入式软件学习.assets/image-20250219202712694.png)例如HAL_DeInit函数被定义了，但是未被使用，工程编译时进行删除。



1. 映像符号表 ： 描述各个变量、函数。常量等在寄存器的地址、类型、大小描述 ，详情看原子说明
2. 映像内存分布图 ： 描述函数在寄存器的地址与大小
3. 映像组件大小 ： .o文件大小

![image-20250219201441237](./嵌入式软件学习.assets/image-20250219201441237.png)



![image-20250219203533097](./嵌入式软件学习.assets/image-20250219203533097.png)

![image-20250219203816275](./嵌入式软件学习.assets/image-20250219203816275.png)



![image-20250219210353608](./嵌入式软件学习.assets/image-20250219210353608.png)





**前三后二：flash空间 = code + ro +re        ram空间 = rw + zi** 

![image-20250219210614718](./嵌入式软件学习.assets/image-20250219210614718.png)











### 19.分散加载

[【STM32】sct 分散加载文件的格式与应用_.sct文件-CSDN博客](https://blog.csdn.net/apythonlearner/article/details/133034055?spm=1001.2101.3001.6650.3&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-3-133034055-blog-145427305.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-3-133034055-blog-145427305.235^v43^pc_blog_bottom_relevance_base3)

[Keil MDK的sct分散加载文件详解_单片机_吾爱技术圈-GitCode 开源社区](https://gitcode.csdn.net/65ed7c2f1a836825ed79b012.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzQxNzU5NjM5LCJpYXQiOjE3NDExNTQ4MzksInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.xjIYsvfWWKLyUVNtfwyPhclqYIxOM7E0W2niVK2ne9s&spm=1001.2101.3001.6650.16&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-16-127537956-blog-124562053.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-16-127537956-blog-124562053.235^v43^pc_blog_bottom_relevance_base3)









一般情况又编译器自动生成，将数据放入默认地址。STC文件：

- 可以整体操作，不需要再使用关键字“**attribute**”按具体地址来指定
- 不同类型的内存：许多系统都包含多种不同的物理内存设备，如闪存、 ROM、 SDRAM 和快速 SRAM。分散加载描述可以将代码和数据与最适合的内存类型相匹配。**可以将中断代码放在快速 SRAM 中以缩短中断等待时间，而将不经常使用的配置信息放在较慢的闪存中。**
- H7芯片中，有许多RAN空间，运行速度中：TCRAM > 通用RAM（1、2、3、4） > SDRAM(空间大)



![image-20250305134849009](./嵌入式软件学习.assets/image-20250305134849009.png)

<img src="./嵌入式软件学习.assets/image-20250305134751975.png" alt="image-20250305134751975" style="zoom:50%;" />编译器默认生成的stc文件



<img src="./嵌入式软件学习.assets/image-20250305135158822.png" alt="image-20250305135158822" style="zoom:50%;" />RESET段，存放中断向量表





```c
; *************************************************************
; *** Scatter-Loading Description File generated by uVision ***
    编译器默认生成的 stc文件
; *************************************************************
 
LR_IROM1 0x08000000 0x00020000  {    
	//定义一个加载域，域地址0x08000000，域大小为0x00020000
    //load region size_region 所有代码需要下载到0x08000000 开始的区域中，且这个区域大小只有0x00020000 
  ER_IROM1 0x08000000 0x00020000  {  //load address = execution address 加载地址 = 执行地址   第一个运行时域必须和加载域起始地址相同，其大小一般									也相同
    //只能是只读的代码段和只读数据段
   *.o (RESET, +First)        //表示 所有.O文件中，包含RESET段，需要放在这个执行段的最前段，这里也就是中断向量表，在启动文件中有声明。
   *(InRoot$$Sections)     //IDE自带函数，在__main中调用 ，复制函数用于将执行域ROM中将RW（有初始值的全区变量）复制到RAM中，这里将复制函数存储在ROM中
   .ANY (+RO) // .ANY可以选择所有的.o与lib文件，“.ANY”选择语句的优先级是最低的，所有其它选择语句选择完剩下的数据才会被“.ANY”语句选中。  加载所有匹配目				标文件的只读属性数据，包含：Code代码段、RO-Data只读数据段-常量。
       
  //* 与 .ANY 区别 ，当使用*时该运行域满后不会向其他区域自动分配 ，.ANY当该区域空间不够时会自动向其他区域分配     
       
       
  }
  RW_IRAM1 0x20000000 0x00005000  {  
    //再定义一个运行时域，域基址0x20000000 
	//RW data 执行域是以0x20000000 开始的长度为0x00004000  一段区域
   .ANY (+RW +ZI)    //RW-有初始值的全局变量 ，ZI初始值为0的全局变量
  }
}



加载域名  起始地址 大小{；加载区域大小 （分号后面是注释）
        运行域名 起始地址 大小   ；执行地址
        {
                中断向量表起始地址， +First表示强制放到首地址

                ARM相关库，InRoot$$Sections即ARM库的链接器标号，主要作用COPY RW区到RAM，
                然后再RW区后面创建ZI区。 库函数__main函数中有这个段。 它是__main()的一部分。

                编译文件RO只读在该区域
        }
        运行内存名字 起始地址 大小
        {
                编译可读可写，静态区
        }
}


```



#### 1.定义存储位置

![image-20250305141425681](./嵌入式软件学习.assets/image-20250305141425681.png)

![image-20250305141528995](./嵌入式软件学习.assets/image-20250305141528995.png)







#### 2._ _*attribute* _ _

![image-20250305144816939](./嵌入式软件学习.assets/image-20250305144816939.png)







### 20. stm32h7芯片

[史上最全面的Cortex-M内核所有资料汇总一览表（2022-01-10） - STM32H7 - 硬汉嵌入式论坛 - Powered by Discuz！](https://www.armbbs.cn/forum.php?mod=viewthread&tid=94790)

#### 20.1系统总线架构

![image-20250310212852815](./嵌入式软件学习.assets/image-20250310212852815.png)

















### 21.电源管理









### 22.CRC校验



[循环冗余校验（CRC）算法入门引导_算法_liyuanbhu-GitCode 开源社区](https://gitcode.csdn.net/65e83ef01a836825ed78b5ae.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzQ4NzQwMjgyLCJpYXQiOjE3NDgxMzU0ODIsInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.y00uZPpVZe3iesr2NM3sT7yGH096Me6D34LPOa3AvY8&spm=1001.2101.3001.6650.5&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-5-7882789-blog-127189045.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-5-7882789-blog-127189045.235^v43^pc_blog_bottom_relevance_base3)

[CRC原理和C语言实现CRC_crc c语言-CSDN博客](https://blog.csdn.net/weixin_39531963/article/details/108140809?ops_request_misc=%7B%22request%5Fid%22%3A%2214d89aed408ace45f30de7879bcfe871%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&request_id=14d89aed408ace45f30de7879bcfe871&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-108140809-null-null.142^v102^pc_search_result_base9&utm_term=CRC实现&spm=1018.2226.3001.4187)



- CRC 算法的基本思想是：    **传输的数据当做一个位数很长的数。将这个数除以另一个数。得到的余数作为校验数据附加到原数据后面**

- **除数**：**将传输数据变为一个字节流**   6、23、4 可以看做一个2进制数： 00000110    00010111    00000010

- **被除数** ：被除数有一个专有名称叫做**“生成多项式”**。生成多项式的选取是个很有难度的问题。直接使用专家提供的。<img src="./嵌入式软件学习.assets/image-20250525092516194.png" alt="image-20250525092516194" style="zoom:50%;" />

- **余数**：**CRC校验值**    发送方根据约定的被除数（生成多项式）将余数放在一帧数据流的最后面，接收方获取数据后将前方的数据流进行运算得到一个自己的余数，计算的余数与接受到的判断是否一致，一致表明数据准确。 ![image-20250525093402818](./嵌入式软件学习.assets/image-20250525093402818.png)

- **除法**：**模2除法**   ***除法使用正常多项式除法，但是加减法采用模2运算***       采用模2加减法后，不需要向高位借位了![image-20250525093030045](./嵌入式软件学习.assets/image-20250525093030045.png)![image-20250525093143990](./嵌入式软件学习.assets/image-20250525093143990.png)

  













#### 22.1汇川技术实现

##### 1.CRC16

- 真正的CRC 计算通常与上面描述的还有些出入。这是因为这种最**基本的CRC除法有个很明显的缺陷**，就是数据流的**开头添加一些0并不影响最后校验字的结果**
- 

![image-20250525095146360](./嵌入式软件学习.assets/image-20250525095146360.png)



##### 2.CRC8

![image-20250525095210508](./嵌入式软件学习.assets/image-20250525095210508.png)





### 23.Modbus-RTU

[详细解释什么是Modbus、RTU、485-CSDN博客](https://blog.csdn.net/qq_42343949/article/details/132894182)





### 23.Eeathcat主从站



![image-20250710195201563](./嵌入式软件学习.assets/image-20250710195201563.png)





- 低成本实现100M全双工通讯，物理层和以太网协议一致
- 不能和网络协议兼容，属于自有协议。下层兼容到了IPV4/5上层协议（TCP/IP）无法兼容

![image-20250710195513455](./嵌入式软件学习.assets/image-20250710195513455.png)





![image-20250710200111205](./嵌入式软件学习.assets/image-20250710200111205.png)





- HDR:包含协议内容、源/目的MAC地址等
- 如-过程数据需要发送32个IO数据，也就是4个字节，但是以太网协议的帧最短位82个字节。如果直接使用以太网协议的化利用利不高
- 

![image-20250710200214294](./嵌入式软件学习.assets/image-20250710200214294.png)





- 以太网协议族很大，从底层物理层数据解析后，还得经过上层数据解析后才能得到数据，实时工业现场不需要复杂的路由转发
- 交换机根据MAC地址转发

![image-20250710200916133](./嵌入式软件学习.assets/image-20250710200916133.png)



交换机组网，数据转发路由 带来数据的延时

![image-20250710201311383](./嵌入式软件学习.assets/image-20250710201311383.png)



- 实时以太网，需要交换机单独一对多发数据
- EthereCAT，所有数据串联，一帧数据即可

![image-20250710201432885](./嵌入式软件学习.assets/image-20250710201432885.png)



![image-20250710201758826](./嵌入式软件学习.assets/image-20250710201758826.png) 	



![image-20250710202059495](./嵌入式软件学习.assets/image-20250710202059495.png)



![image-20250710202255572](./嵌入式软件学习.assets/image-20250710202255572.png)





#### 1.物理层



- 线缆不同可以任意组网，例如风力发电使用光阡长距离传输

![image-20250710202433124](./嵌入式软件学习.assets/image-20250710202433124.png)



- 一个帧过来后，立即读写，无需再进行寻址拆包组包过程
- 有一个主站周期性的发送数据，周期最短12.5微妙，一般500微妙-2毫秒

![image-20250710202742234](./嵌入式软件学习.assets/image-20250710202742234.png)



![image-20250710203236190](./嵌入式软件学习.assets/image-20250710203236190.png)



- 主站一个网口，从站两个网口

![image-20250710203322867](./嵌入式软件学习.assets/image-20250710203322867.png)



- 支持热插拔，中间阶段断开后，会激动环回

![image-20250710203638024](./嵌入式软件学习.assets/image-20250710203638024.png)



- 主机多加一个网卡，中间某台设备坏了，其他设备继续保持工作
- 未发生故障时，主站两个端口，数据从1端口到2端口
- 发生故障，两边各自进行回环

![image-20250710203837355](./嵌入式软件学习.assets/image-20250710203837355.png)

![image-20250710204014497](./嵌入式软件学习.assets/image-20250710204014497.png)





- 数据通过RJ45进入ESC芯片，通过PDI（SPI等等）
- E2PROM是必须要的，不是可选的
- 

![image-20250710204451103](./嵌入式软件学习.assets/image-20250710204451103.png)





- 需要对所有从站配置ESI文件
- 主站通过ESI文件识别从站

![image-20250710204738072](./嵌入式软件学习.assets/image-20250710204738072.png)

![image-20250710204824779](./嵌入式软件学习.assets/image-20250710204824779.png)





![image-20250710204956944](./嵌入式软件学习.assets/image-20250710204956944.png)





- 一帧包含所有节点的信息，节点只需要按地址寻址，提高数据帧的利用率

![image-20250710205524586](./嵌入式软件学习.assets/image-20250710205524586.png)



![image-20250710205721933](./嵌入式软件学习.assets/image-20250710205721933.png)



![image-20250710210210178](./嵌入式软件学习.assets/image-20250710210210178.png)



![image-20250710210353435](./嵌入式软件学习.assets/image-20250710210353435.png)



用的比较少

![image-20250710210501958](./嵌入式软件学习.assets/image-20250710210501958.png)





- 一般使用逻辑寻址，配置更加灵活
- FMMU自动管理

![image-20250710210801347](./嵌入式软件学习.assets/image-20250710210801347.png)

![image-20250710210737179](./嵌入式软件学习.assets/image-20250710210737179.png)

![image-20250710211309281](./嵌入式软件学习.assets/image-20250710211309281.png)



![image-20250710211617973](./嵌入式软件学习.assets/image-20250710211617973.png)



![image-20250710211726682](./嵌入式软件学习.assets/image-20250710211726682.png)



![image-20250710211840766](./嵌入式软件学习.assets/image-20250710211840766.png)



![image-20250710212614585](./嵌入式软件学习.assets/image-20250710212614585.png)



![image-20250710212805215](./嵌入式软件学习.assets/image-20250710212805215.png)



![image-20250710213149281](./嵌入式软件学习.assets/image-20250710213149281.png)



## 二、C语言学习



### 1.关键字

#### 1.1 volatile

该关键字表示易变的变量，防止编译器优化

在STM32库文件中<img src="./嵌入式软件学习.assets/image-20240707171501219.png" alt="image-20240707171501219" style="zoom:80%;" />被重定义为__IO

每个结构体成员前增加了一个IO前缀，它的原型在这段代码的第一 行，代表了C语言中的关键字“volatile”，在**C语言中该关键字用于表示变量是易变的**， 要求编译器不要优化。这些结构体内的成员，**都代表着寄存器**，而寄存器很多时候是**由外 设或STM32芯片状态修改的，也就是说即使CPU不执行代码修改这些变量，变量的值也 有可能被外设修改、更新，所以每次使用这些变量的时候，我们都要求CPU去该变量的地 址重新访问**。若没有这个关键字修饰，在某些情况下，编译器认为没有代码修改该变量， 就直接从CPU的某个缓存获取该变量值，这时可以加快执行速度，但该缓存中的是陈旧数 据，与我们要求的寄存器最新状态可能会有出入。



#### 1.2extern

##### 1.2.1头文件作用

1. extern可以修饰变量和函数，用extern声明后，会告诉编译器该变量或者函数在其他文件中已被定义过。将变量或者函数的作用域从源文件扩展到全工程。
2. ![image-20240710230401615](./嵌入式软件学习.assets/image-20240710230401615.png)







##### 1.2.2头文件与extern

[extern 与头文件(*.h)的区别和联系_keil uvision5中extern可以放在.h文件中吗-CSDN博客](https://blog.csdn.net/qq_41786318/article/details/81320022)

[变量的声明和定义 - ren_zhg1992 - 博客园 (cnblogs.com)](https://www.cnblogs.com/renzhuang/articles/6737505.html)

###### 有时候会有问题：

- 用 #include 可以包含其他头文件中变量、函数的声明，为什么还要 [extern](https://so.csdn.net/so/search?q=extern&spm=1001.2101.3001.7020) 关键字？
- 如果我想引用一个全局变量或函数a，我只要直接在源文件中包含 #include<xxx.h> (xxx.h包含了a的声明)不就可以了么，为什么还要用 extern 呢？

答：如果一个文件(假设文件名A)要大量引用另一个文件(假设文件名B)中定义的变量或函数，则使用头文件效率更高，程序结构也更规范。其他文件(例如文件名C、D等)要引用文件名B中定义的变量或函数，则只需用#include包含文件B对应的头文件(当然，这个头文件只有对变量或函数的声明，绝不能有定义)即可



###### 设计建议：

1. 在头文件中，最好是不要直接定义变量，会出现多次重复定义错误![image-20240710230658301](./嵌入式软件学习.assets/image-20240710230658301.png)

2. ![image-20240710230538363](./嵌入式软件学习.assets/image-20240710230538363.png)

3. ```c
   总结：
   /*如果想在 a.c 程序中调用  b.c 的变量和函数*/
   
   方法一：  b.c中定义了 int a = 10 变量， a.c文件中直接声明 extern int a ;进行使用
       
   方法二：  b.c中定义了 int a = 10 变量 ，b.h文件进行声明 extern int a ;    后续所有包含b.h的文件都可以使用变量a
       
       
   1.对于需要被外部调用的函数或者变量，在头文件进行声明，函数不需要加extern，变量加上extern,其他文件需要调用这些函数时，只需包含头文件。
   2.对于不需要被外部调用的函数和变量，可以在源函数进行声明和定义，还可加上static进行限制作用域。
       
   
   
   
   
   ```

   







![image-20240710222342735](./嵌入式软件学习.assets/image-20240710222342735.png)





#### 1.3const

- 常量，程序内不可修改。
- 在定义不可修改的数据、列表、字库等使用const进行修饰

```c
int ver_a ; //存储在SRAM中
const int ver_b ; //存储在FLASH中
```



#### 1.4char

之前在一个项目中，定义一个返回值为char类型的函数，当程序未达到预期结果返回赋值，**但是发现（-1）一直返回的是255**

**原因是：不同的编译器对char的定义不同，这里编译器将char当做为一个无符号字符变量**

解决：返回值尽量为int型，当使用char型时 声明为 singed char (有符号字符变量)

```c
char max(&st_struct)
{
    
    ....
    return (-1);    
}
```





### 2.数据类型

![image-20240710205031091](./嵌入式软件学习.assets/image-20240710205031091.png)

- int8_t是C语言官方封装（stdint.h）的（新版本库函数都用这个），u8/s8是ST库封装的（老版本）

#### 2.1有符号与无符号不可比较

```c
int a = -10 ; 
unsinged int b = 20
    
    if(a < b)  //逻辑上是符合的 -10 < 20 ,但是在比较时会将 a强制转换为无符号类型，根据补码为一个很大的正数。
```



整型提升指的是，当表达式中包含不同类型的整型算术运算时，C语言会将这些值提升为同一类型，然后进行运算。具体来说，当使用一个char或short类型的值与一个int类型的值执行算术运算时，C语言会将这些数值自动转换为int类型，并将计算结果也转换为int类型。

        算术转换指的是，当表达式中包含不同类型的浮点数或整型数时，C语言会自动进行类型转换，以便进行算术运算。例如，在将一个float类型的值与一个double类型的值进行算术运算时，C语言会将float类型的值转换为double类型，然后再执行




#### 2.2浮点数不可使用 == 判断相等

[C语言如何判断两个浮点数是否相等？-CSDN博客](https://blog.csdn.net/Rex_WUST/article/details/89219529)

```c
if(fabs(f) < 1e-6)；
(fabs(f) < 1e-15)；
```

![image-20250521210831997](./嵌入式软件学习.assets/image-20250521210831997.png)



### 3.宏



#### 3.1宏定义与重命名



```c
define ABC 1234  //宏定义
 
typedef unsigned char uint8_t ; //将较长的变量名称进行换名字


/*区别*/
1.define 的新名字在左边 typedef 在右边
2.typedef 有分号；  且只能给变量类型换名字
    
    
    
```



#### 3.2预编译



```c
/*1.防止重复编译*/
#ifndef __LED_H
#define __LED_H

#endif


```











### 4.结构体



```c
/*常用方式*/
typedef struct{
    int a ;
    char b;
    int c ;
} Struct_Able ;

Struct_Able struct_able = {0,0,0} ;
Struct_Able struct_able = {.a= 1} ; //其他未初始化赋值的成员 为0





```





#### 4.1内存对齐



##### 产生这个问题的根本原因：

- 例如存储一个 int a  = 1 ;在四个存储颗粒分别存储一个字节，读取数据时从统一地址进行寻址
- 芯片只能同时访问32个 相对地址 相同的数据位   也就是4个字节一次读取，将芯片划分为四个字节一次读取
- **只能从被4整除的地址开始访问接下来的四个字节** （从0地址开始每次只能取4个字节 ）

![image-20240922150836683](./嵌入式软件学习.assets/image-20240922150836683.png)

![image-20240922151122389](./嵌入式软件学习.assets/image-20240922151122389.png)







##### 对齐规则：

![image-20240922150103894](./嵌入式软件学习.assets/image-20240922150103894.png)





#### 4.2位域

[C语言结构体位域及其存储_c语言_Aspiresky-开放原子开发者工作坊 (csdn.net)](https://openatomworkshop.csdn.net/664eefc9b12a9d168eb72e41.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzI3NTk0NDAyLCJpYXQiOjE3MjY5ODk2MDIsInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.4aNtolzLDUpJxeseEDGtqaXRfennZPq6lOs4_-1uPHY&spm=1001.2101.3001.6650.14&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-14-106891585-blog-137954237.235^v43^pc_blog_bottom_relevance_base8&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-14-106891585-blog-137954237.235^v43^pc_blog_bottom_relevance_base8)

![image-20240922152223599](./嵌入式软件学习.assets/image-20240922152223599.png)





匿名位域只占空间，不可使用

![image-20240922152233391](./嵌入式软件学习.assets/image-20240922152233391.png)



##### 位域存储

**正常配合联合体使用时，声明同一类型的位域和结构体即可，各个位都是紧挨着**



- 定义类型一致，紧挨存储
- 定义类型一致，超过存储空间；另起一片空间进行存储

![image-20240922152630827](./嵌入式软件学习.assets/image-20240922152630827.png)

![image-20240922152820228](./嵌入式软件学习.assets/image-20240922152820228.png)



### 5.枚举



定义一个取值受限的整形变量，用于限制变量的取值范围

![image-20240710211710186](./嵌入式软件学习.assets/image-20240710211710186.png)



```c
typedef enum{
MONDAY,
TUESDAY,
WENDNSDAY
} Week_t ;

Week_t week ;
week = MONDAY ;//只能赋值枚举类型中的值


```





### 6.变量

#### 6.1变量与函数的声明

[变量的声明和定义 - ren_zhg1992 - 博客园 (cnblogs.com)](https://www.cnblogs.com/renzhuang/articles/6737505.html)

程序中变量和函数只能定义一次，但是可以声明很多次

###### 变量：

**只有加入extern才能是声明**

```c
int a;//声明也是定义
int a = 10 ; //定义
extern int a = 10 ; //定义

extern int a ; //声明
```

![image-20240710225247030](./嵌入式软件学习.assets/image-20240710225247030.png)

###### 函数：

**带有{ }为定义，不带为声明**

```c
extern void doc(int a , int b) ; //声明


extern void doc(int a , int b) { } ; //定义
		 void doc(int a , int b) { } ; //定义
```



### 7.指针



#### 7.1数据存储

![image-20240714091321632](./嵌入式软件学习.assets/image-20240714091321632.png)

**内存在中 每一个地址代表一个字节**



##### 7.1.1大端小端



###### 定义：

- 大端：高位字节排放在内存的低地址端，低位字节排放在内存的高地址端
- 小端：低位字节排放在内存的低地址端，高位字节排放在内存的高地址端

**总结：看内存低地址存储，存高位就是大端，地位就是小端**

比如数字 0x12 34 56 78在内存中的表示形式：![image-20240714090901396](./嵌入式软件学习.assets/image-20240714090901396.png)





###### 如何程序判断大端小端：



使用联合体：

union 型数据所占的空间等于其最大的成员所占的空间。对 union 型的成员的存取都是相对于该联合体基地址的偏移量为 0 处开始，也就是**联合体的访问不论对哪个变量的存取都是从 union 的首地址位置开始**。联合是一个**在同一个存储空间里存储不同类型数据的数据类型**。这些**存储区的地址都是一样的，联合里不同存储区的内存是重叠的，修改了任何一个其他的会受影响**

```c
#include <stdio.h>
int main (void)
{
	union
	{
		short i;
		char a[2];
	}u;
	u.a[0] = 0x11;
	u.a[1] = 0x22;
	printf ("0x%x\n", u.i);  //0x2211 为小端  0x1122 为大端
	return 0;
}
输出结果：
0x2211
```



#### 7.1.2定义指针

<img src="./嵌入式软件学习.assets/image-20240714092201535.png" alt="image-20240714092201535" style="zoom:50%;" />小端存储



1. 指针变量存储的是其他数据类型的**首地址**（如上图所示，一个变量存储会跨多个字节）

2. 存储的地址包括**内存地址+数据类型信息**（内存地址为首地址，不同的数据类型占用的空间不同，只知道首地址，不知道取几个数）

3. **指针的数据长度与系统位宽相同**，如STM32的指针位宽为4个字节

4. ```c
   int *p ; //这里的*代表该变量为指针类型，并不是取值的意思，仅为标识符作用。
   int *p,p1 // p为指针类型 ， p1为整型。
   ```

   

![image-20240714092840021](./嵌入式软件学习.assets/image-20240714092840021.png)



#### 7.1.3使用指针

- 定义 int *p ;  
- &取址  
- \*取值  ，区别定义时的*仅为标识符
- 对**指针变量进行加减，移动一个数据宽度**。这里p为int型，++移动4个字节

![image-20240714094739379](./嵌入式软件学习.assets/image-20240714094739379.png)



```c
int *p,a,b;
a=1;
p=&a;
*p = 2 ; //p指向a  此操作为a赋值2
b = *p //将a的值赋值给b b为2



```









#### 7.1.4数组与指针



- 数组名就是指针	例如： **a[10]，a就是指针指向数组首地址，也是第一个元素地址。**
- **[ ]实际上是变址运算符，a[i]按a+i计算地址。在找出该地址对应的值**。
- 

![image-20240714101940894](./嵌入式软件学习.assets/image-20240714101940894.png)

```c
int *p,a[10] ;
p=a ; //a即数组首地址

//下列地址相等
&a[0] == p
&a[1] == p+1
a+1 == p+1
 //下列值相等
    *p = a[0];
	*(P+1) = a[1] ;
```



#### 7.1.5多级指针

- 变量0级指针，对变量取值为一级指针，对一级指针取址为二级指针
- 

![image-20240715220328834](./嵌入式软件学习.assets/image-20240715220328834.png)









#### 7.1.8常见的指针引用 

![image-20240715220005739](./嵌入式软件学习.assets/image-20240715220005739.png)

- 传递参数：1.有时需要处理大量数据，使用形参会使用大量空间且费时。2.C语言只能返回一个参数，有时需要返回大量参数可以使用传地址来返回

  ```c
  void arar(const int * a)   //可以通过对形参加上关键字const 防止指针对应值被修改
  ```

- 传递句柄：常用于封装

  ```c
  int a[3]={1,2,3}
  //封装函数获得全局数组句柄
  int * Getaray( )
  {
      return a ;
  }
  
  
  //使用句柄
  int *p = Getaray( );
  p[2] = 10 ;
  
  ```

  

- 直接访问物理地址

  ```c
  //1.直接复制指针真实物理地址，进行操作
  //2.传递复制数据类型：例如传递浮点数 3.1415 ，正常可以扩大1000倍进行传递整形。下列方式将内存直接进行传输，在下一个设备进行解析。
  现在有一种全新的方式：
      传递该数的内存，浮点数在内存以阶乘的方式进行存储，只需将存储改数据的8个字节内容进行传递，在下一个设备接收后进行解析
      
      
  
      
  
  
  
  
  ```

  

#### 7.1.9空指针

 void *

- 空指针：指向空类型 或者 不指向确定的类型
- **常用于 malloc( )等动态分配地址函数**（定义 void * molloc( )），返回一个空指针。
- **空指针作为函数返回值**，**不指向任何一种具体的数据类型，只提供一个地址数据**，**只有转换为有指向的地址，才能取值**。为了在分配空间后，此空间可以给任何数据类型使用，使用前需要先对数据进行强制转换有数据类型的地址

```c
//在一些编译系统在进行地址赋值时，会自动进行类型转换
int *p ;
p = (int *) macaloc(100) ; //对返回的地址，重新强制转换指向类型

//自动
p = macaloc(100) ; //目前一些编译器会进行自动转换 （int *）

```





#### 7.1.10函数指针

- 函数入口：在编译函数源代码时，会将转换后的**可执行代码放入一段存储空间内**，**该空间的起始地址称为函数的入口**
- **函数名就是函数起始地址，与数组一样（数组名就是数组地址）**
- ![image-20240919230301892](./嵌入式软件学习.assets/image-20240919230301892.png)（*p)在定义时不可省略
- 不可对函数指针进行地址操作 p++ 等
- 使用函数指针调用函数可以，调用不同的函数，可以使用二维函数指针数组，实现二维状态机
- 







```c
/*定义
1.定义函数指针   类型名 （* 指针变量名）（函数参数列表）         函数名代表函数返回值
2.下面定义：一个形参为两个int类型，返回值为int类型的函数指针
3.只能指向相同类型的函数
4.不可省略（*p）的两边括号
*/
int （* p）(int , int) ;

    
    
 /*赋值
 1.函数名代表函数入口，直接将函数名赋值给变量即可  			int max(int a , int b) ;
 */   
  p=max ;  //将函数入口赋值 不可p=max(2,3)这样给的是返回值

/*使用
1.(*p)替代函数名
*/
（*p）(2,3) ;
    
    
```







#### 7.1.11* 与 ++



####  

```c
//*p++  先对p进行取值，再对p地址进行偏移1
i = *p++ ；
      
```

<img src="./嵌入式软件学习.assets/image-20241112224614067.png" alt="image-20241112224614067" style="zoom:50%;" />







```c
//*++p 先对p进行自增1，在对p进行取值
i = *++p
```

<img src="./嵌入式软件学习.assets/image-20241112224735763.png" alt="image-20241112224735763" style="zoom:67%;" />



#### 7.1.12指针常量与常量指针

[指针常量与常量指针解析-CSDN博客](https://blog.csdn.net/qq_36132127/article/details/81940015?spm=1001.2014.3001.5506)



- 指针常量：本身是常量，在初始化时就赋初值，始终指向一个地址。
- 常量指针：本身指针，但是只能指向常量。指向的地址可以改变，但是地址必须是常量。



![image-20250318205900086](./嵌入式软件学习.assets/image-20250318205900086.png)





### 8.联合体：

union 型数据所占的空间等于其最大的成员所占的空间。对 union 型的成员的存取都是相对于该联合体基地址的偏移量为 0 处开始，也就是**联合体的访问不论对哪个变量的存取都是从 union 的首地址位置开始**。联合是一个**在同一个存储空间里存储不同类型数据的数据类型**。这些**存储区的地址都是一样的，联合里不同存储区的内存是重叠的，修改了任何一个其他的会受影响**















### 9.数组

**数组用来存储同一类型的数据**。



#### 9.1一维数组

##### 定义：

- 定义一片连续内存空间用于存储数据
- 不可定义可变长度数组 a[n],n为变量

```c
int arr[10] ;
```

##### 初始化：

```c
int a[5] = {1,2,3,4,5}; //在定义时，将所有元素赋值
int a[5] = {1,2,3};//定义一部分元素，其他未定义的元素都为0
int a[5] = {0}; //元素全为0
int a[] = {1,2,3}; //自动定义一个数据长度为3的数组

```

##### 引用：

```c
a[5];//引用但是是从 0 开始，这里代表第6个元素

//使用指针索引
*(a+5)  *(p+1)
```











### 15.文件作用

#### 15.1头文件

[C语言基础：头文件的使用_c语言_chhttty-GitCode 开源社区 (csdn.net)](https://gitcode.csdn.net/65e95d191a836825ed7908ac.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzIxMjI0NDY1LCJpYXQiOjE3MjA2MTk2NjUsInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.9cbBHEv7v20AunhM8-PXshM5R88ETKgDtRtRo1WD0i8)

- 在C语言编程中，头文件有着十分重要的作用，最核心的作用就是**对外声明函数**或**全局变量**，供应用其头文件的函数调用。              其次：还有**类型定义、宏定义**![image-20240710220941079](./嵌入式软件学习.assets/image-20240710220941079.png)

- 头文件结构

  ![image-20240710221847759](./嵌入式软件学习.assets/image-20240710221847759.png)![image-20240710221857519](./嵌入式软件学习.assets/image-20240710221857519.png)









##### 15.1.1防止重复编译

![image-20240710220543702](./嵌入式软件学习.assets/image-20240710220543702.png)



##### 15.1.2公共头文件

把一些公共的宏定义、类型定义、模块头文件写在一个头文件中，其他头文件再去包含这个都用得到的



##### 15.1.3头文件作用

在预编译阶段，会将#include "a.h"进行展开，在main.c编译时，由于展开有了print（）函数的声明，当出现声明，后面对该函数的调用就成为站位符，在连接的时候会寻找print函数的实现。

main.c

```c
#include "a.h"
int main(){

	print();
}
```



a.c

```
#include "a.h"
void print()
{

....
}

```



a.h

```
#ifndef A_H
#define A_H
	void print();
#endif
```

![image-20260514221414113](./嵌入式软件学习.assets/image-20260514221414113.png)

![image-20260514221434909](./嵌入式软件学习.assets/image-20260514221434909.png)

![image-20260514221447412](./嵌入式软件学习.assets/image-20260514221447412.png)

### 16数据结构



#### 16.1单链表



##### 1.初始化单链表

带头节点，头节点不存放数据元素

![image-20240908171149918](./嵌入式软件学习.assets/image-20240908171149918.png)

###### 定义

```c
//定义一个节点 
typedef  struct {
    unsigned short data ; //数据域
    LinkList *next ;  //指针，指向下一个节点
   
}LNode , *LinkList;
```









##### 2.单链表的插入和删除



###### 插入  



**通过链表头指针，遍历整个链表找到需要操作的位置进行插入**



- ![image-20240910224728953](./嵌入式软件学习.assets/image-20240910224728953.png)
- 如上图插入的顺序不可改变![image-20240910224802397](./嵌入式软件学习.assets/image-20240910224802397.png)

```c


/*单链表插入函数
参数：L-所在链表（头节点） i-需要插入的节点  e-插入的节点
原理：寻找带第i-1个元素插入
*/
int ListInsert(LinkList L ,int i ,LNode *e)
{
    if (i<1)
    {
        return 0 ;
    }
    LNode *P =  L ; //声明一个节点指向头节点，用于遍历的节点
    int j = 0 ; //用于遍历的计数器
    while ((P->next!=0)&&(j<i-1))   //将P指向第 i-1 个节点 
    {
        P = P->next ; //p向后挪
        j++ ;
    }
    
   if (P ==NULL) //当过程中为NULL了表示已经到链尾了
    {
        return 0 ;
    }
    
    e->next = P->next ; //e指向P的下一个节点 也就是之前的第i个节点
    P->next = e ; // 第 i-1 个节点 指向 e
    
    

}
```



**当未给定链表头指针，无法遍历节点，需要前插入一个节点**

**在XY之间插入S节点，再将X与S的数据进行对调**

![image-20240910225546553](./嵌入式软件学习.assets/image-20240910225546553.png)





###### 删除

**如图：如果需要删除i=1 处的节点，通过遍历将前一个节点，让其指向所需删除节点的后一个节点**

<img src="./嵌入式软件学习.assets/image-20240910231810642.png" alt="image-20240910231810642" style="zoom:80%;" />

![image-20240910231859815](./嵌入式软件学习.assets/image-20240910231859815.png)

![image-20240910232120682](./嵌入式软件学习.assets/image-20240910232120682.png)



**当没有链表指针进行索引时**

如图需要删除x节点时，可以删除Y节点，再将Y节点的数据移到x

![image-20240910232505887](./嵌入式软件学习.assets/image-20240910232505887.png)

![image-20240910232715348](./嵌入式软件学习.assets/image-20240910232715348.png)



###### 查找



**按位查找**

![image-20240910233136620](./嵌入式软件学习.assets/image-20240910233136620.png)

**按值查找**

![image-20240910233444314](./嵌入式软件学习.assets/image-20240910233444314.png)



如果需要删除的是最后一个节点，则只能t通过遍历的方式

![image-20240910232829996](./嵌入式软件学习.assets/image-20240910232829996.png)



###### 单链表的建立

- 建立一个链表，在链头或者链尾插入节点

![image-20240910233713782](./嵌入式软件学习.assets/image-20240910233713782.png)



**尾插法**

**创建一个链尾指针，在每次链尾插入节点后，链尾指针都向后移动。好处是，不用每次都从链头进行遍历到尾指针**

![image-20240910234104046](./嵌入式软件学习.assets/image-20240910234104046.png)





 

**头插法**

- 对头结点进行后插操作
- 可以对输入数据进行逆置<img src="./嵌入式软件学习.assets/image-20240910234659474.png" alt="image-20240910234659474" style="zoom:67%;" />

![image-20240910234817785](./嵌入式软件学习.assets/image-20240910234817785.png)









##### 3.双链表

###### 1.定义

带有数据域、两个指向前后节点的指针

![image-20240911204656466](./嵌入式软件学习.assets/image-20240911204656466.png)



###### 2.插入



![image-20240911204802551](./嵌入式软件学习.assets/image-20240911204802551.png)

![image-20240911205547039](./嵌入式软件学习.assets/image-20240911205547039.png)程序优化防止后面空指针

**操作顺序**

- 先操作被插入节点，防止原来的链路断开
- 在操作间接节点Y
- 最后操作直接节点X

![image-20240911205126552](./嵌入式软件学习.assets/image-20240911205126552.png)





###### 3.删除



![image-20240911205828356](./嵌入式软件学习.assets/image-20240911205828356.png)

![image-20240911205836954](./嵌入式软件学习.assets/image-20240911205836954.png)





###### 4.遍历

- 向后遍历：循环判断后继指针是否为空
- 向前遍历：循环判断前继指针是否为空
- 调过头结点：提前判断前节点的前继指针是否为空

![image-20240911210002436](./嵌入式软件学习.assets/image-20240911210002436.png)





##### 4.循环双列表



![image-20240911220108351](./嵌入式软件学习.assets/image-20240911220108351.png)



###### 初始化循环双链表

需要让其两个指针指向自身

![image-20240911220154099](./嵌入式软件学习.assets/image-20240911220154099.png)







### 17.大小端

![image-20240922142150421](./嵌入式软件学习.assets/image-20240922142150421.png)



![image-20240922142243783](./嵌入式软件学习.assets/image-20240922142243783.png)



![image-20240922154005227](./嵌入式软件学习.assets/image-20240922154005227.png)







### 18.编译

[程序详细编译过程（预处理、编译、汇编、链接） - 知乎](https://zhuanlan.zhihu.com/p/476697014)



- 预处理-编译-汇编-链接



![image-20260514212107271](./嵌入式软件学习.assets/image-20260514212107271.png)





















## 三、GIT与vscode配合使用

1. **U**代表未跟踪，也就是新添加文件；**M**代表被修改文件；**D与删除线**代表被删除![image-20240721073811204](./嵌入式软件学习.assets/image-20240721073811204.png)
2. commit提交后，修改会被更新到本地代码中



















## 四、工程经验

### 1.数据处理

#### 1.1滤波



##### 1.1.1迟滞滤波

设置上限和下限：低于下限为0，高于上限为1；信号在之间变换可以滤除，防止单一阈值来回跳变。











#### 1.2值运算保留

##### 1.2.1放大运算保留小数











### 2.算法



#### 2.1CRC校验（循环冗余校验）

- 不需要知道哪一位出错，只需要知道传输的数据出错。**无需纠错，只需判断数据错误**
- **使用模2除法，约定除数，检验余数**。当发送的数据，除以约定的除数，余数为0则认为数据准确
- 冗余位数为约定除数位数 - 1

![image-20241105233727061](./嵌入式软件学习.assets/image-20241105233727061.png)



##### 计算方法



###### 模2除法：只考虑第一位是否够减，不进位或者退位，相同的相减为0，不同相减为1

<img src="./嵌入式软件学习.assets/image-20241106204315645.png" alt="image-20241106204315645" style="zoom:50%;" />

![image-20241106204413702](./嵌入式软件学习.assets/image-20241106204413702.png)







##### 循环

校验数据，选定待校验的数据前五位：第一位为1-进行异或运算，第一位为0向后移1位，知道最后一个数据，如果全零则认为余数为0数据准确，反之数据错误

![image-20241106205457059](./嵌入式软件学习.assets/image-20241106205457059.png)





**标准CRC校验**



计算CRC冗余值：



![image-20241106211857795](./嵌入式软件学习.assets/image-20241106211857795.png)





**一字节CRC校验**

![image-20241106211921213](./嵌入式软件学习.assets/image-20241106211921213.png)









### 3.通讯协议

#### 3.1MODBUS





#### 3.2MQTT



### 4.信息处理模型



![image-20250525113918837](./嵌入式软件学习.assets/image-20250525113918837.png)

![image-20250525113933928](./嵌入式软件学习.assets/image-20250525113933928.png)

![image-20250525113943488](./嵌入式软件学习.assets/image-20250525113943488.png)







### 5.hardfault中断的处理

- 进入中中断后，记录R1-R15寄存器值，保存在flash中用于调试现场，缩小故障排查范围。
- 进入中断后，无需要一直循环，可以延时一段时间，看看会不会触发看门狗中断（一般重要任务都会有喂狗任务），当没有触发喂狗任务，则说明跑飞任务并不重要，软件可以“带病”运行，当机器空闲外界条件满足复位条件时可以主动进行复位。





## 五、freertos



### 5.1内核源码编写（野火 ）



#### 5.1.1新建FreeRtos工程



##### 1.新建文件夹

![image-20240907222649755](./嵌入式软件学习.assets/image-20240907222649755.png)

##### 2.新建工程



###### 创建KEIL工程

![image-20240907220408842](./嵌入式软件学习.assets/image-20240907220408842.png)

![image-20240907220507626](./嵌入式软件学习.assets/image-20240907220507626.png)

![image-20240907220642197](./嵌入式软件学习.assets/image-20240907220642197.png)



###### 创建分组



![image-20240907220952627](./嵌入式软件学习.assets/image-20240907220952627.png)



![image-20240907221312050](./嵌入式软件学习.assets/image-20240907221312050.png)

###### 配置仿真时钟



![image-20240907221701279](./嵌入式软件学习.assets/image-20240907221701279.png)

![image-20240907221820198](./嵌入式软件学习.assets/image-20240907221820198.png)



![image-20240907221936327](./嵌入式软件学习.assets/image-20240907221936327.png)

###### 添加头文件



![image-20240907222250397](./嵌入式软件学习.assets/image-20240907222250397.png)







#### 5.1.2裸机系统与多任务系统

##### 裸机系统

裸机系统分为**轮询系统**（while）、**前后台系统**（中断处理）

<img src="./嵌入式软件学习.assets/image-20240907223921168.png" alt="image-20240907223921168" style="zoom:50%;" />



<img src="./嵌入式软件学习.assets/image-20240907223937430.png" alt="image-20240907223937430" style="zoom:80%;" />





##### 多任务系统

![image-20240907224424593](./嵌入式软件学习.assets/image-20240907224424593.png)





##### 两种系统之间对比

- 轮询系统：主程序进行时间响应、事件处理
- 前后台系统：在中断中响应事件（**短时间处理可以在中断中进行处理，需要长时间的置标记位在主程序进行处理**），主函数轮询处理
- 多任务系统：**在中断响应事件置标记位，直接根据标记位唤醒对应的任务**；**实时响应和处理事件**

![image-20240907224655664](./嵌入式软件学习.assets/image-20240907224655664.png)



#### 5.1.3数据结构-列表与列表项



##### 1.单链表

- 需有一个节点指针，指向下一个节点，相互串联组成列表
- 每个节点还可以携带一些私有信息

![image-20240908143349612](./嵌入式软件学习.assets/image-20240908143349612.png)



- 定义节点，指向下一个节点
- 任意数据结构体，**内嵌节点**就可以将自身**挂载在链表上**

![image-20240908143804789](./嵌入式软件学习.assets/image-20240908143804789.png)



###### 链表操作

- 链表：将离散的数据串联起来，常规操作就是 插入和删除
- 根节点：人为定义的，根节点自带一个计数器，用于记录这个链表的节点个数。



![image-20240908144256106](./嵌入式软件学习.assets/image-20240908144256106.png)







##### 2.双向链表

双向链表：节点有两个指针，可以两个方向遍历列表

![image-20240908144612246](./嵌入式软件学习.assets/image-20240908144612246.png)



##### 3.链表与数组的区别

- 链表在逻辑上是连续的，物理存储上是离散的

- 数组在逻辑和物理上都是连续的

  

![image-20240908144851514](./嵌入式软件学习.assets/image-20240908144851514.png)





##### 4.FreeRtos链表的实现





###### 1.定义节点

![image-20240908145553241](./嵌入式软件学习.assets/image-20240908145553241.png)

![image-20240908160113881](./嵌入式软件学习.assets/image-20240908160113881.png)

###### 2.初始化节点

传入节点指针，将**节点的所在链表初始化为 NULL**

```c
/* 节点初始化 */
void vListInitialiseItem( ListItem_t * const pxItem )
{
	/* 初始化该节点所在的链表为空，表示节点还没有插入任何链表 */
	pxItem->pvContainer = NULL; //该节点未指向任何更节点，即没有挂载在任何链表上
}
```





###### 3.MINI节点

精简节点，**无需挂载其他信息，只需连接链表即可**

```c
/* mini节点结构体定义，作为双向链表的结尾
   因为双向链表是首尾相连的，头即是尾，尾即是头 */
struct xMINI_LIST_ITEM
{
	TickType_t xItemValue;                      /* 辅助值，用于帮助节点做升序排列 */
	struct xLIST_ITEM *  pxNext;                /* 指向链表下一个节点 */
	struct xLIST_ITEM *  pxPrevious;            /* 指向链表前一个节点 */
};
typedef struct xMINI_LIST_ITEM MiniListItem_t;  /* 最小节点数据类型重定义 */
```







###### 4.根节点

**定义根节点**

根节点 头就是尾 头和尾在同一节点内

```c
/* 链表结构体定义 */
typedef struct xLIST
{
	UBaseType_t uxNumberOfItems;    /* 链表节点计数器 */
	ListItem_t *  pxIndex;			/* 链表节点索引指针，指向为尾节点 */
	MiniListItem_t xListEnd;		/* 链表最后一个节点（包含在根节点中） , 不需要携带其他信息，使用mini节点即可*/  
} List_t;
```

![image-20240908160432126](./嵌入式软件学习.assets/image-20240908160432126.png)



**根节点初始化**

**节点初始化时，需要两个指针指向自身**

<img src="./嵌入式软件学习.assets/image-20240911221110449.png" alt="image-20240911221110449" style="zoom:50%;" />

![image-20240908162616546](./嵌入式软件学习.assets/image-20240908162616546.png)

```c
/* 链表根节点初始化 */
void vListInitialise( List_t * const pxList )
{
	/* 将链表索引指针指向最后一个节点 */
	pxList->pxIndex = ( ListItem_t * ) &( pxList->xListEnd );  //索引节点指向本身自带的尾节点

	/* 将链表最后一个节点的辅助排序的值设置为最大，确保该节点就是链表的最后节点 */
	pxList->xListEnd.xItemValue = portMAX_DELAY; //将链表的最后一个节点的辅助升序复制为最大，确保为最后一个节点

    /* 将最后一个节点的pxNext和pxPrevious指针均指向节点自身，表示链表为空 */
	pxList->xListEnd.pxNext = ( ListItem_t * ) &( pxList->xListEnd );   //最后一个节点的前后指针都指向自身，表示为空
	pxList->xListEnd.pxPrevious = ( ListItem_t * ) &( pxList->xListEnd );

	/* 初始化链表节点计数器的值为0，表示链表为空 */
	pxList->uxNumberOfItems = ( UBaseType_t ) 0U; //节点记数为0
}
```



![image-20240908161943829](./嵌入式软件学习.assets/image-20240908161943829.png)





###### 5.将节点插入链表尾部



```c
/* 将节点插入到链表的尾部 */
void vListInsertEnd( List_t * const pxList, ListItem_t * const pxNewListItem )
{
	ListItem_t * const pxIndex = pxList->pxIndex; //索引指向尾节点

	pxNewListItem->pxNext = pxIndex; //所需插入的节点 ---下一节点为根节点
	pxNewListItem->pxPrevious = pxIndex->pxPrevious; //所需插入的节点 ---上一节点为根节点上一节点 （原来的尾部）
    
    
	pxIndex->pxPrevious->pxNext = pxNewListItem;   //根节点的下一节点的下一节点为新节点
	pxIndex->pxPrevious = pxNewListItem; //根节点的下一节点为新节点

	/* 记住该节点所在的链表 */
	pxNewListItem->pvContainer = ( void * ) pxList;

	/* 链表节点计数器++ */
	( pxList->uxNumberOfItems )++;
}
```

![image-20240908163609920](./嵌入式软件学习.assets/image-20240908163609920.png)



1.插入第一个节点：在尾节点尾部插入一个节点

![image-20240911221558156](./嵌入式软件学习.assets/image-20240911221558156.png)

2.插入第二个节点



![image-20240911222110917](./嵌入式软件学习.assets/image-20240911222110917.png)





###### 6.按节点升序插入列表



![image-20240911222257343](./嵌入式软件学习.assets/image-20240911222257343.png)



```c
/* 将节点按照升序排列插入到链表 */
void vListInsert( List_t * const pxList, ListItem_t * const pxNewListItem )
{
	ListItem_t *pxIterator; //声明一个遍历指针
	
	/* 获取节点的排序辅助值 */
	const TickType_t xValueOfInsertion = pxNewListItem->xItemValue; //获取升序值

	/* 寻找节点要插入的位置 */
	if( xValueOfInsertion == portMAX_DELAY ) //当升序值为为最大，直接寻找最后一个节点进行尾插，不再需要遍历
	{
		pxIterator = pxList->xListEnd.pxPrevious; 
	}
	else
	{
		for( pxIterator = ( ListItem_t * ) &( pxList->xListEnd ); //寻找需要进行后插的节点（此节点的后节点升序大于于所需插入的节点）
		     pxIterator->pxNext->xItemValue <= xValueOfInsertion; 
			 pxIterator = pxIterator->pxNext )
		{
			/* 没有事情可做，不断迭代只为了找到节点要插入的位置 */			
		}
	}

	pxNewListItem->pxNext = pxIterator->pxNext; //进行后插操作
	pxNewListItem->pxNext->pxPrevious = pxNewListItem;
	pxNewListItem->pxPrevious = pxIterator;
	pxIterator->pxNext = pxNewListItem;

	/* 记住该节点所在的链表 */
	pxNewListItem->pvContainer = ( void * ) pxList;

	/* 链表节点计数器++ */
	( pxList->uxNumberOfItems )++;
}
```







###### 7.将节点从链表中移除



将节点从，该节点所在链表进行删除



```c
/* 将节点从链表中删除 */
UBaseType_t uxListRemove( ListItem_t * const pxItemToRemove ) 
{
	/* 获取节点所在的链表 */
	List_t * const pxList = ( List_t * ) pxItemToRemove->pvContainer; //获取该节点所在链表

	pxItemToRemove->pxNext->pxPrevious = pxItemToRemove->pxPrevious; //解除连续 
	pxItemToRemove->pxPrevious->pxNext = pxItemToRemove->pxNext;

	/* Make sure the index is left pointing to a valid item. */
	if( pxList->pxIndex == pxItemToRemove )
	{
		pxList->pxIndex = pxItemToRemove->pxPrevious;
	}

	/* 初始化该节点所在的链表为空，表示节点还没有插入任何链表 */
	pxItemToRemove->pvContainer = NULL;
	
	/* 链表节点计数器-- */
	( pxList->uxNumberOfItems )--;

	/* 返回链表中剩余节点的个数 */
	return pxList->uxNumberOfItems;
}

```



![image-20240911223515702](./嵌入式软件学习.assets/image-20240911223515702.png)





###### 8.带参宏函数

通过宏函数，定义一些常用操作

![image-20240911230308570](./嵌入式软件学习.assets/image-20240911230308570.png)







#### 5.1.4任务

![image-20240911230846997](./嵌入式软件学习.assets/image-20240911230846997.png)





##### 1.栈

###### 定义：

- **环境变量存储在栈**
- 栈是在**RAM中的一段连续的内存空间**
- 在启动文件、脚本中定义

![image-20240911231010145](./嵌入式软件学习.assets/image-20240911231010145.png)



- 每个任务都有独立的栈
- 栈一般是全局数组，连续的RAM空间

![image-20240911231243322](./嵌入式软件学习.assets/image-20240911231243322.png)







###### 如何实现

![image-20240911231822955](./嵌入式软件学习.assets/image-20240911231822955.png)裸机





![image-20240911231905855](./嵌入式软件学习.assets/image-20240911231905855.png)由于**全局变量是单独定义在RAM中，一般任务定义一个全局数组模拟作为任务的独立****栈**



###### freertos实现栈

```c
#define portSTACK_TYPE	uint32_t
typedef portSTACK_TYPE StackType_t;

#define TASK1_STACK_SIZE                    128		//栈大小默认最小为128
StackType_t Task1Stack[TASK1_STACK_SIZE]; //这里申请一个栈，本质就是一个uint32的数组 

```



- 默认栈的大小，FreeRTos推荐使用**最小为 128** 
- **portmacro.h**：这个头文件，**凡是涉及数据类型的地方，都会在这个文件中进行重新取名定义**

![image-20240912224514837](./嵌入式软件学习.assets/image-20240912224514837.png)







##### 2.任务

任务作为一个**独立的函数**，函数整体**无限循环且不能返回**

两个任务都是无限循环的

![image-20240912225224635](./嵌入式软件学习.assets/image-20240912225224635.png)



##### 3.任务控制块

- 在**裸机系统中，程序的主体是 CPU 按照顺序执行**的。而在**多任务系统中，任务的执行 是由系统调度**的。
- 系统为了顺利的调度任务，为每个任务都额外定义了一个任务控制块， 这个任**务控制块就相当于任务的身份证**，里面存**有任务的所有信息，比如任务的栈指针， 任务名称，任务的形参等**。有了这个任务控制块之后，以后系统对任务的全部操作都可以 通过这个任务控制块来实现。
- 定义一个任务控制块需要一个新的数据类型，该数据类型在 task.c 这 C 头文件中声明

```c
typedef struct tskTaskControlBlock
{
	volatile StackType_t    *pxTopOfStack;    /* 栈顶 */

	ListItem_t			    xStateListItem;   /* 任务节点 ,其他链表可以指向此，这是一个内置在 TCB 控制块中的链表节点，通过这个节点，可以将任务控制											块挂接到各种链表中。这个节点就类似晾衣架的钩子，TCB 就是衣服 */
    
    StackType_t             *pxStack;         /* 任务栈起始地址 */
	                                          
	char                    pcTaskName[ configMAX_TASK_NAME_LEN ];  /* 任务名称，字符串形式 默认16个字节长度 */
} tskTCB;
typedef tskTCB TCB_t;


TCB_t Task1TCB; //定义一个任务控制块
```





##### 4.任务创建函数

- **任务栈 + 任务实体函数 + 任务控制块** ：将这三者**联系起来才能同一进行调度**。这个**联系的工作就由任务创建函数 xTaskCreateStatic()**来实现
- 任务创建分为：
  1. 动态创建：任务控制块和栈的内存是创建任务时**动态分配**的，**任务删除时，内存可以释放**
  2. 静态创建：内存需要事先定义好，是**静态的内存** ， 任务**删除时 ， 内存不能释放** 。 configSUPPORT_STATIC_ALLOCATION == 1时为静态创建



```c
typedef void (*TaskFunction_t)( void * ); //返回值和形参都是void类型

typedef void * TaskHandle_t; //任务创建函数 的返回值是空指针


/*
任务的创建有两种方法，一种是使用动态创建，一种是使用静态创建。
动态创建时，任务控制块和栈的内存是创建任务时动态分配的，任务删除时，内存可以释放。
静态创建时，任务控制块和栈的内存需要事先定义好，是静态的内存 ， 任务删除时 ， 内存不能释放 。 设置为1时为静态创建
*/
#if( configSUPPORT_STATIC_ALLOCATION == 1 )

TaskHandle_t xTaskCreateStatic(	TaskFunction_t pxTaskCode,           /* 任务入口，即任务的函数名称 */
					            const char * const pcName,           /* 任务名称，字符串形式 */
					            const uint32_t ulStackDepth,         /* 任务栈大小，单位为字 */
					            void * const pvParameters,           /* 任务形参 */
					            StackType_t * const puxStackBuffer,  /* 任务栈起始地址 */
					            TCB_t * const pxTaskBuffer )         /* 任务控制块指针 */
{
	TCB_t *pxNewTCB; //定义任务控制块指针变量
	TaskHandle_t xReturn;

	if( ( pxTaskBuffer != NULL ) && ( puxStackBuffer != NULL ) )
	{		
		pxNewTCB = ( TCB_t * ) pxTaskBuffer; 
		pxNewTCB->pxStack = ( StackType_t * ) puxStackBuffer;

		/* 创建新的任务 具体函数在下方 */
		prvInitialiseNewTask( pxTaskCode,        /* 任务入口 */
                              pcName,            /* 任务名称，字符串形式 */
                              ulStackDepth,      /* 任务栈大小，单位为字 */ 
                              pvParameters,      /* 任务形参 */
                              &xReturn,          /* 任务句柄 */ 
                              pxNewTCB);         /* 任务栈起始地址 */      

	}
	else
	{
		xReturn = NULL;
	}

	/* 返回任务句柄，如果任务创建成功，此时xReturn应该指向任务控制块 */
    return xReturn;
}

#endif
```







具体创建任务函数

```c
static void prvInitialiseNewTask( 	TaskFunction_t pxTaskCode,              /* 任务入口 */
									const char * const pcName,              /* 任务名称，字符串形式 */
									const uint32_t ulStackDepth,            /* 任务栈大小，单位为字 */
									void * const pvParameters,              /* 任务形参 */
									TaskHandle_t * const pxCreatedTask,     /* 任务句柄 */
									TCB_t *pxNewTCB )                       /* 任务控制块指针 */

{
	StackType_t *pxTopOfStack;
	UBaseType_t x;	
	
	/* 获取栈顶地址 */
	pxTopOfStack = pxNewTCB->pxStack + ( ulStackDepth - ( uint32_t ) 1 );
	//pxTopOfStack = ( StackType_t * ) ( ( ( portPOINTER_SIZE_TYPE ) pxTopOfStack ) & ( ~( ( portPOINTER_SIZE_TYPE ) portBYTE_ALIGNMENT_MASK ) ) );
	/* 向下做8字节对齐 */
	pxTopOfStack = ( StackType_t * ) ( ( ( uint32_t ) pxTopOfStack ) & ( ~( ( uint32_t ) 0x0007 ) ) );	

	/* 将任务的名字存储在TCB中 */
	for( x = ( UBaseType_t ) 0; x < ( UBaseType_t ) configMAX_TASK_NAME_LEN; x++ )
	{
		pxNewTCB->pcTaskName[ x ] = pcName[ x ];

		if( pcName[ x ] == 0x00 )
		{
			break;
		}
	}
	/* 任务名字的长度不能超过configMAX_TASK_NAME_LEN */
	pxNewTCB->pcTaskName[ configMAX_TASK_NAME_LEN - 1 ] = '\0';

    /* 初始化TCB中的xStateListItem节点 */
    vListInitialiseItem( &( pxNewTCB->xStateListItem ) );
    /* 设置xStateListItem节点的拥有者 */
	listSET_LIST_ITEM_OWNER( &( pxNewTCB->xStateListItem ), pxNewTCB );
    
    
    /* 初始化任务栈 */
	pxNewTCB->pxTopOfStack = pxPortInitialiseStack( pxTopOfStack, pxTaskCode, pvParameters );   


	/* 让任务句柄指向任务控制块 */
    if( ( void * ) pxCreatedTask != NULL )
	{		
		*pxCreatedTask = ( TaskHandle_t ) pxNewTCB;
	}
}
```





![image-20240923231631570](./嵌入式软件学习.assets/image-20240923231631570.png)

































### 5.2freertos基础知识

#### 5.2.1基础知识



- 优先级：当使用软件优先级时，优先级等级数量上没有限制。当使用到硬件优先级时，例如stm32只支持32个硬件中断优先级。
- 多任务可以处于同一优先级。
- 但是每一个任务都有任务堆栈，所以在物理存储上是有任务数量上限的。



<img src="./嵌入式软件学习.assets/image-20250310214903686.png" alt="image-20250310214903686" style="zoom:80%;" />



裸机

![image-20250310215549703](./嵌入式软件学习.assets/image-20250310215549703.png)





RTOS

- 不会空等待，在空闲时也会执行空闲任务
- 中断可以打断任意任务，任务可以同一优先级

![image-20250310215720847](./嵌入式软件学习.assets/image-20250310215720847.png)























##### 1.任务调度器

1. ![image-20241013135347659](./嵌入式软件学习.assets/image-20241013135347659.png)

- 调度器就是使用相关算法，决定当前需要执行的程序
- **抢占式调度**：针对是**不同优先级任务**之间，高优先级先执行。**优先级数值越大，优先级越高**
- **时间片调度**：多个任务在统一优先级下，**相同优先级之间 轮询**

![image-20250311220509836](./嵌入式软件学习.assets/image-20250311220509836.png) 





###### 抢占式：

- **高优先级一直执行，不停止低优先级就无法执行。高优先级可以抢占低优先级**
- **低优先级被抢占进入就绪态**

![image-20241013140143576](./嵌入式软件学习.assets/image-20241013140143576.png)



###### 时间片调度

- 每个任务只执行一个时间片（**不能设置时间片数量，只能设置时间片时间**）时间，时间片时间有系统滴答定时器设定（一**个时间片等于滴答定时器中断周期**）。
- **当任务在执行时阻塞，直接切换到下个任务**，并不会等剩余的时钟片执行完成。

![image-20241013140750066](./嵌入式软件学习.assets/image-20241013140750066.png)





##### 2.任务状态

![image-20241013141547742](./嵌入式软件学习.assets/image-20241013141547742.png)







- 其他任何状态想运行，都需要转换为就绪态
- 仅有就绪态可以进入运行态：   1.**就绪态中有更高优先级任务，抢占进入运行态**            2.**运行态任务挂起或者阻塞，就绪态中根据优先级排序进入运行态**
- 

![image-20241013141929965](./嵌入式软件学习.assets/image-20241013141929965.png)







- **除了运行状态（有且只有一个任务运行），每种状态都有链表**，就绪态有多个优先级链表，挂起和阻塞态只有一个链表。

- 在阻塞和挂起列表中的任务，需要先进入各自的就绪列表再进入运行态。

- 就绪状态有多条链表，当使用硬件优先级时（共32个优先级），共有32条就绪链表，每条链表代表一个优先级

- **使用一个32位变量，每一位代表每个就绪链表是否有任务**，**调度器 总是在就绪列表中寻找最高优先级进行执行**，通过由高位到低位遍历标志变量可以获取最高优先级链表，再在该链表中轮询执行同优先级任务。

  

![image-20241013142946953](./嵌入式软件学习.assets/image-20241013142946953.png)

![image-20250311220002602](./嵌入式软件学习.assets/image-20250311220002602.png)



### 5.3代码移植

#### 1.源码文件夹



##### 1.1源码

- 只需要使用FreeRTOS文件夹-也就是内核文件
- FreeRTOS-Plus为官方组件，一般不使用官方的

![image-20250313212019064](./嵌入式软件学习.assets/image-20250313212019064.png) 		     

##### 1.2内核文件夹

![image-20250313212254200](./嵌入式软件学习.assets/image-20250313212254200.png)



- 其中DEMO文件夹，包含多种芯片架构、与芯片型号。移植代码可以参考这些。

<img src="./嵌入式软件学习.assets/image-20250313212412300.png" alt="image-20250313212412300" style="zoom:80%;" />



- Source文件夹源码

![image-20250313212843208](./嵌入式软件学习.assets/image-20250313212843208.png)



**portable文件夹**

**protable用于建立freertos软件与底层硬件之间的联系，软件与硬件的桥梁**

不同开发环境与芯片其桥梁文件也不同

![image-20241013151309845](./嵌入式软件学习.assets/image-20241013151309845.png)





#### 1.移植步骤

参考正点原子指导手册进行移植





##### 建立FreeRtos文件夹，复制文件

![image-20241013155127181](./嵌入式软件学习.assets/image-20241013155127181.png)



##### 裁切protable文件夹

由于使用MDK 与 STM32 留下这几个文件即可

![image-20241013155255901](./嵌入式软件学习.assets/image-20241013155255901.png)

**说明：**

memmang文件夹是运用与内存管理，文件夹内有几种内存管理算法

![image-20241013155508378](./嵌入式软件学习.assets/image-20241013155508378.png)

RVDS文件夹 是不同芯片的硬件接口 ，根据所使用的芯片进行使用

<img src="./嵌入式软件学习.assets/image-20250313215539321.png" alt="image-20250313215539321" style="zoom:50%;" />不同内核芯片使用的prot.c也不一样

![image-20241013160404009](./嵌入式软件学习.assets/image-20241013160404009.png)

![image-20250313215250697](./嵌入式软件学习.assets/image-20250313215250697.png)每个内核下都有对应的protect.c文件 



##### 工程添加文件

在工程下创建两个组，并移植源文件

![image-20250313215105123](./嵌入式软件学习.assets/image-20250313215105123.png)

![image-20241013160924623](./嵌入式软件学习.assets/image-20241013160924623.png)



##### 添加头文件路径

在工程中添加头文件路径

![image-20241013161145642](./嵌入式软件学习.assets/image-20241013161145642.png)

![image-20241013161242988](./嵌入式软件学习.assets/image-20241013161242988.png)

![image-20241013161301771](./嵌入式软件学习.assets/image-20241013161301771.png)



##### 移植FreeRTOSConfig.h 

- FreeRTOS 的配置文件，文件内都是宏，通过对宏的重新定义达到裁剪FreeRtos的目的，类似于库函数中![image-20250313215752106](./嵌入式软件学习.assets/image-20250313215752106.png)文件，可以选择哪些库函数被包含进入工程
- 可以在官方给的实例中获取，并进行重新修改<img src="./嵌入式软件学习.assets/image-20250313220203731.png" alt="image-20250313220203731" style="zoom:50%;" />在对应示例中获取FreeRTOSConfig.h 文件
- 这里使用正点原子实例代码编写好的

复制到User目录下：

![image-20241013162712881](./嵌入式软件学习.assets/image-20241013162712881.png)



**裁剪FreeRTOSConfig.h**

通过对各种宏，进行定义。使能各类API， 达到裁剪功能的目的。

- include宏<img src="./嵌入式软件学习.assets/image-20250317205400652.png" alt="image-20250317205400652"  />
- config宏用来配置系统的功能
- 

![image-20241014230108295](./嵌入式软件学习.assets/image-20241014230108295.png)



- 软件方法适用于所有硬件，但是效率比较低，优先级无限
- 硬件方法适用于部分硬件，效率较高 ，优先级有限

![image-20241014230705806](./嵌入式软件学习.assets/image-20241014230705806.png)



```c
/* 基础配置项 */

 /* 1: 抢占式调度器, 0: 协程式调度器, 无默认需定义 */
#define configUSE_PREEMPTION      1


/* 1: 使用硬件计算下一个要运行的任务,有些芯片不支持硬件方式, 0: 使用软件算法计算下一个要运行的任务, 默认: 0 */
#define configUSE_PORT_OPTIMISED_TASK_SELECTION         1  
    
 /* 1: 使能tickless低功耗模式, 默认: 0 */    
#define configUSE_TICKLESS_IDLE                         0       

/* 定义CPU主频, 单位: Hz, 无默认需定义 */
#define configCPU_CLOCK_HZ                              SystemCoreClock   

/* 定义SysTick时钟频率，当SysTick时钟频率与内核时钟频率不同时才可以定义, 单位: Hz, 默认: 不定义 */
#define configSYSTICK_CLOCK_HZ                          (configCPU_CLOCK_HZ / 8)

 /* 定义系统时钟节拍频率, 单位: Hz, 无默认需定义 滴答定时器的中断频率，也就是时间片的长度 */
#define configTICK_RATE_HZ                              1000                   

/* 定义最大优先级数, 最大优先级=configMAX_PRIORITIES-1, 无默认需定义 */
#define configMAX_PRIORITIES                            32                      

/* 定义空闲任务的栈空间大小, 单位: Word, 无默认需定义。空闲任务是在CPU空闲时执行的函数 */
#define configMINIMAL_STACK_SIZE                        128             

/* 定义任务名最大字符数, 默认: 16 */
#define configMAX_TASK_NAME_LEN                         16       

 /* 1: 定义系统时钟节拍计数器的数据类型为16位无符号数, 无默认需定义 */
#define configUSE_16_BIT_TICKS                          0  

  /* 1: 使能在抢占式调度下,同优先级的任务能抢占空闲任务, 默认: 1 。空闲任务的优先级为0，当其他优先级为0的任务可以抢占空闲任务*/
#define configIDLE_SHOULD_YIELD                         1  



#define configUSE_TASK_NOTIFICATIONS                    1                       /* 1: 使能任务间直接的消息传递,包括信号量、事件标志组和消息邮箱, 默认: 1 */
#define configTASK_NOTIFICATION_ARRAY_ENTRIES           1                       /* 定义任务通知数组的大小, 默认: 1 */
#define configUSE_MUTEXES                               1                       /* 1: 使能互斥信号量, 默认: 0 */
#define configUSE_RECURSIVE_MUTEXES                     1                       /* 1: 使能递归互斥信号量, 默认: 0 */
#define configUSE_COUNTING_SEMAPHORES                   1                       /* 1: 使能计数信号量, 默认: 0 */
#define configUSE_ALTERNATIVE_API                       0                       /* 已弃用!!! */
#define configQUEUE_REGISTRY_SIZE                       8                       /* 定义可以注册的信号量和消息队列的个数, 默认: 0 */
#define configUSE_QUEUE_SETS                            1                       /* 1: 使能队列集, 默认: 0 */
#define configUSE_TIME_SLICING                          1                       /* 1: 使能时间片调度, 默认: 1 */
#define configUSE_NEWLIB_REENTRANT                      0                       /* 1: 任务创建时分配Newlib的重入结构体, 默认: 0 */
#define configENABLE_BACKWARD_COMPATIBILITY             0                       /* 1: 使能兼容老版本, 默认: 1 */
#define configNUM_THREAD_LOCAL_STORAGE_POINTERS         0                       /* 定义线程本地存储指针的个数, 默认: 0 */
#define configSTACK_DEPTH_TYPE                          uint16_t                /* 定义任务堆栈深度的数据类型, 默认: uint16_t */
#define configMESSAGE_BUFFER_LENGTH_TYPE                size_t                  /* 定义消息缓冲区中消息长度的数据类型, 默认: size_t */

/* 内存分配相关定义 */
#define configSUPPORT_STATIC_ALLOCATION                 0                       /* 1: 支持静态申请内存, 默认: 0 */
#define configSUPPORT_DYNAMIC_ALLOCATION                1                       /* 1: 支持动态申请内存, 默认: 1 */
#define configTOTAL_HEAP_SIZE                           ((size_t)(10 * 1024))   /* FreeRTOS堆中可用的RAM总量, 单位: Byte, 无默认需定义 */
#define configAPPLICATION_ALLOCATED_HEAP                0                       /* 1: 用户手动分配FreeRTOS内存堆(ucHeap), 默认: 0 */
#define configSTACK_ALLOCATION_FROM_SEPARATE_HEAP       0                       /* 1: 用户自行实现任务创建时使用的内存申请与释放函数, 默认: 0 */

/* 钩子函数相关定义 */
#define configUSE_IDLE_HOOK                             0                       /* 1: 使能空闲任务钩子函数, 无默认需定义  */
#define configUSE_TICK_HOOK                             0                       /* 1: 使能系统时钟节拍中断钩子函数, 无默认需定义 */
#define configCHECK_FOR_STACK_OVERFLOW                  0                       /* 1: 使能栈溢出检测方法1, 2: 使能栈溢出检测方法2, 默认: 0 */
#define configUSE_MALLOC_FAILED_HOOK                    0                       /* 1: 使能动态内存申请失败钩子函数, 默认: 0 */
#define configUSE_DAEMON_TASK_STARTUP_HOOK              0                       /* 1: 使能定时器服务任务首次执行前的钩子函数, 默认: 0 */

/* 运行时间和任务状态统计相关定义 */
#define configGENERATE_RUN_TIME_STATS                   0                       /* 1: 使能任务运行时间统计功能, 默认: 0 */
#if configGENERATE_RUN_TIME_STATS
#include "./BSP/TIMER/btim.h"
#define portCONFIGURE_TIMER_FOR_RUN_TIME_STATS()        ConfigureTimeForRunTimeStats()
extern uint32_t FreeRTOSRunTimeTicks;
#define portGET_RUN_TIME_COUNTER_VALUE()                FreeRTOSRunTimeTicks
#endif
#define configUSE_TRACE_FACILITY                        1                       /* 1: 使能可视化跟踪调试, 默认: 0 */
#define configUSE_STATS_FORMATTING_FUNCTIONS            1                       /* 1: configUSE_TRACE_FACILITY为1时，会编译vTaskList()和vTaskGetRunTimeStats()函数, 默认: 0 */

/* 协程相关定义 */
#define configUSE_CO_ROUTINES                           0                       /* 1: 启用协程, 默认: 0 */
#define configMAX_CO_ROUTINE_PRIORITIES                 2                       /* 定义协程的最大优先级, 最大优先级=configMAX_CO_ROUTINE_PRIORITIES-1, 无默认configUSE_CO_ROUTINES为1时需定义 */

/* 软件定时器相关定义 */
#define configUSE_TIMERS                                1                               /* 1: 使能软件定时器, 默认: 0 */
#define configTIMER_TASK_PRIORITY                       ( configMAX_PRIORITIES - 1 )    /* 定义软件定时器任务的优先级, 无默认configUSE_TIMERS为1时需定义 */
#define configTIMER_QUEUE_LENGTH                        5                               /* 定义软件定时器命令队列的长度, 无默认configUSE_TIMERS为1时需定义 */
#define configTIMER_TASK_STACK_DEPTH                    ( configMINIMAL_STACK_SIZE * 2) /* 定义软件定时器任务的栈空间大小, 无默认configUSE_TIMERS为1时需定义 */

/* 可选函数, 1: 使能 */
#define INCLUDE_vTaskPrioritySet                        1                       /* 设置任务优先级 */
#define INCLUDE_uxTaskPriorityGet                       1                       /* 获取任务优先级 */
#define INCLUDE_vTaskDelete                             1                       /* 删除任务 */
#define INCLUDE_vTaskSuspend                            1                       /* 挂起任务 */
#define INCLUDE_xResumeFromISR                          1                       /* 恢复在中断中挂起的任务 */
#define INCLUDE_vTaskDelayUntil                         1                       /* 任务绝对延时 */
#define INCLUDE_vTaskDelay                              1                       /* 任务延时 */
#define INCLUDE_xTaskGetSchedulerState                  1                       /* 获取任务调度器状态 */
#define INCLUDE_xTaskGetCurrentTaskHandle               1                       /* 获取当前任务的任务句柄 */
#define INCLUDE_uxTaskGetStackHighWaterMark             1                       /* 获取任务堆栈历史剩余最小值 */
#define INCLUDE_xTaskGetIdleTaskHandle                  1                       /* 获取空闲任务的任务句柄 */
#define INCLUDE_eTaskGetState                           1                       /* 获取任务状态 */
#define INCLUDE_xEventGroupSetBitFromISR                1                       /* 在中断中设置事件标志位 */
#define INCLUDE_xTimerPendFunctionCall                  1                       /* 将函数的执行挂到定时器服务任务 */
#define INCLUDE_xTaskAbortDelay                         1                       /* 中断任务延时 */
#define INCLUDE_xTaskGetHandle                          1                       /* 通过任务名获取任务句柄 */
#define INCLUDE_xTaskResumeFromISR                      1                       /* 恢复在中断中挂起的任务 */

/* 中断嵌套行为配置 */
#ifdef __NVIC_PRIO_BITS
    #define configPRIO_BITS __NVIC_PRIO_BITS
#else
    #define configPRIO_BITS 4
#endif

#define configLIBRARY_LOWEST_INTERRUPT_PRIORITY         15                  /* 中断最低优先级 */
#define configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY    5                   /* FreeRTOS可管理的最高中断优先级 */
#define configKERNEL_INTERRUPT_PRIORITY                 ( configLIBRARY_LOWEST_INTERRUPT_PRIORITY << (8 - configPRIO_BITS) )
#define configMAX_SYSCALL_INTERRUPT_PRIORITY            ( configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY << (8 - configPRIO_BITS) )
#define configMAX_API_CALL_INTERRUPT_PRIORITY           configMAX_SYSCALL_INTERRUPT_PRIORITY

/* FreeRTOS中断服务函数相关定义 */
#define xPortPendSVHandler                              PendSV_Handler
#define vPortSVCHandler                                 SVC_Handler

/* 断言 */
#define vAssertCalled(char, int) printf("Error: %s, %d\r\n", char, int)
#define configASSERT( x ) if( ( x ) == 0 ) vAssertCalled( __FILE__, __LINE__ )
```





### 5.4任务创建与删除



#### 5.4.1API接口

![image-20250326203932283](./嵌入式软件学习.assets/image-20250326203932283.png)

![image-20250326203954560](./嵌入式软件学习.assets/image-20250326203954560.png) 	![image-20250326204207931](./嵌入式软件学习.assets/image-20250326204207931.png)

- **动态创建**，**Freertos自动从其管理的堆中**进行分配空间给**任务控制块和栈空间**
- **静态创建**，**由用户进行创建**，可以是全局数组

![image-20241013164404480](./嵌入式软件学习.assets/image-20241013164404480.png)



##### 1.动态创建任务



- 指向任务函数的指针，也就是函数入口
- 任务名字，在cofig.h中定义其最长长度
- 堆栈大小 --- 以字为单位
- 传递给任务函数的参数，一般不传递为NULL
- 任务优先级：0-31
- 任务句柄，也就是任务控制块

![image-20241015224635281](./嵌入式软件学习.assets/image-20241015224635281.png)

![image-20250317215935235](./嵌入式软件学习.assets/image-20250317215935235.png)

![image-20250317215853652](./嵌入式软件学习.assets/image-20250317215853652.png)



- 使用步骤：**开启动态创建任务宏、定义好API接口参数、编写任务函数**
- 创建任务后，**任务会直接挂载到就绪列表中**。由调度器遍历最高优先级任务进行执行

![image-20241015224925258](./嵌入式软件学习.assets/image-20241015224925258.png)



##### 2.任务控制块

每个任务都有一个任务控制块，类似与任务的身份证，可以通过任务控制块实现对任务的各种控制

- 栈顶指针：在任务切换时上下文保存、任务恢复相关
- 每个任务都有自己的任务控制块
- 任务栈起始地址，就是动态申请堆的起始地址，需要转换一下变成栈顶指针（申请的地址是起始地址，栈是高地址 ）

![image-20241015225437995](./嵌入式软件学习.assets/image-20241015225437995.png)







##### 3.静态创建任务



- 相比动态创建任务：1.需要用户分配栈 、 任务控制块空间
- 返回任务控制块

![image-20241015225902446](./嵌入式软件学习.assets/image-20241015225902446.png)



###### 使用静态创建任务





1. 如果使用静态创建任务，一定要给空闲任务分配任务堆栈和TCB ， 软件定时器任务是可选的，在config函数中定义。

![image-20250317220204595](./嵌入式软件学习.assets/image-20250317220204595.png)





##### 4.删除任务

- 动态创建任务时：传入任务句柄（非空）时，删除其他任务，在删除函数中立即进行内存释放
- **传入NULL**时，那个任务中调用，删除那个任务本身。**该任务是动态创建时，由空闲任务释放被删除任务的内存**（自身正在运行，空间不可立马进行释放）。
- 静态创建任务，需要用户自己进行释放，否则容易产生堆栈溢出
- 删除任务时，将任务从所在任务链表移除



![image-20241015231539671](./嵌入式软件学习.assets/image-20241015231539671.png)



![image-20241015231815941](./嵌入式软件学习.assets/image-20241015231815941.png)







#### 5.4.2动态创建实战编程

start_task任务 创建其他三个任务 ，start_task任务只执行一次。

![image-20250318202947513](./嵌入式软件学习.assets/image-20250318202947513.png)





##### 如何确定堆栈大小

- 首先定义大一点，后需通过API获取其堆栈大小，调整分配空间
- 使用该API函数，获取任务历史最小剩余值，后续调整一个合适的堆栈空间

![image-20250318210846273](./嵌入式软件学习.assets/image-20250318210846273.png)



##### 临界区

- 在任务创建时会进入临界区，创建完成后，退出临界区。
- 进入临界区会关闭所有中断，退出临界区会打开所有中断。
- **由于时间片轮询的调度器，是由系统滴答定时器中断完成系统调度的。当创建任务时不进行任务调度**
- **进入临界区创建任务，防止任务优先级高于创建任务优先级，后续任务无法创建。**





```c
//1.开启支持动态内存申请宏
#define configSUPPORT_STATIC_ALLOCATION			0						//支持静态内存申请
#define configSUPPORT_DYNAMIC_ALLOCATION        1                       //支持动态内存申请
#define configTOTAL_HEAP_SIZE					((size_t)(10*1024))     //系统所有总的堆大小

//2.创建start任务

#define START_TASK_PRIO			1 	    //任务优先级
#define START_STK_SIZE			128	    //任务堆栈大小
TaskHandle_t StartTask_Handler;     //任务句柄
void start_task(void *pvParameters);//任务函数

 //在写入参数时，强制转换类型防止报错
xTaskCreate(	(TaskFunction_t		)start_task,			   // 任务函数指针，也就是函数名
			   (const char*			)"start_task",			  // 任务名字
			   (uint16_t					)START_STK_SIZE,  // 堆栈大小 128 * 4 字节 
			   (void*						)NULL,			 // 任务函数形参
			   (UBaseType_t			)START_TASK_PRIO,		  //  任务优先级
			   (TaskHandle_t*		)&StartTask_Handler );      //  任务句柄

vTaskStartScheduler();//开启任务调度



//start任务实现实体
void start_task(void *pvParameters)
{
	taskENTER_CRITICAL();//进入临界区 ，关闭所有中断，不进行任务调度，等待所有任务创建完成
    
    
	//创建led0任务
	xTaskCreate(			 (TaskFunction_t		)           led0_task,
							(const char*			)         "led0_task",
							(uint16_t					)     128,
							(void*						)     NULL,
							(UBaseType_t			)           2,
							(TaskHandle_t*		)               &LED0Task_Handler);
  
    	xTaskCreate(			 (TaskFunction_t		)           led1_task,
							(const char*			)         "led1_task",
							(uint16_t					)     128,
							(void*						)     NULL,
							(UBaseType_t			)           2,
							(TaskHandle_t*		)               &LED1Task_Handler);
    
	
	vTaskDelete();//任务删除，删除start任务本身，只执行一次
    
	taskEXIT_CRITICAL();//退出临界区

}


```





#### 5.4.3动态创建任务--内部实现分析



![image-20250324213549502](./嵌入式软件学习.assets/image-20250324213549502.png)

![image-20250324214111132](./嵌入式软件学习.assets/image-20250324214111132.png)



```c
	BaseType_t xTaskCreate(	TaskFunction_t pxTaskCode,
							const char * const pcName,
							const uint16_t usStackDepth,
							void * const pvParameters,
							UBaseType_t uxPriority,
							TaskHandle_t * const pxCreatedTask ) 
	{
	TCB_t *pxNewTCB;
	BaseType_t xReturn;

		#if( portSTACK_GROWTH > 0 )  //栈向高地址生长
		{
			/* 条件编译不满足 */
		}
		#else //栈向低地址生长 ， STM32按这种方式栈生长
		{
            
			StackType_t *pxStack; //创建变量存放任务堆栈首地址
            
            //调用 pvPortMalloc 函数生成栈空间。 usStackDepth--函数传入空间大小 由于传入的是字大小 再乘4
            //由于用户申请的地址是stm32的堆空间，这里实际返回的是堆底地址，需要后面转换一下指向堆顶并初始化后当栈用
			pxStack = ( StackType_t * ) pvPortMalloc( ( ( ( size_t ) usStackDepth ) * sizeof( StackType_t ) ) ); 	
			if( pxStack != NULL )  //创建成功
			{
				pxNewTCB = ( TCB_t * ) pvPortMalloc( sizeof( TCB_t ) );  //申请任务控制块内存

				if( pxNewTCB != NULL ) //任务控制块申请成功
				{
					pxNewTCB->pxStack = pxStack;  //将申请成功的任务栈的起始地址赋值给---任务卡块成员-栈起始地址（就是申请堆的地址值）
				}
				else
				{
					vPortFree( pxStack );  //任务块未申请成功 --- 之前释放申请的任务堆栈空间
				}
			}
			else //创建失败---任务块为NULL
			{
				pxNewTCB = NULL;
			}
		}
		#endif 
        
        
        

		if( pxNewTCB != NULL ) //任务控制块非空
		{
			#if( tskSTATIC_AND_DYNAMIC_ALLOCATION_POSSIBLE != 0 )
			{
				pxNewTCB->ucStaticallyAllocated = tskDYNAMICALLY_ALLOCATED_STACK_AND_TCB;
			}
			#endif 

            //初始化任务控制块函数 --- 上边只初始化了任务块的栈顶地址
			prvInitialiseNewTask( pxTaskCode, pcName, ( uint32_t ) usStackDepth, pvParameters, uxPriority, 						pxCreatedTask, pxNewTCB, NULL );
            
            //将任务加入到就绪列表中
			prvAddNewTaskToReadyList( pxNewTCB );
            
			xReturn = pdPASS;
		}
		else
		{
			xReturn = errCOULD_NOT_ALLOCATE_REQUIRED_MEMORY;
		}

		return xReturn;
	}
```





##### 1.初始化任务控制块函数

prvInitialiseNewTask（）---填充任务控制块信息



- mtCOVERAGE_TEST_DELAY特意放在代码的一个关键部分，通常是为了确保通过操作函数时，所有逻辑判断和指针操作都能被仔细测试用来标记该位置比较重要[FreeRTOS学习——代码覆盖率测试宏mt_mtcoverage-CSDN博客](https://blog.csdn.net/rerrick_rose/article/details/142257077)。![image-20250324202338372](./嵌入式软件学习.assets/image-20250324202338372.png)<img src="./嵌入式软件学习.assets/image-20250324202403125.png" alt="image-20250324202403125" style="zoom: 80%;" />
- 

```c
static void prvInitialiseNewTask( 	TaskFunction_t pxTaskCode,
									const char * const pcName,
									const uint32_t ulStackDepth,
									void * const pvParameters,
									UBaseType_t uxPriority,
									TaskHandle_t * const pxCreatedTask,
									TCB_t *pxNewTCB,
									const MemoryRegion_t * const xRegions ) /*lint !e971 Unqualified char types are allowed for strings and single characters only. */
{
StackType_t *pxTopOfStack;
UBaseType_t x;
    
    
    	//条件编译：如果使用到监控堆栈功能
    	#if( ( configCHECK_FOR_STACK_OVERFLOW > 1 ) || ( configUSE_TRACE_FACILITY == 1 ) || ( INCLUDE_uxTaskGetStackHighWaterMark == 1 ) )
	{
		//将栈区都赋值为  0xa5  ;上述已经给任务块赋值了任务栈顶 ； 赋值内容为0xa5 ;大小为栈空间
		( void ) memset( pxNewTCB->pxStack, ( int ) tskSTACK_FILL_BYTE, ( size_t ) ulStackDepth * sizeof( 			StackType_t ) );
	}
	#endif
    
    
    //由于freertos使用macoll函数是在STM32的堆空间内申请的空间，堆是向上生长，这里重新计算堆的最上面地址作为栈顶。
    pxTopOfStack = pxNewTCB->pxStack + ( ulStackDepth - ( uint32_t ) 1 ); //虽然在堆中申请的空间，但是当栈使用
    
    //8字节对齐，提升效率
	pxTopOfStack = ( StackType_t * ) ( ( ( portPOINTER_SIZE_TYPE ) pxTopOfStack ) & ( ~( ( portPOINTER_SIZE_TYPE ) portBYTE_ALIGNMENT_MASK ) ) ); 
    
		//检验是否完成对齐
		configASSERT( ( ( ( portPOINTER_SIZE_TYPE ) pxTopOfStack & ( portPOINTER_SIZE_TYPE ) portBYTE_ALIGNMENT_MASK ) == 0UL ) );
    
    
    //循环将函数形参传入的任务名字传入任务块  ， configMAX_TASK_NAME_LEN为定义的任务名字最长的长度
    for( x = ( UBaseType_t ) 0; x < ( UBaseType_t ) configMAX_TASK_NAME_LEN; x++ )
	{
		pxNewTCB->pcTaskName[ x ] = pcName[ x ];
		if( pcName[ x ] == 0x00 )
		{
			break;
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}

	pxNewTCB->pcTaskName[ configMAX_TASK_NAME_LEN - 1 ] = '\0';  //数据末端，加上'\0'
    
    
    
    
    
    //将设置的任务优先级赋值给任务控制块  ，当大于最大优先级是设置为最大优先级
    if( uxPriority >= ( UBaseType_t ) configMAX_PRIORITIES )  //32
	{
		uxPriority = ( UBaseType_t ) configMAX_PRIORITIES - ( UBaseType_t ) 1U;  // 31  优先级 0-31
	}
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}

	pxNewTCB->uxPriority = uxPriority;
    
    
    
    //互斥量还没学到
    #if ( configUSE_MUTEXES == 1 )
	{
		pxNewTCB->uxBasePriority = uxPriority; //用于优先级继承
		pxNewTCB->uxMutexesHeld = 0;
	}
	#endif 
    
    
    //在使用链表项时，需要先将该链表项初始化，也就是挂载链表为NULL，防止出现异常状态
    vListInitialiseItem( &( pxNewTCB->xStateListItem ) ); //初始化状态列表项
	vListInitialiseItem( &( pxNewTCB->xEventListItem ) ); //初始化事件列表项
    
    
    
    //任务块的状态列表项--- 归属者为自身的任务块
    listSET_LIST_ITEM_OWNER( &( pxNewTCB->xStateListItem ), pxNewTCB );

    //设置事件列表项 ， 如该优先级为 31 着其事件项为 32-31 = 1 ，由于事件为值越小优先级越高。由于事件列表项插入是由小向大插入，这样操作会将优先级高的插在最前面。
	listSET_LIST_ITEM_VALUE( &( pxNewTCB->xEventListItem ), ( TickType_t ) configMAX_PRIORITIES - ( TickType_t ) uxPriority ); 
    
       //任务块的事件列表项--- 归属者为自身的任务块
	listSET_LIST_ITEM_OWNER( &( pxNewTCB->xEventListItem ), pxNewTCB );

    
    //初始化栈内容，并将初始化后的栈顶指针返回给任务控制块
    pxNewTCB->pxTopOfStack = pxPortInitialiseStack( pxTopOfStack, pxTaskCode, pvParameters ); 
    
    
    *pxCreatedTask = ( TaskHandle_t ) pxNewTCB; //将任务控制块赋值给任务句柄
    
    

```





##### 2.初始化堆栈





- ![image-20250324204419683](./嵌入式软件学习.assets/image-20250324204419683.png)

 <img src="./嵌入式软件学习.assets/image-20260106211956991.png" alt="image-20260106211956991" style="zoom:50%;" />



- ![image-20250324205122288](./嵌入式软件学习.assets/image-20250324205122288.png)Thumb指令集为16位指令集
- ![image-20250324213110751](./嵌入式软件学习.assets/image-20250324213110751.png)[Cortex-M3 异常返回值EXC_RETURN-CSDN博客](https://blog.csdn.net/weixin_30633949/article/details/97913894?spm=1001.2101.3001.6661.1&utm_medium=distribute.pc_relevant_t0.none-task-blog-2~default~BlogCommendFromBaidu~PaidSort-1-97913894-blog-126761694.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-2~default~BlogCommendFromBaidu~PaidSort-1-97913894-blog-126761694.235^v43^pc_blog_bottom_relevance_base3)

```c
StackType_t *pxPortInitialiseStack( StackType_t *pxTopOfStack, TaskFunction_t pxCode, void *pvParameters )
{

	pxTopOfStack--; 
	*pxTopOfStack = portINITIAL_XPSR;	/* xPSR   ( 0x01000000 ) --- 开启Thumb指令集 */
	pxTopOfStack--;
	*pxTopOfStack = ( ( StackType_t ) pxCode ) & portSTART_ADDRESS_MASK;	/* PC - -- PC指针将函数地址赋值给PC区*/
	pxTopOfStack--;
	*pxTopOfStack = ( StackType_t ) prvTaskExitError;	/* LR ---- 这里由移植层提供的一个任务错误返回函数 */

	pxTopOfStack -= 5;	/* R12, R3, R2 and R1.    预留寄存器 R1 、 R2 、 R3 、R12 */
	*pxTopOfStack = ( StackType_t ) pvParameters;	/* R0   ---  任务初始化时，传入的形参 */
  
    /** CM3无此项 CM4等有此项 
    
    	pxTopOfStack--;
		*pxTopOfStack = portINITIAL_EXEC_RETURN;    //( 0xfffffffdUL )
   
    */
    
	pxTopOfStack -= 8;	/* R11, R10, R9, R8, R7, R6, R5 and R4.    预留*/

	return pxTopOfStack;  //返回此时的栈顶指针
}
```





##### 3.将新创建的任务添加到就绪列表中

prvAddNewTaskToReadyList( pxNewTCB );

![image-20250324214111132](./嵌入式软件学习.assets/image-20250324214111132.png)

```c
static void prvAddNewTaskToReadyList( TCB_t *pxNewTCB )
{
	
	taskENTER_CRITICAL(); //进入临界区
    
    
	{
		uxCurrentNumberOfTasks++; //全局变量，记录当前所有任务数++
        
		if( pxCurrentTCB == NULL ) //pxCurrentTCB为指向当前优先级最高的任务，当前为NULL证明当前为第一个任务
		{
			pxCurrentTCB = pxNewTCB;  //当前为第一个任务，将当前任务赋值给pxCurrentTCB

			if( uxCurrentNumberOfTasks == ( UBaseType_t ) 1 ) //再次确认当前只有一个任务
			{
				prvInitialiseTaskLists();  //初始化任务列表
			}
			else
			{
				mtCOVERAGE_TEST_MARKER(); //标记
			}
		}
		else  //当前非第一个创建的任务
		{
			
			if( xSchedulerRunning == pdFALSE )  //当前调度器未在运行，xSchedulerRunning指示当前调度器状态 0-停止
			{	
                	//当前创建的任务大于当前最高优先级任务 --- 重新赋值
				if( pxCurrentTCB->uxPriority <= pxNewTCB->uxPriority ) 
				{
					pxCurrentTCB = pxNewTCB;
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}

		uxTaskNumber++; //任务数量 ++ 这个变量是不减的，可以记录任务是第几个创建的

        //使能任务查询
		#if ( configUSE_TRACE_FACILITY == 1 )
		{
			pxNewTCB->uxTCBNumber = uxTaskNumber; //将当前任务数量赋值给任务控制块 ，可以查询该任务是第几个创建的
		}
		#endif 
        
        

		prvAddTaskToReadyList( pxNewTCB ); //插入就绪列表

	}
    
    
	taskEXIT_CRITICAL();  //退出临界区

    
	if( xSchedulerRunning != pdFALSE ) //已启用任务调度器
	{
		
		if( pxCurrentTCB->uxPriority < pxNewTCB->uxPriority ) //如果当前任务为最高优先级
		{
			taskYIELD_IF_USING_PREEMPTION(); //触发一次任务调度
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}
    
    
}
```





###### 初始化任务列表

freertos创建以下几个链表

- 就绪列表，有几个优先级创建几个
- 挂起列表，1个
- 阻塞列表，2个，两个列表循环记录防止溢出
- 删除任务列表，当任务删除自身时不可以立即释放堆空间，需要暂时挂载到删除任务列表中，在空闲任务中进行释放
- 等待就绪列表，在任务调度器挂起时，此时有中断恢复任务，此时将恢复的任务放入这个列表中。因为此时挂起任务调度器是防止任务打断，无法进行任务切换，需要等恢复任务调度器时将该列表的任务恢复到就绪列表，并判断是否还需要进行任务调度

```c
static void prvInitialiseTaskLists( void )
{
UBaseType_t uxPriority;

    //循环创建 32个就绪列表，后续根据不同任务优先级挂载到对应的就绪列表中
	for( uxPriority = ( UBaseType_t ) 0U; uxPriority < ( UBaseType_t ) configMAX_PRIORITIES; uxPriority++ )
	{
		vListInitialise( &( pxReadyTasksLists[ uxPriority ] ) ); //初始化列表
	}

    //初始化延时列表
	vListInitialise( &xDelayedTaskList1 );
	vListInitialise( &xDelayedTaskList2 );
    
    //初始化挂起列表，当挂起全部任务使用到
	vListInitialise( &xPendingReadyList );

    
    
	#if ( INCLUDE_vTaskDelete == 1 )
	{
		vListInitialise( &xTasksWaitingTermination );  //删除任务列表
	}
	#endif 

    
	#if ( INCLUDE_vTaskSuspend == 1 )
	{
		vListInitialise( &xSuspendedTaskList ); //挂起任务列表
	}
	#endif 

	
    //赋值两个该等待列表
	pxDelayedTaskList = &xDelayedTaskList1;
	pxOverflowDelayedTaskList = &xDelayedTaskList2;
}
```







###### 将任务添加到就绪列表

- 将全局变量就绪列表置位：创建一个32位的变量，当该优先级为2就将该变量BIT2置1，这样可以通过遍历该变量获取当前最高优先级
- 

```c
#define prvAddTaskToReadyList( pxTCB )\
	traceMOVED_TASK_TO_READY_STATE( pxTCB );\      //未实现

	taskRECORD_READY_PRIORITY( ( pxTCB )->uxPriority );\ //将全局变量其优先级对应位--置位
        
     //尾插函数，将该任务块中的状态列表项插入其优先级对应的就绪列表中   
	vListInsertEnd( &( pxReadyTasksLists[ ( pxTCB )->uxPriority ] ), &( ( pxTCB )->xStateListItem ) ); \
        
	tracePOST_MOVED_TASK_TO_READY_STATE( pxTCB )  //未实现
```





#### 5.4.4.删除任务--内部分析

![image-20250325200722754](./嵌入式软件学习.assets/image-20250325200722754.png)





- 将任务控制块的：状态链表、事件链表 脱钩。
- 删除其他任务---直接释放任务控制块、栈空间 ； 删除运行任务本身，重新挂载到待删除任务，等待空闲任务删除。
- 如果删除的是运行任务本身，重新再启动任务调度器，切换到其他任务。

```c
	void vTaskDelete( TaskHandle_t xTaskToDelete )
	{
	TCB_t *pxTCB;

		taskENTER_CRITICAL(); //进入临界区
		{
			
//#define prvGetTCBFromHandle( pxHandle )    ( ( ( pxHandle ) == NULL ) ? ( TCB_t * ) pxCurrentTCB : ( 			TCB_t * ) ( pxHandle ) )   判断传入任务句柄是否为空,为空就是运行函数本身 ，不为空则是指定的函数句柄
			pxTCB = prvGetTCBFromHandle( xTaskToDelete ); 


			//删除任务块的 状态列表项所在链表（运行、挂起、就绪、阻塞）
			if( uxListRemove( &( pxTCB->xStateListItem ) ) == ( UBaseType_t ) 0 )
			{
				taskRESET_READY_PRIORITY( pxTCB->uxPriority );  //判断其优先级对应的就绪列表是否为空，为空将全局变量所对应的位清零
                /*	函数原型：#define taskRESET_READY_PRIORITY( uxPriority )	{										
		if( listCURRENT_LIST_LENGTH( &( pxReadyTasksLists[ ( uxPriority ) ] ) ) == ( UBaseType_t ) 0 )	
		{																								
			portRESET_READY_PRIORITY( ( uxPriority ), ( uxTopReadyPriority ) );							
		}}*/
                
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}

			//判断任务控制块的事件列表项是否为0 ， 如果不为0在等待事件，直接删除其所在事件列表项
			if( listLIST_ITEM_CONTAINER( &( pxTCB->xEventListItem ) ) != NULL )
			{
				( void ) uxListRemove( &( pxTCB->xEventListItem ) );
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}

		
			uxTaskNumber++; //调试所用变量

			if( pxTCB == pxCurrentTCB ) //删除任务为正在运行的函数本身: 将任务挂载在待删除任务链表中
			{   
				//将本身的状态列表项插入到等待删除链表
				vListInsertEnd( &xTasksWaitingTermination, &( pxTCB->xStateListItem ) );

			
				++uxDeletedTasksWaitingCleanUp; //等待删除任务++

			
				portPRE_TASK_DELETE_HOOK( pxTCB, &xYieldPending );  //无用
			}
			else  //删除其他任务 ：直接释放任务栈、任务控制块空间
			{
				--uxCurrentNumberOfTasks; //任务总数 -- 
				prvDeleteTCB( pxTCB ); //释放任务栈空间 、任务控制块空间。

				prvResetNextTaskUnblockTime();  //这暂时不理解，将阻塞任务更新
			}

			traceTASK_DELETE( pxTCB );
		}
		taskEXIT_CRITICAL(); //退出临界区

		
        
		if( xSchedulerRunning != pdFALSE ) //当前调度器正在工作
		{
			if( pxTCB == pxCurrentTCB ) //删除的任务正是任务本身
			{
				configASSERT( uxSchedulerSuspended == 0 );
				portYIELD_WITHIN_API();  //这里切换到其他任务运行 ，不在自身任务中释放本身而是在空闲任务中释放
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
	}
```





##### 1.空闲任务中删除带删除链表



- 在任务调度器中，创建了一个空闲任务<img src="./嵌入式软件学习.assets/image-20250325205002525.png" alt="image-20250325205002525" style="zoom: 80%;" />，空闲任务调用一个函数删除待释放任务。
- 循环释放待删除任务链表中的任务空间。

```c
//空闲任务中的子函数
static void prvCheckTasksWaitingTermination( void )
{

	/** THIS FUNCTION IS CALLED FROM THE RTOS IDLE TASK **/

	#if ( INCLUDE_vTaskDelete == 1 )
	{
		BaseType_t xListIsEmpty;

		/* ucTasksDeleted is used to prevent vTaskSuspendAll() being called
		too often in the idle task. */
		while( uxDeletedTasksWaitingCleanUp > ( UBaseType_t ) 0U ) //待删除任务计数器非零
		{
			vTaskSuspendAll();
			{
				xListIsEmpty = listLIST_IS_EMPTY( &xTasksWaitingTermination );
			}
			( void ) xTaskResumeAll();

            
			if( xListIsEmpty == pdFALSE ) //带删除任务链表不为空
			{
				TCB_t *pxTCB;

				taskENTER_CRITICAL();
				{
                    //获取待删除的链表项
					pxTCB = ( TCB_t * ) listGET_OWNER_OF_HEAD_ENTRY( ( &xTasksWaitingTermination ) ); 
                    
                    //将该链表项从链表中删除
					( void ) uxListRemove( &( pxTCB->xStateListItem ) );
                    
					--uxCurrentNumberOfTasks; //任务数总数--
                    
					--uxDeletedTasksWaitingCleanUp;//待删除链表项数量--
				}
				taskEXIT_CRITICAL();

				prvDeleteTCB( pxTCB ); //释放上述链表项栈空间、任务控制块空间
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
	}
	#endif /* INCLUDE_vTaskDelete */
}
```





#### 5.4.5静态创建任务

![image-20250326193112350](./嵌入式软件学习.assets/image-20250326193112350.png)



![image-20250326193558768](./嵌入式软件学习.assets/image-20250326193558768.png)



###### 1.开启支持静态创建任务宏

- 将静态内存申请宏开启，动态的可以不用关闭

```c
#define configSUPPORT_STATIC_ALLOCATION			1						//支持静态内存申请
#define configSUPPORT_DYNAMIC_ALLOCATION        1                       //支持动态内存申请
#define configTOTAL_HEAP_SIZE					((size_t)(10*1024))     //系统所有总的堆大小
```





###### 2.空闲任务内存分配



- 动态创建任务中，在开启调度器后，会由其任务内自动初始化空闲任务
- 在静态创建任务时，需要用户自己分配空闲任务的栈、任务控制块



![image-20250326194843492](./嵌入式软件学习.assets/image-20250326194843492.png)

```c


/* 空闲任务配置 */
StaticTask_t idle_task_tcb;  //创建空闲任务 --- 使用一个全局变量来开辟空间

//使用一个全局数组作为空闲任务栈空间  configMINIMAL_STACK_SIZE 在confing文件中定义了空闲任务的大小
StackType_t	 idle_task_stack[configMINIMAL_STACK_SIZE]; 

/* 空闲任务内存分配 */
void vApplicationGetIdleTaskMemory(StaticTask_t ** ppxIdleTaskTCBBuffer,	//空闲任务控制块
                                   StackType_t ** ppxIdleTaskStackBuffer,	//空闲任务栈空间
                                   uint32_t * pulIdleTaskStackSize)			//空闲任务栈空间大小
{
    * ppxIdleTaskTCBBuffer = &idle_task_tcb;  //传入的是一级指针的地址，用二级指针指向一级指针。对一级指针指向内容赋值
    * ppxIdleTaskStackBuffer = idle_task_stack;
    * pulIdleTaskStackSize = configMINIMAL_STACK_SIZE;
}


		//函数调用
		StaticTask_t *pxIdleTaskTCBBuffer = NULL;
		StackType_t *pxIdleTaskStackBuffer = NULL;
		uint32_t ulIdleTaskStackSize;
		vApplicationGetIdleTaskMemory( &pxIdleTaskTCBBuffer, &pxIdleTaskStackBuffer, &ulIdleTaskStackSize );
```





###### 3.软件定时器任务内存分配



软件定时器任务，可以定时进行启动回调函数

![image-20250326201051564](./嵌入式软件学习.assets/image-20250326201051564.png)



该函数在启动任务调度器中实现

![image-20250326201343509](./嵌入式软件学习.assets/image-20250326201343509.png)

![image-20250326201809862](./嵌入式软件学习.assets/image-20250326201809862.png)



```c

/***************************************************************************************************************/
/*                                FreeRTOS与软件定时器有关的配置选项，在freertos——confing文件中进行配置                                            */
/***************************************************************************************************************/
#define configUSE_TIMERS				        1                               //为1时启用软件定时器
#define configTIMER_TASK_PRIORITY		        (configMAX_PRIORITIES-1)        //软件定时器优先级
#define configTIMER_QUEUE_LENGTH		        5                               //软件定时器队列长度
#define configTIMER_TASK_STACK_DEPTH	        (configMINIMAL_STACK_SIZE*2)    //软件定时器任务堆栈大小



/* 软件定时器任务配置 */
StaticTask_t timer_task_tcb;  //软件定时器任务控制块
StackType_t	 timer_task_stack[configTIMER_TASK_STACK_DEPTH]; //软件定时器栈地址

/* 软件定时器任务内存分配 */
void vApplicationGetTimerTaskMemory(StaticTask_t ** ppxTimerTaskTCBBuffer,	//软件定时器任务控制块
                                    StackType_t ** ppxTimerTaskStackBuffer,	//软件定时器任务栈空间
                                    uint32_t * pulTimerTaskStackSize)		//软件定时器任务栈空间大小
{
    * ppxTimerTaskTCBBuffer = &timer_task_tcb;
    * ppxTimerTaskStackBuffer = timer_task_stack;
    * pulTimerTaskStackSize = configTIMER_TASK_STACK_DEPTH;
}


```



###### 4.创建静态任务





```c
 #define START_TASK_PRIO			1				 	 //优先级
#define START_TASK_STACK_SIZE  	128					 //堆栈大小
TaskHandle_t start_task_handler; 					 //任务句柄(控制块的指针)
StackType_t	 start_task_stack[START_TASK_STACK_SIZE];//任务堆栈空间(用户自定义)
StaticTask_t start_task_tcb;						 //任务控制块(任务身份证,储存任务信息)


//返回任务控制块句柄，用于任务删除等操作
start_task_handler = xTaskCreateStatic((TaskFunction_t)start_task,      /* 任务函数(指向任务函数的指针) */
                                           (char *)"start_task",            /* 任务名称 */
                                           (uint32_t)START_TASK_STACK_SIZE, /* 任务堆栈大小 */
                                           (void *)NULL,                    /* 传入给任务函数的参数 */
                                           (UBaseType_t)START_TASK_PRIO,    /* 任务优先级 */
                                           (StackType_t *)start_task_stack, /* 任务堆栈空间(用户自定义) */
                                           (StaticTask_t *)&start_task_tcb);//任务控制块地址
```



###### 5.静态创建任务解析

![image-20250326203531972](./嵌入式软件学习.assets/image-20250326203531972.png)



#### 5.4.6任务挂起与恢复

- 挂起：将任务暂停
- 恢复：将暂停的任务恢复到就绪列表
- FromlSR：在中断中恢复挂起的任务

![image-20250402211927484](./嵌入式软件学习.assets/image-20250402211927484.png)



##### 1.任务挂起函数

- 参数为任务句柄；当传入为NULL时，挂起正在执行的任务本身
- <img src="./嵌入式软件学习.assets/image-20250402213426944.png" alt="image-20250402213426944" style="zoom:50%;" />开启配置宏。

![image-20250402212308744](./嵌入式软件学习.assets/image-20250402212308744.png)





###### 1.1实现

![image-20250407201615174](./嵌入式软件学习.assets/image-20250407201615174.png)



```c
#if ( INCLUDE_vTaskSuspend == 1 )  //宏开启 --- 任务挂起

	void vTaskSuspend( TaskHandle_t xTaskToSuspend )
	{
	TCB_t *pxTCB;

		taskENTER_CRITICAL(); //进入临界区
		{
			//如果传入是NULL ，则挂起的任务是正在运行的任务本身
			pxTCB = prvGetTCBFromHandle( xTaskToSuspend );

			traceTASK_SUSPEND( pxTCB );

			//将任务控制块的状态列表项，从链表中移除
			if( uxListRemove( &( pxTCB->xStateListItem ) ) == ( UBaseType_t ) 0 )
			{
                //当其判断所对应的链表项为空时，检测就绪链表的全局变量是否为空，进行清零
				taskRESET_READY_PRIORITY( pxTCB->uxPriority ); 
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}

			//将任务的事件列表项删除
			if( listLIST_ITEM_CONTAINER( &( pxTCB->xEventListItem ) ) != NULL )
			{
				( void ) uxListRemove( &( pxTCB->xEventListItem ) );
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
            
            
			//将任务插入挂起链表项末尾
			vListInsertEnd( &xSuspendedTaskList, &( pxTCB->xStateListItem ) );
		}
		taskEXIT_CRITICAL();

        
        
        
        
		if( xSchedulerRunning != pdFALSE ) //调度器正在运行
		{
			taskENTER_CRITICAL();
			{
				prvResetNextTaskUnblockTime(); //更新阻塞超时时间，防止挂起任务为下一个任务的阻塞超时
			}
			taskEXIT_CRITICAL();
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
        
        

		if( pxTCB == pxCurrentTCB ) //挂起的任务是自身
		{
			if( xSchedulerRunning != pdFALSE ) //调度器正在运行
			{
				configASSERT( uxSchedulerSuspended == 0 );
				portYIELD_WITHIN_API();  //触发一次任务调度 ，触发一次pendsv中断
			}
			else //调度器未在运行
			{
				//如果挂起的任务总量 == 任务总数
				if( listCURRENT_LIST_LENGTH( &xSuspendedTaskList ) == uxCurrentNumberOfTasks )
				{
					pxCurrentTCB = NULL; //将当前执行任务赋值为NULL，一般还会有空闲任务，这里是预防特殊情况误处理
				}
				else
				{
					vTaskSwitchContext(); //寻找下一个就绪列表中，最高优先级任务
				}
			}
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}

#endif
```



##### 2.任务恢复



###### 2.1任务中恢复任务

- 在**任务中**调用的函数
- 不支持挂起嵌套，**当任务被挂起多次，只需要恢复一次**。恢复的任务进入就绪态。
- <img src="./嵌入式软件学习.assets/image-20250402213405244.png" alt="image-20250402213405244" style="zoom:50%;" />开启配置宏

![image-20250402212650447](./嵌入式软件学习.assets/image-20250402212650447.png)







![image-20250407205749869](./嵌入式软件学习.assets/image-20250407205749869.png)

```c
#if ( INCLUDE_vTaskSuspend == 1 ) //允许任务中恢复宏

	void vTaskResume( TaskHandle_t xTaskToResume )
	{
	TCB_t * const pxTCB = ( TCB_t * ) xTaskToResume;

		/* It does not make sense to resume the calling task. */
		configASSERT( xTaskToResume );

        
		//不能为NULL ， 或者恢复任务本身
		if( ( pxTCB != NULL ) && ( pxTCB != pxCurrentTCB ) )
		{
			taskENTER_CRITICAL(); //进入临界区
			{
				if( prvTaskIsTaskSuspended( pxTCB ) != pdFALSE ) //所需的任务在挂起链表中
				{
					traceTASK_RESUME( pxTCB );

					//将该任务从挂起链表中移除
					( void ) uxListRemove(  &( pxTCB->xStateListItem ) );
                    
                    
					prvAddTaskToReadyList( pxTCB ); //添加到就绪链表

                    
					//判断当前任务的优先级 是否大于 当前执行的任务优先级
					if( pxTCB->uxPriority >= pxCurrentTCB->uxPriority )
					{
						taskYIELD_IF_USING_PREEMPTION(); //执行一次任务切换 
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			taskEXIT_CRITICAL(); //退出临界区
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}

#endif /* INCLUDE_vTaskSuspend */
```









###### 2.2在中断中恢复任务



- 形参是需要恢复的任务，该函数需要在**中断中调用**
- <img src="./嵌入式软件学习.assets/image-20250402213446108.png" alt="image-20250402213446108" style="zoom:50%;" />开启配置宏
- 有返回值：
  1. pdTRUE:表示当前恢复的任务优先级大于正在运行的任务，需要进行任务调度。
  2. pdFALSE:无需在该任务恢复后启动任务调度
- ![image-20250402213844101](./嵌入式软件学习.assets/image-20250402213844101.png)**中断服务函数优先级需要小于FreeRTos管理的优先级（5-15)，其优先级不可为0-4**

![image-20250402213033437](./嵌入式软件学习.assets/image-20250402213033437.png)

![image-20250407211101807](./嵌入式软件学习.assets/image-20250407211101807.png)

```c
#if ( ( INCLUDE_xTaskResumeFromISR == 1 ) && ( INCLUDE_vTaskSuspend == 1 ) )

	BaseType_t xTaskResumeFromISR( TaskHandle_t xTaskToResume )
	{
	BaseType_t xYieldRequired = pdFALSE;
        
	TCB_t * const pxTCB = ( TCB_t * ) xTaskToResume;
	UBaseType_t uxSavedInterruptStatus; //保存中断状态

		configASSERT( xTaskToResume );

        //判断当前中断是否是FreeRtos管理的中断优先级，是否设置为中断组为4
		portASSERT_IF_INTERRUPT_PRIORITY_INVALID();

        /*保存当前中断状态 basepri 用于管理中断，在关闭中断前返回中断前的状态
        
		mrs ulReturn, basepri    保存中前的状态
		msr basepri, ulNewBASEPRI   关闭中断
		return ulReturn;
        */
        
        //保存中断前的basepri状态，将basepri设置为freeRTos管理的最大管理优先级configMAX_SYSCALL_INTERRUPT_PRIORITY
		uxSavedInterruptStatus = portSET_INTERRUPT_MASK_FROM_ISR(); 
        
		{
			if( prvTaskIsTaskSuspended( pxTCB ) != pdFALSE ) //当前任务在挂起链表中
			{
				traceTASK_RESUME_FROM_ISR( pxTCB );

				if( uxSchedulerSuspended == ( UBaseType_t ) pdFALSE ) //调度器未挂起
				{
                    
                    //当前任务优先级 》 当前执行的任务优先级     返回TRUE
					if( pxTCB->uxPriority >= pxCurrentTCB->uxPriority )
					{
						xYieldRequired = pdTRUE;
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}

					( void ) uxListRemove( &( pxTCB->xStateListItem ) ); //挂起链表移除
					prvAddTaskToReadyList( pxTCB ); //添加到就绪链表
				}
				else //任务调度器被挂起
				{
					//将任务挂载在等待就绪链表，需要等调度器回复后，从等待就绪链表移入就绪链表中
					vListInsertEnd( &( xPendingReadyList ), &( pxTCB->xEventListItem ) );
				}
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		portCLEAR_INTERRUPT_MASK_FROM_ISR( uxSavedInterruptStatus ); //恢复到执行中断前的状态

		return xYieldRequired;
	}

#endif /* ( ( INCLUDE_xTaskResumeFromISR == 1 ) && ( INCLUDE_vTaskSuspend == 1 ) ) */
```







##### 3.程序实验



![image-20250402223834537](./嵌入式软件学习.assets/image-20250402223834537.png)



- 任务重挂起与恢复任务

```c

void task3(void * pvParameters)
{
    uint8_t key = 0;

    while(1)
    {
        key = key_scan(0);

        if(key == KEY1_PRESS)
        {
			printf("挂起task1\r\n");
            vTaskSuspend(Task1_Handler);	//任务挂起(待挂起任务句柄)
        }
		else if(key == KEY2_PRESS)
		{
			printf("在任务中恢复task1\r\n");
			vTaskResume(Task1_Handler);		//任务恢复(待恢复任务句柄)
		}

        vTaskDelay(10);
    }
}
```







- 中断中挂起与恢复任务
- ![image-20250402225824215](./嵌入式软件学习.assets/image-20250402225824215.png)将**中断组配置为4，所有中断都是抢断优先级。**方便FreeRtos方便管理。
- ![image-20250402230251288](./嵌入式软件学习.assets/image-20250402230251288.png)**在中断中调用结尾为FromlSR函数时，调用的中断函数其对应中断优先级要在 5 - 15 之间。**红框中的宏为5，FreeRtos只管理这区间的中断优先级。

```c
NVIC_PriorityGroupConfig(NVIC_PriorityGroup_4);//将所有优先级位设定为抢占优先级位，不留下任何子优先级位


//配置外部中断，先使能AFIO,GPIO，再选择AFIO通道,进EXTI对应线路，配置NVIC中断线路，编写对应中断函数
void exti_init(void)
{
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE);
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO,ENABLE);
	
    //GPIO初始化
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
	GPIO_InitStructure.GPIO_Pin = KEY3_GPIO_PIN;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(KEY3_GPIO_PORT,&GPIO_InitStructure);
	
    //AFIO选择GPIO线进入中断
	GPIO_EXTILineConfig(GPIO_PortSourceGPIOB,GPIO_PinSource14);	//AFIO通道选择
	
    //初始化外部中断
	EXTI_InitTypeDef EXTI_InitStructure;
	EXTI_InitStructure.EXTI_Line = EXTI_Line14;
	EXTI_InitStructure.EXTI_LineCmd = ENABLE;
	EXTI_InitStructure.EXTI_Mode = EXTI_Mode_Interrupt;
	EXTI_InitStructure.EXTI_Trigger = EXTI_Trigger_Falling;
	EXTI_Init(&EXTI_InitStructure);
	
    //初始化外部中断
	NVIC_InitTypeDef NVIC_InitStructure;
	NVIC_InitStructure.NVIC_IRQChannel = EXTI15_10_IRQn;
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 5 ;		//抢占优先级
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;				//子优先级
	NVIC_Init(&NVIC_InitStructure);
}

extern TaskHandle_t Task1_Handler;
void EXTI15_10_IRQHandler(void)
{
	BaseType_t xYieldRequired;
	if(EXTI_GetFlagStatus(EXTI_Line14) == SET)					//判断EXTI标志位
	{
		xYieldRequired = xTaskResumeFromISR(Task1_Handler);		//返回pdTRUE:恢复任务优先级高于目前任务，需要手动调用切换任务函数
		if(xYieldRequired == pdTRUE)							//返回pdFALSE:恢复任务优先级低于目前任务，不需要操作
		{
			portYIELD_FROM_ISR(xYieldRequired);					//任务切换函数
			printf("在中断中恢复task1\r\n");
		}
		if(xYieldRequired == pdTRUE)							
		{
					
		}
		EXTI_ClearITPendingBit(EXTI_Line14);					//清除EXTI标志位
	}
}
```





### 5.5中断管理

- ARM-m内核，**使用8位寄存器来管理优先级，但是只使用了高四位。**
- 后面设置中断优先级，代码中多处采用   **所设置的优先级等级  << (8-4)**

![image-20250408204208146](./嵌入式软件学习.assets/image-20250408204208146.png)

![image-20250408231022630](./嵌入式软件学习.assets/image-20250408231022630.png)





- configMAX_...PRIORITY，表示所管理的最高等级优先级；在FreeRtos这个值为5，也就是只能管理5-15优先等级的优先级

![image-20250408195823881](./嵌入式软件学习.assets/image-20250408195823881.png)



#### 5.5.1将PendSV与滴答定时器中断设置为最低优先级

- PendSV:处理任务调度与任务切换
- SysTick:用于提供任务节拍
- 一个寄存器32位，一个配置项8位

![image-20250408200543812](./嵌入式软件学习.assets/image-20250408200543812.png)



- 优先等级：15<<(8-4)，由于M3内核，中断优先级是8位寄存器的高4位设置。这里在**高4位设置为15，最低优先级。**
- 由于SHPR3为32位寄存器，在对应的位置设置PendSV与Systick的优先级，**设置为最低优先级**。
- **保证任务切换不会打断其他的中断的响应**，1ms一次的频繁任务切换中断避免影响其他中断

![image-20250408201226112](./嵌入式软件学习.assets/image-20250408201226112.png)



```c
#define configPRIO_BITS       		4                  


#define configLIBRARY_LOWEST_INTERRUPT_PRIORITY			15             //中断最低优先级
#define configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY	5               //系统可管理的最高中断优先级

#define configKERNEL_INTERRUPT_PRIORITY 		( configLIBRARY_LOWEST_INTERRUPT_PRIORITY << (8 - configPRIO_BITS) ) // 15 <<  (8 - 4)
#define configMAX_SYSCALL_INTERRUPT_PRIORITY 	( configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY << (8 - configPRIO_BITS) )// 15 <<  (8 - 4)





```



#### 5.5.2中断屏蔽

- PRIMASK ： 0-不屏蔽中断   1-屏蔽除**NMI与硬fault中断**
- FAULTMMASK ：  0-不屏蔽中断   1-屏蔽除**NMI中断**
- **BASEPRI ： 屏蔽优先级号大于此设定值的中断优先级，也就是该位设定屏蔽最高等级中断**；  **0---不关闭任何中断**
- FreeRtos就是利用basepri中断寄存器，写入 5 << (8-4) ,**优先级5-15的优先级都被屏蔽，0-4可以正常响应**

![image-20250408202523455](./嵌入式软件学习.assets/image-20250408202523455.png)

- FreeRtos实现关中断，就是将 0x50写入basepri寄存器中

![image-20250408203503270](./嵌入式软件学习.assets/image-20250408203503270.png)

```c
static portFORCE_INLINE void vPortRaiseBASEPRI( void )
{
uint32_t ulNewBASEPRI = configMAX_SYSCALL_INTERRUPT_PRIORITY;

	__asm
	{
		/* Set BASEPRI to the max syscall priority to effect a critical
		section. */
		msr basepri, ulNewBASEPRI
		dsb
		isb
	}
}
/*---
```



- **开中断就将basepri寄存器设置为0 ，打开所有中断。**

![image-20250408204052383](./嵌入式软件学习.assets/image-20250408204052383.png)





#### 5.5.3中断使用

- 需要将中断分组设置为 **4 ，也就是所有中断都为抢占式**
- 当需要调用FreeRtos的API函数时，其中断优先级需要在管理的范围内 ： **5- 15** 
- 需要调用API，后缀为“FromlSR”

![image-20250408203753089](./嵌入式软件学习.assets/image-20250408203753089.png)



#### 5.3.4实验

- 关闭中断后，优先级6的停止打印 ， 优先级4的不受影响
- 开中断后，优先级6的恢复打印

![image-20250408204928463](./嵌入式软件学习.assets/image-20250408204928463.png)



- 为什么在执行函数中不使用非阻塞式延时：**因为非阻塞式延时中有进入与退出临界区操作，当退出临界区时会将中断打开**


<img src="./嵌入式软件学习.assets/image-20250408231451910.png" alt="image-20250408231451910" style="zoom:50%;" />



### 5.6临界段代码保护

- 临界段也叫临界区，指的是**运行时不可被中途打断，必须完整运行的代码**。例如外设初始化、系统处理等。
- 什么可以打断任务：**1.中断   2.任务调度**（使用pendSV中断实现，最低优先级中断）



#### 1.临界区函数

- **成对使用，支持嵌套**。与上述中断管理原理都是操作basepri寄存器，但是临界区函数支持嵌套
- 尽量使得临界区耗时短
- 中断进入临界区后，回复时回回到执行临界区前的状态。

![image-20250409183915887](./嵌入式软件学习.assets/image-20250409183915887.png)



- 实现嵌套功能，当最后一次退出临界区（也就是嵌套计数器清零后），退出中断

```c
uxCriticalNesting = 0 ； //在开启任务调度器时，嵌套计数器初始化为0
    
#define  portENTER_CRITICAL()			vPortEnterCritical();
#define  portEXIT_CRITICAL()			vPortExitCritical();
    
void vPortEnterCritical( void ) //进入临界区
{
	portDISABLE_INTERRUPTS(); //关闭中断
    
	uxCriticalNesting++; //嵌套计数器++

	if( uxCriticalNesting == 1 )
	{
		configASSERT( ( portNVIC_INT_CTRL_REG & portVECTACTIVE_MASK ) == 0 );
	}
}




void vPortExitCritical( void ) //退出临界区
{
	configASSERT( uxCriticalNesting );
	uxCriticalNesting--; //嵌套计数器 --
	if( uxCriticalNesting == 0 ) //当嵌套计数器值为0，打开打开中断
	{
		portENABLE_INTERRUPTS();
	}
}
```





终端中临界区操作：

- 不具备嵌套功能
- 退出临界区后，恢复进入临界区之前的屏蔽状态

```c
#define portSET_INTERRUPT_MASK_FROM_ISR()		ulPortRaiseBASEPRI()
#define portCLEAR_INTERRUPT_MASK_FROM_ISR(x)	vPortSetBASEPRI(x)



static portFORCE_INLINE uint32_t ulPortRaiseBASEPRI( void )
{
uint32_t ulReturn, ulNewBASEPRI = configMAX_SYSCALL_INTERRUPT_PRIORITY;

	__asm
	{
		/* Set BASEPRI to the max syscall priority to effect a critical
		section. */
		mrs ulReturn, basepri
		msr basepri, ulNewBASEPRI
		dsb
		isb
	}

	return ulReturn;
}





static portFORCE_INLINE void vPortSetBASEPRI( uint32_t ulBASEPRI )
{
	__asm
	{
		msr basepri, ulBASEPRI
	}
}

	
```





### 5.7调度器挂起与恢复

- 任务调度器的挂起与恢复，就是关闭任务调度
- **临界区：屏蔽 中断 + 任务 打断**
- **任务调取器挂起 ： 屏蔽 任务打断，中断可以正常执行**。**用于防止任务之间的资源争夺，不用延时任务的响应**
- 具有嵌套功能
- 实现原理解释++/--标记位计数器，不在定时器中断中触发pendSV中断。但是此时系统时钟还在计数，系统滴答定时没有停止运行。



![image-20250409190956378](./嵌入式软件学习.assets/image-20250409190956378.png)



#### 1.原理讲解

- 任务调度是通过pendSV中断实现
- 系统滴答定时器，定时进入定时中断触发pendSV中断，触发任务调度。
- 在滴答定时器中断中在**地址0xE000_ED04的寄存器中的第28位置一，触发pendSV中断。**![image-20250409192917259](./嵌入式软件学习.assets/image-20250409192917259.png)
- **实现任务调度器挂起：操作uxSchedulerSuspended++ ，实现在系统滴答定时器中断中判断这个计数不为0，不触发pendSV中断，也就不实现任务调度，系统滴答定时器还在跑**
- 例如：队列等待接受列表中，如果调度器挂起，不操作就绪列表。**而是将任务的状态列表项，挂载在等待就绪列表项中**        **在调度器恢复时，会将等待就绪列表中的任务重新加载到就绪列表中。**
- 系统节拍数在调度器未挂起时进行计数，在挂起后使用另一个变量进行计数，在恢复任务调度器时将丢失的节拍数更新到系统节拍中



![image-20250409195741726](./嵌入式软件学习.assets/image-20250409195741726.png)

```c
//系统滴答定时器中端服务函数
void SysTick_Handler(void)
{	
    if(xTaskGetSchedulerState()!=taskSCHEDULER_NOT_STARTED)//系统已经运行
    {
        xPortSysTickHandler();	
    }
}


void xPortSysTickHandler( void )
{
	vPortRaiseBASEPRI(); //关闭中断
	{
		if( xTaskIncrementTick() != pdFALSE ) //当该函数返回值为pdFALSE时，触发pendSV中断
		{
            //    ( * ( ( volatile uint32_t * ) 0xe000ed04 ) ) = ( 1UL << 28UL )
			portNVIC_INT_CTRL_REG = portNVIC_PENDSVSET_BIT; //触发pendSV中断
		}
	}
	vPortClearBASEPRIFromISR(); //打开中断
}


/*当挂起任务调度器，操作uxSchedulerSuspended ++ ，让其不等于pdFALSE。函数固定返回pdFALSE，不触发pendSV中断。
*/
BaseType_t xTaskIncrementTick( void )
{

BaseType_t xSwitchRequired = pdFALSE;

	traceTASK_INCREMENT_TICK( xTickCount );
	if( uxSchedulerSuspended == ( UBaseType_t ) pdFALSE ) //嵌套计数器非空，返回pdFALSE，挂起pendSV
	{
        。。。。。
    }
    else
    {
        
        。。。。。
            uxPendedCounts ++ ； //在任务调度器挂起时统计丢失的系统节拍数
    }
    
    return xSwitchRequired;
```



```c
//调度器挂起
void vTaskSuspendAll( void )
{
	++uxSchedulerSuspended; //不执行pendSV，带有嵌套功能
}


//调度器恢复
BaseType_t xTaskResumeAll( void )
{
TCB_t *pxTCB = NULL;
BaseType_t xAlreadyYielded = pdFALSE;


	configASSERT( uxSchedulerSuspended );

	taskENTER_CRITICAL(); //进入临界区
	{
		--uxSchedulerSuspended; //嵌套计数器 -- 

		if( uxSchedulerSuspended == ( UBaseType_t ) pdFALSE ) //嵌套计数器结束，开启pendSV
		{
			if( uxCurrentNumberOfTasks > ( UBaseType_t ) 0U ) //当前总任务 > 0 
			{

                //在中断任务恢复函数中，当判断调度器当前为挂起状态，会将任务挂载到等待就绪列表中。
                //这里任务调度器恢复，会将等待就绪列表中的任务，一次挂载到就绪列表中。
				while( listLIST_IS_EMPTY( &xPendingReadyList ) == pdFALSE )
				{
                    //获取等待就绪列表中的任务
					pxTCB = ( TCB_t * ) listGET_OWNER_OF_HEAD_ENTRY( ( &xPendingReadyList ) ); 
                    
                    //将时间列表项，从等待就绪列表中移除
					( void ) uxListRemove( &( pxTCB->xEventListItem ) );
                    
                    //将状态列表项从其列表中移除
					( void ) uxListRemove( &( pxTCB->xStateListItem ) );
					prvAddTaskToReadyList( pxTCB ); //加载到就绪列表中
					if( pxTCB->uxPriority >= pxCurrentTCB->uxPriority )
					{
						xYieldPending = pdTRUE;
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
                
                

				if( pxTCB != NULL )
				{

					prvResetNextTaskUnblockTime(); //更新一下下次阻塞的时间
				}

				
                //恢复系统节拍：恢复任务调度器挂起时丢失的系统节拍数
				{
					UBaseType_t uxPendedCounts = uxPendedTicks; /* Non-volatile copy. */

					if( uxPendedCounts > ( UBaseType_t ) 0U )
					{
						do
						{
							if( xTaskIncrementTick() != pdFALSE )  //该语句在滴答定时器中断内判断是否执行Pendsv中断，这里多次调用模拟在系统滴答定时器中断中调用，更新系统节拍
							{
								xYieldPending = pdTRUE;
							}
							else
							{
								mtCOVERAGE_TEST_MARKER();
							}
							--uxPendedCounts;
						} while( uxPendedCounts > ( UBaseType_t ) 0U );

						uxPendedTicks = 0;
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}

                
                	//当等待就绪列表中的任务优先级 >当前运行任务优先级 ，触发任务调度
				if( xYieldPending != pdFALSE )
				{
					#if( configUSE_PREEMPTION != 0 )
					{
						xAlreadyYielded = pdTRUE;
					}
					#endif
					taskYIELD_IF_USING_PREEMPTION(); //执行任务切换
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
	taskEXIT_CRITICAL();

	return xAlreadyYielded;
}
```



### 5.8列表与列表项



#### 1.列表项介绍

- 列表就是一个**循环双向链表**，每一个列表项就是一个节点

- 列表项的地址是非连续的，可以任意删减

- 数组地址是连续的，定义后后续无法改变大小

- 列表项的相关函数与定义都在**Llist.c 与 List.h**

  

![image-20250410200900191](./嵌入式软件学习.assets/image-20250410200900191.png)



<img src="./嵌入式软件学习.assets/image-20250410204746803.png" alt="image-20250410204746803" style="zoom:50%;" />

列表项时任务控制块的一个成员，每个列表项外部是一个任务控制块

![image-20250410204717627](./嵌入式软件学习.assets/image-20250410204717627.png)









#### 2.列表构成



- 列表项的主要组成：数量、遍历指针、末尾迷你列表项 <img src="./嵌入式软件学习.assets/image-20250410202218733.png" alt="image-20250410202218733" style="zoom:33%;" />

![image-20250410201536371](./嵌入式软件学习.assets/image-20250410201536371.png)

```c
typedef struct xLIST
{
	listFIRST_LIST_INTEGRITY_CHECK_VALUE		//已知常量，用于检测链表是否被破坏，一般不开启
        
        
	configLIST_VOLATILE UBaseType_t uxNumberOfItems;  	//记录列表中列表项数目(不包含末尾列表项)
	ListItem_t * configLIST_VOLATILE pxIndex;			//用于遍历列表项的指针，指向某一列表项，任务切换中使用此进行时间片轮询		
	MiniListItem_t xListEnd;						//末尾列表项（头即是尾，尾即是头） --- 迷你列表项				
    
    
	listSECOND_LIST_INTEGRITY_CHECK_VALUE		//已知常量，用于检测链表是否被破坏，一般不开启		
} List_t;
```



#### 3.列表项构成

- 每个任务控制块都有事件列表项（事件列表项的列表值，在任务初始化时赋值，优先级总数-任务当前优先级-----实现优先级高的列表值小升序在列表前面优先执行）、状态列表项，通过这些列表项表征任务的状态，通过列表项所在列表pxContainer来标识任务当前状态

<img src="./嵌入式软件学习.assets/image-20250410203135470.png" alt="image-20250410203135470" style="zoom:50%;" />通过任务控制块，遍历其列表项所挂载在那个列表，来判断其状态：就绪/挂起/阻塞

![image-20250410202903482](./嵌入式软件学习.assets/image-20250410202903482.png)



```c
struct xLIST_ITEM
{
	listFIRST_LIST_ITEM_INTEGRITY_CHECK_VALUE	//用于检测数据是否被破坏，一般不使用	
        
        
	configLIST_VOLATILE TickType_t xItemValue;	//	列表项值，用于列表升序	
	struct xLIST_ITEM * configLIST_VOLATILE pxNext;		//指向下一个列表项
	struct xLIST_ITEM * configLIST_VOLATILE pxPrevious;	//指向上一个列表项
	void * pvOwner;			//指向该列表的拥有者，也就是对应的任务控制块							
	void * configLIST_VOLATILE pvContainer;	//指向该列表项所挂载的列表	
    
    
	listSECOND_LIST_ITEM_INTEGRITY_CHECK_VALUE //用于检测数据是否被破坏，一般不使用	
};
typedef struct xLIST_ITEM ListItem_t;					
```





##### 迷你列表项

- 用于标记列表末尾、方便其他列表项插入
- 相比正常列表项，缺少拥有者、所挂载链表

![image-20250410204256191](./嵌入式软件学习.assets/image-20250410204256191.png)



#### 4.列表的API函数

![image-20250410205233871](./嵌入式软件学习.assets/image-20250410205233871.png)



##### 1.vListInitialise 列表初始化

<img src="./嵌入式软件学习.assets/image-20250410210839956.png" alt="image-20250410210839956" style="zoom:50%;" />

```c
void vListInitialise( List_t * const pxList ) //参数为带初始化列表
{
	pxList->pxIndex = ( ListItem_t * ) &( pxList->xListEnd );	 //列表索引指针 - 指向末尾列表项		

    
    //初始化末尾列表项（）
	pxList->xListEnd.xItemValue = portMAX_DELAY;		//列表项值为0xFFFF FFFF升序最大处末尾
	pxList->xListEnd.pxNext = ( ListItem_t * ) &( pxList->xListEnd );	//两个连接指针都指向自身
	pxList->xListEnd.pxPrevious = ( ListItem_t * ) &( pxList->xListEnd );
    
    
	pxList->uxNumberOfItems = ( UBaseType_t ) 0U; //当前列表项数量清零，末尾列表项不算计数


    //检测数据完整性
	listSET_LIST_INTEGRITY_CHECK_1_VALUE( pxList );
	listSET_LIST_INTEGRITY_CHECK_2_VALUE( pxList );
}
```







##### 2.vListInitialiseItem列表项初始化



- 只将数据拥有者复位，<img src="./嵌入式软件学习.assets/image-20250410211225284.png" alt="image-20250410211225284" style="zoom:33%;" />

```c
void vListInitialiseItem( ListItem_t * const pxItem ) //参数为待初始化列表项
{

	pxItem->pvContainer = NULL; //将所有者复位


    //检查数据完整性
	listSET_FIRST_LIST_ITEM_INTEGRITY_CHECK_VALUE( pxItem );
	listSET_SECOND_LIST_ITEM_INTEGRITY_CHECK_VALUE( pxItem );
}
```





##### 3.vListInsert列表项升序插入



<img src="./嵌入式软件学习.assets/image-20250410231939985.png" alt="image-20250410231939985" style="zoom: 33%;" />



<img src="./嵌入式软件学习.assets/image-20250410232009724.png" alt="image-20250410232009724" style="zoom:33%;" />

根据列表项的值，进行升序插入

```c
void vListInsert( List_t * const pxList, ListItem_t * const pxNewListItem ) //参数：列表 、 列表项
{
    
    
ListItem_t *pxIterator; //指向待插入位置的前一个列表项
    
const TickType_t xValueOfInsertion = pxNewListItem->xItemValue; //获取待插入列表项的升序值

	
	listTEST_LIST_INTEGRITY( pxList );
	listTEST_LIST_ITEM_INTEGRITY( pxNewListItem );

//如果升序值 == 0xFFFF FFFF 将P直接指向尾项的前一个数据，插在尾项的前面
	if( xValueOfInsertion == portMAX_DELAY ) 
	{
		pxIterator = pxList->xListEnd.pxPrevious;
	}
	else 
	{
		//不停遍历下一个列表项的数字是否小于待插入列表项的数字，否则往后移动。
		for( pxIterator = ( ListItem_t * ) &( pxList->xListEnd ); //p 初始值指向尾项
            pxIterator->pxNext->xItemValue <= xValueOfInsertion; 
            pxIterator = pxIterator->pxNext )
	}
    
    //在pxIterator所指向列表项进行后插操作
	pxNewListItem->pxNext = pxIterator->pxNext;
	pxNewListItem->pxNext->pxPrevious = pxNewListItem;
	pxNewListItem->pxPrevious = pxIterator;
	pxIterator->pxNext = pxNewListItem;

	
	pxNewListItem->pvContainer = ( void * ) pxList; //列表项所属链表赋值

	( pxList->uxNumberOfItems )++; //列表中的记录列表项数量++
}
```







##### 4.vListInsert列表项尾插

- 将列表项插入到列表**pxInde指针指向列表项的前面**，无序插入

- 当pxIndex恰好指向尾项时，该API就是尾插

  ![image-20260527212449777](./嵌入式软件学习.assets/image-20260527212449777.png)

```c
void vListInsertEnd( List_t * const pxList, ListItem_t * const pxNewListItem )
{
ListItem_t * const pxIndex = pxList->pxIndex;


    
	listTEST_LIST_INTEGRITY( pxList );
	listTEST_LIST_ITEM_INTEGRITY( pxNewListItem );

//列表pxInde指针前插
	pxNewListItem->pxNext = pxIndex;
	pxNewListItem->pxPrevious = pxIndex->pxPrevious;

	pxIndex->pxPrevious->pxNext = pxNewListItem;
	pxIndex->pxPrevious = pxNewListItem;

	/* Remember which list the item is in. */
	pxNewListItem->pvContainer = ( void * ) pxList;

	( pxList->uxNumberOfItems )++;
}
```









##### 5.uxListRemove删除列表项

<img src="./嵌入式软件学习.assets/image-20250410231840299.png" alt="image-20250410231840299" style="zoom:50%;" />

<img src="./嵌入式软件学习.assets/image-20250410231851244.png" alt="image-20250410231851244" style="zoom:50%;" />



```c
UBaseType_t uxListRemove( ListItem_t * const pxItemToRemove )
{

List_t * const pxList = ( List_t * ) pxItemToRemove->pvContainer; //获取待删除列表项

    //将列表项在列表中断开
	pxItemToRemove->pxNext->pxPrevious = pxItemToRemove->pxPrevious;
	pxItemToRemove->pxPrevious->pxNext = pxItemToRemove->pxNext;


	mtCOVERAGE_TEST_DELAY();

	//如果列表指针指向待删除列表项，将列表项指针向前偏移
	if( pxList->pxIndex == pxItemToRemove )
	{
		pxList->pxIndex = pxItemToRemove->pxPrevious;
	}
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}

    
	pxItemToRemove->pvContainer = NULL; //待删除列表项的所挂载列表为NULL
	( pxList->uxNumberOfItems )--; //列表项数量 -- 

	return pxList->uxNumberOfItems;
}
```





#### 5.实验







### 5.9任务调度器



#### 1.启动任务调度器



##### 1.1vTaskStartScheduler

![image-20250414212034204](./嵌入式软件学习.assets/image-20250414212034204.png)

```c
void vTaskStartScheduler( void )
{
BaseType_t xReturn;

	
	#if( configSUPPORT_STATIC_ALLOCATION == 1 ) //静态创建任务宏
	{
		//...静态创建任务省略
	}
	#else //动态创建任务
	{
		//创建空闲任务
		xReturn = xTaskCreate(	prvIdleTask,
								"IDLE", configMINIMAL_STACK_SIZE,
								( void * ) NULL,
								( tskIDLE_PRIORITY | portPRIVILEGE_BIT ),
								&xIdleTaskHandle ); 
	}
	#endif 

    
    
    
    
	#if ( configUSE_TIMERS == 1 )  //使能软件定时器任务
	{
		if( xReturn == pdPASS )
		{
			xReturn = xTimerCreateTimerTask(); //使能定时器任务
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
	#endif 
    
    
    
    
    
    

	if( xReturn == pdPASS )
	{

		portDISABLE_INTERRUPTS(); //关中断，在开启第一个任务前，也就是触发SVC中断前会开启中断

		#if ( configUSE_NEWLIB_REENTRANT == 1 )
		{
			_impure_ptr = &( pxCurrentTCB->xNewLib_reent );
		}
		#endif /* configUSE_NEWLIB_REENTRANT */

		xNextTaskUnblockTime = portMAX_DELAY; //下一个任务的阻塞超时时间   赋值为   系统阻塞时间赋值最大
		xSchedulerRunning = pdTRUE; //调度器状态为正在运行
		xTickCount = ( TickType_t ) 0U;  //系统节拍复位

	
		portCONFIGURE_TIMER_FOR_RUN_TIME_STATS(); //接口-实现任务运行时间统计功能


		if( xPortStartScheduler() != pdFALSE ) //开启任务调度器，运行这里不再返回直接进入下一个任务运行
		{
			
		}
		else
		{
			
		}
	}
	else
	{
		
		configASSERT( xReturn != errCOULD_NOT_ALLOCATE_REQUIRED_MEMORY );
	}

	( void ) xIdleTaskHandle;
}
```



##### 1.2xPortStartScheduler



![image-20250414213812346](./嵌入式软件学习.assets/image-20250414213812346.png)



- 运行完该函数后，程序无返回直接通过任务调度器运行任务。
- 用于完成**启动任务调度器**与**硬件架构相关配置**、**以及启动第一个任务**。

![image-20250414213054808](./嵌入式软件学习.assets/image-20250414213054808.png)

```c
BaseType_t xPortStartScheduler( void )
{
    
    //用于检测用户在FreeRTosconfig.h中对中断相关配置是否有误 ， 这里不要求掌握
	#if( configASSERT_DEFINED == 1 )  
	{
		volatile uint32_t ulOriginalPriority;
		volatile uint8_t * const pucFirstUserPriorityRegister = ( uint8_t * ) ( portNVIC_IP_REGISTERS_OFFSET_16 + portFIRST_USER_INTERRUPT_NUMBER );
		volatile uint8_t ucMaxPriorityValue;

		ulOriginalPriority = *pucFirstUserPriorityRegister;

		*pucFirstUserPriorityRegister = portMAX_8_BIT_VALUE;

		ucMaxPriorityValue = *pucFirstUserPriorityRegister;

		configASSERT( ucMaxPriorityValue == ( configKERNEL_INTERRUPT_PRIORITY & ucMaxPriorityValue ) );
		ucMaxSysCallPriority = configMAX_SYSCALL_INTERRUPT_PRIORITY & ucMaxPriorityValue;
		ulMaxPRIGROUPValue = portMAX_PRIGROUP_BITS;
		while( ( ucMaxPriorityValue & portTOP_BIT_OF_BYTE ) == portTOP_BIT_OF_BYTE )
		{
			ulMaxPRIGROUPValue--;
			ucMaxPriorityValue <<= ( uint8_t ) 0x01;
		}
		ulMaxPRIGROUPValue <<= portPRIGROUP_SHIFT;
		ulMaxPRIGROUPValue &= portPRIORITY_GROUP_MASK;
		*pucFirstUserPriorityRegister = ulOriginalPriority;
	}
	#endif 
    //以上不要求掌握
    
    
    

	
	portNVIC_SYSPRI2_REG |= portNVIC_PENDSV_PRI;  //pendSV中断优先级为15，最低优先级
	portNVIC_SYSPRI2_REG |= portNVIC_SYSTICK_PRI; //系统滴答定时器中断优先级为15，最低优先级

	
	vPortSetupTimerInterrupt(); //初始化系统滴答定时器


	uxCriticalNesting = 0; //复位临界区嵌套计数器

	
	prvEnableVFP(); //使能FPU：M3内核不支持，M4/M7内核支持 --- 开启相关寄存器位
	*( portFPCCR ) |= portASPEN_AND_LSPEN_BITS; //使能进出异常时，自动恢复和保存FPU相关运算寄存器：任务切换时自动保存S0-S15浮点数寄存器


	prvStartFirstTask(); //开启第一个任务


	return 0;
}
```



###### 初始化系统滴答定时器

![image-20250414214644954](./嵌入式软件学习.assets/image-20250414214644954.png)



```c
//初始化滴答定时器	
void vPortSetupTimerInterrupt( void )
	{
		//LOAD-重装载计数器 = 系统时钟（72M） / 设定频率（1000） - 1 
        //也就是72 000 -1  也就是 1ms触发一次定时器中断，也就是时间为1ms
		portNVIC_SYSTICK_LOAD_REG = ( configSYSTICK_CLOCK_HZ / configTICK_RATE_HZ ) - 1UL;
		
        
		portNVIC_SYSTICK_CTRL_REG = (
        	portNVIC_SYSTICK_CLK_BIT  //( 1UL << 2UL ) 选择AHB为滴答定时器时钟
        	| portNVIC_SYSTICK_INT_BIT  //( 1UL << 1UL ) 使能定时器计数到零中断
        	| portNVIC_SYSTICK_ENABLE_BIT ); //( 1UL << 0UL ) 使能定时器
        
	}
```



##### 1.3开启第一个任务



![image-20250419181831321](./嵌入式软件学习.assets/image-20250419181831321.png)

- **切换或者启动一个任务，将任务堆栈中的寄存器值加载到CPU寄存器中**
- **中断产生时**，硬件**自动将xPSR、PC、LR 、R12、R3-R0保存和恢复**，**R4-R11需要手动保存**。
- **进入中断后，硬件强制使用MSP指针，LR值在中断中会被自动更新为特定值**

![image-20250415202110415](./嵌入式软件学习.assets/image-20250415202110415.png)

![image-20260119211959743](./嵌入式软件学习.assets/image-20260119211959743.png)

![image-20260119212021448](./嵌入式软件学习.assets/image-20260119212021448.png)

![image-20260119212047700](./嵌入式软件学习.assets/image-20260119212047700.png)





- FreeRtos使用**双堆栈**，使得程序运行更安全。**中断使用MSP-主堆栈指针** ，**中断以外使用PSP-进程堆栈**，进程更安全。
- 裸机因为main函数在reset中断中运行，使用的是MSP指针。
- 使用汇编时，操作堆栈需要手动8字节对齐
- <img src="./嵌入式软件学习.assets/image-20250415205059335.png" alt="image-20250415205059335" style="zoom: 50%;" />LDR寄存器，将存储器中加载一个字到寄存器中。
- **开启第一个任务时会重置msp指针**，msp的值指向中断向量表的第一个元素地址也就是栈初始地址，**在启动第一个任务前的创建任务等的栈都会被清空**![image-20260119213628157](./嵌入式软件学习.assets/image-20260119213628157.png)
- FreeRtos使用**SVC中断来开启第一个任务**
- SP指针堆栈指针，SP指针先分为PSP指针与MSP指针，裸机程序一般使用MSP指针即可，FreeRtos采用双堆栈指针。





![image-20250415202857450](./嵌入式软件学习.assets/image-20250415202857450.png)

![image-20260121195006390](./嵌入式软件学习.assets/image-20260121195006390.png)



- 栈无论什么时候，都需要4字节对齐
- 栈在开始的时候，需要8字节对齐
- 复位MSP指针，触发SVC中断

![image-20250415204459878](./嵌入式软件学习.assets/image-20250415204459878.png)



```c
__asm void prvStartFirstTask( void )
{
	PRESERVE8 //8字节对齐，栈需要4字节对齐，C编程会自动对齐，这里使用汇编需要手动对齐。

	//将MSP地址，加载到寄存器R0
        
    //VTOR（中断向量表重定向寄存器）地址就是 0XE000ED08 ，获取中断向量表起始地址，M3芯片固定    
	ldr r0, =0xE000ED08  //R0=0XE000ED08
	ldr r0, [r0]		//取 R0 所保存的地址处的值赋给 R0 --- 中端向量表起始地址
	ldr r0, [r0]		//获取MSP指针 ，中断向量表起始地址存储的就是MSP地址

        
	//将R0中的MSP堆栈指针地址重新加载到msp寄存器中，在调用第一个任务后，前面所有的变量全部丢失，只在任务中切换
	msr msp, r0			
	
    //这里开中断，在之前的启动调度器中关闭过中断   
	cpsie i  //使能中断(清除 PRIMASK)
	cpsie f  //使能中断(清除 FAULTMASK)
	dsb		//数据同步屏障
	isb		//指令同步屏障
        
        
	//FreeRTOS 中仅仅使用 SVC 异常来启动第一个任务，后面的程序中就再也用不到 SVC
	svc 0 //触发SVC中断
	nop
	nop
}
```



###### 1.SVC中断-开启第一个任务



- **将R4-R11手动出栈，将堆栈地址赋值给PSP，退出SVC中断。退出后PSP会自动出栈，更新PC指针的值还有其他寄存器，正式进入任务中运行。**





- <img src="./嵌入式软件学习.assets/image-20250415214858862.png" alt="image-20250415214858862" style="zoom:50%;" />放在R11后面，是使用指令自动偏移加载到R14-LR寄存器中
- R14-LR寄存器只有6个合法值。由于FreeRTos工作在双堆栈(中断使用MSP，任务使用PSP)下，M4,M7内核只有两个该可选值 **0XFFFF FFED、0XFFFF FFFD**-配置是否使用浮点FPU，有FPU选择所以会需要保存R14的值<img src="./嵌入式软件学习.assets/image-20250415215139959.png" alt="image-20250415215139959" style="zoom:67%;" />
- 由于M3内核无FPU，无需配置BIT4，只需在最后配置为0x0d。表示退出 异常以后 CPU 进入线程模式并且使用进程栈
- ![image-20250415222147146](./嵌入式软件学习.assets/image-20250415222147146.png)R14每一位代表含义

![image-20250415211657812](./嵌入式软件学习.assets/image-20250415211657812.png)

![image-20250415212208589](./嵌入式软件学习.assets/image-20250415212208589.png)

![image-20250415220318678](./嵌入式软件学习.assets/image-20250415220318678.png)

SVC中断，将pxCurrentTCB指向的任务出栈到CPU，并设置R14值

```c
__asm void vPortSVCHandler( void )
{
	PRESERVE8 //8字节对齐

	ldr	r3, =pxCurrentTCB	// "="表示获取地址，这里获取pxCurrentTCB的地址   指向（正在运行任务-最高优先级）任务控制块地址
	ldr r1, [r3]			//获取pxCurrentTCB指向任务控制块的地址
	ldr r0, [r1]			//获取任务控制块首地址 - 也就是第一个成员存储的值 --- 任务栈顶指针
        
        
	ldmia r0!, {r4-r11}		//出栈，将R0地址指向任务栈的值依次出栈到CPU寄存器r4-r11 ---手动出栈 - 这里M3无需配置R14
    //stmdb r0!, {r1, r4-r11, r14}	 M4,M7需要单独加载R14-LR寄存器 ，该值在任务创建时配置在r11后的任务堆栈区
    
	msr psp, r0				//设置PSP指针为当前r0值，当退出SVC中断后硬件自动恢复剩余的值
	isb
        
        
        
	mov r0, #0				//r0设置为0
	msr	basepri, r0			//开启中断
        
        
     //R14 寄存器的值与 0X0D 进行或运算，得到的结果就是 R14 寄存器的新值。表示退出异常以后 CPU 进入线程模式并且使用进程栈   
	orr r14, #0xd			//M3将R14-LR寄存器设置 0x0d 表示使用PSP;M4不需要这句话，因为上方已自动设置完
        
	bx r14 /*跳转到任务函数中执行 ，硬件自动恢复寄存器 R0~R3、R12、LR、PC 和 xPSR 的值，堆栈使
用进程栈 PSP，然后执行寄存器 PC 中保存的任务函数。至此，FreeRTOS 的任务调度器正式开
始运行！ */
}
```



#### 2.任务切换

- **任务切换（也称上下文切换），将需要运行的任务堆栈加载到CPU寄存器中（恢复现场），将被抢占的任务从CPU寄存器中加载到任务堆栈中（保存现场）**![image-20250419181759634](./嵌入式软件学习.assets/image-20250419181759634.png)

![image-20250417211658084](./嵌入式软件学习.assets/image-20250417211658084.png)

![image-20260121193401871](./嵌入式软件学习.assets/image-20260121193401871.png)

##### 2.1pendSV中断

- 在PendSV中，对两个任务进行任务切换
- 一部分寄存器由硬件自动操作，一部分寄存器值由人为手动操作

![image-20250417212722553](./嵌入式软件学习.assets/image-20250417212722553.png)



**两种方式调用Pendsv :   1.系统滴答定时器中断     2.API接口 - portYIELD()**

- **通系统滴答定时器中断，周期触发pendSV中断，也可以通过屏蔽任务调度器，使得滴答定时器中断不调用pendSV中断，但是系统时间计数继续运行，滴答定时器不停止。**
- 启动pendSV，使用portYIELD()  API函数,**本质是在ICSR寄存器的BIT28写入1**

![image-20250417213043876](./嵌入式软件学习.assets/image-20250417213043876.png)

![image-20250419181909748](./嵌入式软件学习.assets/image-20250419181909748.png)

###### 2.1.1系统滴答定时器中断

自动触发pendsv中断

- 阻塞任务唤醒，且唤醒任务大于当前运行任务
- 时间片轮询（当前运行任务的就绪列表有多个任务）

```c
//系统滴答定时器中断
void SysTick_Handler(void)
{	
    if(xTaskGetSchedulerState()!=taskSCHEDULER_NOT_STARTED) //判断调度器是否在运行
    {
        xPortSysTickHandler();	 //调度器正在运行，判断是否需要启动任务切换
    }
}



void xPortSysTickHandler( void )
{

	vPortRaiseBASEPRI(); //关闭中断 basepri寄存器写入 5
	{
		if( xTaskIncrementTick() != pdFALSE ) //是否需要启动任务切换，任务调度器是否挂起
		{
			portNVIC_INT_CTRL_REG = portNVIC_PENDSVSET_BIT; //ICSR寄存器的BIT28写入1 ，启动pendSV中断
		}
	}
	vPortClearBASEPRIFromISR(); //打开中断  basepri寄存器写入 0
}




//是否需要任务调度，这里只截取部分代码 1.阻塞唤醒  2.时间片轮询
//外部还有一个if判断是判断是否挂起调度器。
BaseType_t xTaskIncrementTick( void )
{
TCB_t * pxTCB;
TickType_t xItemValue;
BaseType_t xSwitchRequired = pdFALSE;


		if( xConstTickCount >= xNextTaskUnblockTime ) //当系统时间到达阻塞时间
		{ 
			for( ;; )
			{
				if( listLIST_IS_EMPTY( pxDelayedTaskList ) != pdFALSE )//当前阻塞列表上没有任务，更新阻塞时间为最大时间，并返回
				{

					xNextTaskUnblockTime = portMAX_DELAY; 
					break;
				}
				else
				{
					pxTCB = ( TCB_t * ) listGET_OWNER_OF_HEAD_ENTRY( pxDelayedTaskList );
					xItemValue = listGET_LIST_ITEM_VALUE( &( pxTCB->xStateListItem ) ); //获取最先唤醒任务的时间

					if( xConstTickCount < xItemValue ) //未到唤醒时间返回
					{
						xNextTaskUnblockTime = xItemValue;
						break;
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}

					( void ) uxListRemove( &( pxTCB->xStateListItem ) ); //将最先阻塞唤醒任务从阻塞列表上移除


					if( listLIST_ITEM_CONTAINER( &( pxTCB->xEventListItem ) ) != NULL ) //将该任务的事件列表也进行移除
					{
						( void ) uxListRemove( &( pxTCB->xEventListItem ) );
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}


					prvAddTaskToReadyList( pxTCB ); //将阻塞时间到达任务添加到就绪列表中
                    
                    

					#if (  configUSE_PREEMPTION == 1 ) //判断当前阻塞恢复的任务优先级与当前运行任务优先级比较。
					{
						if( pxTCB->uxPriority >= pxCurrentTCB->uxPriority )
						{
							xSwitchRequired = pdTRUE; //触发pendSV
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					#endif 
				}
			}
		}

    
    
     //时间片调度
		#if ( ( configUSE_PREEMPTION == 1 ) && ( configUSE_TIME_SLICING == 1 ) ) //开启抢占式与时间片轮转 宏
		{
            //判断当前所执行的任务是否有其他同优先级任务 ---- 执行一次任务调度
			if( listCURRENT_LIST_LENGTH( &( pxReadyTasksLists[ pxCurrentTCB->uxPriority ] ) ) > ( UBaseType_t ) 1 )
			{
				xSwitchRequired = pdTRUE; //触发pendSV
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		#endif /* ( ( configUSE_PREEMPTION == 1 ) && ( configUSE_TIME_SLICING == 1 ) ) */


	return xSwitchRequired;
}
```





###### 2.1.2pendSV中断实现



- 使用双堆栈指针，在任务运行时使用PSP，进入中断使用MSP<img src="./嵌入式软件学习.assets/image-20250419152533515.png" alt="image-20250419152533515" style="zoom:67%;" />
- **在进入和退出中断都需要对PSP进行压栈和出栈操作**，操作的都是PSP指针
- **在进入中断前任务A的psp已经自动对部分寄存器进行了压栈，进入中断后手动将任务A剩余的寄存器进行压栈，更新任务A的栈顶指针。再将任务B的部分寄存器手动出栈，退出pendsv中断，退出后PSP自动对剩下的任务B寄存器进行出栈。**

![image-20250417220607479](./嵌入式软件学习.assets/image-20250417220607479.png)



- 在进入中断时，硬件会自动压栈，**PSP任务堆栈指针将R0-xPSR寄存器的值压入任务堆栈中**。
- **进入中断后，PSP指针指向R0**

<img src="./嵌入式软件学习.assets/image-20250417220900383.png" alt="image-20250417220900383" style="zoom:50%;" />

<img src="./嵌入式软件学习.assets/image-20250419155439926.png" alt="image-20250419155439926" style="zoom: 80%;" />

```c
__asm void xPortPendSVHandler( void )
{
	extern uxCriticalNesting;
	extern pxCurrentTCB;
	extern vTaskSwitchContext;

	PRESERVE8 //8字节对齐
        
        
//上一个任务保存现场，手动压栈+将PSP值保存在任务控制块的栈顶指针变量下，便于下次任务恢复
        
	mrs r0, psp //在进入中断前，PSP已进行压栈，将xPSR-R0压入任务堆栈
	isb

	ldr	r3, =pxCurrentTCB		//r3 = &pxCurrentTCB ,获取pxCurrentTCB的地址
	ldr	r2, [r3]				// r2 = *pxCurrentTCB ,获取pxCurrentTCB中的值，也就是指向正在运行的任务地址，也就是第一个元素栈顶指针的地址

	stmdb r0!, {r4-r11}			//压栈任务A的PSP栈地址，手动将寄存器向栈进行压入 ： 从R4寄存器依次将CPU寄存器内容压入R0（psp-任务堆栈指针）地址
	str r0, [r2]				//将r0 z=值写入R2地址下，更新当前任务控制块的栈顶指针（保存上PSP的值，用于下次任务恢复） ： 将当前PSP指针的值写入当前运行任务的堆栈指针变量地址下，便于下次恢复
        
        
        

	stmdb sp!, {r3, r14} //压栈： 这里SP是MSP ，保存当前R3为pxCurrentTCB地址 、R14寄存器值
    
    
    
    
    
    
    //关中断，关闭5-15优先等级中断 
	mov r0, #configMAX_SYSCALL_INTERRUPT_PRIORITY //R0写入5
	msr basepri, r0
	dsb
	isb
        
        
	bl vTaskSwitchContext //进入改函数，该函数作用是查找下一个需要运行的函数。更新pxCurrentTCB指向的任务控制块
     
     
     //开中断
	mov r0, #0 //R0赋值为0
	msr basepri, r0 
        
    //将之前保存的R3-pxCurrentTCB的地址 R14出栈     
	ldmia sp!, {r3, r14}

    
    
    
    
    
	ldr r1, [r3]	//获取R1= *R3 ：获取R3指向地址的值，  R3地址为pxCurrentTCB地址，其值为下一个运行任务TCB地址，也就是上述函数更新的最新需要运行的任务B任务控制块
	ldr r0, [r1]	//第一个元素的值，当前需要运行结构体的栈顶指针，任务B的栈顶指针		
  
	ldmia r0!, {r4-r11}		//将R0寄存器也就是任务栈顶指针R4-R11依次出栈到CPU寄存器
	msr psp, r0				//将当前r0寄存器值赋值给PSP，退出中断后自动出栈
	isb
        
        
        
	bx r14					//返回任务，PSP自动出栈，PC指针出运行函数
	nop
        
        
        
        
}
```





###### 2.1.3寻找当前最高优先等级任务

- 通过一个32位变量来标识该任务优先级下列表有任务。**__CLZ为前导置零指令**，计算该32位变量前有多少个零。获取最高优先级位置
- 31-前方零的个数，获取当前最高优先级任务。<img src="./嵌入式软件学习.assets/image-20250419172918057.png" alt="image-20250419172918057" style="zoom:50%;" />

<img src="./嵌入式软件学习.assets/image-20250419172740671.png" alt="image-20250419172740671" style="zoom: 80%;" />





- 通过上述前导置零指令获取当前最高优先级
- 在最高优先级列表下轮询获取任务，实现时间片流转
- 列表初始化时pxIndex指向末尾列表项，第一次获取列表项1，pxIndex指向1 ； 第二次pendSV触发后，获取列表项2，pxIndex指向2；第三次跳过末尾列表项，指向1，诸如此循环，实现同优先级下任务时间片切换。

<img src="./嵌入式软件学习.assets/image-20250419174523931.png" alt="image-20250419174523931" style="zoom:50%;" />



```c
//查找下一个运行任务函数
void vTaskSwitchContext( void )
{
	if( uxSchedulerSuspended != ( UBaseType_t ) pdFALSE )
	{
		....
	}
	else
	{
		....
		taskSELECT_HIGHEST_PRIORITY_TASK(); //通过此宏定义参数实现

	}
}





	#define taskSELECT_HIGHEST_PRIORITY_TASK()														
	{																								
	UBaseType_t uxTopPriority;																		
																									
	//#define portGET_HIGHEST_PRIORITY( uxTopPriority, uxReadyPriorities ) uxTopPriority = ( 31 - __clz( 		        uxReadyPriorities ) ) //获取当前最高优先级，uxTopPriority-最高优先级等级								
	portGET_HIGHEST_PRIORITY( uxTopPriority, uxTopReadyPriority );
        
    //判断当前优先级对应的就绪列表是否为空    
	configASSERT( listCURRENT_LIST_LENGTH( &( pxReadyTasksLists[ uxTopPriority ] ) ) > 0 );	
        
        //获取该链表项下的任务，并pxCurrentTCB指向该任务控制块      函数具体实现在下方
	listGET_OWNER_OF_NEXT_ENTRY( pxCurrentTCB, &( pxReadyTasksLists[ uxTopPriority ] ) );		
	}








//根据当前优先级在列表下获取任务，循环获取，同优先级下实现时间片切换
#define listGET_OWNER_OF_NEXT_ENTRY( pxTCB, pxList )										
{																							
	List_t * const pxConstList = ( pxList );	//获列表										
						
	( pxConstList )->pxIndex = ( pxConstList )->pxIndex->pxNext; //列表指针指向下一个列表项，通过这里实现循环，也就是时间片调度
    
    //跳过末尾列表项，当循环一轮时指向末尾列表项，跳过
	if( ( void * ) ( pxConstList )->pxIndex == ( void * ) &( ( pxConstList )->xListEnd ) )	
	{																						
		( pxConstList )->pxIndex = ( pxConstList )->pxIndex->pxNext;						
	}	
    
    //将当前指向的任务赋值给 当前需要运行的任务控制块指针
	( pxTCB ) = ( pxConstList )->pxIndex->pvOwner;											
}
```



#### 3.时间片调度

- 同优先级下，不同任务相互切换运行
- 时间片可以通过改变系统滴答定时器中断频率，来改变时间片大小
- 

![image-20250419182708065](./嵌入式软件学习.assets/image-20250419182708065.png)



- 50ms通过修改滴答定时器-重装值，改变中断频率

![image-20250419182948403](./嵌入式软件学习.assets/image-20250419182948403.png)







#### 4.相关API



##### 任务状态查询API

![image-20250419183406477](./嵌入式软件学习.assets/image-20250419183406477.png)



##### 任务时间统计API

![image-20250419184355380](./嵌入式软件学习.assets/image-20250419184355380.png)



###### 配置宏定于与实现接口函数

- 开启3个宏，还需要实现两个接口，提供给系统调用。
- ConfigureTimeForRunTimeStats定时精度需要是时间片的 10-100倍。也就是当时间片为1ms时，定时器一拍为10us - 100us
- <img src="./嵌入式软件学习.assets/image-20250419190012825.png" alt="image-20250419190012825" style="zoom: 80%;" />在开启任务调度器函数中调用。

![image-20250419184856369](./嵌入式软件学习.assets/image-20250419184856369.png)



```c

#define configGENERATE_RUN_TIME_STATS	        1                       //为1时启用运行时间统计功能
#define configUSE_TRACE_FACILITY				1                       //为1启用可视化跟踪调试
#define configUSE_STATS_FORMATTING_FUNCTIONS	1                       //与宏configUSE_TRACE_FACILITY同时为1时会编译下面3个函数

//这里是FreeRtos提供的接口函数
#define portCONFIGURE_TIMER_FOR_RUN_TIME_STATS()        ConfigureTimeForRunTimeStats() //初始化定时器 在开启任务调度器函数vTaskStartScheduler()函数中进行初始化
extern uint32_t FreeRTOSRunTimeTicks;
#define portGET_RUN_TIME_COUNTER_VALUE()                FreeRTOSRunTimeTicks //返回定时器计数值


/*时基定时器的初始化，精度需要时系统滴答定时器的10-100被，时间片为1ms，这里实现时钟需要0.1ms - 0.001ms中断，该函数在开启任务调度器时调用*/
uint32_t FreeRTOSRunTimeTicks; //定义计数器
void ConfigureTimeForRunTimeStats(void)
{
    timx_init(TIM2,100-1,72-1);//100倍的系统时钟节拍 1MHZ-一次1us，计数100次-0.1ms 10倍精度 
    FreeRTOSRunTimeTicks = 0;
}
/*定时器2中断服务函数，每次中断对计数值++*/
void TIM2_IRQHandler(void)
{
    if (TIM_GetITStatus(TIM2, TIM_IT_Update) != RESET)
    {
        FreeRTOSRunTimeTicks++;
        TIM_ClearITPendingBit(TIM2, TIM_IT_Update);// 处理TIM2更新中断
    }
}


void timx_init(TIM_TypeDef* TIMx,uint16_t arr, uint16_t psc)
{
	TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;
	TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;
	TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;
	TIM_TimeBaseInitStructure.TIM_Period = arr;//自动重装值
	TIM_TimeBaseInitStructure.TIM_Prescaler = psc;//预分频值
	TIM_TimeBaseInitStructure.TIM_RepetitionCounter = 0;
	TIM_TimeBaseInit(TIM2,&TIM_TimeBaseInitStructure);
    
    TIM_ClearFlag(TIM2,TIM_FLAG_Update);
	TIM_ITConfig(TIM2,TIM_IT_Update,ENABLE);//开启更新中断
    
    
	NVIC_InitTypeDef NVIC_InitStructure;
	NVIC_InitStructure.NVIC_IRQChannel = TIM2_IRQn;
	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;
	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 1;//定时器2抢占优先级
	NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;
	NVIC_Init(&NVIC_InitStructure); //配置中断优先级，因为RreeRtos为组4，无响应优先级
    
    
    TIM_Cmd(TIM2,ENABLE);
}
```













###### 任务使用该函数

![image-20250419185244032](./嵌入式软件学习.assets/image-20250419185244032.png)

```c
/*任务二：实现任务运行时间统计API函数使用*/
void task2(void *pvParameters)
{
    uint8_t key = 0;
    while (1)
    {
        key = key_scan(0);
        if(key == KEY1_PRESS)
        {
            vTaskGetRunTimeStats((char *)task_buffer);
            printf("======================================\r\n");
            printf("任务名        运行计数        使用率 \r\n");
            printf("%s\r\n",task_buffer);
            printf("======================================\r\n");
        }
        vTaskDelay(10);
    }
}
```

![image-20250419191625090](./嵌入式软件学习.assets/image-20250419191625090.png)

task2的真实运行时间 = 62834 * 定时器的周期

### 5.10时间管理

- [FreeRTOS中相对延时和绝对延时的区别-CSDN博客](https://blog.csdn.net/ybhuangfugui/article/details/99826764)
- 例如原来任务需要1ms，相对延时需要 10 + 1 = 11ms；而绝对延时会将任务视为一个整体，保证任务运行时间为10ms,自动调整。
- 绝对延时用于需要以一定频率运行的任务，例如采样、载频等

![image-20250420134251281](./嵌入式软件学习.assets/image-20250420134251281.png)





#### 1.vTaskDelay相对延时函数



- 调用该任务后，进入阻塞态
- 

```c

	void vTaskDelay( const TickType_t xTicksToDelay )
	{
	BaseType_t xAlreadyYielded = pdFALSE;

		if( xTicksToDelay > ( TickType_t ) 0U ) //延时时间是否大于0 
		{
			configASSERT( uxSchedulerSuspended == 0 );
			vTaskSuspendAll(); //挂起任务调度器
			{
				traceTASK_DELAY(); //接口函数未实现

				prvAddCurrentTaskToDelayedList( xTicksToDelay, pdFALSE ); //将任务挂载到阻塞列表
                
			}
			xAlreadyYielded = xTaskResumeAll(); //恢复任务调度器
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}


		if( xAlreadyYielded == pdFALSE )//需要任务调度，进行一次任务切换
		{
			portYIELD_WITHIN_API(); 
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
```



###### 1.1prvAddCurrentTaskToDelayedList将任务挂起到阻塞列表中



- xTickCount 系统滴答定时器每中断一次，计数器+1 。类似于时间戳，不过会溢出。重新从头开始计数。
- xNextTaskUnblockTime，表示下一个最近需要唤醒阻塞任务的时间戳
- 逻辑就是获取任务需要唤醒的系统时间戳，溢出挂载在溢出阻塞列表，未溢出挂载在阻塞列表，升序排列，并更新全局变量---下一个最近需要唤醒任务的事件戳

```c
static void prvAddCurrentTaskToDelayedList( TickType_t xTicksToWait, const BaseType_t xCanBlockIndefinitely )
{
TickType_t xTimeToWake;
const TickType_t xConstTickCount = xTickCount; //记录当前系统时钟

    
	//从就绪列表中移除，返回其在列表（就绪列表）的列表项个数
	if( uxListRemove( &( pxCurrentTCB->xStateListItem ) ) == ( UBaseType_t ) 0 ) 
	{
		portRESET_READY_PRIORITY( pxCurrentTCB->uxPriority, uxTopReadyPriority ); //将其所在列表对应32位标记变量位清零
	}
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}
    
    
    
    

	#if ( INCLUDE_vTaskSuspend == 1 )
	{
        //阻塞时间0xffff ffff ,由于上述延时函数调用时第二个参数为pdFALSE，此语句不执行
		if( ( xTicksToWait == portMAX_DELAY ) && ( xCanBlockIndefinitely != pdFALSE ) )
		{

			vListInsertEnd( &xSuspendedTaskList, &( pxCurrentTCB->xStateListItem ) ); //直接挂载到挂起列表
		}
		else
		{
			xTimeToWake = xConstTickCount + xTicksToWait; //需要唤醒的事件戳 = 当前系统时间 + 需要设定的延时时间
			
            //将延时时间写入列表项值，用于列表升序排列，时间越长的排在越后面，越后解除阻塞
			listSET_LIST_ITEM_VALUE( &( pxCurrentTCB->xStateListItem ), xTimeToWake ); 

			if( xTimeToWake < xConstTickCount ) //需要唤醒的事件戳 < 当前系统时间 --- 计数时间溢出了（最大时间戳32位）
			{
                //挂载在溢出阻塞列表中
				vListInsert( pxOverflowDelayedTaskList, &( pxCurrentTCB->xStateListItem ) );
			}
			else
			{
			
                //升序插入阻塞列表项 ， 根据列表项值-也就是阻塞时间来插入列表项
				vListInsert( pxDelayedTaskList, &( pxCurrentTCB->xStateListItem ) );

				//当前任务阻塞时间戳 <  下一个准备唤醒任务的事件戳
				if( xTimeToWake < xNextTaskUnblockTime )
				{
					xNextTaskUnblockTime = xTimeToWake; //更新下一个需要唤醒任务的事件戳
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
                
                
			}
		}
	}
}
```





###### 1.2-阻塞态解除



- 在系统滴答定时器中断中调用该函数

  ![image-20260121205019302](./嵌入式软件学习.assets/image-20260121205019302.png)

- ![image-20260121210211870](./嵌入式软件学习.assets/image-20260121210211870.png)taskSWITCH_DELAYED_LISTS（)  当计数满时，切换阻塞队列和溢出阻塞队列

```c
BaseType_t xTaskIncrementTick( void )
{
TCB_t * pxTCB;
TickType_t xItemValue;
BaseType_t xSwitchRequired = pdFALSE;



	if( uxSchedulerSuspended == ( UBaseType_t ) pdFALSE ) //任务调度器未挂起
	{
		const TickType_t xConstTickCount = xTickCount + 1; //每次进入滴答定时器中断 ，事件戳++
		xTickCount = xConstTickCount;
        
        

		if( xConstTickCount == ( TickType_t ) 0U ) //如=如果时间戳 =0 ，表示溢出。新一轮计数
		{
          	//交互 溢出阻塞列表和 阻塞列表 ，两个列表交换使用。xNumOfOverflows++溢出次数++
			taskSWITCH_DELAYED_LISTS(); 
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
        
		
        //如果当前时间戳，超过下一个最近需要唤醒任务时间戳，需要唤醒任务。
		if( xConstTickCount >= xNextTaskUnblockTime )
		{
			for( ;; )
			{
				if( listLIST_IS_EMPTY( pxDelayedTaskList ) != pdFALSE ) //如果当前阻塞列表为空
				{
					xNextTaskUnblockTime = portMAX_DELAY;  //更新下一个最近任务唤醒时间为最大
					break; //退出循环
				}
				else
				{
                    
                    //获取阻塞列表的第一个 列表项
					pxTCB = ( TCB_t * ) listGET_OWNER_OF_HEAD_ENTRY( pxDelayedTaskList );
                    
                    //获取任务所在列表项的列表值，也就是阻塞时间戳
					xItemValue = listGET_LIST_ITEM_VALUE( &( pxTCB->xStateListItem ) );

					if( xConstTickCount < xItemValue ) //发生故障，xNextTaskUnblockTime不正确。如果当前系统时间戳 < 最近一次任务需要唤醒时间戳
					{
						xNextTaskUnblockTime = xItemValue; //重新更新xNextTaskUnblockTime
						break;
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}


					( void ) uxListRemove( &( pxTCB->xStateListItem ) ); //所在状态列表项移除，从阻塞列表移除


					if( listLIST_ITEM_CONTAINER( &( pxTCB->xEventListItem ) ) != NULL ) //事件列表项移除
					{
						( void ) uxListRemove( &( pxTCB->xEventListItem ) );
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
                    
                    
					//添加到就绪列表项
					prvAddTaskToReadyList( pxTCB );

	
                    
                    
					#if (  configUSE_PREEMPTION == 1 ) //开启抢占式宏
					{
						//当前恢复的任务优先级 > 当前执行任务的优先级
						if( pxTCB->uxPriority >= pxCurrentTCB->uxPriority )
						{ 
							xSwitchRequired = pdTRUE; //返回后，进行一次任务调度
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					#endif /* configUSE_PREEMPTION */
				}
			}
		}

	}
}
```





### 5.11消息队列

#### 5.11.1介绍



- 消息队列---用于中断与任务、任务与任务之间的数据交互
- 使用全局变量的弊端：数据无保护，不安全，多个任务操作一个变量时数据易受损。
- 如下：任务1进行第二步时，被任务2打断（任务二将a进行++，此时a为1），当切换到任务1时，r0给a赋值，a=1。与预期a=2不符合

![image-20250420155222659](./嵌入式软件学习.assets/image-20250420155222659.png)





- 在读写队列时，都会进入临界区，防止任务和中断导致访问冲突。
- FreeRtos基于队列，实现多种功能。
- 队列类似于环形缓冲区，只不过读写数据操作时加入了临界区操作

![image-20250420160136544](./嵌入式软件学习.assets/image-20250420160136544.png)



- 在创建队列时，需要指定队列长度 与 队列项目大小
- 

![image-20250420160537627](./嵌入式软件学习.assets/image-20250420160537627.png)





- 入队出队方式：队列一般采用FIFO，也可以配置LIFO
- 数据传递：一般采用**值传递**，这样入队后数据本身可以继续被处理。在传递大量数据时，使用**指针传递**，坏处是值会实时变化。
- 任何任务和中断都可以操作
- 当入队或者出队，可以设定阻塞时间。 **非阻塞式等待，挂载到阻塞列表**<img src="./嵌入式软件学习.assets/image-20250420161849950.png" alt="image-20250420161849950" style="zoom:80%;" />
  1. 0-入队如果队列已满，不会等待直接进行下一步运行
  2.  0 - 0xffff ffff 阻塞等待一段时间后，任无法操作队列执行下一步
  3. 0xffff ffff ,死等只到对列可被操作

![image-20250420161350286](./嵌入式软件学习.assets/image-20250420161350286.png)





- 入队满：根据阻塞时间，将任务挂载在**阻塞列表**中。将事件列表项挂载在**等待发送列表**中
- 出队空：根据阻塞时间，将任务挂载在阻塞列表中。将事件列表项挂载在**等待接受列表**中
- 当事件解除后，将任务从阻塞列表中移除

![image-20250420162643424](./嵌入式软件学习.assets/image-20250420162643424.png)





- 当多个任务都在阻塞等待操作对列时，需要将那个任务优先获取队列呢？
  1. **优先级高的先解除阻塞，获取队列操作权**
  2. **同优先级下，那个任务阻塞时间最长**，最先获取队列操作权

![image-20250420163404299](./嵌入式软件学习.assets/image-20250420163404299.png)







![image-20250420164152919](./嵌入式软件学习.assets/image-20250420164152919.png)



#### 5.11.2队列结构体与操作API



##### 1.队列结构体

<img src="./嵌入式软件学习.assets/image-20250421211942282.png" alt="image-20250421211942282" style="zoom:50%;" />队列分为两个部分，结构体用于标识存储区，存储区实际储存数据区域

<img src="./嵌入式软件学习.assets/image-20250421212842696.png" alt="image-20250421212842696" style="zoom:50%;" />区别这两个元素，一个为列表最后一个元素，一个为最后的地址<img src="./嵌入式软件学习.assets/image-20250421212922406.png" alt="image-20250421212922406" style="zoom:50%;" />



- 两个头尾指针，限制地址长度
- 两个头尾指针 + 一个非空闲数量，用于循环队列
- 队列长度 + 队列项大小 ，队列属性
- 两个读写锁

![image-20250420164852265](./嵌入式软件学习.assets/image-20250420164852265.png)



```c
typedef struct QueueDefinition
{

    int8_t * pcHead					/* 存储区域的起始地址      存储区起始地址*/
    int8_t * pcWriteTo;        				/* 指向下一个写入的位置     初始化时指向起始地址 */
    
    //联合体，队列可以同时用于 队列传输信息、互斥信号量。
    union
    {
        	QueuePointers_t     xQueue;   //队列
		   SemaphoreData_t  xSemaphore;  //互斥信号量
    } u ;
    
    
    List_t xTasksWaitingToSend; 			/* 等待发送列表  阻塞态时挂载在该链表*/
    List_t xTasksWaitingToReceive;			/* 等待接收列表  阻塞态时挂载在该链表*/
    
    volatile UBaseType_t uxMessagesWaiting; 		/* 非空闲队列项目的数量   列表项中有数据的个数*/
    UBaseType_t uxLength；						/* 队列长度   列表项的个数 */
    UBaseType_t uxItemSize;                 		/* 队列项目的大小     列表项的大小*/
    
    
    volatile int8_t cRxLock; 				/* 读取上锁计数器 */
    volatile int8_t cTxLock；				/* 写入上锁计数器 */


} xQUEUE;


//队列联合体内部定义
typedef struct QueuePointers
{
     int8_t * pcTail; 			/* 存储区的结束地址         指向队列项最后的地址*/
     int8_t * pcReadFrom;		/* 读取地址，初始化指向最后一个读取队列的地址     与上面元素有区别，指向最后一个元素，与上述地址差一个列表项大小*/
} QueuePointers_t;

//互斥信号量联合体内部定义
typedef struct SemaphoreData
{
    TaskHandle_t xMutexHolder;		/* 互斥信号量持有者 */
    UBaseType_t uxRecursiveCallCount;	/* 递归互斥信号量的获取计数器 */
} SemaphoreData_t;


```













##### 2.队列相关API函数



###### 2.1初始化队列

- 动态和静态创建队列之间的区别：动态 ----队列所需的内存空间由 FreeRTOS 从 FreeRTOS 管理的堆中分配，而静态创建需要用户自行分配内存。
- 一般使用动态

![image-20250421213140290](./嵌入式软件学习.assets/image-20250421213140290.png)





![image-20250421213656318](./嵌入式软件学习.assets/image-20250421213656318.png)

<img src="./嵌入式软件学习.assets/image-20250420164852265.png" alt="image-20250420164852265" style="zoom: 80%;" />

```c
//实际调用 xQueueGenericCreate 函数  参数化1-队列长度  参数2-队列项大小 ；创建成功-返回句柄 ， 创建失败返回NULL
#define xQueueCreate( uxQueueLength, uxItemSize ) \
xQueueGenericCreate( ( uxQueueLength ), ( uxItemSize ), ( queueQUEUE_TYPE_BASE ) )


//参数3 - 由于FreeRtos使用队列实现多种结构，该变量设定该队列实现功能

#define queueQUEUE_TYPE_BASE                  			( ( uint8_t ) 0U )	/* 队列 */
#define queueQUEUE_TYPE_SET                  			( ( uint8_t ) 0U )	/* 队列集 */
#define queueQUEUE_TYPE_MUTEX                 			( ( uint8_t ) 1U )	/* 互斥信号量 */
#define queueQUEUE_TYPE_COUNTING_SEMAPHORE    	( ( uint8_t ) 2U )	/* 计数型信号量 */
#define queueQUEUE_TYPE_BINARY_SEMAPHORE     	( ( uint8_t ) 3U )	/* 二值信号量 */
#define queueQUEUE_TYPE_RECURSIVE_MUTEX       		( ( uint8_t ) 4U )	/* 递归互斥信号量 


```





![image-20250422193541526](./嵌入式软件学习.assets/image-20250422193541526.png)





```c
	
//队列创建函数
QueueHandle_t xQueueGenericCreate( const UBaseType_t uxQueueLength, const UBaseType_t uxItemSize, const uint8_t 	ucQueueType )
	{
	Queue_t *pxNewQueue;
	size_t xQueueSizeInBytes;
	uint8_t *pucQueueStorage;

		configASSERT( uxQueueLength > ( UBaseType_t ) 0 );

		if( uxItemSize == ( UBaseType_t ) 0 )
		{
			xQueueSizeInBytes = ( size_t ) 0; //队列长度为零 --- 使用信号量
		}
		else
		{
            
			//计算队列存储区的总内存 ： 队列长度 * 队列项大小
			xQueueSizeInBytes = ( size_t ) ( uxQueueLength * uxItemSize ); 
		}
        
        
		//申请内存大小 ： 队列大小 + 队列结构体大小
		pxNewQueue = ( Queue_t * ) pvPortMalloc( sizeof( Queue_t ) + xQueueSizeInBytes );

		if( pxNewQueue != NULL )
		{ //队列空间申请成功
		
            // 计算队列存储区的首地址 ： 队列首地址 + 队列队列结构体大小
			pucQueueStorage = ( ( uint8_t * ) pxNewQueue ) + sizeof( Queue_t );

			//队列初始化函数 ：  队列长度 、 队列项大小 、 队列项存储区首地址、队列使用方式 、队列首地址
			prvInitialiseNewQueue( uxQueueLength, uxItemSize, pucQueueStorage, ucQueueType, pxNewQueue );
		}

		return pxNewQueue; //返回队列首地址 --- 外部句柄指向这个地址后操作队列
	}



//初始化队列，参数：队列长度 、 队列项大小 、 队列项存储区首地址、队列使用方式 、队列首地址
static void prvInitialiseNewQueue( const UBaseType_t uxQueueLength, const UBaseType_t uxItemSize, uint8_t *pucQueueStorage, const uint8_t ucQueueType, Queue_t *pxNewQueue )
{

	( void ) ucQueueType;

	if( uxItemSize == ( UBaseType_t ) 0 ) //当队列项大小为0时，不使用队列存储 ---- 用于信号量使用
	{
		pxNewQueue->pcHead = ( int8_t * ) pxNewQueue; // 信号量： pcHead指向自身
	}
	else
	{
		pxNewQueue->pcHead = ( int8_t * ) pucQueueStorage; //用作队列 --- 指向队列存储区的首地址
	}
    
    
	pxNewQueue->uxLength = uxQueueLength; //队列长度
	pxNewQueue->uxItemSize = uxItemSize; //队列项大小
    
    
    //复位队列 ： 第一个参数---所需复位的队列  、 第二个参数---是否为新队列（TURE-新  FLAS-老队列）
	( void ) xQueueGenericReset( pxNewQueue, pdTRUE ); 

 

}



//复位队列
BaseType_t xQueueGenericReset( QueueHandle_t xQueue, BaseType_t xNewQueue )
{
	Queue_t * const pxQueue = ( Queue_t * ) xQueue;



	taskENTER_CRITICAL(); //关闭中断
    
	{
        //队列存储区尾部 = 队列存储区首地址 + 队列长度 * 队列项大小
		pxQueue->pcTail = pxQueue->pcHead + ( pxQueue->uxLength * pxQueue->uxItemSize );
        
        //最后一个队列项地址 与上述尾部差一个队列项大小
        pxQueue->u.pcReadFrom = pxQueue->pcHead + ( ( pxQueue->uxLength - ( UBaseType_t ) 1U ) * pxQueue->uxItemSize );
        
        //当前数据非空队列项
		pxQueue->uxMessagesWaiting = ( UBaseType_t ) 0U;
        
        //下一个写入队列项 ，初始化指向第一个队列项
		pxQueue->pcWriteTo = pxQueue->pcHead;
	
        //解锁标记位 都解锁
		pxQueue->cRxLock = queueUNLOCKED;
		pxQueue->cTxLock = queueUNLOCKED;

        
		if( xNewQueue == pdFALSE ) //老队列复位
		{

			if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToSend ) ) == pdFALSE ) //等待写入列表非空
			{
                //将等待写入列表清空
				if( xTaskRemoveFromEventList( &( pxQueue->xTasksWaitingToSend ) ) != pdFALSE ) 
				{
					queueYIELD_IF_USING_PREEMPTION(); 
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
        
        
		else //新列表
		{
			
            
            //初始化创建等待写入 、等待读取列表
			vListInitialise( &( pxQueue->xTasksWaitingToSend ) );
			vListInitialise( &( pxQueue->xTasksWaitingToReceive ) );
		}
	}
    
	taskEXIT_CRITICAL(); //开启中断


	return pdPASS;
}

```





###### 2.2队列写入





- xQueueSend --- **尾部写入**
- xQueueSendToBack --- 与上述尾部插入一致

- xQueueSendToFront --- **头部写入**
- xQueueOverwrite --- **覆写**（只能用于**长度为1**的队列，对之前内容进行**覆盖写入**）



![image-20250421214458043](./嵌入式软件学习.assets/image-20250421214458043.png)





**事件列表项与阻塞态**

- **事件列表项和阻塞态**：当阻塞时，将任务根据阻塞时间挂载到阻塞列表中，时间最大时直接挂载到挂起列表中。并将其因为某个事件，将其事件列表项挂载到其等待的事件列表下
- 当事件未满足，在滴答定时器中断中，根据阻塞时间将阻塞任务唤醒
- 当阻塞时间没达到，但是事件满足，也可以唤醒任务。





- 其调用的函数都是 **xQueueGenericSend**，只是传递的参数不一样
- **第3个参数为阻塞时间，只有覆写无需阻塞，覆写时直接对队列进行写入无需阻塞。**
- **第4个参数为写入方式，用于区分操作方式** ，**覆写操作**需要**队列长度为1**时才能进行<img src="./嵌入式软件学习.assets/image-20250421215711133.png" alt="image-20250421215711133" style="zoom:50%;" />

![image-20250421214816455](./嵌入式软件学习.assets/image-20250421214816455.png)

![image-20250421215912599](./嵌入式软件学习.assets/image-20250421215912599.png)

- 队列未满，按照写入位置写入，当等待接受列表非空时，将等待阻塞列表任务释放出来
- 队列已满

```c
BaseType_t xQueueGenericSend( QueueHandle_t xQueue, const void * const pvItemToQueue, TickType_t xTicksToWait, const BaseType_t xCopyPosition )
{
BaseType_t xEntryTimeSet = pdFALSE, xYieldRequired;
TimeOut_t xTimeOut;
Queue_t * const pxQueue = ( Queue_t * ) xQueue;

	//断言 ： 写的队列长度必须为1
	configASSERT( !( ( pvItemToQueue == NULL ) && ( pxQueue->uxItemSize != ( UBaseType_t ) 0U ) ) );

	//循环阻塞 
    //位列未满，
	for( ;; ) 
	{
		taskENTER_CRITICAL(); //进入临界区，关闭中断
        
		{
			
            // 1.有数据的队列项个数 < 队列长度 -- 表示队列有空闲位置     2.为覆写操作（不管是否空闲直接写）
			if( ( pxQueue->uxMessagesWaiting < pxQueue->uxLength ) || ( xCopyPosition == queueOVERWRITE ) )
			{
				
                //拷贝数据操作：将数据，根据写入的位置，写入指定队列
				xYieldRequired = prvCopyDataToQueue( pxQueue, pvItemToQueue, xCopyPosition );

				#if ( configUSE_QUEUE_SETS == 1 ) //使用队列集
				{
					//... 队列集实现方式-后面在解析
				}
				#else //使用队列
				{
					
                    //等待接受列表非空 --- 有任务在等待列表项
					if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToReceive ) ) == pdFALSE )
					{
                        
                        //将等待接受事件列表项的，第一个元素移除，挂载到继就绪列表项中，当恢复任务优先级比当前任务优先级高，需要触发任务调度
						if( xTaskRemoveFromEventList( &( pxQueue->xTasksWaitingToReceive ) ) != pdFALSE )
						{
							queueYIELD_IF_USING_PREEMPTION(); //触发一次任务调度
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					else if( xYieldRequired != pdFALSE ) 
					{

						queueYIELD_IF_USING_PREEMPTION();
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
				#endif /* configUSE_QUEUE_SETS */

				taskEXIT_CRITICAL(); //退出临界区
				return pdPASS; //返回写入成功
			}
            
            
            
            //队列已满，需要阻塞任务
			else 
			{
                
                //如果阻塞时间为0 ，退出返回队列满错误 ,不阻塞
				if( xTicksToWait == ( TickType_t ) 0 )
				{

					taskEXIT_CRITICAL(); //退出临界区
                    
					return errQUEUE_FULL; //返回队列满错误
				}
                
                //
				else if( xEntryTimeSet == pdFALSE ) //阻塞时间非0，函数开头 xYieldRequired = pdFALSE这里必满足
				{
					vTaskSetTimeOutState( &xTimeOut ); //记录当前时间时间
/*                    
void vTaskSetTimeOutState( TimeOut_t * const pxTimeOut )
{
	pxTimeOut->xOverflowCount = xNumOfOverflows; //当定时器中断xTickCount溢出为0时，xNumOfOverflows++为溢出计数
	pxTimeOut->xTimeOnEntering = xTickCount; //系统滴答定时器，xTickCount每次自增1,系统时间计数值
}*/
                    
					xEntryTimeSet = pdTRUE; //当前时间记录完成
				}
				else
				{
					
					mtCOVERAGE_TEST_MARKER();
				}
			}
		}
        
		taskEXIT_CRITICAL(); //退出临界区


        
        

		vTaskSuspendAll(); //挂起任务调度器
		prvLockQueue( pxQueue ); //锁定队列

//这里防止上述运行一段程序需要一段时间，一些短时间阻塞的时间已经完成，可以直接释放掉，并更新一下阻塞时间更准确（此时已经运行一段时间，阻塞时间减去过去的事件，再挂入阻塞列表更准确）
        
        //传入上述记录时间，与阻塞时间。返回pdFALSE - 证明阻塞时间还没到 ，返回pdTURE - 证明阻塞时间已到
		if( xTaskCheckForTimeOut( &xTimeOut, &xTicksToWait ) == pdFALSE )  //阻塞时间未到
		{
			if( prvIsQueueFull( pxQueue ) != pdFALSE ) //队列满
			{
				//将任务：事件-挂载在等待发送时间列表    状态-阻塞态
				vTaskPlaceOnEventList( &( pxQueue->xTasksWaitingToSend ), xTicksToWait );


				prvUnlockQueue( pxQueue ); //解锁队列

				if( xTaskResumeAll() == pdFALSE ) //阻塞该任务判断是否需要启动任务调度
				{
					portYIELD_WITHIN_API();
				}
			}
			else //队列未满
			{

				prvUnlockQueue( pxQueue );
				( void ) xTaskResumeAll();
			}
		}
		else //阻塞时间已到
		{
			prvUnlockQueue( pxQueue ); //解锁队列
			( void ) xTaskResumeAll(); //恢复任务调度器

			traceQUEUE_SEND_FAILED( pxQueue );
			return errQUEUE_FULL; //返回队列已满
		}
	}
}
```



**将队列项，按照写入位置写入队列中**

```c

//尾部写入，写在pxQueue->pcWriteTo指向地址
//头部与覆写写入，写在pxQueue->u.pcReadFrom指向地址 
//两个写入指针，都有循环，超过存储区便捷返回到初始位置

static BaseType_t prvCopyDataToQueue( Queue_t * const pxQueue, const void *pvItemToQueue, const BaseType_t xPosition )
{
BaseType_t xReturn = pdFALSE;
UBaseType_t uxMessagesWaiting;

	

	uxMessagesWaiting = pxQueue->uxMessagesWaiting; //获取非空闲队列项个数

	if( pxQueue->uxItemSize == ( UBaseType_t ) 0 ) //队列项大小为0 --- 信号量
	{
		#if ( configUSE_MUTEXES == 1 )
		{
			if( pxQueue->uxQueueType == queueQUEUE_IS_MUTEX )
			{
				/* The mutex is no longer being held. */
				xReturn = xTaskPriorityDisinherit( ( void * ) pxQueue->pxMutexHolder );
				pxQueue->pxMutexHolder = NULL;
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		#endif /* configUSE_MUTEXES */
	}
    
    
    //写入位置 --- 尾部插入
	else if( xPosition == queueSEND_TO_BACK )
	{
        //C库函数连续地址拷贝函数：将pvItemToQueue待写入数据，拷贝到下一个写入的队列项中 pxQueue->pcWriteTo ，大小为uxItemSize（一个队列项大小）
		( void ) memcpy( ( void * ) pxQueue->pcWriteTo, pvItemToQueue, ( size_t ) pxQueue->uxItemSize ); 
        
        
		pxQueue->pcWriteTo += pxQueue->uxItemSize; //下一个写入地址，自增一个队列项大小。
        
        //如果下一个写入地址，大于尾部地址，回到队列存储区头部地址。
		if( pxQueue->pcWriteTo >= pxQueue->pcTail ) 
		{
			pxQueue->pcWriteTo = pxQueue->pcHead;
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
    
    
    
    //头部写入 、 覆写
	else
	{
        //将待写入数据 复制到 尾部队列项指针
		( void ) memcpy( ( void * ) pxQueue->u.pcReadFrom, pvItemToQueue, ( size_t ) pxQueue->uxItemSize ); 
        
		pxQueue->u.pcReadFrom -= pxQueue->uxItemSize; //自减一个队列项大小
        
		if( pxQueue->u.pcReadFrom < pxQueue->pcHead )  //如果读取指针地址自减后，小于头部地址，返回尾部地址再减一个队列项大小，实现循环
		{
			pxQueue->u.pcReadFrom = ( pxQueue->pcTail - pxQueue->uxItemSize );
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}

        //如果为覆写操作
		if( xPosition == queueOVERWRITE )
		{
			if( uxMessagesWaiting > ( UBaseType_t ) 0 ) //覆写操作，如果当前有非空闲队列项，进行--，因为后面有对非空闲队列项进行++操作，不--会溢出
			{
			
				--uxMessagesWaiting;
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
    
    
    

	pxQueue->uxMessagesWaiting = uxMessagesWaiting + 1; //非空闲队列项++

	return xReturn;
}
```



**移除事件列表项**

- 将等待接受时间列表 中 的第一个列表项从列表中移除
- 调度器未挂起 ： 将状态列表项从当前列表（阻塞列表）下移除
- 调度器挂起 ： 将任务事件列表项 加载到 待就绪列表项-事件 中。 此时任务还是阻塞状态，需要调度器恢复才能恢复到就绪列表中  

```c
BaseType_t xTaskRemoveFromEventList( const List_t * const pxEventList )
{
TCB_t *pxUnblockedTCB;
BaseType_t xReturn;

    //获取等待接受事件列表 中 第一个列表项的任务
	pxUnblockedTCB = ( TCB_t * ) listGET_OWNER_OF_HEAD_ENTRY( pxEventList ); 
	configASSERT( pxUnblockedTCB );
    
    
	( void ) uxListRemove( &( pxUnblockedTCB->xEventListItem ) ); //将任务的事件列表，从 等待接受事件列表 中移除
    
    
	//调度器未挂起 ： 将状态列表项从当前列表（阻塞列表）下移除
	if( uxSchedulerSuspended == ( UBaseType_t ) pdFALSE )
	{
		( void ) uxListRemove( &( pxUnblockedTCB->xStateListItem ) ); //阻塞列表移除
		prvAddTaskToReadyList( pxUnblockedTCB );	//将列表项添加到就绪列表
	}
    
    //调度器挂起 ： 将任务事件列表项 加载到 待就绪列表项-事件 中。 此时任务还是阻塞状态，需要调度器恢复才能恢复到就绪列表中  
	else
	{
		vListInsertEnd( &( xPendingReadyList ), &( pxUnblockedTCB->xEventListItem ) );
	}

    //上述操作，操作了就绪列表。 如果此时恢复的任务优先级  >  当前运行的任务  返回pdTRUE表示需要进行一次任务调度
	if( pxUnblockedTCB->uxPriority > pxCurrentTCB->uxPriority )
	{

		xReturn = pdTRUE;

		xYieldPending = pdTRUE;
	}
	else
	{
		xReturn = pdFALSE;
	}

	#if( configUSE_TICKLESS_IDLE != 0 )
	{

		prvResetNextTaskUnblockTime();
	}
	#endif

	return xReturn;
}
```





- 返回pdTRUE,表示发生超时
- 返回pdFALSE,表示不会发生超时
- ![image-20250428191457740](./嵌入式软件学习.assets/image-20250428191457740.png)表示计数时间已超过0XFFFF FFFF（最大记时时间)
- 

```c
//pxTimeOut - 当前系统时间      pxTicksToWait - 超时时间
BaseType_t xTaskCheckForTimeOut( TimeOut_t * const pxTimeOut, TickType_t * const pxTicksToWait )
{
BaseType_t xReturn;


	taskENTER_CRITICAL(); //进入临界区
	{

		const TickType_t xConstTickCount = xTickCount; //记录当前系统节拍


		#if ( INCLUDE_vTaskSuspend == 1 )
			if( *pxTicksToWait == portMAX_DELAY ) //如果等待时间为最大 0xffff ffff
			{
				xReturn = pdFALSE; //还有时间不会发生超时
			}
			else
		#endif
                
        
        //溢出计数不等于上次溢出计数，且本次计数时间 > 上次记录时间 。---表示时间已过去超过0xffff ffff 
		if( ( xNumOfOverflows != pxTimeOut->xOverflowCount ) && ( xConstTickCount >= pxTimeOut->xTimeOnEntering )) 
		{
			xReturn = pdTRUE; //超时，记时比最大设置时间还长，已超时
		}
        
        //xConstTickCount - pxTimeOut->xTimeOnEntering  当前系统计时 - 上次记录时间  = 从上次记录到运行程序所用时间
        // 从上次记录到运行程序所用时间 < 阻塞时间，表示还在阻塞，更新阻塞时间。
		else if( ( ( TickType_t ) ( xConstTickCount - pxTimeOut->xTimeOnEntering ) ) < *pxTicksToWait ) 
		{
			*pxTicksToWait -= ( xConstTickCount - pxTimeOut->xTimeOnEntering ); //更新阻塞时间 ，减去运行时间
			vTaskSetTimeOutState( pxTimeOut ); //记录本次时间
			xReturn = pdFALSE; //还有时间不会发生超时
		}
		else
		{
			xReturn = pdTRUE; //返回已超时，从上次记录到运行程序所用时间 > 所剩阻塞时间
		}
	}
	taskEXIT_CRITICAL(); //退出临界区

	return xReturn;
}
```







###### 2.3队列读取



- 队列读取   ： 一个是会删除所读头部取队列项，一个不删除

![image-20250421220051364](./嵌入式软件学习.assets/image-20250421220051364.png)

![image-20250421220435244](./嵌入式软件学习.assets/image-20250421220435244.png)

![image-20250421220521334](./嵌入式软件学习.assets/image-20250421220521334.png)







```c
//重定义队列读取函数

//最后一个参数 pdFALSE-表示读取后删除 ，pdTRUE- 表示读取后不删除
#define xQueueReceive( xQueue, pvBuffer, xTicksToWait )  \
xQueueGenericReceive( ( xQueue ), ( pvBuffer ), ( xTicksToWait ), pdFALSE )

#define xQueuePeek( xQueue, pvBuffer, xTicksToWait ) xQueueGenericReceive( ( xQueue ), ( pvBuffer ), ( xTicksToWait ), pdTRUE )


BaseType_t xQueueGenericReceive( QueueHandle_t xQueue, void * const pvBuffer, TickType_t xTicksToWait, const BaseType_t xJustPeeking )
{
BaseType_t xEntryTimeSet = pdFALSE;
TimeOut_t xTimeOut;
int8_t *pcOriginalReadPosition;
Queue_t * const pxQueue = ( Queue_t * ) xQueue;




	for( ;; )
	{
		taskENTER_CRITICAL(); //关中断
		{
			const UBaseType_t uxMessagesWaiting = pxQueue->uxMessagesWaiting; //获取非空闲队列项个数

			if( uxMessagesWaiting > ( UBaseType_t ) 0 ) //非空，有数据
			{

				pcOriginalReadPosition = pxQueue->u.pcReadFrom; //获取读取指针

				prvCopyDataFromQueue( pxQueue, pvBuffer ); //从队列中拷贝数据到 pvBuffer中，尾部指针读取，偏移到头部指针开始读取

				if( xJustPeeking == pdFALSE ) //读取队列后删除队列项
				{
					traceQUEUE_RECEIVE( pxQueue );

				
					pxQueue->uxMessagesWaiting = uxMessagesWaiting - 1; // 非空闲队列项--

                  
				//等待写入时间列表非空 --- 表示有任务在等待写入
					if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToSend ) ) == pdFALSE )
					{
                        
                        //与写入逻辑一致，将事件列表第一个元素移除
						if( xTaskRemoveFromEventList( &( pxQueue->xTasksWaitingToSend ) ) != pdFALSE )
						{
							queueYIELD_IF_USING_PREEMPTION(); //任务调度
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
                    
                    
				}
                
                
                
                //读取后，不删除队列项
				else
				{
					traceQUEUE_PEEK( pxQueue );

					/* The data is not being removed, so reset the read
					pointer. */
					pxQueue->u.pcReadFrom = pcOriginalReadPosition;

					/* The data is being left in the queue, so see if there are
					any other tasks waiting for the data. */
					if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToReceive ) ) == pdFALSE )
					{
						if( xTaskRemoveFromEventList( &( pxQueue->xTasksWaitingToReceive ) ) != pdFALSE )
						{
							/* The task waiting has a higher priority than this task. */
							queueYIELD_IF_USING_PREEMPTION();
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
                
                
                
                

				taskEXIT_CRITICAL(); //退出临界区
				return pdPASS;
			}
            
            
            //队列为空，读取任务需要阻塞
			else
			{
				if( xTicksToWait == ( TickType_t ) 0 )   //阻塞时间为0
				{
					taskEXIT_CRITICAL();  //退出临界区
					traceQUEUE_RECEIVE_FAILED( pxQueue );
					return errQUEUE_EMPTY; //返回队列为空
				}
				else if( xEntryTimeSet == pdFALSE ) //这个必满足，时间非零进入这个
				{
					vTaskSetTimeOutState( &xTimeOut ); //更新当前时间
					xEntryTimeSet = pdTRUE;
				}
				else
				{
					/* Entry time was already set. */
					mtCOVERAGE_TEST_MARKER();
				}
			}
		}
		taskEXIT_CRITICAL(); //退出临界区

	

		vTaskSuspendAll(); //关闭任务调度
		prvLockQueue( pxQueue ); //队列上锁
        

	
        
        //更新当前时间
		if( xTaskCheckForTimeOut( &xTimeOut, &xTicksToWait ) == pdFALSE ) //当前阻塞时间还有，需要进入阻塞
		{
			if( prvIsQueueEmpty( pxQueue ) != pdFALSE ) //队列满
			{
				traceBLOCKING_ON_QUEUE_RECEIVE( pxQueue );

                //将任务挂入阻塞态，并将任务事件项挂载到等待接受列表中
				vTaskPlaceOnEventList( &( pxQueue->xTasksWaitingToReceive ), xTicksToWait );
				prvUnlockQueue( pxQueue );
				if( xTaskResumeAll() == pdFALSE ) //挂载后需要任务切换
				{
					portYIELD_WITHIN_API(); //启动任务调度
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			else //队列空
			{
				
				prvUnlockQueue( pxQueue );
				( void ) xTaskResumeAll();
			}
		}
        
        //当前阻塞时间已完成，任务继续运行
		else
		{
			prvUnlockQueue( pxQueue ); //解锁队列
			( void ) xTaskResumeAll(); //恢复调度器

			if( prvIsQueueEmpty( pxQueue ) != pdFALSE )
			{
				traceQUEUE_RECEIVE_FAILED( pxQueue );
				return errQUEUE_EMPTY;
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
        
        
        
        
        
	}
}

```





**获取队列中数据**

<img src="./嵌入式软件学习.assets/image-20250428205933230.png" alt="image-20250428205933230" style="zoom: 80%;" />

- 读取时，将pcReadFrom从指向最后一个队列项，指向队列头部。

```c
static void prvCopyDataFromQueue( Queue_t * const pxQueue, void * const pvBuffer )
{
	if( pxQueue->uxItemSize != ( UBaseType_t ) 0 ) //队列项大小 > 0  ---- 表示为队列，非信号量
	{
		pxQueue->u.pcReadFrom += pxQueue->uxItemSize; //尾部读取指针，向后偏移一个队列项
		if( pxQueue->u.pcReadFrom >= pxQueue->pcTail )  //当到达尾部时，偏移到队列存储区头部
		{
			pxQueue->u.pcReadFrom = pxQueue->pcHead;
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
        
        //从尾部指针获取数据
		( void ) memcpy( ( void * ) pvBuffer, ( void * ) pxQueue->u.pcReadFrom, ( size_t ) pxQueue->uxItemSize ); 
	}
}
```





##### 3.读写实验

- 通过值写入、地址写入 ，值读出、地址读出

![image-20250421221018503](./嵌入式软件学习.assets/image-20250421221018503.png)

```c
//队列创建，创建成功后返回队列句柄
QueueHandle_t key_queue;      // 键值队列句柄
QueueHandle_t big_data_queue; // 大数据队列句柄

key_queue = xQueueCreate(2, sizeof(uint8_t));
big_data_queue = xQueueCreate(1, sizeof(char *));

//写入队列
uint8_t key    = 0; //值传递
xQueueSend(key_queue,  &key , portMAX_DELAY);

char buff[100] = {"鸡你太美，babe，哦哦哦，鸡你实在是太美，babe\r\n"};
char *buf = buff; //指针传递，传递的值是一个地址
xQueueSend(big_data_queue, &buf, portMAX_DELAY);


//读取数据
uint8_t key    = 0;
xQueueReceive(key_queue, &key,portMAX_DELAY);

char *buf;
xQueueReceive(big_data_queue, &buf,portMAX_DELAY);
```







### 5.12 信号量



#### 5.12.1信号量介绍

- 信号量，类似于标记位，用于标定一些状态，发送信号。
- 在下列表述中，信号量代表资源，大于零代表该资源有空闲
- 使用时，信号量-- 。释放时，信号量++ 。 
- 队列相当于是对数据的获取，信号量通过计数标识资源，任务对资源的获取。
- 同步：有些任务需要进行通知才能进行运行

![image-20250428213851461](./嵌入式软件学习.assets/image-20250428213851461.png)



- **信号量用于传递状态，信号量大于0，表示有资源。使用资源--，释放资源++**
- **信号量计数值限制：代表资源数。**   **计数值为1-二值信号量   计数值最大值不是1---计数型信号量**
- 信号量使用 **队列结构体中的 --- 非空闲队列计数** 来标识资源。

![image-20250428214324148](./嵌入式软件学习.assets/image-20250428214324148.png)





- 空间：队列-结构体+存储区 ； 信号量-结构体，无存储区
- 信号量释放无阻塞，当释放++计数值大于最大值返回错误
- 获取信号量时，无信号量可阻塞任务。

![image-20250428214618562](./嵌入式软件学习.assets/image-20250428214618562.png)





#### 5.12.2二值信号量



- 二值信号量本质：长度为1的队列
- 用于互斥访问、任务同步。
- 与互斥信号量类似，但是二值信号量可能导致优先级翻转问题，适用于任务同步
- 在中断或者任务中，可以释放或获取信号量（GIVE/TACK）

![image-20250429192044916](./嵌入式软件学习.assets/image-20250429192044916.png)



##### 1.相关API

- 创建、释放、获取
- **二值信号量，在创建时，信号量为0。 需要在初始化时释放一次。**

![image-20250429193055623](./嵌入式软件学习.assets/image-20250429193055623.png)



###### 1.1创建二值信号量



![image-20250429194543931](./嵌入式软件学习.assets/image-20250429194543931.png)

```c
//创建二值信号量，本质是调用创建队列函数。  队列长度为1，队列项大小为0 
#define xSemaphoreCreateBinary()   \     
xQueueGenericCreate( ( UBaseType_t ) 1, semSEMAPHORE_QUEUE_ITEM_LENGTH, queueQUEUE_TYPE_BINARY_SEMAPHORE )
    
//参数1 - 队列长度为1
    
//参数2 - 队列大小为0 
    
//参数3 - 由于FreeRtos使用队列实现多种结构，该变量设定该队列实现功能，这里使用为二值信号量

#define queueQUEUE_TYPE_BASE                  			( ( uint8_t ) 0U )	/* 队列 */
#define queueQUEUE_TYPE_SET                  			( ( uint8_t ) 0U )	/* 队列集 */
#define queueQUEUE_TYPE_MUTEX                 			( ( uint8_t ) 1U )	/* 互斥信号量 */
#define queueQUEUE_TYPE_COUNTING_SEMAPHORE    	( ( uint8_t ) 2U )	/* 计数型信号量 */
#define queueQUEUE_TYPE_BINARY_SEMAPHORE     	( ( uint8_t ) 3U )	/* 二值信号量 */
#define queueQUEUE_TYPE_RECURSIVE_MUTEX       		( ( uint8_t ) 4U )	/* 递归互斥信号量 



```





###### 1.2释放二值信号量



- 释放，利用队列的操作，对非空闲队列项计数++。 **信号量使用非空闲队列计数来标识** ，无阻塞时间。

- 成功释放：将非空闲队列计数++ ，并将等待资源的任务唤醒

- 失败释放：当释放时，发现信号量满，返回错误。

  

![image-20250429194918527](./嵌入式软件学习.assets/image-20250429194918527.png)

![image-20250429195646633](./嵌入式软件学习.assets/image-20250429195646633.png)

```c
//信号量释放函数，参数为所需释放的信号量句柄，底层调用队列的写入参数。
#define xSemaphoreGive( xSemaphore )		 \
xQueueGenericSend( ( QueueHandle_t ) ( xSemaphore ), NULL, semGIVE_BLOCK_TIME, queueSEND_TO_BACK )
//参数1 - xSemaphore所需操作的队列
//参数2 - 写入的数据，信号量无
//参数3 -阻塞时间：这里semGIVE_BLOCK_TIME为0，释放无需阻塞，
//参数4 - 写入方式尾部写入 queueSEND_TO_BACK

```



```c

BaseType_t xQueueGenericSend( QueueHandle_t xQueue, const void * const pvItemToQueue, TickType_t xTicksToWait, const BaseType_t xCopyPosition )
{
BaseType_t xEntryTimeSet = pdFALSE, xYieldRequired;
TimeOut_t xTimeOut;
Queue_t * const pxQueue = ( Queue_t * ) xQueue;


	for( ;; )
	{
		taskENTER_CRITICAL();
		{
		
            // 非空闲队列项计数（0） < 队列长度 （1）
			if( ( pxQueue->uxMessagesWaiting < pxQueue->uxLength ) || ( xCopyPosition == queueOVERWRITE ) )
			{
				//队列为复制数据 ，二值信号量为 非空闲队列计数++
				xYieldRequired = prvCopyDataToQueue( pxQueue, pvItemToQueue, xCopyPosition );
/*
static BaseType_t prvCopyDataToQueue( Queue_t * const pxQueue, const void *pvItemToQueue, const BaseType_t xPosition )
{

	uxMessagesWaiting = pxQueue->uxMessagesWaiting; 获取当前信号量计数 为0

	if( pxQueue->uxItemSize == ( UBaseType_t ) 0 ) 信号量
	{
		#if ( configUSE_MUTEXES == 1 ) 使用互斥信号量
		{
			。。。。。。
		}
		#endif 
	}
		。。。。。
	pxQueue->uxMessagesWaiting = uxMessagesWaiting + 1; 当前信号量 +1

	return xReturn;
}


*/

				
					//如果有等待接受列表，唤醒阻塞任务
					if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToReceive ) ) == pdFALSE )
					{
						if( xTaskRemoveFromEventList( &( pxQueue->xTasksWaitingToReceive ) ) != pdFALSE )
						{
				
							queueYIELD_IF_USING_PREEMPTION();
						}
						else
						{
							mtCOVERAGE_TEST_MARKER();
						}
					}
					else if( xYieldRequired != pdFALSE )
					{
	
						queueYIELD_IF_USING_PREEMPTION();
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				
					
				


				taskEXIT_CRITICAL();
				return pdPASS;
			}
            
            // 非空闲队列项计数（1）  > = 队列长度 （1）
			else
			{
				if( xTicksToWait == ( TickType_t ) 0 ) //信号量
				{
	
					taskEXIT_CRITICAL();

				
					traceQUEUE_SEND_FAILED( pxQueue );
					return errQUEUE_FULL; //返回错误
				}
                。。。。。。
			}
		}
		taskEXIT_CRITICAL();
```



###### 1.3获取二值信号量



- 获取信号量，可以设置阻塞时间。    *0不阻塞 ；1-0xffff ffff 阻塞一段时间后，返回超时 ； 0xffff ffff - 挂起*

![image-20250429200125109](./嵌入式软件学习.assets/image-20250429200125109.png)

```c
//信号量获取函数，参数为所需释放的信号量句柄、阻塞时间，底层调用队列的接受参数。
#define xSemaphoreTake( xSemaphore, xBlockTime )	 \
xQueueGenericReceive( ( QueueHandle_t ) ( xSemaphore ), NULL, ( xBlockTime ), pdFALSE )
//参数1 - xSemaphore所需操作的队列
//参数2 - 获取的数据，信号量无
//参数3 -阻塞时间
//参数4 - 读取时，是否删除所读取的数据，这里不删除

```

```c
BaseType_t xQueueGenericReceive( QueueHandle_t xQueue, void * const pvBuffer, TickType_t xTicksToWait, const BaseType_t xJustPeeking )
{


	for( ;; )
	{
		taskENTER_CRITICAL();
		{
			const UBaseType_t uxMessagesWaiting = pxQueue->uxMessagesWaiting; //获取信号量 ，非空闲队列项计数


			if( uxMessagesWaiting > ( UBaseType_t ) 0 ) //信号量非空，
			{

				pcOriginalReadPosition = pxQueue->u.pcReadFrom;

				prvCopyDataFromQueue( pxQueue, pvBuffer ); //信号量不获取数据

				if( xJustPeeking == pdFALSE )
				{

					pxQueue->uxMessagesWaiting = uxMessagesWaiting - 1; //信号量 - 1


                    	//释放不会阻塞
					if( listLIST_IS_EMPTY( &( pxQueue->xTasksWaitingToSend ) ) == pdFALSE )
					{
						。。。。。
					}
                    else
                    { 。。。}
	

				taskEXIT_CRITICAL();
				return pdPASS; //返回获取成功
			}
                
                
             //信号量为空，根据阻塞时间，加入到等待接受列表，并将任务挂载到阻塞态。基本步骤和队列是一致的。   
			else
			{
				if( xTicksToWait == ( TickType_t ) 0 )
				{
			
					taskEXIT_CRITICAL();
					traceQUEUE_RECEIVE_FAILED( pxQueue );
					return errQUEUE_EMPTY;
				}
				else if( xEntryTimeSet == pdFALSE )
				{

					vTaskSetTimeOutState( &xTimeOut );
					xEntryTimeSet = pdTRUE;
				}
				else
				{
					
					mtCOVERAGE_TEST_MARKER();
				}
			}
		}
		taskEXIT_CRITICAL();



		vTaskSuspendAll();
		prvLockQueue( pxQueue );

	
		if( xTaskCheckForTimeOut( &xTimeOut, &xTicksToWait ) == pdFALSE )
		{
			if( prvIsQueueEmpty( pxQueue ) != pdFALSE )
			{
				traceBLOCKING_ON_QUEUE_RECEIVE( pxQueue );

				#if ( configUSE_MUTEXES == 1 )
				{
					if( pxQueue->uxQueueType == queueQUEUE_IS_MUTEX )
					{
						taskENTER_CRITICAL();
						{
							vTaskPriorityInherit( ( void * ) pxQueue->pxMutexHolder );
						}
						taskEXIT_CRITICAL();
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
				#endif

				vTaskPlaceOnEventList( &( pxQueue->xTasksWaitingToReceive ), xTicksToWait );
				prvUnlockQueue( pxQueue );
				if( xTaskResumeAll() == pdFALSE )
				{
					portYIELD_WITHIN_API();
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			else
			{
		
				prvUnlockQueue( pxQueue );
				( void ) xTaskResumeAll();
			}
		}
		else
		{
			prvUnlockQueue( pxQueue );
			( void ) xTaskResumeAll();

			if( prvIsQueueEmpty( pxQueue ) != pdFALSE )
			{
				traceQUEUE_RECEIVE_FAILED( pxQueue );
				return errQUEUE_EMPTY;
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
	}
}
```





##### 2.实验

![image-20250429200716608](./嵌入式软件学习.assets/image-20250429200716608.png)

```c
void task1(void *pvParameters)
{
    uint8_t key = 0;
    BaseType_t err;
    while (1)
    {
        key = key_scan(0);
        if (key == KEY1_PRESS)
        {
            if (semaphore_handle != NULL)
            {
                err = xSemaphoreGive(semaphore_handle); // 释放二值信号量
                if (err == pdPASS)
                {
                    printf("信号量释放成功 \r\n");
                }
                else
                    printf("信号量释放失败 \r\n");
            }
        }
        vTaskDelay(10);
    }
}
/*任务二：获取二值信号量*/
void task2(void *pvParameters)
{
    BaseType_t err;
    while (1)
    {
        err = xSemaphoreTake(semaphore_handle, portMAX_DELAY); // 获取信号量(死等),获取到信号量前将一直阻塞
        if(err == pdTRUE)
        {
            printf("获取信号量成功 \r\n"); 
        }else printf("已超时 \r\n"); 
        
    }
}
```





#### 5.12.3计数型信号量



##### 1.介绍计数型信号量

*创建时需要确定：**最大计数值   与  初始计数值***

- 计数型信号量，可以容纳**多个资源**。相当于**队列长度大于1**的队列。     常用于：**事件计数 + 资源计数**
- 事件计数：例如**生产者与消费者**，当某个模块产生数据，信号量+1；其他函数使用该数据，信号量-1 。**初始计数值为0**
- 资源管理：例如停车位管理，释放+1，获取-1。 **创建时初始计数值应为最大**



![image-20250429210559243](./嵌入式软件学习.assets/image-20250429210559243.png)



##### 2.常用API

- 创建函数不一样（需要对非空闲队列项计数值---也就是**信号量进行初始化**），释放和获取与二值信号量一致
- 获取信号量计数值

![image-20250429211814941](./嵌入式软件学习.assets/image-20250429211814941.png)





###### 2.1创建计数型信号量

![image-20250429212434569](./嵌入式软件学习.assets/image-20250429212434569.png)

```c
#define xSemaphoreCreateCounting( uxMaxCount, uxInitialCount )    \
xQueueCreateCountingSemaphore( ( uxMaxCount ), ( uxInitialCount ) )

//参数1 - 最大信号量计数值
//参数2 - 初始化信号量计数值 ，当用于事件计数时---初始化为0   当用于资源管理时 ---- 初始化最大



	QueueHandle_t xQueueCreateCountingSemaphore( const UBaseType_t uxMaxCount, const UBaseType_t uxInitialCount )
	{
		QueueHandle_t xHandle;
		configASSERT( uxMaxCount != 0 );
		configASSERT( uxInitialCount <= uxMaxCount ); //初始值 <= 最大值

        //创建队列函数，队列长度-信号量最大值   队列项大小0 ，创建方式-计数型信号量
		xHandle = xQueueGenericCreate( uxMaxCount, queueSEMAPHORE_QUEUE_ITEM_LENGTH, queueQUEUE_TYPE_COUNTING_SEMAPHORE );

		if( xHandle != NULL )
		{
            //队列创建成功后，初始化信号量值 --- 非空闲队列项计数值初始化
			( ( Queue_t * ) xHandle )->uxMessagesWaiting = uxInitialCount; 

			traceCREATE_COUNTING_SEMAPHORE();
		}
		else
		{
			traceCREATE_COUNTING_SEMAPHORE_FAILED();
		}

		return xHandle;
	}

```







###### 2.2获取信号量计数







###### ![image-20250429212600511](./嵌入式软件学习.assets/image-20250429212600511.png)值























##### 3.实验

![image-20250429212711260](./嵌入式软件学习.assets/image-20250429212711260.png)





```c
 count_semaphore_handle = xSemaphoreCreateCounting(100,0); // 创建计数型信号量(最大值，初始值)，返回句柄


/*任务一：释放计数型信号量*/
void task1(void *pvParameters)
{
    uint8_t key = 0;
    while (1)
    {
        key = key_scan(0);
        if (key == KEY1_PRESS)
        {
            if (count_semaphore_handle != NULL)
            {
                xSemaphoreGive(count_semaphore_handle); // 释放计数型信号量    一直+1            
            }
        }
        vTaskDelay(10);
    }
}
/*任务二：获取计数型信号量*/
void task2(void *pvParameters)
{
    BaseType_t err;
    while (1)
    {
        err = xSemaphoreTake(count_semaphore_handle, portMAX_DELAY); // 获取信号量(死等),获取到信号量前将一直阻塞
        if(err == pdTRUE)
        {
            printf("信号量计数值：%d\r\n",(int)uxSemaphoreGetCount(count_semaphore_handle));//获取信号量计数值
        }
        vTaskDelay(1000);   
    }
}

```





#### 5.12.4优先级翻转

##### 1介绍



- 优先级翻转：**高优先级满执行，低优先级快执行**
- 抢**占式内核非常常见**，应该避免会产生验证后果。**在使用二值信号量时，非常容易出现**

![image-20250429214056509](./嵌入式软件学习.assets/image-20250429214056509.png)



- 例如下列例子：任务H为高优先任务运行时需要获取信号量，运行结束时释放信号量。 任务L也需该信号量执行。而任务M无需该信号量
- 当低优先任务L，提前任务创建、高优先级任务阻塞等原因，先获取到资源。由于中优先级任务M一直执行，导致任务L需要等任务M阻塞后才能运行。释放信号量后，任务H高优先级才能运行。
- 该现象导致，**中优先级任务一直运行，而低优先级任务无法运行，又由于低优先级任务一直把持着信号量，导致高优先级一直阻塞。**

![image-20250502095715768](./嵌入式软件学习.assets/image-20250502095715768.png)



#### 5.12.5互斥信号量



- **二值信号量适用于---任务同步**，低优先级通过信号量可以阻塞高优先级运行，例如需要在低优先级运行完后，高优先级才能运行。
- **互斥信号量适用于 --- 互斥获取资源**，需要尽可能保证高优先级的运行。当低优先级使用该资源时，通过优先级继承，将低优先级任务提升到高优先级任务一样

![image-20250502102143173](./嵌入式软件学习.assets/image-20250502102143173.png)



##### 1.优先级继承

- 当信号量被低优先级获取时，将**低优先级任务优先级提升到高优先级一致**，这样任务无法被中优先级任务打断，**高优先级任务的阻塞时间就是低优先级任务的运行事件。**
- **适用于资源互斥访问，保证高优先级更快的获取资源。**

![image-20250502102655397](./嵌入式软件学习.assets/image-20250502102655397.png)



##### 2.互斥信号量只能使用在任务间

- 任务继承不可以完全消除优先级翻转问题，低优先级释放资源也需运行一段时间，但是将影响降低。
- **互斥信号量不可在中断中使用：无优先级继承 、 中断不可阻塞**

![image-20250502103139394](./嵌入式软件学习.assets/image-20250502103139394.png)



##### 3.相关API函数





###### 3.1创建互斥信号量

- ###### 使用时需要开启相关宏

- **使用API创建互斥信号量时，函数内部会自动初始化释放一次信号量**， 相比二值信号量，需要手动在运行前将信号量释放一次

- 信号量API只能在任务中使用。

![image-20250502104339076](./嵌入式软件学习.assets/image-20250502104339076.png)

![image-20250502104828606](./嵌入式软件学习.assets/image-20250502104828606.png)



```c
//调用，创建成功返回句柄
mutex_semaphore_handle = xSemaphoreCreateMutex(); // 创建互斥信号量，并主动释放一次信号量

//定义
#define xSemaphoreCreateMutex() xQueueCreateMutex( queueQUEUE_TYPE_MUTEX )

QueueHandle_t xQueueCreateMutex( const uint8_t ucQueueType )
{
Queue_t *pxNewQueue;
const UBaseType_t uxMutexLength = ( UBaseType_t ) 1, uxMutexSize = ( UBaseType_t ) 0;

    //创建队列函数，长度为1，队列项大小为0 ，创建类型为互斥信号量
    pxNewQueue = ( Queue_t * ) xQueueGenericCreate( uxMutexLength, uxMutexSize, ucQueueType );
    
    //初始化互斥信号量，初始化释放一次
    prvInitialiseMutex( pxNewQueue );

    return pxNewQueue;
}


//初始化互斥信号量，释放一次信号量
static void prvInitialiseMutex( Queue_t *pxNewQueue )
{
    if( pxNewQueue != NULL )
    {

        pxNewQueue->pxMutexHolder = NULL;
        pxNewQueue->uxQueueType = queueQUEUE_IS_MUTEX;


        pxNewQueue->u.uxRecursiveCallCount = 0;

        traceCREATE_MUTEX( pxNewQueue );
        
		//队列释放函数，初始化释放一次
        ( void ) xQueueGenericSend( pxNewQueue, NULL, ( TickType_t ) 0U, queueSEND_TO_BACK ); 
    }
    else
    {
        traceCREATE_MUTEX_FAILED();
    }
}
```



### 5.13队列集

#### 1.介绍

- **多个队列与信号量集合，任何一个消息到来都可以退出阻塞状态**，根据返回的队列句柄进行不同的处理
- 左边未使用队列集时：等待接受队列此时为阻塞态，即使信号量优先也无法获取。
- 右边使用队列集：当队列集获取任何一个消息都会将任务唤醒，任务根据获取的消息自行分情况处理。

![image-20250502112226584](./嵌入式软件学习.assets/image-20250502112226584.png)



#### 2.相关API函数





- 从队列集中  获取有效消息 的**队列  **， 获取有消息的队列
- 将队列添加或者移除，**需要确保队列中无消息**

![image-20250502112953151](./嵌入式软件学习.assets/image-20250502112953151.png)



![image-20250502115243729](./嵌入式软件学习.assets/image-20250502115243729.png)





##### 2.1创建队列集

- 创建时，参数为---可容纳的队列数量。如果两个之间通讯使用3个队列，可以创建一个队列集，最多可容纳3个队列。
- 创建成功，返回队列集的句柄

![image-20250502113258482](./嵌入式软件学习.assets/image-20250502113258482.png)





##### 2.2将队列添加到队列集中



- 函数形参：1.待加入的队列      2.添加的目标队列集
- 返回值：是否添加成功
- **将队列添加到队列集时，确保队列中无消息**

![image-20250502113614802](./嵌入式软件学习.assets/image-20250502113614802.png)





##### 2.3将队列从队列集中移除



- 函数形参：1.待移除的队列      2.准备移除的目标队列集
- 返回值：是否移除成功
- **将队列移除到队列集时，确保队列中无消息**

![image-20250502114035988](./嵌入式软件学习.assets/image-20250502114035988.png)





##### 2.4在任务中获取队列集中有效消息的队列

- 参数： 1.所需操作的队列集   2.阻塞时间
- 返回值：当某个队列有数据时，**返回该队列的句柄**

![image-20250502114733640](./嵌入式软件学习.assets/image-20250502114733640.png)









#### 3.实验

![image-20250502115151365](./嵌入式软件学习.assets/image-20250502115151365.png)



![image-20250502115234256](./嵌入式软件学习.assets/image-20250502115234256.png)





```c
QueueSetHandle_t queueset_handle;
QueueHandle_t queue_handle;
QueueHandle_t semphr_handle;


	/*创建队列集*/
    queueset_handle = xQueueCreateSet(2); // 可以存放两个队列

    /*创建队列*/
    queue_handle = xQueueCreate(1, sizeof(uint8_t));

	/*创建二值信号量*/
	semphr_handle = xSemaphoreCreateBinary();

    /*将队列、信号量添加到队列集中*/
    xQueueAddToSet(queue_handle,queueset_handle);
    xQueueAddToSet(semphr_handle,queueset_handle);





/*任务一：KEY1按下 写入队列，KEY2按下 释放二值信号量*/
void task1(void *pvParameters)
{
    uint8_t key = 0;
    BaseType_t err;
    while (1)
    {
        key = key_scan(0);
        if(key == KEY1_PRESS)
        {
            err = xQueueSend(queue_handle,&key,portMAX_DELAY);
            if(err == pdPASS)
            {
                printf("往队列写入数据成功 \r\n");
            }
        }
        else if(key == KEY2_PRESS)
        {
            err = xSemaphoreGive(semphr_handle);
            if(err == pdPASS)
            {
                printf("释放信号量成功 \r\n");
            }
        }
        vTaskDelay(10);
    }
}





/*任务二：获取队列集消息，根据不同队列、信号量的句柄进行不同的处理*/
void task2(void *pvParameters)
{
    QueueSetMemberHandle_t member_handle;
    uint8_t key;
    while (1)
    {
        //获取队列集消息，获取到任意成员消息则停止阻塞
        member_handle = xQueueSelectFromSet(queueset_handle,portMAX_DELAY);
        if(member_handle == queue_handle)
        {
            xQueueReceive(member_handle,&key,portMAX_DELAY);
            printf("获取到的队列数据为：%d \r\n",key);
        }
        else if (member_handle == semphr_handle)
        {
            xSemaphoreTake(member_handle,portMAX_DELAY);
            printf("获取信号量成功 \r\n");
        }
    }
}

```





### 5.14事件标志组





#### 1.介绍

##### 1.1简介

- 使用某个BIT未来标识一个事件，所有事件的集合（事件标志组）就是一个整数
- 事件标志组--特点：
  1. **中断任务和任务都可读写**
  2. **可以等待某一位、或者多位同时成立**
  3. **高8位，不可被用于标识事件**，用于事件标志组本身控制。

![image-20250502134153023](./嵌入式软件学习.assets/image-20250502134153023.png)



##### 1.2事件标志组结构

- 通过宏设置事件标志组类型（32位/16位），一般为32位。
- **高八位用于事件标志组本身的控制信息，低24位用于标记位**
- 

![image-20250502134749699](./嵌入式软件学习.assets/image-20250502134749699.png)

```c
typedef TickType_t EventBits_t;

#if( configUSE_16_BIT_TICKS == 1 ) //通过宏来设置时间标准组为 32位 还是16位，FreeRtos一般为32位事件标准组
	typedef uint16_t TickType_t;
#else
	typedef uint32_t TickType_t;

#endif


```

![image-20250502135424448](./嵌入式软件学习.assets/image-20250502135424448.png)



##### 1.3事件标志组与队列的区别



- 队列**只能唤醒一个任务**，无资源其他任务依旧阻塞。**消耗性资源**。
- 标志事件组，**广播**，符合条件的队列都可被唤醒，任务可**以选择事件保留不动 或者 清除**。

![image-20250502135840544](./嵌入式软件学习.assets/image-20250502135840544.png)



#### 2.相关API函数

- 创建、设置、等待、清零
- <img src="./嵌入式软件学习.assets/image-20250502140844576.png" alt="image-20250502140844576" style="zoom:50%;" />既可以设置，也可以等待

![image-20250502140529433](./嵌入式软件学习.assets/image-20250502140529433.png)



##### 2.1动态创建



- 创建成功 --- 返回事件标志组的句柄

![image-20250502141018537](./嵌入式软件学习.assets/image-20250502141018537.png)







##### 2.2清除事件标志位

- 参数1---待清除句柄    参数2 ---- 清除标位位置
- 返回值 ： **未清除前的事件标记组的值--- 整数**

![image-20250502141155865](./嵌入式软件学习.assets/image-20250502141155865.png)





##### 2.3设置事件标志位



- 参数1---事件标志位句柄    参数2---待设置的标记位
- 返回参数 --- **函数返回时，事件组的整数值。**

![image-20250502141430836](./嵌入式软件学习.assets/image-20250502141430836.png)





##### 2.4等待时间标志位

1. **可以等待一位，或者多位标记位都有效** 
2. 等待成功后，可**选择是否清除所等待的标记位**



- 参数1：等待的事件标志组的句柄
- 参数2：所等待的标记位 ，可以使用 逻辑或  等待多个标记位
- 参数3 ： 等待成功后，是否需要清除所等待的标记位
- 参数4：等待位全部满足  / 等待位任意一个满足 ，唤醒任务
- 参数5：等待阻塞时间

![image-20250502142222316](./嵌入式软件学习.assets/image-20250502142222316.png)



##### 2.5同步

该函数作用： 

例如：TASK1 做饭 ， TASK2 做菜

在TASK1做完饭后，置位做饭完成标记位后。需等待TASK2的做菜完成标记位，当做菜完成后，开始后续的吃饭步骤。



- 置位标记位同时，等待其他标记位
- 参数1：所操作的事件标记组
- 参数2：所需设置的事件标记
- 参数3：等待的事件标记
- 参数4：阻塞时间

![image-20250502143845328](./嵌入式软件学习.assets/image-20250502143845328.png)







#### 3.实验



![image-20250502151153872](./嵌入式软件学习.assets/image-20250502151153872.png)





```c
EventGroupHandle_t eventgroup_handle;

    /*创建事件标志组*/
    eventgroup_handle = xEventGroupCreate();


/*任务一：读取键值，根据不同键值将事件标志组相应事件位置1,按下对应的标记位，对应的事件标记位置位*/
void task1(void *pvParameters)
{
    uint8_t key;
    while (1)
    {
        key = key_scan(0);
        if (key == KEY1_PRESS)
        {
            xEventGroupSetBits(eventgroup_handle, 0x01); // 将事件标志组的bit0置1
        }
        else if (key == KEY2_PRESS)
        {
            xEventGroupSetBits(eventgroup_handle, 0x02); // 将事件标志组的bit1置1
        }
        else if (key == KEY3_PRESS)
        {
            xEventGroupSetBits(eventgroup_handle, 0x04); // 将事件标志组的bit2置1
        }
        vTaskDelay(10);
    }
}


/*任务二：同时等待事件标志组中的多个事件位，当这些位都置1时*/
void task2(void *pvParameters)
{
    EventBits_t event_bit;
    while (1)
    {
        
        event_bit = xEventGroupWaitBits(eventgroup_handle,                    // 等待的事件标志组句柄
                                        0x01 | 0x02 | 0x04, // 等待的事件标志位
                                        pdTRUE, // 成功等待后是否清除对应的标志位，这里等待成功后，清除BIT0-BIT2
                                        pdTRUE,// 是否等待全部事件标志位有效， pdTRUE：等待的位 全部为1，pdFALSE：等待的位 某个为1
                                        portMAX_DELAY);  // 阻塞时间，这里最大为挂起状态，一直等待到标记位有效唤醒
    
    }
}
```







### 5.15任务通知



#### 1.介绍



- **间接通知**：例如： *队列/信号量/事件标志组*，都是通过创建的新结构体，**间接传递消息**。

- **直接通知**：在**任务控制块中定义了消息传递结构体**，**任务与任务间直接传递**。  适用于**任务一对一传输**

  ```c
  	//直接在任务控制块中定义
  #if( configUSE_TASK_NOTIFICATIONS == 1 )
  		volatile uint32_t ulNotifiedValue;  //任务通知传递值
  		volatile uint8_t ucNotifyState; 	//任务通知传递状态
  	#endif
  ```

  

![image-20250502153021999](./嵌入式软件学习.assets/image-20250502153021999.png)





##### 1.1通知方式



- 在**一些场合**下，可以通过任务通知模拟：队列、信号量、事件标记组

![image-20250502154537596](./嵌入式软件学习.assets/image-20250502154537596.png)

![image-20250502160024443](./嵌入式软件学习.assets/image-20250502160024443.png)





##### 1.2任务通知优劣



###### 优点：

1. **效率更高**，使用任务通知传递消息，相比中间结构体传递，速度提高45%
2. **使用内存更小**，无需创建额外的内存空间



###### 缺点：

1. **无法传递给中断**：只能任务间传递、中断通知给任务。
2. **无法广播**：只能点对点通讯
3. **无法缓存多个数据** ： 一级缓存，只能存放一个数据
4. **不支持发送阻塞** ： 发送方无法进入阻塞状态



![image-20250502155205795](./嵌入式软件学习.assets/image-20250502155205795.png)





##### 1.3任务通知状态

*状态对任务本身*

- **任务未等待通知**：初始化状态。
- **任务在等待通知**：任务在等待其他任务对其发送消息，**已调用接受函数，但无数据导致任务进入阻塞状态**
- **任务在等待接受**：**其他任务已发送消息给任务本身，需要任务接受处理**。   **其他任务已对任务发送消息，消息非空**



![image-20250502160344921](./嵌入式软件学习.assets/image-20250502160344921.png)







#### 2.相关API函数



- 发送：任务和中断都可以发送
- 接受：**中断不可使用，中断无任务控制块**

![image-20250503124918268](./嵌入式软件学习.assets/image-20250503124918268.png)



##### 2.1发送通知

![image-20250503125350103](./嵌入式软件学习.assets/image-20250503125350103.png)



- 底层调用的都是同一个函数，通过不同参数来实现不同功能

![image-20250503130513170](./嵌入式软件学习.assets/image-20250503130513170.png)

<img src="./嵌入式软件学习.assets/image-20250503131217429.png" alt="image-20250503131217429" style="zoom:50%;" />

![image-20250503131301817](./嵌入式软件学习.assets/image-20250503131301817.png)

![image-20250503143016611](./嵌入式软件学习.assets/image-20250503143016611.png)

- xTaskNotify、xTaskGenericNotify 用于队列、事件标志组，通知方式可选
- xTaskNotifyAndQuery 用于信号量，固定对任务通知值+1

```c

//下列两个都带通知值，区别是最后第一个参数---是否保留被通知任务原通知值

    //带通知值
    #define xTaskNotify( xTaskToNotify, ulValue, eAction ) 		\
    xTaskGenericNotify( ( xTaskToNotify ), ( ulValue ), ( eAction ), NULL )

    //带通知值 ， 保留被通知任务原通知值
    #define xTaskNotifyAndQuery( xTaskToNotify, ulValue, eAction, pulPreviousNotifyValue ) 		\
    xTaskGenericNotify( ( xTaskToNotify ), ( ulValue ), ( eAction ), ( pulPreviousNotifyValue ) )




//不带通知值，使用任务通知值加一的方式

    //不带通知值
    #define xTaskNotifyGive( xTaskToNotify )		\
    xTaskGenericNotify( ( xTaskToNotify ), ( 0 ), eIncrement, NULL )


//参数1 ：被通知任务 的 任务句柄
//参数2 ： 任务通知值
//参数3 ： 通知方式 0-无操作	1-指定更新对应的BIT位	 2-通知值加一 	3-覆写方式更新通知值		4-不覆写通知值
//参数4 ： 是否返回原任务通知值
	BaseType_t xTaskGenericNotify( TaskHandle_t xTaskToNotify, uint32_t ulValue, eNotifyAction eAction, uint32_t *pulPreviousNotificationValue )
	{
	TCB_t * pxTCB;
	BaseType_t xReturn = pdPASS;
	uint8_t ucOriginalNotifyState;

		configASSERT( xTaskToNotify );
		pxTCB = ( TCB_t * ) xTaskToNotify; //获取需要操作的任务控制块

		taskENTER_CRITICAL();
		{
			if( pulPreviousNotificationValue != NULL ) //参数4 - 需要保存被通知任务的 原通知值
			{
				*pulPreviousNotificationValue = pxTCB->ulNotifiedValue;
			}

			ucOriginalNotifyState = pxTCB->ucNotifyState; //记录任务原来的  任务通知状态

			pxTCB->ucNotifyState = taskNOTIFICATION_RECEIVED; //修改被通知任务的 任务通知状态 --- 等待接受状态-其他任务已发送消息，需要任务本身处理

			switch( eAction ) //根据第3参数，选择不同的处理
			{
				case eSetBits	: //设置对应的BIT位   --- 事件标志组处理
					pxTCB->ulNotifiedValue |= ulValue;
					break;

				case eIncrement	:	//对通知值进行++  ----  信号量
					( pxTCB->ulNotifiedValue )++;
					break;

				case eSetValueWithOverwrite	:   // 覆写操作 --- 直接赋值
					pxTCB->ulNotifiedValue = ulValue;
					break;

				case eSetValueWithoutOverwrite : //不覆写操作 
                    //需要判断通知状态 非等待接受状态 （表示无消息） --- 进行写入
					if( ucOriginalNotifyState != taskNOTIFICATION_RECEIVED ) 
					{
						pxTCB->ulNotifiedValue = ulValue;
					}
					else
					{
						xReturn = pdFAIL; //写入失败
					}
					break;

				case eNoAction:
			
					break;
			}

			traceTASK_NOTIFY();

			
            
            //判断被写入任务，原状态是否为 等待发送状态（任务已调用过接受函数，但无数据进入阻塞状态） ，需要恢复任务
			if( ucOriginalNotifyState == taskWAITING_NOTIFICATION )
			{
                //将任务时间状态清除，并将状态列表项加入到 就绪态
				( void ) uxListRemove( &( pxTCB->xStateListItem ) );
				prvAddTaskToReadyList( pxTCB );

		
				//任务恢复后，判断优先级是否需要触发任务调度
				if( pxTCB->uxPriority > pxCurrentTCB->uxPriority )
				{
	
					taskYIELD_IF_USING_PREEMPTION();
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
                
                
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		taskEXIT_CRITICAL();

		return xReturn;
	}

```







##### 2.2接受通知

- ulTaskNotifyTake 用于**信号量读取** ，读取后信号量--
- xTaskNotifyWait 用于**队列/事件标记组** 读取 ，读取后可以选择清除消息或者部分BIT位
- **接受函数只有任务级**

![image-20250503133059741](./嵌入式软件学习.assets/image-20250503133059741.png)





###### 2.2.1ulTaskNotifyTake --- 用于信号量



```c
//参数1 ：接受成功后，通知值的操作    0-减1（模拟计数型信号量）		1-清零（模拟二值信号量）
//参数2 ： 阻塞任务时间。当未获取信号量，阻塞一定时间
//返回值 ： 0-接受失败   非0-返回接受通知值
uint32_t ulTaskNotifyTake( BaseType_t xClearCountOnExit, TickType_t xTicksToWait )
	{
	uint32_t ulReturn;

		taskENTER_CRITICAL();
		{
			
            //此时信号量为0 ，需要阻塞
			if( pxCurrentTCB->ulNotifiedValue == 0UL )
			{
				//将任务通知状态设置为 等待任务通知状态
				pxCurrentTCB->ucNotifyState = taskWAITING_NOTIFICATION;

                //阻塞时间>0 ,阻塞任务并启动任务调度
				if( xTicksToWait > ( TickType_t ) 0 ) 
				{
					prvAddCurrentTaskToDelayedList( xTicksToWait, pdTRUE );
					traceTASK_NOTIFY_TAKE_BLOCK();
					portYIELD_WITHIN_API();
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
                
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		taskEXIT_CRITICAL();

    	//信号量非零，或者上述阻塞后恢复
    
		taskENTER_CRITICAL();
		{
			ulReturn = pxCurrentTCB->ulNotifiedValue;

			if( ulReturn != 0UL ) //判断信号量 > 0 
			{
                //根据参数3 设置 清零 还是 减1
				if( xClearCountOnExit != pdFALSE )  //设置1-清零
				{
					pxCurrentTCB->ulNotifiedValue = 0UL;
				}
				else //设置0 - 减1
				{
					pxCurrentTCB->ulNotifiedValue = ulReturn - 1;
				}
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}

            	//将任务通知状态 设置为 未等待通知状态
			pxCurrentTCB->ucNotifyState = taskNOT_WAITING_NOTIFICATION;
		}
		taskEXIT_CRITICAL();

		return ulReturn; //返回当前信号量值
	}

```



###### 2.2.2xTaskNotifyWait --- 用于模拟队列/事件标志组 

![image-20250503140242783](./嵌入式软件学习.assets/image-20250503140242783.png)

![image-20250503140846942](./嵌入式软件学习.assets/image-20250503140846942.png)

```c
	
//参数1：在等待前清除对应BIT位
//参数2：在等待后清除对应BIT位
//参数3：是否需要取出通知值 ，NULL--不需要取出
//参数4：阻塞最大时间 
BaseType_t xTaskNotifyWait( uint32_t ulBitsToClearOnEntry, uint32_t ulBitsToClearOnExit, uint32_t *pulNotificationValue, TickType_t xTicksToWait )
	{
	BaseType_t xReturn;

		taskENTER_CRITICAL();
		{
			//被写入任务 --- 非等待接受状态 --- 即其他任务还未发送数据
			if( pxCurrentTCB->ucNotifyState != taskNOTIFICATION_RECEIVED )
			{
				//等待前 --- 清除原来消息值
				pxCurrentTCB->ulNotifiedValue &= ~ulBitsToClearOnEntry;
				
                //状态改为等待消息通知状态
				pxCurrentTCB->ucNotifyState = taskWAITING_NOTIFICATION;

                
                //当阻塞时间大于0 --- 将任务阻塞，并启动任务调度
				if( xTicksToWait > ( TickType_t ) 0 )
				{
					prvAddCurrentTaskToDelayedList( xTicksToWait, pdTRUE );
					traceTASK_NOTIFY_WAIT_BLOCK();
					portYIELD_WITHIN_API();
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
                
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		taskEXIT_CRITICAL();

    
    //运行到此有三种状态： 1.其他任务已发送消息  2.事件恢复，其他任务在阻塞时发送消息   3.时间阻塞恢复，已阻塞到最大时间
    
    
		taskENTER_CRITICAL();
		{
			traceTASK_NOTIFY_WAIT();

			if( pulNotificationValue != NULL ) //需要保存原任务通知值
			{
				*pulNotificationValue = pxCurrentTCB->ulNotifiedValue;
			}


            //状态依旧为 等待通知状态 ，即一直未收到消息，直接返回失败
			if( pxCurrentTCB->ucNotifyState == taskWAITING_NOTIFICATION )
			{
				xReturn = pdFALSE;
			}
			else //否则 将任务通知值清除对应位，返回成功
			{
				pxCurrentTCB->ulNotifiedValue &= ~ulBitsToClearOnExit;
				xReturn = pdTRUE;
			}

			pxCurrentTCB->ucNotifyState = taskNOT_WAITING_NOTIFICATION; //重新复位状态--- 未等待操作
		}
		taskEXIT_CRITICAL();

		return xReturn;
	}
```





#### 3.相关实验



##### 3.1任务通知模拟信号量



- 相比使用其他中间消息结构体，任务通知在任务控制块创建时就有，无需初始化

![image-20250503141502303](./嵌入式软件学习.assets/image-20250503141502303.png)

```c
/* 任务一，发送任务通知值 */
void task1( void * pvParameters )
{
    uint8_t key = 0;
    
    while(1) 
    {
        key = key_scan(0);
        if(key == KEY0_PRES)
        {
            printf("任务通知模拟二值信号量释放！\r\n");
            xTaskNotifyGive(task2_handler); //对任务通知值 ++ 
        }
        vTaskDelay(10);
    }
}

/* 任务二，接收任务通知值 */
void task2( void * pvParameters )
{
    uint32_t rev = 0;
    while(1)
    {
        rev = ulTaskNotifyTake(pdTRUE , portMAX_DELAY); //任务通知值 清零
        if(rev != 0)
        {
            printf("接收任务通知成功，模拟获取二值信号量！\r\n");
        }
    }
}
```



##### 3.2任务通知模拟消息邮箱（队列）



```c
/* 任务一，发送任务通知值 */
void task1( void * pvParameters )
{
    uint8_t key = 0;
    
    while(1) 
    {
        key = key_scan(0);
        if((key != 0) && (task2_handler != NULL))
        {
            printf("任务通知模拟消息邮箱发送，发送的键值为：%d\r\n",key);
            xTaskNotify( task2_handler, key, eSetValueWithOverwrite ); //将按下不同按键的 键值 发送给对应任务 ，覆写操作
        }
        vTaskDelay(10);
    }
}

/* 任务二，接收任务通知值 */
void task2( void * pvParameters )
{
    uint32_t noyify_val = 0;
    while(1)
    {
        //等待前不清除BIT位，获取后将信号量清零 ， 取出通知值 ，阻塞时间最大直接挂起
        xTaskNotifyWait( 0, 0xFFFFFFFF, &noyify_val, portMAX_DELAY );
        
        switch(noyify_val) //根据传递来的信号量---键值进行不同处理
        {
            case KEY0_PRES:
            {
                printf("接收到的通知值为：%d\r\n",noyify_val);
                LED0_TOGGLE();
                break;
            }
            case KEY1_PRES:
            {
                printf("接收到的通知值为：%d\r\n",noyify_val);
                LED1_TOGGLE();
                break;
            }
            default : break;
        }
    }
}
```







##### 3.3任务通知模拟事件标志组





```c

//任务1 ： 按下对应按键置位 对应的标记位
void task1( void * pvParameters )
{
    uint8_t key = 0;
    while(1) 
    {
        key = key_scan(0);
        if(key == KEY0_PRES)
        {

            xTaskNotify( task2_handler, 0x01, eSetBits ); //实现方式为 eSetBits ---置位对应位
        }else if(key == KEY1_PRES)
        {
            xTaskNotify( task2_handler, 0x02, eSetBits );
        }
        vTaskDelay(10);
    }
}



/* 任务二，接收任务通知值 */
void task2( void * pvParameters )
{
    uint32_t notify_val = 0,event_bit = 0;
    while(1)
    {
        //等待接受前不清楚 、 接受后清除所有标记位 、 返回所有标记位 、 设置最大阻塞时间
        xTaskNotifyWait( 0, 0xFFFFFFFF, &notify_val, portMAX_DELAY );
        
        //响应位按下后，置位另一个变量，通过另一个变量判断手哦否两个按键都按下
        if(notify_val & 0x01)
        {
            event_bit |= 0x01;
        }
        if(notify_val & 0x02)
        {
            event_bit |= 0x02;
        }
        if(event_bit == 0x03 )
        {
            event_bit = 0;
        }
    }
}
```



### 5.16软件定时器



[FreeRTOS--软件定时器 - 流翎 - 博客园](https://www.cnblogs.com/hjx168/p/17991228)

#### 5.16.1介绍

- 定时器：闹钟功能，到达设定时间调用对应的函数
- 硬件定时器：功能多（输入捕获，输出比较）、精度高；但是资源有限
- 软件定时器：可由用户定义任意数量。

![image-20250503161541235](./嵌入式软件学习.assets/image-20250503161541235.png)



- 优点：成本低、创建多个
- 缺点：精度低

![image-20250503161947160](./嵌入式软件学习.assets/image-20250503161947160.png)



##### 1.特点

- **可裁剪**：通过宏开启或者关闭此功能
- **单次或周期**：定时器可以只执行一次、或者周期执行
- 在启动任务调度器中，会自动创建两个任务：**1.空闲任务  2.软件定时器服务任务**。 
- 软件定时器服务任务功能：
  1. **负责定时器超时判断**：多个定时器，需要实时判断那个定时器时间到达
  2. **负责超时函数回调：**当某个定时器超时后，服务任务回调其对应函数。**在超时回调函数中，不可以调用会阻塞任务的API，会直接阻塞软件定时器服务任务，导致所有定时器都不可工作**
  3. **处理软件定时器命令队列** ： 任务需要控制和配置定时器时，通过向命令队列赋值来控制定时器

![image-20250503162509040](./嵌入式软件学习.assets/image-20250503162509040.png)





##### 2.命令队列



- 任务**只能通过调用API**来控制定时器，本质是通过命令队列，发送命令给软件定时器服务任务进行处理。
- 用户不可直接访问命令队列

![image-20250503163412979](./嵌入式软件学习.assets/image-20250503163412979.png)









##### 3.相关配置

- 开启软件定时器功能
- 设置软件定时器服务任务为**最高优先级 31**
- 配置命令队列长度为 5



- 由于软件定时器服务任务，**服务所有的定时器**。这要求定时器需要不影响服务任务的运行： **不可阻塞软件定时器服务任务**

  



![image-20250503164255832](./嵌入式软件学习.assets/image-20250503164255832.png)



##### 4.软件定时器状态



- **休眠态**：可以操作，但是未运行。**当创建时，为该状态。需要调用对应的命令队列才能进入运行态**
- **运行态**：到达运行时间后，回调超时函数

![image-20250503164809785](./嵌入式软件学习.assets/image-20250503164809785.png)



- 创建定时器后，首先进入休眠态，并不会直接进入运行。
- 在休眠态，调用API：1.复位  2.启动  3.重新设置超时时间     会自动启动定时器运行
- 调用停止API后，停止运行。单次定时器，会在超时后自动进入休眠态。

![image-20250503165600582](./嵌入式软件学习.assets/image-20250503165600582.png)











##### 5.定时器模式



- 单次定时器/周期定时器

![image-20250503165142303](./嵌入式软件学习.assets/image-20250503165142303.png)



![image-20250503165210914](./嵌入式软件学习.assets/image-20250503165210914.png)



##### 6.软件定时器服务函数



###### 6.1创建软件定时器任务

```c
/***************************************************************************************************************/
/*                                FreeRTOS与软件定时器有关的配置选项                                            */
/***************************************************************************************************************/
#define configUSE_TIMERS				        1                               //为1时启用软件定时器
#define configTIMER_TASK_PRIORITY		        (configMAX_PRIORITIES-1)        //软件定时器优先级
#define configTIMER_QUEUE_LENGTH		        5                               //软件定时器队列长度
#define configTIMER_TASK_STACK_DEPTH	        (configMINIMAL_STACK_SIZE*2)    //软件定时器任务堆栈大小


//在启动任务调度器函数中，会开启两个任务：1.空闲任务  2.软件定时器服务函数
#if ( configUSE_TIMERS == 1 )
{
    if( xReturn == pdPASS )
    {
        xReturn = xTimerCreateTimerTask(); //创建软件定时器服务函数
    }
    else
    {
        mtCOVERAGE_TEST_MARKER();
    }
}



BaseType_t xTimerCreateTimerTask( void )
{
BaseType_t xReturn = pdFAIL;

	prvCheckForValidListAndQueue(); //创建 两个时间列表 与 一个命令队列

	if( xTimerQueue != NULL )
	{
       //创建 软件定时器服务函数 
        xReturn = xTaskCreate(	prvTimerTask, //函数地址
                                "Tmr Svc", //任务名称
                                configTIMER_TASK_STACK_DEPTH, //栈大小
                                NULL, //形参
                                ( ( UBaseType_t ) configTIMER_TASK_PRIORITY ) | portPRIVILEGE_BIT, //任务优先级
                                &xTimerTaskHandle ); //任务句柄
		
	}
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}

	configASSERT( xReturn );
	return xReturn;
}

```





创建两个计数列表 与 一个命令队列

```c
static void prvCheckForValidListAndQueue( void )
{

	taskENTER_CRITICAL();
	{
		if( xTimerQueue == NULL )
		{
            
            //创建两个计时列表，由于是基于系统节拍定时，定时时间会溢出。所以采用双列表，一个正在计时，一个溢出计时
			vListInitialise( &xActiveTimerList1 );
			vListInitialise( &xActiveTimerList2 );
			pxCurrentTimerList = &xActiveTimerList1;
			pxOverflowTimerList = &xActiveTimerList2;

		//创建一个命令队列
		xTimerQueue = xQueueCreate( ( UBaseType_t ) configTIMER_QUEUE_LENGTH, sizeof( DaemonTaskMessage_t ) );


			#if ( configQUEUE_REGISTRY_SIZE > 0 )
			{
				if( xTimerQueue != NULL )
				{
					vQueueAddToRegistry( xTimerQueue, "TmrQ" );
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
			#endif /* configQUEUE_REGISTRY_SIZE */
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
	taskEXIT_CRITICAL();
}
```



###### 6.2软件定时器服务函数



[FreeRTOS--软件定时器 - 流翎 - 博客园](https://www.cnblogs.com/hjx168/p/17991228)

![image-20250504160933491](./嵌入式软件学习.assets/image-20250504160933491.png)



```c
static void prvTimerTask( void *pvParameters )
{
TickType_t xNextExpireTime;
BaseType_t xListWasEmpty;


	( void ) pvParameters;



	for( ;; )
	{

        //xNextExpireTime --- 返回最近的超时时间      xListWasEmpty --- 返回当前计时列表是否还有定时器
		xNextExpireTime = prvGetNextExpireTime( &xListWasEmpty );


        //判断定时器是否超时，执行回调函数。重复定时器重新添加回链表中
        //判断链表计数是否溢出，调换链表
        //当当前链表无定时器，将定时器服务函数阻塞 ，如果溢出链表也无定时器直接挂起。 等待计时时间到 或者有 命令队列
		prvProcessTimerOrBlockTask( xNextExpireTime, xListWasEmpty );

		
        //处理命令队列
		prvProcessReceivedCommands();
	}
}
```



获取下一个超时定时器

```c
static TickType_t prvGetNextExpireTime( BaseType_t * const pxListWasEmpty )
{
TickType_t xNextExpireTime;


	*pxListWasEmpty = listLIST_IS_EMPTY( pxCurrentTimerList ); //判断当前计时队列是否为空
    
	if( *pxListWasEmpty == pdFALSE ) //不为空说明有定时计在等待超时
	{
        //获取第一个队列项的 计数值； 由于是升序排列，第一个也就是最近的超时定时器
		xNextExpireTime = listGET_ITEM_VALUE_OF_HEAD_ENTRY( pxCurrentTimerList ); 
	}
	else
	{

		xNextExpireTime = ( TickType_t ) 0U;
	}

	return xNextExpireTime; //返回最近的超时时间
}
```









```c
static void prvProcessTimerOrBlockTask( const TickType_t xNextExpireTime, BaseType_t xListWasEmpty )
{
TickType_t xTimeNow;
BaseType_t xTimerListsWereSwitched;

	vTaskSuspendAll();
	{

        //调用prvSampleTimeNow获取当前tick，同时接口出参xTimerListsWereSwitched返回是否切换了定时器链表
		xTimeNow = prvSampleTimeNow( &xTimerListsWereSwitched );
        
        
        
        
        
		if( xTimerListsWereSwitched == pdFALSE ) //定时器未切换链表
		{
			//当前定时器链表还有定时器 && 下一个超时时间<当前系统时间 （超时）
			if( ( xListWasEmpty == pdFALSE ) && ( xNextExpireTime <= xTimeNow ) )
			{
				( void ) xTaskResumeAll(); //退出临界区 
				prvProcessExpiredTimer( xNextExpireTime, xTimeNow ); //唤醒定时器，执行对应的回调函数
			}
            
            //未超时
			else
			{
				if( xListWasEmpty != pdFALSE ) //当前定时器链表为空
				{

					xListWasEmpty = listLIST_IS_EMPTY( pxOverflowTimerList ); //判断溢出定时器链表是否为空
				}
                
				//用于阻塞当前软件定时器服务任务（直至有定时器到期，或者命令队列有数据到达），当移除队列也没有定时器直接挂起
				vQueueWaitForMessageRestricted( xTimerQueue, ( xNextExpireTime - xTimeNow ), xListWasEmpty );

                //由于上面挂起了任务，判断是否需要切换任务调度
				if( xTaskResumeAll() == pdFALSE )
				{
					portYIELD_WITHIN_API();
				}
				else
				{
					mtCOVERAGE_TEST_MARKER();
				}
			}
		}
        
        //定时器切换链表
		else
		{
			( void ) xTaskResumeAll();
		}
	}
}
```





```c
static TickType_t prvSampleTimeNow( BaseType_t * const pxTimerListsWereSwitched )
{
TickType_t xTimeNow;
PRIVILEGED_DATA static TickType_t xLastTime = ( TickType_t ) 0U; //静态变量 记录上一次值

	xTimeNow = xTaskGetTickCount(); //获取当前系统时间

	if( xTimeNow < xLastTime ) //小于上次计时时间 --- 发生溢出
	{
		prvSwitchTimerLists(); //调换两个定时器计数链表  切换定时器两个该链表
		*pxTimerListsWereSwitched = pdTRUE; //移除调换列表
	}
	else
	{
		*pxTimerListsWereSwitched = pdFALSE; //未发生溢出
	}

	xLastTime = xTimeNow; //记录本时刻系统时钟

	return xTimeNow;
}





```





```c

//参数1：超时时间  参数2：当前系统时间
static void prvProcessExpiredTimer( const TickType_t xNextExpireTime, const TickType_t xTimeNow )
{
BaseType_t xResult;
    
    //获取最近需要唤醒的定时器
Timer_t * const pxTimer = ( Timer_t * ) listGET_OWNER_OF_HEAD_ENTRY( pxCurrentTimerList );

    	//从当前定时链表移除
	( void ) uxListRemove( &( pxTimer->xTimerListItem ) );


	//重复定时器
	if( pxTimer->uxAutoReload == ( UBaseType_t ) pdTRUE )
	{
        
        //重复定时器 --- 重新将定时器挂载到定时链表上
		if( prvInsertTimerInActiveList( pxTimer, ( xNextExpireTime + pxTimer->xTimerPeriodInTicks ), xTimeNow, xNextExpireTime ) != pdFALSE ) //TURE --- 表示 在上述函数执行期间定时已到
		{
			//重新开启定时器任务
			xResult = xTimerGenericCommand( pxTimer, tmrCOMMAND_START_DONT_TRACE, xNextExpireTime, NULL, 					tmrNO_DELAY );

			( void ) xResult;
		}
		else
		{
			mtCOVERAGE_TEST_MARKER();
		}
	}
    
    
    
    //非重复定时器 --- 直接移除
	else
	{
		mtCOVERAGE_TEST_MARKER();
	}

    //执行定时器的任务回调函数
	pxTimer->pxCallbackFunction( ( TimerHandle_t ) pxTimer );
}
```



























































































#### 5.16.2相关API



##### 1.软件定时器结构体



![image-20250503173651735](./嵌入式软件学习.assets/image-20250503173651735.png)



```c
typedef struct tmrTimerControl
{
	const char				*pcTimerName; //定时器名称
    
	ListItem_t				xTimerListItem; //软件定时器列表项 ，有两个，因为及时是根据FreeRtos的系统系统及时来的，会有溢出的现象		
    
	TickType_t				xTimerPeriodInTicks; //定时器超时时间 
    
    
	UBaseType_t				uxAutoReload;		//TURE--未重复定时器  FALT为单次定时器
    
	void 					*pvTimerID;		//软件定时器ID，多个定时器调用同一个回调函数时，可以通过ID号进行区分
    
	TimerCallbackFunction_t	pxCallbackFunction;	 //回调函数
    
    
    

    
    
} xTIMER;


typedef xTIMER Timer_t;
```









##### 2.相关API函数



- **创建、开启、复位、修改超时时间、停止** API函数

![image-20250503174433661](./嵌入式软件学习.assets/image-20250503174433661.png)



###### 2.1创建软件定时器



- **ID号，当多个任务调用同一个回调函数时，区分定时器**

![image-20250503174755866](./嵌入式软件学习.assets/image-20250503174755866.png)







###### 2.2启动定时器



- 阻塞时间，本质是将命令写入到命令队列中，当队列满时设置阻塞时间

![image-20250503175032749](./嵌入式软件学习.assets/image-20250503175032749.png)







###### 2.3停止定时器





![image-20250503175149488](./嵌入式软件学习.assets/image-20250503175149488.png)





###### 2.4复位定时器



- 调用该函数时，重新复位超时计数时间。

![image-20250503175357650](./嵌入式软件学习.assets/image-20250503175357650.png)







###### 2.5修改定时器超时时间



- 参数2 -- 为新超时时间。  **单位为系统节拍，也就是系统滴答定时器的中断周期**

![image-20250503175604104](./嵌入式软件学习.assets/image-20250503175604104.png)







##### 3.实验



![image-20250504180214107](./嵌入式软件学习.assets/image-20250504180214107.png)







```c
TimerHandle_t timer1_handle = 0; // 单次定时器句柄
TimerHandle_t timer2_handle = 0; // 周期定时器句柄    


/* 单次定时器 */
    timer1_handle = xTimerCreate((char *)"timer1",                          // 定时器名字
                                 (TickType_t)1000,                          // 超时周期(ms)
                                 (UBaseType_t)pdFALSE,                      // pdTRUE周期定时器，pdFALSE单次定时器
                                 (void *)1,                                 // 软件定时器ID，用于多个软件定时器共用一个超时回调函数
                                 (TimerCallbackFunction_t)timer1_callback); // 超时回调函数





    /* 周期定时器 */
    timer2_handle = xTimerCreate((char *)"timer2",                          // 定时器名字
                                 (TickType_t)1000,                          // 超时周期(ms)
                                 (UBaseType_t)pdTRUE,                       // pdTRUE周期定时器，pdFALSE单次定时器
                                 (void *)2,                                 // 软件定时器ID，用于多个软件定时器共用一个超时回调函数
                                 (TimerCallbackFunction_t)timer2_callback); // 超时回调函数






/*任务一：按键扫描控制软件定时器*/
void task1(void *pvParameters)
{
    uint8_t key = 0;
    while (1)
    {
        key = key_scan(0);
        if (key == KEY1_PRESS)
        {
            xTimerStart(timer1_handle, portMAX_DELAY); // 开启的定时器的句柄，发送命令阻塞时间
            xTimerStart(timer2_handle, portMAX_DELAY);
        }
        else if (key == KEY2_PRESS)
        {
            xTimerStop(timer1_handle, portMAX_DELAY); // 需关闭的定时器的句柄，发送命令阻塞时间
            xTimerStop(timer2_handle, portMAX_DELAY);
        }
        vTaskDelay(10);
    }
}

/*timer1超时回调函数*/
void timer1_callback(TimerHandle_t pxTimer)
{
    static uint32_t timer = 0;
    printf("timer1的运行次数：%d\r\n", ++timer);
}

/*timer2超时回调函数*/
void timer2_callback(TimerHandle_t pxTimer)
{
    static uint32_t timer = 0;
    printf("timer2的运行次数：%d\r\n", ++timer);
}
```







### 5.17Tickless低功耗模式



#### 5.17.1介绍

- 裸机开发：通过指令或者寄存器未进入低功耗状态。**睡眠、停止、待机** 三种状态
- **FreeRtos采用stm32的睡眠状态**
  1. **__WFI**指令进入睡眠模式 ，**任意中断唤醒**
  2. **__WFE**指令进入睡眠模式，**任意唤醒事件唤醒**，wake上升沿事件、定时器计时时间完成事件等。

![image-20250504182958741](./嵌入式软件学习.assets/image-20250504182958741.png)





- freertos采用**__WFI指令**进入低功耗，**任何中断都可以退出低功耗**
- **Tickless设计思想** ： 在系统运行中，大部分时间在运行空闲任务，为了降低功耗又不影响系统运行。**可以在空闲任务进入睡眠模式。当其他任务运行时退出低功耗模式**
- 实现难点：1.需要计算下一个任务运行的时间   2.滴答定时器中断也会将睡眠模式退出
- **解决**：**算出下一个任务运行事件T1，在进入低功耗前，将滴答定时器中断时间改为T1。当T1时间到后，滴答定时器中断唤醒睡眠模式，并将延时时间T1加到系统时间中。**

![image-20250504184406934](./嵌入式软件学习.assets/image-20250504184406934.png)

![image-20250504184520902](./嵌入式软件学习.assets/image-20250504184520902.png)



##### 1.配置

![image-20250504185716722](./嵌入式软件学习.assets/image-20250504185716722.png)

```c
//0---关闭低功耗模式 	1---启用低功耗tickless模式		2---自定义低功耗模式
#define configUSE_TICKLESS_IDLE						1                       


//系统进入低功耗模式最短时长 单位：ms ，当进入低功耗时间大于2ms才允许进入，否则时间太短进入低功耗无意义
#define configEXPECTED_IDLE_TIME_BEFORE_SLEEP 		2

//进入低功耗模式前执行的事务 ，例如关闭外设时钟，达到低功耗目的
#define configPRE_SLEEP_PROCESSING( X )         PRE_SLEEP_PROCESSING()  


//退出低功耗模式后执行的事务 ，例如开启时钟，确保系统正常运行
#define configPOSR_SLEEP_PROCESSING( X )        POSR_SLEEP_PROCESSING() 



/*进入低功耗模式前所需要执行的函数*/
void PRE_SLEEP_PROCESSING(void)
{
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOA,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOB,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOC,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOD,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOE,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOF,DISABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOG,DISABLE);
}
/*退出低功耗模式后所需要执行的函数*/
void POSR_SLEEP_PROCESSING(void)
{
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOB,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOC,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOD,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOE,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOF,ENABLE);
    RCC_AHBPeriphClockCmd(RCC_APB2Periph_GPIOG,ENABLE);
}

```





#### 5.17.2实现说明



##### 1.空闲任务进入低功耗





**空闲任务在执行释放内存等事情后，进入低功耗模式**



```c
		#if ( configUSE_TICKLESS_IDLE != 0 )
		{
		TickType_t xExpectedIdleTime;

			//获取下一个任务恢复时间，即低功耗模式持续时间。  返回0-不需要进入低功耗    返回非0 --- 低功耗维持时间
			xExpectedIdleTime = prvGetExpectedIdleTime();

            //进入低功耗时间 > 设定阈值 （防止短时间频繁进入低功耗）
			if( xExpectedIdleTime >= configEXPECTED_IDLE_TIME_BEFORE_SLEEP )
			{
				vTaskSuspendAll();
				{
					//重新计算低功耗维持时间，减少上述运行时延
					xExpectedIdleTime = prvGetExpectedIdleTime();

                    //重新判断 低功耗维持时间 是否大于 设定阈值
					if( xExpectedIdleTime >= configEXPECTED_IDLE_TIME_BEFORE_SLEEP )
					{
        
                        //进入低功耗
						portSUPPRESS_TICKS_AND_SLEEP( xExpectedIdleTime );
                        
					}
					else
					{
						mtCOVERAGE_TEST_MARKER();
					}
				}
				( void ) xTaskResumeAll();
			}
			else
			{
				mtCOVERAGE_TEST_MARKER();
			}
		}
		#endif /* configUSE_TICKLESS_IDLE */
```



###### 1.1获取下一个任务唤醒事件



```c
	static TickType_t prvGetExpectedIdleTime( void )
	{
	TickType_t xReturn;
	UBaseType_t uxHigherPriorityReadyTasks = pdFALSE;


			// uxLeastSignificantBit 表示当前空闲任务优先级最低，在32位就绪态标记位中为0x01
			const UBaseType_t uxLeastSignificantBit = ( UBaseType_t ) 0x01;
        //uxTopReadyPriority 为就绪态任务优先级标记位   当就绪任务优先级有 > 0的任务 ，返回pdTRUE ，用于下方判断
			if( uxTopReadyPriority > uxLeastSignificantBit ) 
			{
				uxHigherPriorityReadyTasks = pdTRUE;
			}
		}

		

//下列有三种情况不需要进入低功耗模式

		//tskIDLE_PRIORITY 宏为0   ，当前执行的任务 优先级是否大于0
		if( pxCurrentTCB->uxPriority > tskIDLE_PRIORITY )
		{
			xReturn = 0;
		}
		//是否有与空闲任务同等优先级 的任务 ？   判断优先级为0的列表 是否还有其他列表项>0
		else if( listCURRENT_LIST_LENGTH( &( pxReadyTasksLists[ tskIDLE_PRIORITY ] ) ) > 1 )
		{
		
			xReturn = 0;
		}
		//上述判断，就绪态有其他优先级大于0的任务
		else if( uxHigherPriorityReadyTasks != pdFALSE )
		{
			xReturn = 0;
		}



		//只有空闲任务的情况 ，进入低功耗模式
		else
		{
			xReturn = xNextTaskUnblockTime - xTickCount; //需要阻塞时间 = 下一个任务阻塞唤醒事件 - 当前系统时间
		}

		return xReturn;
	}
```





###### 2.进入低功耗模式





```c
   
//调度器开启时，会对下列值赋值
#if configUSE_TICKLESS_IDLE == 1
    {
        //一个系统节拍，定时器需要计数值
        ulTimerCountsForOneTick = ( configSYSTICK_CLOCK_HZ / configTICK_RATE_HZ );
        
        
        // portMAX_24_BIT_NUMBER-0xffffff(系统滴答定时器最大计数周期) ，这里求出定时器最大定时时间，也就是低功耗最长维持时间，高于这个时间滴答定时器没办法计数唤醒。
        xMaximumPossibleSuppressedTicks = portMAX_24_BIT_NUMBER / ulTimerCountsForOneTick;
        
        
        /*CPU运行补偿时间 ，从滴答定时器停止运行，到把统计得到的低功耗模式运行时间补偿给 FreeRTOS系统时钟，也是需要时间的，这期间也是有程序在运行的。这段程序运行的时间我们要留出来，具体的时间没法去统计，因为平台不同、编译器的代码优化水平不同导致了程序的执行时间也不同。这里只能大概的留出一个时间值，这个时间值由变量 ulStoppedTimerCompensation 来确定，这是一个全局变量。
        由freeRtos提供
*/
        ulStoppedTimerCompensation = portMISSED_COUNTS_FACTOR / ( configCPU_CLOCK_HZ / configSYSTICK_CLOCK_HZ );
    }
    #endif 




//进入低功耗函数


	__weak void vPortSuppressTicksAndSleep( TickType_t xExpectedIdleTime )
	{
	uint32_t ulReloadValue, ulCompleteTickPeriods, ulCompletedSysTickDecrements, ulSysTickCTRL;
	TickType_t xModifiableIdleTime;

		//如果低功耗时间 > 滴答定时器最大的延时时间 ，为最大延时时间。定时器计时满后立即中断唤醒。
		if( xExpectedIdleTime > xMaximumPossibleSuppressedTicks )
		{
			xExpectedIdleTime = xMaximumPossibleSuppressedTicks;
		}

	
        //系统滴答定时器 BIT0位 清零   --- 禁能滴答定时器
		portNVIC_SYSTICK_CTRL_REG &= ~portNVIC_SYSTICK_ENABLE_BIT;

		// 下一次中断计数值 = 当前计数器值 +  一个节拍需要的计数值 * 所需进入低功耗的事件 ---- 将低功耗时间转换为滴答定时器计数次数
        //由于定时器定时时间肯定不会超过  ulTimerCountsForOneTick ，这里减1。是因为下面重新计时只能从0开始
		ulReloadValue = portNVIC_SYSTICK_CURRENT_VALUE_REG + ( ulTimerCountsForOneTick * ( xExpectedIdleTime - 1UL ) );
        
        //如果 设定中断计数时间 > 补偿时间   ， 减去补偿时间。     此时因为已经运行了一段时间。
		if( ulReloadValue > ulStoppedTimerCompensation )
		{
			ulReloadValue -= ulStoppedTimerCompensation;
		}
        

		//关闭中断 ，只是中断不会执行服务函数。但是标记位还是会置位，依旧可以唤醒睡眠模式WFI。
		__disable_irq();
		__dsb( portSY_FULL_READ_WRITE );
		__isb( portSY_FULL_READ_WRITE );
        
        
        

		//eTaskConfirmSleepModeStatus函数，判断是否可以进入低功耗 （1.准备就绪列表有队列项  2. 准备启动任务调度器）
        
		if( eTaskConfirmSleepModeStatus() == eAbortSleep ) //不可进入低功耗，滴答定时器恢复最初的状态
		{
			//使能系统滴答定时器
			portNVIC_SYSTICK_LOAD_REG = portNVIC_SYSTICK_CURRENT_VALUE_REG;//获取滴答定时器地址
			portNVIC_SYSTICK_CTRL_REG |= portNVIC_SYSTICK_ENABLE_BIT;

			//重装载值 重新赋值为系统节拍
			portNVIC_SYSTICK_LOAD_REG = ulTimerCountsForOneTick - 1UL;

			__enable_irq();
		}
        
        //可以进入休眠
		else
		{
			//计数重装载值设置为。上述计算后的低功耗维持时间，
			portNVIC_SYSTICK_LOAD_REG = ulReloadValue;

			
            //当前计数值清零，
			portNVIC_SYSTICK_CURRENT_VALUE_REG = 0UL;

			///使能滴答定时器
			portNVIC_SYSTICK_CTRL_REG |= portNVIC_SYSTICK_ENABLE_BIT;


			xModifiableIdleTime = xExpectedIdleTime;
			configPRE_SLEEP_PROCESSING( xModifiableIdleTime ); //进入低功耗前的自定义函数，可以关闭时钟
			if( xModifiableIdleTime > 0 )
			{
				__dsb( portSY_FULL_READ_WRITE );
				__wfi();  //__WFI指令进入睡眠模式
				__isb( portSY_FULL_READ_WRITE );
			}
			configPOST_SLEEP_PROCESSING( xExpectedIdleTime ); //退出低功耗后的自定义函数，开启时钟

	
            
            //关闭滴答定时器
			ulSysTickCTRL = portNVIC_SYSTICK_CTRL_REG; 
			portNVIC_SYSTICK_CTRL_REG = ( ulSysTickCTRL & ~portNVIC_SYSTICK_ENABLE_BIT );

			__enable_irq(); //使能中断

            
   
    //补偿休眠时 系统节拍时间。滴答定时器唤醒 与其他中断唤醒 所补偿的事件不同
            
            
            //通过滴答定时器唤醒的休眠
			if( ( ulSysTickCTRL & portNVIC_SYSTICK_COUNT_FLAG_BIT ) != 0 )
			{
				uint32_t ulCalculatedLoadValue;


				ulCalculatedLoadValue = ( ulTimerCountsForOneTick - 1UL ) - ( ulReloadValue - portNVIC_SYSTICK_CURRENT_VALUE_REG );


				if( ( ulCalculatedLoadValue < ulStoppedTimerCompensation ) || ( ulCalculatedLoadValue > ulTimerCountsForOneTick ) )
				{
					ulCalculatedLoadValue = ( ulTimerCountsForOneTick - 1UL );
				}

				portNVIC_SYSTICK_LOAD_REG = ulCalculatedLoadValue;

				ulCompleteTickPeriods = xExpectedIdleTime - 1UL;
			}
            
            //其他中断唤醒的休眠
			else
			{

				ulCompletedSysTickDecrements = ( xExpectedIdleTime * ulTimerCountsForOneTick ) - portNVIC_SYSTICK_CURRENT_VALUE_REG;


				ulCompleteTickPeriods = ulCompletedSysTickDecrements / ulTimerCountsForOneTick;


				portNVIC_SYSTICK_LOAD_REG = ( ( ulCompleteTickPeriods + 1UL ) * ulTimerCountsForOneTick ) - ulCompletedSysTickDecrements;
			}
            
            
            
            


			portNVIC_SYSTICK_CURRENT_VALUE_REG = 0UL;
			portENTER_CRITICAL();
			{
				portNVIC_SYSTICK_CTRL_REG |= portNVIC_SYSTICK_ENABLE_BIT;
				vTaskStepTick( ulCompleteTickPeriods ); //将上述计算的补偿时间 ，添加到系统时钟中
				portNVIC_SYSTICK_LOAD_REG = ulTimerCountsForOneTick - 1UL;
			}
			portEXIT_CRITICAL();
            
            
            
            
		}
	}

```





### 5.18内存管理





#### 5.48.1介绍



- 创建任务、队列、信号量等对象时，需要内存空间。使用**动态或者静态**的方式创建
- 动态创建更灵活

![image-20250511125319044](./嵌入式软件学习.assets/image-20250511125319044.png)



**C库函数的缺点：**

- 占用大量代码空间，freertos为轻量级操作系统。
- 没有线程相关安全机制
- 运行不确定，运行时间不一致
- 内存碎片化，在多次申请和释放后，内存空间不连续，碎片化使用

![image-20250511130344582](./嵌入式软件学习.assets/image-20250511130344582.png)







##### 1.内存管理方式

共有五种内存管理算法



![image-20250511131354973](./嵌入式软件学习.assets/image-20250511131354973.png)





###### 1.heap1 - 只能申请



- **只能申请内存，不能释放内存**，实现方法比较简单从数组中分出合适大小的空间
- **使用工况：只需要分配内存，无需释放**

![image-20250511132141934](./嵌入式软件学习.assets/image-20250511132141934.png)

![image-20250511132407494](./嵌入式软件学习.assets/image-20250511132407494.png)







###### 2.heap2 - 无法合并，产生碎片



- 采用**最适应算法申请内存**，并支持释放内存
- **不可合并相邻内存块**，多次申请释放后产生很多**内存碎片**。

![image-20250511132843989](./嵌入式软件学习.assets/image-20250511132843989.png)



**最适宜算法**



- **找到最小可以满足需要的内存块**，内存块**按照大小排列**
- 例如下列举例，找到25，将其划分为20 和 5 ，返回20 。5用于其他的内存申请使用。
- 多次划分后，会产生许多内存碎片。

![image-20250511133123103](./嵌入式软件学习.assets/image-20250511133123103.png)



**内存碎片**

- 在多次申请和释放后，由于无法合并内空间，产生很多**内存碎片，后续需要申请大的连续内存无法申请**。
- **在申请创建时，所需的内存空间一致时。不会产生内存碎片化。**

![image-20250511133358749](./嵌入式软件学习.assets/image-20250511133358749.png)



###### 3.heap3



与C库一样





###### 4.heap4 合并内存块





- 采用首次适应算法：内存块**按照地址排序**，**由低地址先高地址寻找第一个**满足大小的内存块。

![image-20250511134654886](./嵌入式软件学习.assets/image-20250511134654886.png)



**内存碎片合并**

![image-20250511134938329](./嵌入式软件学习.assets/image-20250511134938329.png)













###### 5.heap5 管理多块内存块



- 在有些系统中，外扩SRAM模块，需要使用heap5管理多块非连续空间。



![image-20250511135051007](./嵌入式软件学习.assets/image-20250511135051007.png)



![image-20250511135202563](./嵌入式软件学习.assets/image-20250511135202563.png)





#### 5.30韦东山快速入门视频









## 六、芯片使用



### 6.1 MPU6050

![image-20250310205005912](./嵌入式软件学习.assets/image-20250310205005912.png)





- 量化范围一定，根据满量程来确定分辨率。例如：满量程为正负2g时，分辨率为 2/32767 
- AD0是芯片一个引脚，用来灵活配置芯片的设备地址。

![image-20250310205704368](./嵌入式软件学习.assets/image-20250310205704368.png)









### 6.2 电源反激2844















## 	七、算法实现



### 7.1PID



#### 7.1.1PID介绍



 **p是控制现在，i是纠正曾经，d是管控未来**



- P-比例 ，I-积分 	D-微分
- **PID基于误差控制**，始终目标为使误差调整到零。使得目标值 = 实际值

![image-20250318215817242](./嵌入式软件学习.assets/image-20250318215817242.png)



开环闭环控制

![image-20250318220540625](./嵌入式软件学习.assets/image-20250318220540625.png)





一般使用这个公式<img src="./嵌入式软件学习.assets/image-20250318221127620.png" alt="image-20250318221127620" style="zoom:50%;" />

![image-20250318220726792](./嵌入式软件学习.assets/image-20250318220726792.png)





#### 7.2.1KP调节

- 只有比例项：**KP小响应慢，KP大响应快但是容易出现超调**

- **KP越大，稳态误差越小**。 外力平衡输出 = KP * 稳态误差 ，由于外力平衡输出一定，KP越大则稳态误差减小。

  

![image-20250318221409308](./嵌入式软件学习.assets/image-20250318221409308.png)



- KP越大越容易超调，但是稳态误差越小

![image-20250318221822653](./嵌入式软件学习.assets/image-20250318221822653.png)

#####  

##### 稳态误差

- 无干扰的系统，比例控制即可已完成控制，但是现实系统中存在各种干扰，**这种干扰就导致KP控制存在稳态误差**。
- 当**系统KP补偿 == 外部干扰时，误差不变，KP补偿恒定 = 外部干扰** 。



**简单接受KP控制稳态误差**

比例控制算法

我们先说PID中最简单的比例控制，还是用一个经典的例子吧。假设我有一个水缸，最终的控制目的是要保证水缸里的水位永远的维持在1米的高度。假设初始时刻，水缸里的水位是0.2米，那么当前时刻的水位和目标水位之间是存在一个误差的error，且error为0.8.这个时候，假设旁边站着一个人，这个人通过往缸里加水的方式来控制水位。如果单纯的用比例控制算法，就是指加入的水量u和误差error是成正比的。即u=kp * error

假设kp取0.5，

- 那么t=1时（表示第1次加水，也就是第一次对系统施加控制），那么u=0.5*0.8=0.4，所以这一次加入的水量会使水位在0.2的基础上上升0.4，达到0.6.
- 接着，t=2时刻（第2次施加控制），当前水位是0.6，所以error是0.4。u=0.5*0.4=0.2，会使水位再次上升0.2，达到0.8.
- 如此这么循环下去，就是比例控制算法的运行方法。
- 可以看到，最终水位会达到我们需要的1米。



但是，单单的比例控制存在着一些不足，其中一点就是 –**稳态误差！**（我也是看了很多，并且想了好久才想通什么是稳态误差以及为什么有稳态误差）。

像上述的例子，根据kp取值不同，系统最后都会达到1米，只不过kp大了到达的快，kp小了到达的慢一些。不会有稳态误差。但是，考虑另外一种情况，假设这个水缸在加水的过程中，存在漏水的情况，假设每次加水的过程，都会漏掉0.1米高度的水。仍然假设kp取0.5，那么会存在着某种情况，假设经过几次加水，水缸中的水位到0.8时，水位将不会再变换！！！因为，水位为0.8，则误差error=0.2. 所以每次往水缸中加水的量为u=0.50.2=0.1.同时，每次加水，缸里又会流出去0.1米的水！！！**加入的水和流出的水相抵消，水位将不再变化！！**

也就是说，我的目标是1米，但是最后系统达到0.8米的水位就不再变化了**，且系统已经达到稳定。由此产生的误差就是稳态误差了**。



##### 7.2.2KI控制



- 从程序开始就开始累计误差和，当KP有稳态误差时，KI一直有误差产生驱动力去消除稳态误差。
- 当误差为0时，由于历史误差的原因。KP*err = 0 ,但是 **KI * 历史误差 可以持续输出 抵消 系统外部干扰**。保持误差为0
- KI越大，稳态误差消失越快。但是会产生滞后性：![image-20250319195750063](./嵌入式软件学习.assets/image-20250319195750063.png)红色为系统输出，当输出方向为负时，需要一定时间将正向误差抵消掉，相比KP是实时变化的。

![image-20250319195010300](./嵌入式软件学习.assets/image-20250319195010300.png)

积分控制算法
还是用上面的例子，如果仅仅用比例，可以发现存在暂态误差，最后的水位就卡在0.8了。于是，在控制中，我们再引入一个分量，该分量和误差的积分是正比关系。所以，比例+积分控制算法为：
u=kp*error+ ki∗∫ error
还是用上面的例子来说明，第一次的误差error是0.8，第二次的误差是0.4，至此，误差的积分（离散情况下积分其实就是做累加），∫error=0.8+0.4=1.2. 这个时候的控制量，除了比例的那一部分，还有一部分就是一个系数ki乘以这个积分项。由于这个积分项会将前面若干次的误差进行累计，所以可以很好的消除稳态误差（假设在仅有比例项的情况下，系统卡在稳态误差了，即上例中的0.8，由于加入了积分项的存在，会让输入增大，从而使得水缸的水位可以大于0.8，渐渐到达目标的1.0.）这就是积分项的作用。







![image-20250319200421190](./嵌入式软件学习.assets/image-20250319200421190.png)



KP过大产生超调和震荡

![image-20250319200443078](./嵌入式软件学习.assets/image-20250319200443078.png)





#### 7.2.3KD控制



- **KD控制取决于当前误差变化，不根据当前误差大小**。 当前误差恒定100不变化，KD也不变化为0
- 产生反作用阻尼，抑制震荡，防止超调。 尤其是惯性比较大的系统
- KD过大，会对本身KPKI产生的变化产生阻碍，出现卡顿现象。

![image-20250319200918458](./嵌入式软件学习.assets/image-20250319200918458.png)





当出现途中上部分这种情况时，实际值在靠近目标值时并未靠近目标值，而是直接扎进去，需要加入KD调节，增加阻尼。<img src="./嵌入式软件学习.assets/image-20250319202037376.png" alt="image-20250319202037376" style="zoom:50%;" />增加阻尼后，由左图变为右图。

![image-20250319201802354](./嵌入式软件学习.assets/image-20250319201802354.png)



- 倒立摆实验中，没有引入KD，系统拉回震荡，当加入合适的KD可以有效避免来回震荡
- 可以观察到，当KD=0时，实际值接近目标值时，速率越来越快，猛地扎进去
- KD合适时，当实际值接近目标值时，缓慢靠近。

![image-20250515231347006](./嵌入式软件学习.assets/image-20250515231347006.png)

#### 7.2.4离散化PID





- PID诞生于模拟电路时代，根据微分与积分电路实现PID。由于进入数字时代，需要离散化执行。
- 将T调控周期融入系数中，**当改变调控周期时需要调节KI/KD系数**

![image-20250319204221587](./嵌入式软件学习.assets/image-20250319204221587.png)







- 积分离散化：将曲线面积离散为矩形面积
- 微分离散化：将某点的斜率近似   与上一点差 / T
- T越小，离散化越接近连续

![image-20250319204409483](./嵌入式软件学习.assets/image-20250319204409483.png)



##### 7.2.5位置式与增量式PID

- 位置式PID也叫全量式PID，使用与各种场合
- 当K - (k-1) 变为增量式PID

![ ](./嵌入式软件学习.assets/image-20250319205252153.png)



- 位置式PID：直接输出给被控对象，例如直接输出95%速度
- 增量式PID：**只能输出增量，比如在上次基础上增加5%速度。当在一些具有累加变化的场景时可以使用增量式PID，例如电磁阀，步进电机，其一直保持上一时刻位置，可以使用变化PID。   2.也可以：对上一次输出进行记录：OUT ，本次输出 = 上次OUT+ 本次变化OUT，直接作用给被控对象实现全量控制。**

![image-20250319210031755](./嵌入式软件学习.assets/image-20250319210031755.png)



##### 7.2.6软件实现PID



##### 1.确定调控周期T

- 根据被控对象变化时间，例如无人机控制需要达到ms级，热水温度变化缓慢可以10s调一次。
- 根据数据更新频率来调控时间T
- 如何实现精确地T控制呢 ： **汇川技术使用系统滴答定时器，实现0.5ms与2ms函数。在程序设计中采用非阻塞式等待滤波。**



![image-20250319210746761](./嵌入式软件学习.assets/image-20250319210746761.png)



##### 2.代码实现

![image-20250319212246520](./嵌入式软件学习.assets/image-20250319212246520.png)

  

- 控制器内保留上一次的全量输出值，这样根据本次变化值也能得出本次全量输出值。
- 增量式PID使用：1.如果被控对象可以保持上一时刻的状态，可以将增量直接输出        2.如果被控对象无法接受增量，可以对输出值进行积分，将计算后的增量直接加到上一次输出的全量中。

![image-20250319212436599](./嵌入式软件学习.assets/image-20250319212436599.png)





#### 7.7.7调节PID

##### 1.关于参数量级

有的KP/KI/KD为 0.1，有的为100，关于这个量级怎么确定

- 给出最大目标速度后，实际速度最大在±180 <img src="./嵌入式软件学习.assets/image-20250521205251766.png" alt="image-20250521205251766" style="zoom:50%;" />，此时OUT驱动为100满驱动但是实际速度无法上升。
- 由于 OUT = KP * ERR  --- KP的数量级 = OUT/180 = 0.x ，数量级为小数点后1位



##### 2.具体调参经验

**按照KP、KI、KD 顺序依次调参**

- 先加大调节KP，会存在稳态误差，KP越大稳态误差越小，但是无法避免稳态误差。KP太大会有剧烈抖动<img src="./嵌入式软件学习.assets/image-20250521212221873.png" alt="image-20250521212221873" style="zoom:33%;" />。
- **调节KP，出现抖动，在逐步调小KP抖动消失，此时KP确定**
- 调节KI消除稳态误差，调节目标值观察实际值是否紧贴目标值，有无迟滞
- KD查看实际值在靠近目标值时有无  **扎进去的趋势** ，没有的话为0，有时候加了会有卡顿现象

![image-20250521212634714](./嵌入式软件学习.assets/image-20250521212634714.png)











#### 7.2.8定速控制

- 尽量确保编码器采样周期 与 PID 调控周期一致
- 速度单位 与 PWM 单位不一致 ，可以转换一下





##### 1.位置式PID

```c
float Target, Actual, Out;			//目标值，实际值，输出值
float Kp, Ki, Kd;					//比例项，积分项，微分项的权重
float Error0, Error1, ErrorInt;		//本次误差，上次误差，误差积分

void Main_2msFunctionB(void)
{
    Lg_OtherMainDeal() ;
    Kp = 0.5 ;
    Ki = 0.2 ;
    Kd = 0.1 ;


    static uint16_t uc_timerrncoder = 0 ;
    Target = 40 ;


    uc_timerrncoder += 2 ;
    if (uc_timerrncoder >= 40 ) //每40ms调控一次
    {
        uc_timerrncoder = 0 ;
        Actual = (float )Encoder_Get() ; //获取编码器实际值


        Error1 = Error0;			//获取上次误差
        Error0 = Target - Actual;	//获取本次误差，目标值减实际值，即为误差值

        ErrorInt += Error0; //累计积分

        Out = Kp * Error0 + Ki * ErrorInt + Kd * (Error0 - Error1);

                /*输出限幅*/
        if (Out > 100) {Out = 100;}		//限制输出值最大为100
        if (Out < -100) {Out = -100;}	//限制输出值最小为100

        
        Phy_SetPwmSize(Out) ;

    }
    

    

}
```





##### 2.增量式PID

- 没有积分饱和问题 
- 当KPKIKD为0时，△OUT=0，保持上一时刻。

```c
        Error2 = Error1;
        Error1 = Error0;			//获取上次误差
        Error0 = Target - Actual;	//获取本次误差，目标值减实际值，即为误差值

       

        TOut = Kp * (Error0 -Error1) + Ki * Error0 +  Kd * (Error0 + Error2 - 2*Error1); 

        Out += TOut ;

                /*输出限幅*/
        if (Out > 100) {Out = 100;}		//限制输出值最大为100
        if (Out < -100) {Out = -100;}	//限制输出值最小为100

        
        Phy_SetPwmSize(Out) ;
```









#### 7.2.9定位置控制

- 编码器是增量的 但是stm32的编码器接口是绝对式的。可以定时采集增量累加起来，位置变成绝对式的
- 未使用KI，会引起震荡
- 使用KD，超调时可以抑制尖峰，波形更平稳。<img src="./嵌入式软件学习.assets/image-20250521230402058.png" alt="image-20250521230402058" style="zoom: 67%;" />防止出现蓝线尖波，导致震荡

```c
float Target, Actual, Out,TOut;			//目标值，实际值，输出值
float Kp, Ki, Kd;					//比例项，积分项，微分项的权重
float Error0, Error1, Error2, ErrorInt;		//本次误差，上次误差，误差积分

void Main_2msFunctionB(void)
{
    Lg_OtherMainDeal() ;
    Kp = 0.2 ;
    Ki = 0;
    Kd = 0.3;


    static uint16_t uc_timerrncoder = 0 ;
    static uint16_t uc_timertarget = 0 ;
    

    uc_timertarget += 2 ;
    if (uc_timertarget >= 2000 )
    {
        uc_timertarget = 0 ;
        Target += 200  ;
        if (Target >=1500)
        {
            Target = 0 ;
        }
        
    } 

    


    uc_timerrncoder += 2 ;
    if (uc_timerrncoder >= 40 )
    {
        uc_timerrncoder = 0 ;
        Actual += (float )Encoder_Get() ;


        Error1 = Error0;			//获取上次误差
        Error0 = Target - Actual;	//获取本次误差，目标值减实际值，即为误差值

        ErrorInt += Error0;

        Out = Kp * Error0 + Ki * ErrorInt + Kd * (Error0 - Error1);

                /*输出限幅*/
        if (Out > 100) {Out = 100;}		//限制输出值最大为100
        if (Out < -100) {Out = -100;}	//限制输出值最小为100

        
        Phy_SetPwmSize(Out) ;
        
    }
```









#### 7.2.10PID优化

![image-20250521231404724](./嵌入式软件学习.assets/image-20250521231404724.png)



##### 1.积分项优化



###### 1.1 积分限幅

- 如果执行器不能消除误差，导致**积分一直累计**。当执行器恢复到正常时，由于**积分项比较大，电机会一直维持满速**运行知道将积分项拉回正常

![image-20250522213824515](./嵌入式软件学习.assets/image-20250522213824515.png)

![image-20250522214300481](./嵌入式软件学习.assets/image-20250522214300481.png)



**实现**



- 对误差积分进行限幅，误差累计到一定程度不进行累计，幅值可以测试观察正常积分项的变化范围，加入一些余量。
- 对误差积分项进行限幅，**好处是和输出项OUT时一个数量级**，积分限幅值可以保持和输出限幅一致，例如输出限幅±100，那么积分项限幅也可以是±100

![image-20250522215311066](./嵌入式软件学习.assets/image-20250522215311066.png)

![image-20250522220457280](./嵌入式软件学习.assets/image-20250522220457280.png)



###### 





###### 1.2积分分离

**积分分离 --- 改善I项导致的超调问题**

- **积分限一般在调控后期起作用**，例如前期靠KP项来缩减误差，后期靠积分项来抵消稳态误差
- 调控前期一般误差较大不需要积分项作用，前期使用就KI可能导致前期积分项一直累加，到后期起作用时力度太大导致超调。
- 定位置控制前期累计过大误差，当到达位置后无需外力保持位置，但是过大的KI会导致超调需要反向力来消除这部分KI
- **积分分离：**     例如在**定位置控制时，在最后需要一点KI来减小静态误差。实现思路就是：误差绝对值小于阈值在引入KI，否则误差清零**
- **也就是在例如定位置控制时。前期使用PD控制，后期误差小时引入PID控制**

![image-20250522220734516](./嵌入式软件学习.assets/image-20250522220734516.png)





- **定速控制使用KI控制**，由于达到稳态后，需要**KI一直保持力来抵消摩擦力**
- **定位置控制使用KD控制**，当保持位置**无需外力，此时KI会导致超调**



<img src="./嵌入式软件学习.assets/image-20250522222808461.png" alt="image-20250522222808461" style="zoom:200%;" />

<img src="./嵌入式软件学习.assets/image-20250522221351272.png" alt="image-20250522221351272" style="zoom:50%;" />



**实现**

- 实现1：当误差值小于一定值后，误差才累加；一旦误差大于阈值对积分清零。 阈值例如在定位置控制时，一般稳态误差的取值，进入KI来抵消稳态误差。![image-20250522224930804](./嵌入式软件学习.assets/image-20250522224930804.png)
- 实现2，阈积分过大，直接将KI想分离出去，但是积分还是还会累加，可以清零累加值。

![image-20250522223941732](./嵌入式软件学习.assets/image-20250522223941732.png)



引入分离KI的方式，积分项在调节后期起作用，后期误差比较小不会超调，并且在外部小规模扰动也可以使用KI项来抵消。

![image-20250522225102965](./嵌入式软件学习.assets/image-20250522225102965.png)





###### 1.3变速积分



在**积分分离中****，当处于误差为零时，积分项可以抵消小扰动的干扰。但是一旦扰动大于设定阈值，积分项失去作用。只有KP输出恒定的力。**



- **变速积分：KI可变，误差越大KI越小**，KI不会发生突变，但是在**误差较大的前期，KI项的作用也被削弱**

![image-20250522225815233](./嵌入式软件学习.assets/image-20250522225815233.png)





- 积分分离：阶跃性
- 线性变速积分
- 非线性变速积分

![image-20250522225953922](./嵌入式软件学习.assets/image-20250522225953922.png)





- **调节误差累计的速度**：根据误差大小调节误差累计的速度，来控制误差积分的大小
- **调节KI项的作用力**，根据误差大小调节KI项的作用力

![image-20250522230412867](./嵌入式软件学习.assets/image-20250522230412867.png)









##### 2.微分项优化



###### 1.1微分先行---防止目标值突变时，KD项输出一个较大的正向力



- 在目标值突变时，由于采样T的存在，会有一个正向的非常大的力类似于KP作用推实际值靠近目标值，随后由反向输出反作用力，阻碍突变
- **在目标值发生突变的瞬间，KD也会突变施加正向力**

![image-20250523230109228](./嵌入式软件学习.assets/image-20250523230109228.png)





- 对误差积分改为对实际值微分，**但是需要加一个负号。因为实际值变大，KD需要输出一个负向的调控力**
- 

![image-20250523231228978](./嵌入式软件学习.assets/image-20250523231228978.png)



![image-20250523231438731](./嵌入式软件学习.assets/image-20250523231438731.png)









###### 2.不完全微分 --- 当实际值采样有噪声，加入KD项低通滤波



- 其实可以直接对实际采样值直接加入低通滤波，但是因为低通滤波会有滞后，影响系统实时性

- 这里只对KD项加入低通滤波，因为KD项受干扰最大

-  一阶滤波算法是比较常用的滤波算法，它的**滤波结果=a*本次采样值+（1-a）*上次滤波结果**，其中，a为0~1之间的数。一阶滤波相当于是将新的采样值与上次的滤波结果计算一个加权平均值。a的取值决定了算法的灵敏度，a越大，新采集的值占的权重越大，算法越灵敏，但平顺性差；相反，a越小，新采集的值占的权重越小，灵敏度差，但平顺性好。

  优点：对周期干扰有良好的抑制作用，适用于波动频率比较高的场合，它不用记录历史数据。
  缺点：滞后、灵敏度低。

```c
float final=0;
float a=0.25;
 
float firstOrderFilter(float in_data)
{
    //本次输出 = a * 本次输入 + （1-a）*上次输出
	final = a*in_data + (1-a)*final;	
	return(final);
}
```

 ![image-20250523233209714](./嵌入式软件学习.assets/image-20250523233209714.png)



![image-20250523233144581](./嵌入式软件学习.assets/image-20250523233144581.png)







##### 3.输出偏移&输入死区



###### 3.1输出偏移



- 在定位置控制中，使用PD控制，存在静态误差。由于存在误差KP一直有作用力，**但是驱动力太小，电机无法对抗摩擦力完成旋转。但是持续的小电流输出也会有能量损耗，造成发热**

![image-20250524095411711](./嵌入式软件学习.assets/image-20250524095411711.png)



- 输出偏移：例如测试发现电机在占空比为5%以下无驱动力。**可以直接跳过5%以下的输出。** **驱动器直接跳过无动作阶段**
- 当输出为0时，依旧为0
- 输出有值，加入固定偏移。   这个固定偏移测试电机旋转最小的输出力度。  

![image-20250524095838859](./嵌入式软件学习.assets/image-20250524095838859.png)





- 可以将区间放大点，比如在±1内 为0 。


![image-20250524100342923](./嵌入式软件学习.assets/image-20250524100342923.png)





###### 3.2输入死区



- 在上述输出偏移中，只要有误差，电机就会动。但是由于微小波动造成电机一直来回调节，不能稳定下来。
- **当误差稳定在一个合适区间内，不进行调控**。

![image-20250524101008836](./嵌入式软件学习.assets/image-20250524101008836.png)

![image-20250524101505080](./嵌入式软件学习.assets/image-20250524101505080.png)







#### 7.2.11 多级串级PID



##### 1.简介

- 单环PID只能控制一个物理量，多环PID可以控制多个物理量。在性能上串级PID提高系统的性能。
- 当需要对一个被控对象，控制其多个物理量时就需要使用串级PID。 例如需要以一定速度，停在一定位置。
- 
- **外环的输入作为内环的输入**



![image-20250524102339126](./嵌入式软件学习.assets/image-20250524102339126.png)





##### 2.解析

- 单环PD定位置控制器：在外部干扰时，由于误差固定,KP输出固定，对抗外力的输出力比较小。目标是位置确定下来后“稳如泰山”，当外部干扰时，可以快速输出力来对抗。

![image-20250524104635793](./嵌入式软件学习.assets/image-20250524104635793.png)



- 将内环看做为一个整体，只要有目标速度，PI控制器就会驱动电机运行到目标速度。抽象成一个输入多少速度就运行到多少速度的电机，可以自适应负载。
- 将单环位置环输出PWM的开环控制![image-20250524105830652](./嵌入式软件学习.assets/image-20250524105830652.png)，看做输出速度的闭环控制，整体更稳定 。对于位置环没有变化，只是输出控制电机的PWM开环控制，变成输出速度，电机自动执行到对应速度。

![image-20250524105346898](./嵌入式软件学习.assets/image-20250524105346898.png)



###### 2.1动态分析



**启动**

- 当静止时，实际速度与实际位置都为0，输出为0。当给定目标速度为100时，输出速度为50，速度环输出PWM控制电机旋转提速。
- 电机运行，实际位置增加，速度增加。当到带目标位置时，输出速度为0，输出环控制PWM也为0，电机停止。

![image-20250524110243143](./嵌入式软件学习.assets/image-20250524110243143.png)



**抗外部干扰**



- 当目标位置和实际位置都为100时，输出速度以及速度环输出PWM都为0
- 当外部负向力，产生速度，速度环首先反应输出负向PWM控制电机
- 由于外部负向力改变实际位置，产生正向位置，输出速度为负值，加大速度环负调节。



![image-20250524110913445](./嵌入式软件学习.assets/image-20250524110913445.png)



##### 3.程序设计

- 主函数给出外环目标值

![image-20250524113235665](./嵌入式软件学习.assets/image-20250524113235665.png)





- **内外环调控周期可以不一样。 外环调控周期 >= 内环调控周期**， 内环一般控制反应更快的物理量有更高的反应频率。
- 内外环**读取的实际值物理量不同**，内环速度，外环绝对位置
- **外环输出作用于内环输入，内环输出作用于执行器**

![image-20250524113450232](./嵌入式软件学习.assets/image-20250524113450232.png)















##### 4.调参

**先调节内环参数(此时不然外环工作，因为内环可以单独运行)，再调节外环参数（打开内环）。**















### 7.2软件缓存



##### 1.环形队列

[环形队列原理与实现-CSDN博客](https://blog.csdn.net/sking002007/article/details/6584590?spm=1001.2101.3001.6650.8&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-8-6584590-blog-134026253.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-8-6584590-blog-134026253.235^v43^pc_blog_bottom_relevance_base3)





























## 八、M3内核





​	





## 九、电机控制方法



#### 9.1-FOC

##### 9.1.1无刷电机基本概念

- 通过改变电流交变频率 、 波

![image-20250503172956149](./嵌入式软件学习.assets/image-20250503172956149.png)



#### 9.2BLDC







#### 9.3SPWM





#### 9.4电机控制学习路径



[电机控制入门学习 - 知乎](https://zhuanlan.zhihu.com/p/231322308?login=from_csdn)



[电机控制个人学习路线及专业书籍资源分享_电机控制学习路线-CSDN博客](https://blog.csdn.net/weixin_45182459/article/details/136909991?spm=1001.2014.3001.5506)



[电机控制入门——学习路线规划以及学习书籍推荐_吮指原味张-2048 AI社区](https://2048ai.net/682be08e606a8318e8579fcf.html?dp_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpZCI6MjA3MjM3NywiZXhwIjoxNzU0NzQ4MTg0LCJpYXQiOjE3NTQxNDMzODQsInVzZXJuYW1lIjoibTBfNDc5NTE3OTQifQ.PepJl21K8fjra0KO7wrL73uRfXM2TsYFE62AS4kiUIk&spm=1001.2101.3001.6650.2&utm_medium=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~activity-2-112914373-blog-136909991.235^v43^pc_blog_bottom_relevance_base3&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~activity-2-112914373-blog-136909991.235^v43^pc_blog_bottom_relevance_base3)



## 十、电池相关知识



### 10.1充放电





### 10.2SOC



### 10.3双向BUCK-BOOST





## 十一、汽车相关

### 11.1ARTOSAR





## 十二、Zephyr学习

- 聆思科技：[欢迎使用 | 聆思文档中心](https://docs2.listenai.com/x/T7H8NYpx58)
- [Zephyr 入门，看这篇就够了(含FreeRTOS对比)-CSDN博客](https://blog.csdn.net/2201_75889983/article/details/129366754)
- [Zephyr Project 学习文档 — Zephyr 项目中文文档](https://zephyr-doc.readthedocs.io/zh-cn/latest/)
- 官方文档 ： [简介 — Zephyr 项目文档](https://docs.zephyrproject.org/latest/introduction/index.html)
- [理解Zephyr编译与配置系统 - jayant97 - 博客园](https://www.cnblogs.com/jayant97/articles/17794813.html)
- [全网最全学习Zephyr开发教程资料汇总-从基础文档视频到上手实操示例 - FreeRTOS & Zephyr & RTEMS & Contiki - 硬汉嵌入式论坛 - Powered by Discuz!](https://forum.anfulai.cn/forum.php?mod=viewthread&tid=128694)
- [Zephy移植到STM32 - 韦东山嵌入式开发者社区](https://100ask.net/article/906)
- [11.zephyr自定义板子、设备树和SOC - 《zephyr 中文文档 - 帮助手册 - 教程》 - 极客文档](https://geekdaxue.co/read/embedded.net@bi5t7v/93b5bf33148b1c073203356d59d7a5e4)
- [nRF Connect SDK安装与入门 - Nordic Semiconductor中文官网](https://www.nordicsemi.cn/blog/nrf-connect-sdk-guide/)

![image-20250808094850167](./嵌入式软件学习.assets/image-20250808094850167.png)



### 1.源码目录

![image-20250808101828723](./嵌入式软件学习.assets/image-20250808101828723.png)



![image-20250808101205769](./嵌入式软件学习.assets/image-20250808101205769.png)



![image-20250808101305207](./嵌入式软件学习.assets/image-20250808101305207.png)



![image-20250808101638563](./嵌入式软件学习.assets/image-20250808101638563.png)



示例参考目录

![image-20250808101756644](./嵌入式软件学习.assets/image-20250808101756644.png)





### 2.内核特性





#### 2.1构建系统

[理解Zephyr编译与配置系统 - jayant97 - 博客园](https://www.cnblogs.com/jayant97/articles/17794813.html)





![image-20250808102759008](./嵌入式软件学习.assets/image-20250808102759008.png)



![image-20250808102735738](./嵌入式软件学习.assets/image-20250808102735738.png)





#### 2.2设备驱动模型

- zephyr为所有的外设驱动提供统一的API接口，芯片原厂为这些接口提供自己的实现。用户使用时直接使用官方的API接口实现即可
- 由于同一了系统层面的API接口，不同芯片厂家提供自身的API实现方式，使得上层代码在不同的硬件平台可以随意移植。
- 
- 

![image-20250808104526632](./嵌入式软件学习.assets/image-20250808104526632.png)







设备外设都是在同一个地方进行初始化，提供了宏用于注册初始化函数。设备初始化函数会在main函数执行前运行。

![image-20250808104951829](./嵌入式软件学习.assets/image-20250808104951829.png)



![image-20250808115230943](./嵌入式软件学习.assets/image-20250808115230943.png)



![image-20250808115252946](./嵌入式软件学习.assets/image-20250808115252946.png)



#### 2.3RTOS内核：线程与调度

-   z允许在编译期进行线程创建（可以像创建一个变量一样创建一个任务），F支持在运行时进行创建
- Z**同时**支持协助式与抢占式线程（可共存），F只支持选择其一
- <img src="./嵌入式软件学习.assets/image-20250808150828158.png" alt="image-20250808150828158" style="zoom: 33%;" />协作式线程不允许任务抢占，省去很多上下文切换导致的消耗，线程天然互斥，减少很多锁的使用。缺点是会一直占用CPU
- 

![image-20250808145956800](./嵌入式软件学习.assets/image-20250808145956800.png)



#### 2.4内存管理



- M-Heaps有点类似与H4.c内存管理方式，合并相邻内存块减少内存碎片
- M-Slabs 分配固定大小的内存块，分配速度会更快。

![image-20250808151344394](./嵌入式软件学习.assets/image-20250808151344394.png)



### 3.设备树

[详解Zephyr设备树（DeviceTree）与驱动模型 - jayant97 - 博客园](https://www.cnblogs.com/jayant97/articles/17209392.html)



#### 3.1设备树简介

- 设备树是一种描述特定硬件的数据结构，以便于操作系统更高的使用和管理硬件外设，它是一种树状的形态结构。
- 树状结构，从根节点可以遍历所有的外设，![image-20250808153007368](./嵌入式软件学习.assets/image-20250808153007368.png)外设类，![image-20250808153023898](./嵌入式软件学习.assets/image-20250808153023898.png)具体的实例化对象。

![image-20250808152856087](./嵌入式软件学习.assets/image-20250808152856087.png)



- 在Linux2.0时，驱动代码和设备信息被统一编译连接到内核中。3.0后驱动与设备信息被分开。
- **驱动和设备是独立编译出的两个文件，实现了驱动driver和外设device解耦**。引用设备树之后，**驱动代码只负责处理驱动的逻辑**，**关于设备的信息（硬件信息）放在设备树文件中**，如果只是硬件接口发生变化，只需要修改设备树和修改对应的硬件设备，而不需要修改驱动代码。

![image-20250808153344293](./嵌入式软件学习.assets/image-20250808153344293.png)





- Z并不会单独的类似与Linux编译一个dtb的文件，在运行过程中动态加载解析dtb文件。而是在**编译期间直接写死设备信息**。
- 完成驱动与硬件设备的解耦
- 

![image-20250808154210073](./嵌入式软件学习.assets/image-20250808154210073.png)





![image-20250808162440742](./嵌入式软件学习.assets/image-20250808162440742.png)



#### 3.2设备树如何参与编译

![image-20250808155001398](./嵌入式软件学习.assets/image-20250808155001398.png)



![image-20250808155011510](./嵌入式软件学习.assets/image-20250808155011510.png)

![image-20250808155053129](./嵌入式软件学习.assets/image-20250808155053129.png)



![image-20250808161042247](./嵌入式软件学习.assets/image-20250808161042247.png)

![image-20250808161317468](./嵌入式软件学习.assets/image-20250808161317468.png)





![image-20250808161621100](./嵌入式软件学习.assets/image-20250808161621100.png)



#### 3.3设备树的结构与语法

- 层次：先**总线**主从，再**硬件包含**关系
- ds3231为I2C总线，挂载在总线下面。LED等为硬件设备挂在根节点下。
- 

![image-20250808163812754](./嵌入式软件学习.assets/image-20250808163812754.png)



![image-20250808164124347](./嵌入式软件学习.assets/image-20250808164124347.png)



![image-20250808164428822](./嵌入式软件学习.assets/image-20250808164428822.png)



- 引用其他dts，复用
- 引用C文件，这样可以使用文件的枚举和宏值

![image-20250808164956865](./嵌入式软件学习.assets/image-20250808164956865.png)





- 用户不需要编写dts,只需要在自己工程中通过overlay覆盖开发板配置即可
- 

![image-20250808165244830](./嵌入式软件学习.assets/image-20250808165244830.png)



- 代码内访问设备信息本质是访问头文件。

![image-20250808165501540](./嵌入式软件学习.assets/image-20250808165501540.png)

![image-20250808165644000](./嵌入式软件学习.assets/image-20250808165644000.png)



![image-20250808171648957](./嵌入式软件学习.assets/image-20250808171648957.png)



#### 3.4简单配置硬件信息



#### 3.5在C代码中访问设备节点ID



### 4.搭建Windows开发环境



- [Zephyr开发环境搭建--基于Windows_zephyr编译出的bin文件,-CSDN博客](https://blog.csdn.net/weixin_41289611/article/details/133939954?ops_request_misc=elastic_search_misc&request_id=ceb75be7f7f96f1bab19c40d6bb48f2d&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~baidu_landing_v2~default-4-133939954-null-null.142^v102^pc_search_result_base9&utm_term=zephyr开发环境&spm=1018.2226.3001.4187)
- [Zephyr笔记1 - 在Windows下搭建开发环境_3df zephyr 如何使用windows命令行操作-CSDN博客](https://blog.csdn.net/jackailson/article/details/106029896?ops_request_misc=elastic_search_misc&request_id=ceb75be7f7f96f1bab19c40d6bb48f2d&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~ElasticSearch~search_v2-17-106029896-null-null.142^v102^pc_search_result_base9&utm_term=zephyr开发环境&spm=1018.2226.3001.4187)
- [Zephyr笔记1 - 在Windows下搭建开发环境 | 说好一起走](http://www.only2fire.com/archives/117.html)
- [在 Windows 下设置开发环境 — Zephyr 项目中文文档](https://zephyr-doc.readthedocs.io/zh-cn/latest/getting_started/installation_win.html)



#### 4.1安装Choncolatey

**powershell** 管理员下执行如下命令

```
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```



**推荐离线安装**

[Zephyr笔记1 - 在Windows下搭建开发环境_3df zephyr 如何使用windows命令行操作-CSDN博客](https://blog.csdn.net/jackailson/article/details/106029896?ops_request_misc=elastic_search_misc&request_id=ceb75be7f7f96f1bab19c40d6bb48f2d&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~ElasticSearch~search_v2-17-106029896-null-null.142^v102^pc_search_result_base9&utm_term=zephyr开发环境&spm=1018.2226.3001.4187)



## 十三、LWIP



### 13.1正点原子源码解析

学习视频：[嵌入式lwip网络协议栈深入详解（第一集）_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1rU4y1H7z1/?spm_id_from=333.337.search-card.all.click&vd_source=17dd572c908edff475f44fb6b145fc53)

代码参考正点原子 

![image-20251010222053125](./嵌入式软件学习.assets/image-20251010222053125.png)

![image-20251010222120915](./嵌入式软件学习.assets/image-20251010222120915.png)



![image-20251010222135521](./嵌入式软件学习.assets/image-20251010222135521.png)



![image-20251010222159700](./嵌入式软件学习.assets/image-20251010222159700.png)

![image-20251010222222674](./嵌入式软件学习.assets/image-20251010222222674.png)

![image-20251010222235768](./嵌入式软件学习.assets/image-20251010222235768.png)



#### 13.1.1MAC内核获取网络数据



[STM32开发笔记47：STM32F4+DP83848以太网通信指南系列（一）：知识储备_stm32f407的频率-CSDN博客](https://snmplink.blog.csdn.net/article/details/84845677)

![image-20251010225013942](./嵌入式软件学习.assets/image-20251010225013942.png)

![image-20251010224928229](./嵌入式软件学习.assets/image-20251010224928229.png)





- 在PHY芯片发送给MAC地址解析后，DMA完成后触发中断
- 

![image-20251010223052141](./嵌入式软件学习.assets/image-20251010223052141.png)



![image-20251010223016379](./嵌入式软件学习.assets/image-20251010223016379.png)

```c
void ETH_IRQHandler(void)
{
    while (ethernet_get_eth_rx_size(g_eth_handler.RxDesc)) //检测是否接受到数据
    {
        lwip_pkt_handle();      /* 处理以太网数据，即将数据提交给LWIP */
    }

    __HAL_ETH_DMA_CLEAR_IT(&g_eth_handler, ETH_DMA_IT_NIS);   /* 清除DMA中断标志位 */
    __HAL_ETH_DMA_CLEAR_IT(&g_eth_handler, ETH_DMA_IT_R);     /* 清除DMA接收中断标志位 */
}


//底层是调用 ethernetif_input函数
void lwip_pkt_handle(void)
{
    /* 从网络缓冲区中读取接收到的数据包并将其发送给LWIP处理 */
    ethernetif_input(&g_lwip_netif);
}

//往下调用low_level_input， ton过ETH_Get_Received_fRAME获取DMA数据，再将数据放到pbuf中给到LWIP
ethernetif_input(struct netif *netif)
{
    struct pbuf *p;

    /* move received packet into a new pbuf */
    p = low_level_input(netif);
    /* if no packet could be read, silently ignore this */
    if (p != NULL)
    {
        /* pass all packets to ethernet_input, which decides what packets it supports */
        if (netif->input(p, netif) != ERR_OK)
        {
            LWIP_DEBUGF(NETIF_DEBUG, ("ethernetif_input: IP input error\n"));
            pbuf_free(p);
            p = NULL;
        }
    }

} 
```



#### 13.1.2LWIP目录夹结构



- LWIP源码
- contrib - 应用实例

![image-20251103204454107](./嵌入式软件学习.assets/image-20251103204454107.png)





- CHANGELOG文件记录了LwIP在版本升级过程中源代码发生的变化。
- COPYING文件记录了LwIP这个开源软件的license。一个软件开源，不代表你能无限制地使用它，你需要在使用它的过程中遵守一定的规则，这些规则就是license。大家可以用记事本打开这个COPYING文件看看它的内容。开源软件的license有很多种，LwIP的属于BSD License。LwIP的开源程度是很高的，你几乎可以无限制地使用它。
- FILES文件用于介绍当前目录下的目录信息。
- README文件对LwIP进行了一个简单的介绍。
- UPGRADING文件记录了LwIP每个大版本的更新，会对用户使用和移植LwIP造成的影响。所谓大版本更新指的是：1.3.x - 1.4.x – 2.0.x – 2.1.x。小版本更新，比如2.0.1 – 2.0.2 – 2.0.3，这个过程只是一些bug的修复和性能的改善，不会对用户的使用造成影响。用户只要将原有工程的目录中与LwIP相关的旧版本文件替换成新版本的文件，重新编译，就能直接使用。
- test文件夹里面是测试LwIP内核性能的源码，将它们和LwIP源码加入到工程中一起编译，调用它们提供的函数，可以获得许多与LwIP内核性能有关的指标。这种内核性能测试功能，只有非常专业的人士才用的到。
- **src文件夹里面就是我们最关心的LwIP源码文件**

![image-20251103204611710](./嵌入式软件学习.assets/image-20251103204611710.png)



- api文件夹里面装的是NETCONN API和Socket API相关的源文件，只有在操作系统的环境中，才能被编译。
- apps文件夹里面装的是应用程序的源文件，包括常见的应用程序，如httpd、mqtt、tftp、sntp、snmp等。
- core文件夹里面是LwIP的内核源文件，后续会详细讲解。
- include文件夹里面是LwIP所有模块对应的头文件。
- netif文件夹里面是与网卡移植有关的文件，这些文件为我们移植网卡提供了模板，我们可以直接使用。

![image-20251103204945644](./嵌入式软件学习.assets/image-20251103204945644.png)





### 13.1.2野火视频教程





#### 1.网卡

通过网卡抽象硬件层，网卡实例可以是以太网、WIFI等



##### 1.1网卡初始化

![image-20251012194342183](./嵌入式软件学习.assets/image-20251012194342183.png)

- 该函数会初始化网卡结构体、绑定地址、回调函数 ethernetif_init---以太网初始化 
- netif_add()挂载网卡到网卡链表上![image-20251012192005010](./嵌入式软件学习.assets/image-20251012192005010.png)<img src="./嵌入式软件学习.assets/image-20251012192021383.png" alt="image-20251012192021383" style="zoom:50%;" />

```c
netif_add(&gnetif, &ipaddr, &netmask, &gw, NULL, &ethernetif_init, &tcpip_input);
```

![image-20251105200346139](./嵌入式软件学习.assets/image-20251105200346139.png)



###### ethernetif_init

ethernetif_init()函数是在上层管理网卡netif的到时候会被调用的函数，如使用netif_add()添加网卡的时候，就会调用ethernetif_init()函数对网卡进行初始化，其实该函数的最终调用的初始化函数就是low_level_init()函数，我们目前只有一个网卡，就暂时不用对该函数进行改写，直接使用即可，它内部会将网卡的name、output、linkoutput等字段进行初始化，这样子就能将内核与网卡无缝连接起来。



- 初始化网口结构体，绑定input、output、linkoutput API
- 调用low_level_init  根据实际情况对网卡进行一系列的初始化工作，例如：初始化MAC地址、长度， 设置最大传输包的大小，设置网卡的属性字段，支持广播、多播、ARP等功能， 如果是使用操作系统的话，还需要建立接收数据、发送数据的任务以及一些需要的消息队列、 信号量等





##### 网卡发送



- 初始化时将该API绑定在网卡上 ![image-20251105204502805](./嵌入式软件学习.assets/image-20251105204502805.png)
- ![image-20251105204737716](./嵌入式软件学习.assets/image-20251105204737716.png)上层调用ethernet_output函数，该函数最终还是会调用low_level_output，该函数主要是将传入的pbuff通过硬件发送出去
- 上层协议栈在调用时，会将Pbuff传入该函数，由该函数解析Pbuff并将数据发送到物理层

```c
 static err_t low_level_output(struct netif *netif, struct pbuf *p);
```



```c
static err_t low_level_output(struct netif *netif, struct pbuf *p)
{
	static sys_sem_t ousem = NULL;
	if(ousem == NULL)
  {
    sys_sem_new(&ousem,0);
    sys_sem_signal(&ousem);
  }
  err_t errval;
  struct pbuf *q;

  uint8_t *buffer = (uint8_t *)(heth.TxDesc->Buffer1Addr);
  __IO ETH_DMADescTypeDef *DmaTxDesc;
  uint32_t framelength = 0;
  uint32_t bufferoffset = 0;
  uint32_t byteslefttocopy = 0;
  uint32_t payloadoffset = 0;
  DmaTxDesc = heth.TxDesc;
  bufferoffset = 0;
  /* Check if the descriptor is owned by the ETHERNET DMA (when set) or CPU (when reset) */
  /* Is this buffer available? If not, goto error
  
  判断是否可以发送*/
  if((DmaTxDesc->Status & ETH_DMATXDESC_OWN) != (uint32_t)RESET)
  {
    errval = ERR_USE;
    goto error;
  }
  sys_sem_wait(&ousem);
    
    
    
    
  
  /* copy frame from pbufs to driver buffers
  
  将上层传输的PBUFF数据解析，放到缓冲区*/
  for(q = p; q != NULL; q = q->next)
    {
      /* Get bytes in current lwIP buffer */
      byteslefttocopy = q->len;
      payloadoffset = 0;
    
      /* Check if the length of data to copy is bigger than Tx buffer size*/
      while( (byteslefttocopy + bufferoffset) > ETH_TX_BUF_SIZE )
      {
        /* Copy data to Tx buffer*/
        memcpy( (uint8_t*)((uint8_t*)buffer + bufferoffset), (uint8_t*)((uint8_t*)q->payload + payloadoffset), (ETH_TX_BUF_SIZE - bufferoffset) );
      
        /* Point to next descriptor */
        DmaTxDesc = (ETH_DMADescTypeDef *)(DmaTxDesc->Buffer2NextDescAddr);
      
        /* Check if the buffer is available */
        if((DmaTxDesc->Status & ETH_DMATXDESC_OWN) != (uint32_t)RESET)
        {
          errval = ERR_USE;
          goto error;
        }
      
        buffer = (uint8_t *)(DmaTxDesc->Buffer1Addr);
      
        byteslefttocopy = byteslefttocopy - (ETH_TX_BUF_SIZE - bufferoffset);
        payloadoffset = payloadoffset + (ETH_TX_BUF_SIZE - bufferoffset);
        framelength = framelength + (ETH_TX_BUF_SIZE - bufferoffset);
        bufferoffset = 0;
      }
    
      /* Copy the remaining bytes */
      memcpy( (uint8_t*)((uint8_t*)buffer + bufferoffset), (uint8_t*)((uint8_t*)q->payload + payloadoffset), byteslefttocopy );
      bufferoffset = bufferoffset + byteslefttocopy;
      framelength = framelength + byteslefttocopy;
    }
  
  /* Prepare transmit descriptors to give to DMA   调用底层接口，将数据发送出去*/ 
  HAL_ETH_TransmitFrame(&heth, framelength);
  
  errval = ERR_OK;

error:
  
  /* When Transmit Underflow flag is set, clear it and issue a Transmit Poll Demand to resume transmission */
  if ((heth.Instance->DMASR & ETH_DMASR_TUS) != (uint32_t)RESET)
  {
    /* Clear TUS ETHERNET DMA flag */
    heth.Instance->DMASR = ETH_DMASR_TUS;

    /* Resume DMA transmission*/
    heth.Instance->DMATPDR = 0;
  }
  
  sys_sem_signal(&ousem);
  
  return errval;
}
```









##### 网口接受

![image-20251012202152015](./嵌入式软件学习.assets/image-20251012202152015.png)

- low_level_init初始化时，启动任务ethernetif_input<img src="./嵌入式软件学习.assets/image-20251012200002484.png" alt="image-20251012200002484" style="zoom:50%;" />

- ethernetif_input函数循环监听信号量是否被释放，该信号量释放由ETH中断接受到数据后释放。信号量释放后，调用low_level_input获取数据并组包为Pbuff形式。并将PBUFF传送给上层

- ```c
  void ethernetif_input(void *pParams) {
  	struct netif *netif;
  	struct pbuf *p = NULL;
  	netif = (struct netif*) pParams;
    LWIP_DEBUGF(NETIF_DEBUG, ("ethernetif_input: IP input error\n"));
    
  	while(1) 
    {
          //循环等待信号量，该信号量在ETH以太网接受中断中释放，表示有数据到了
      if(xSemaphoreTake( s_xSemaphore, portMAX_DELAY ) == pdTRUE)
      {
        /* move received packet into a new pbuf */
        taskENTER_CRITICAL();
  TRY_GET_NEXT_FRAGMENT:
        p = low_level_input(netif); //low_level_input获取网卡中的数据并组包为PBUFF
        taskEXIT_CRITICAL();
        /* points to packet payload, which starts with an Ethernet header */
        if(p != NULL)
        {
          taskENTER_CRITICAL();
          /* full packet send to tcpip_thread to process */
          if (netif->input(p, netif) != ERR_OK) //将PBUFF传递给网络层，该函数在初始化网卡时被绑定
          {
            LWIP_DEBUGF(NETIF_DEBUG, ("ethernetif_input: IP input error\n"));
            pbuf_free(p);
            p = NULL;
          }
          else
          {
            xSemaphoreTake( s_xSemaphore, 0);
            goto TRY_GET_NEXT_FRAGMENT;
          }
          taskEXIT_CRITICAL();
        }
      }
  	}
  }
  ```

  











```c
static struct pbuf * low_level_input(struct netif *netif)
{
  struct pbuf *p = NULL;
  struct pbuf *q = NULL;
  uint16_t len = 0;
  uint8_t *buffer;
  __IO ETH_DMADescTypeDef *dmarxdesc;
  uint32_t bufferoffset = 0;
  uint32_t payloadoffset = 0;
  uint32_t byteslefttocopy = 0;
  uint32_t i=0;
  
  
  /* get received frame  接受到MAC层的数据*/
  if (HAL_ETH_GetReceivedFrame(&heth) != HAL_OK)
  {
//    PRINT_ERR("receive frame faild\n");
    return NULL;
  }
  /* Obtain the size of the packet and put it into the "len" variable. */
  len = heth.RxFrameInfos.length; //获取数据长度
  buffer = (uint8_t *)heth.RxFrameInfos.buffer; //获取数据
  
  PRINT_INFO("receive frame len : %d\n", len);
  
  if (len > 0)
  {
    /* We allocate a pbuf chain of pbufs from the Lwip buffer pool */
    p = pbuf_alloc(PBUF_RAW, len, PBUF_POOL);  //申请Pbuff空间
  }
  
  if (p != NULL) //申请成功 ，下面构建PBUFF数据结构，将数据放入有效载荷中
  {
    dmarxdesc = heth.RxFrameInfos.FSRxDesc;
    bufferoffset = 0;
    for(q = p; q != NULL; q = q->next)
    {
      byteslefttocopy = q->len;
      payloadoffset = 0;
      
      /* Check if the length of bytes to copy in current pbuf is bigger than Rx buffer size*/
      while( (byteslefttocopy + bufferoffset) > ETH_RX_BUF_SIZE )
      {
        /* Copy data to pbuf */
        memcpy( (uint8_t*)((uint8_t*)q->payload + payloadoffset), (uint8_t*)((uint8_t*)buffer + bufferoffset), (ETH_RX_BUF_SIZE - bufferoffset));
        
        /* Point to next descriptor */
        dmarxdesc = (ETH_DMADescTypeDef *)(dmarxdesc->Buffer2NextDescAddr);
        buffer = (uint8_t *)(dmarxdesc->Buffer1Addr);
        
        byteslefttocopy = byteslefttocopy - (ETH_RX_BUF_SIZE - bufferoffset);
        payloadoffset = payloadoffset + (ETH_RX_BUF_SIZE - bufferoffset);
        bufferoffset = 0;
      }
      /* Copy remaining data in pbuf */
      memcpy( (uint8_t*)((uint8_t*)q->payload + payloadoffset), (uint8_t*)((uint8_t*)buffer + bufferoffset), byteslefttocopy);
      bufferoffset = bufferoffset + byteslefttocopy;
    }
  }  
    
    
  //以太网配置
    
    /* Release descriptors to DMA */
    /* Point to first descriptor */
    dmarxdesc = heth.RxFrameInfos.FSRxDesc;
    /* Set Own bit in Rx descriptors: gives the buffers back to DMA */
    for (i=0; i< heth.RxFrameInfos.SegCount; i++)
    {  
      dmarxdesc->Status |= ETH_DMARXDESC_OWN;
      dmarxdesc = (ETH_DMADescTypeDef *)(dmarxdesc->Buffer2NextDescAddr);
    }
    
    /* Clear Segment_Count */
    heth.RxFrameInfos.SegCount =0;  
  
  /* When Rx Buffer unavailable flag is set: clear it and resume reception */
  if ((heth.Instance->DMASR & ETH_DMASR_RBUS) != (uint32_t)RESET)  
  {
    /* Clear RBUS ETHERNET DMA flag */
    heth.Instance->DMASR = ETH_DMASR_RBUS;
    /* Resume DMA reception */
    heth.Instance->DMARPDR = 0;
  }
    
    
    
//返回Pbuff    
  return p;
}
```







以太网模块释放信号量

```c
extern xSemaphoreHandle s_xSemaphore;
void HAL_ETH_RxCpltCallback(ETH_HandleTypeDef *heth)
{
  LED2_TOGGLE;
  portBASE_TYPE xHigherPriorityTaskWoken = pdFALSE;
  xSemaphoreGiveFromISR( s_xSemaphore, &xHigherPriorityTaskWoken );
  portYIELD_FROM_ISR(xHigherPriorityTaskWoken);
}
```









#### 2.Pbuff

- LWIP在各层之间没有完全隔离，各层之间数据相当于明文，这样可以实现数据零拷贝，提高效率
- RAW模式：协议栈运行使用回调函数实现与用户数据的交互；
- NETCONN API和Socket API模式：用户程序、协议栈分别为不同线程，使用系统的信号量和邮箱等IPC通信机制联系起来，进行数据的交互



##### 数据结构

```c
 struct pbuf
 {
     /*next是一个pbuf类型的指针，指向下一个pbuf， 因为网络中的数据包可能很大，而pbuf能管理的数据包大小有限， 就会采用链表的形式将所有的pbuf包连接起来，这样子才能完整描述一个数据包， 这些连接起来的pbuf包会组成一个链表，我称之为pbuf链表。*/
     struct pbuf *next;    //下一个Pbuf           
     
     

     /*payload是一个指向数据区域的指针，指向该pbuf管理的数据区域起始地址， 这里的数据区域可以是紧跟在pbuf结构体地址后面的RAM空间，也可以是ROM中的某个地址上，取决于pbuf的类型 */
     void *payload;   //指向数据区域                                 


     
     
     /*tot_len中记录的是当前pbuf及其后续pbuf所有数据的长度， 例如如果当前pbuf是pbuf链表上第一个数据结构，那么tot_len就记录着整个pbuf链表中所有pbuf中数据的长度； 如果当前pbuf是链表上最后一个数据结构，那就记录着当前pbuf的长度。*/
     u16_t tot_len;  //后面数据总长度                             

     
     
     
     /*len表示当前pbuf中有效的数据长度*/
     u16_t len; //当前Pbuf数据长度                                   

     
     
     
     u8_t type_internal; //type_internal表示pbuf的类型, PBUF_RAM、PBUF_POOL 、PBUF_ROM、PBUF_REF ;表示pubuf所在的区域
     
     
     
     
     

     /** misc flags */
     u8_t flags;                                     (6)

     LWIP_PBUF_REF_T ref;  //ref表示该pbuf被引用的次数，引用表示有其他指针指向当前pbuf ,防止数据被同时使用修改 初始化时设置为1，为0时才允许删除

     /** if_idx用于记录传入的数据包中输入netif的索引，也就是netif中num字段*/
     u8_t if_idx;                                    (8)
 };
```



##### PBUF类型



###### 1.PBUF_RAM

- 整个pbuf就是一个连续的内存区域，**layer（offset）就是各层协议的首部**， 如TCP报文首部、IP首部、以太网帧首部等，预留出来的这些空间是为了在各个协议层中灵活地处理这些数据
- PBUF_RAM类型的pbuf空间是通过**内存堆**分配而来



```c
pbuf_alloc(pbuf_layer layer, u16_t length, pbuf_type type);  //协议帧layer类型 ， 数据长度 ， PBUF_RAM类型

 p = pbuf_alloc(PBUF_RAW, (u16_t)(req_len + 1), PBUF_RAM);

 p = pbuf_alloc(PBUF_TRANSPORT, 1472, PBUF_RAM);
```

<img src="./嵌入式软件学习.assets/image-20251014214203228.png" alt="image-20251014214203228" style="zoom:80%;" />





###### 2.PBUF_POOL

- PBUF_POOL类型的pbuf与PBUF_RAM类型的pbuf都是差不多的，其pbuf结构体与数据缓冲区也是存在于连续的内存块中
- 空间是通过内存池分配的，这种类型的pbuf可以在极短的时间内分配得到，因为这是内存池分配策略的优势
- 网卡接收数据的时候，LwIP一般就使用这种类型的pbuf来存储接收到的数据
- pbuf链表中**第一个pbuf是有layer字段**的，用于存放协议头部，而在它后面的pbuf则是没有该字段
- PBUF_POOL类型**pbuf都是以固定长度分配**的，在最后一个pbuf中，可能会被浪费大量的空间





![image-20251014215256729](./嵌入式软件学习.assets/image-20251014215256729.png)



###### 3.PBUF_ROM和PBUF_REF



- PBUF_ROM和PBUF_REF：内存池申请的pbuf不包含数据区域，只包含pbuf结构体。
- PBUF_ROM只申请结构体区，指向FALSH中的常量数据
- PBUF_REF只申请结构体区，指向RAM数据



![image-20251014215635619](./嵌入式软件学习.assets/image-20251014215635619.png)







###### 4.多种PBUF组合

![image-20251014215932852](./嵌入式软件学习.assets/image-20251014215932852.png)





#### 3.有操作系统移植LWIP

<img src="./嵌入式软件学习.assets/image-20251108182659431.png" alt="image-20251108182659431" style="zoom:50%;" />



##### 1.工程中添加FREERTOS



##### 2.lwipopts.h配置

该文件用于LWIP的相关配置





##### 3.sys_arch.c/h文件 操作系统接口文件

针对各种操作系统都有其特定的接口文件，直接替换就行

![image-20251108183546089](./嵌入式软件学习.assets/image-20251108183546089.png)



![image-20251108185250720](./嵌入式软件学习.assets/image-20251108185250720.png)



##### 4.网卡编写



- 在网卡初始化的时候，将接受函数创建为一个独立的线程，当以太网中断触发后释放线程的阻塞获取以太网的数据。



![image-20251108185148165](./嵌入式软件学习.assets/image-20251108185148165.png)

![image-20251108184353699](./嵌入式软件学习.assets/image-20251108184353699.png)

![image-20251108184341411](./嵌入式软件学习.assets/image-20251108184341411.png)



##### 5.协议栈初始化

![image-20251108185500040](./嵌入式软件学习.assets/image-20251108185500040.png)



## 十四、EIP

[EtherNET/IP协议基础知识（上）_虹科工业智能互联-2048 AI社区](https://2048ai.net/6825acaca5baf817cf4c5a44.html?spm=1001.2101.3001.6650.18&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-18-108398509-blog-145794938.235^v43^pc_blog_bottom_relevance_base8&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~activity-18-108398509-blog-145794938.235^v43^pc_blog_bottom_relevance_base8)





ODVA组织

![image-20260416195020085](./嵌入式软件学习.assets/image-20260416195020085.png)



**EIP = CIP+标准TCP/IP协议栈**

- 端口号-818
- 应用层采用CIP

![image-20260416195338061](./嵌入式软件学习.assets/image-20260416195338061.png)

![image-20260416200106004](./嵌入式软件学习.assets/image-20260416200106004.png)









![image-20260416200143196](./嵌入式软件学习.assets/image-20260416200143196.png)

![image-20260416200257465](./嵌入式软件学习.assets/image-20260416200257465.png)

EtherNet/IP的以太网包与其中的封装部分结构如上图所示，其中封装部分各细分部分功能如下：

1. Command：代表该包的功能和作用；
2. Length：指示后方数据域的长度；
3. Session Handle：会话句柄，表示会话建立或响应的请求；
4. Status：指示改包的命令有无正确执行，可比对状态码表进行确认；
5. Sender Context：包含描述发送者信息的内容；
6. Options：可选设置；
7. Command-specific Data：根据命令的不同有所区别

![image-20260416200930488](./嵌入式软件学习.assets/image-20260416200930488.png)

![image-20260416201025458](./嵌入式软件学习.assets/image-20260416201025458.png)



![image-20260416201349005](./嵌入式软件学习.assets/image-20260416201349005.png)





## 二十、软件使用



### 1.KEIL

#### 10.1基本使用方法



##### 10.1.1中间文件清除

在keil文件编译后，会产生大量中间文件，使用脚本清除所产生的中间文件



##### 10.1.2仿真

##### 调试

![image-20240711220429781](./嵌入式软件学习.assets/image-20240711220429781.png)从左往右依次是：单步运行、跳过当前单步调试、跳过当前函数单步运行、跳到光标位置

![image-20240711220825973](./嵌入式软件学习.assets/image-20240711220825973.png)

- ![image-20240711220837901](./嵌入式软件学习.assets/image-20240711220837901.png)命令窗口，打开关闭命令窗口
- ![image-20240711220921471](./嵌入式软件学习.assets/image-20240711220921471.png)反汇编窗口，显示当前运行程序所对应的汇编语言
- ![image-20240711221020073](./嵌入式软件学习.assets/image-20240711221020073.png)符号窗口，可以实时查看程序文件下所有变量的值







##### 10.1.3书签

![image-20240804174827305](./嵌入式软件学习.assets/image-20240804174827305.png)

- ![image-20240804174845745](./嵌入式软件学习.assets/image-20240804174845745.png)标记某一行代码，可以同时标记多行
- ![image-20240804174910585](./嵌入式软件学习.assets/image-20240804174910585.png)在标记的代码中进行切换，快速浏览
- ![image-20240804174936328](./嵌入式软件学习.assets/image-20240804174936328.png)清除所有书签





### 10.2keil仿真示波器功能

[【STM32】【调试】【示波器】【逻辑分析仪】如何使用Keil5输出波形-CSDN博客](https://blog.csdn.net/weixin_51089092/article/details/122924739?spm=1001.2101.3001.6650.3&utm_medium=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~Ctr-3-122924739-blog-136063680.235^v43^pc_blog_bottom_relevance_base8&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~baidujs_baidulandingword~Ctr-3-122924739-blog-136063680.235^v43^pc_blog_bottom_relevance_base8)



##### 1.软件仿真初始化配置

![image-20240811171637381](./嵌入式软件学习.assets/image-20240811171637381.png)

- **Load Application at Startup：**启动时加载应用程序。

- **Run to main()：**程序执行到main()函数，进入调试模式时，程序自动运行到main函数处。

- **Initialization File：**加载、编辑初始化文件。

- ![image-20240811172212321](./嵌入式软件学习.assets/image-20240811172212321.png)

- ![image-20240811172336242](./嵌入式软件学习.assets/image-20240811172336242.png)配置环境变量需要根据仿真芯片进行设置，此时仿真为F103CB

- **第一项：*****\*SARMCM3.DLL\****

  ​         **-REMAP**

  **第二项：****DARMSTM.DLL**

  ​        **-pSTM32F103ZE**  **根据芯片选择例如STM32F103ZE、STM32F103C8**  



##### 2.设置外部输出晶振频率

此处设置晶振频率，只用于软件仿真

![image-20240811172936250](./嵌入式软件学习.assets/image-20240811172936250.png)



##### 3.使用在线仿真

1. ![image-20240811174025545](./嵌入式软件学习.assets/image-20240811174025545.png)选择逻辑分析仪功能
2. 创建监控变量：***\*PORTx.n     x为端口号 n为引脚号\****例如：GPIOB_ODR.5  == PB5
3. <img src="./嵌入式软件学习.assets/image-20240811174224241.png" alt="image-20240811174224241" style="zoom:67%;" />设置为监控BIT
4. 运行程序<img src="./嵌入式软件学习.assets/image-20240811174254636.png" alt="image-20240811174254636" style="zoom:67%;" />





### 10.3KEIL具体配置界面

[keil工程目标选项配置：Options for Target_keil options for target-CSDN博客](https://blog.csdn.net/qq_37863513/article/details/130457152?spm=1001.2014.3001.5502) 



## 

# 



