# 1.电梯代码总结



## 1.1代码架构



### 1.1.1头文件包含





分为物理层、逻辑层、对象层三层代码分层；每一层通过共同调佣公共文件夹进行交互

**结构体、联合体**：结构体**原型**在公共文件夹进行声明（多个文件可共用），需要使用的源文件中进行**变量定义**，变量在公共文件夹声明

![image-20240824120900567](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824120900567.png)

![image-20240824120920676](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824120920676.png)

**变量**：

![image-20240824115901839](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824115901839.png)



### 1.2.1代码结构分层

电梯代码分为：应用层、hdf层、bsp层、库函数驱动层

- 应用层：产品业务逻辑代码
- hdf层：对设备与外设与设备进行抽象，按类进行分类，与应用层绑定
- bsp层：用于hdf层与库函数进行连接，便于上层软件在不同的芯片上进行移植，只需重新配置bsp层，建立hdf与底层物理层的联系
- 驱动层：底层库函数驱动

















## 1.2交互



### 1.2.1同一工程下功能性能交互

性能负责底层控制算法编写，功能负责上层电梯逻辑运用。共用同一工程，性能编译.O文件后交给功能负责统一合并程序。

功性能之间使用RAM数组进行交互

![image-20240824121636616](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824121636616.png)





## 1.3调试代码



### 1.3.1通过MAP地址查看变量的值

功能码1输入变量内存地址，另一个功能码2输入该地址对应变量的值

![image-20240824122208636](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824122208636.png)

### 1.3.1功能码监控代码

#### 个位百位显示

![image-20240824144241018](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824144241018.png)

#### 不同BIT位显示













## 1.4编程习惯



### 1.4.1结构体与联合体配合



#### STM32库函数编程

```c
#define ADC1                ((ADC_TypeDef *) ADC1_BASE) //将外设地址转换为结构体地址
typedef struct		//外设地址为结构体首地址，也是元素第一个地址；每个元素32位对应每个寄存器向后偏移
{
  __IO uint32_t SR;
  __IO uint32_t CR1;
  __IO uint32_t CR2;
  __IO uint32_t SMPR1;
  __IO uint32_t SMPR2;
  __IO uint32_t JOFR1;
  __IO uint32_t JOFR2;
  __IO uint32_t JOFR3;
  __IO uint32_t JOFR4;
  __IO uint32_t HTR;
  __IO uint32_t LTR;
  __IO uint32_t SQR1;
  __IO uint32_t SQR2;
  __IO uint32_t SQR3;
  __IO uint32_t JSQR;
  __IO uint32_t JDR1;
  __IO uint32_t JDR2;
  __IO uint32_t JDR3;
  __IO uint32_t JDR4;
  __IO uint32_t DR;
} ADC_TypeDef;
```



#### 电梯编程

联合体套结构体和变量，可以实现**变量的拆包和组包**



##### 8位用于传输数据拆包与组包

![image-20240824124051192](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824124051192.png)

![image-20240824124101379](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824124101379.png)

##### 16位-常用于标志位

单个BIT位作为标志位，节约内存空间

![image-20240824124229776](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240824124229776.png)



##### 功能码遍历

联合体中的元素是变量、数组、可以t通过数组对变量进行遍历













## 1.5功能码系统

人机接口，通过对功能码的配置，对运行参数或者指令进行选择。常采用BIT位调试、数值、特殊数值（密码）

![image-20240831160531355](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240831160531355.png)



### 1.5.1功能码存储



#### 1.存入FLASH

初始化阶段，将flash中的数据复制到内存SRAM功能码联合体中，在程序运行过程中将SRAM中的功能码值再重新存入FLASH中



#### 2.存入E2PROM

1. 在主板中功能码分为：立即存储、断电存储（在即将断电前会触发存储，目的是：频繁写入的参数例如运行次数，频繁操作会导致部分存储区域失效，所以后来升级：1.负载均衡代码  2.写入参数断电存储）
2. 掉电判断：在**反激电源后M24v判断掉电**，因为电网波动不稳定、母线会根据负载大小呈现不同的跌落速度、**M24V比较稳定，当跌落到22V判断要掉电了**



### 1.5.2功能码实现



#### 1.功能码定义

```c
#define F0UNM 2 //定义每组功能码最大值
#define F1UNM 2 //定义每组功能码最大值

typedef union FUNCCODE_ALL_UINON //定义功能码联合体
{
    unsigned short func [F0UNM + F1UNM ] // 通过这个数组可以便利所有功能码
        
    struct{
        unsigned short f0[F0UNM] ; //这样通过组访问便利每组所有功能码
        unsigned short f1[F1UNM] ;
    }group;
    
    struct{
        //F1
         unsigned short function1 ;
         unsigned short function2 ;   
        //F2
         unsigned short function3 ;
         unsigned short function4 ;            
    }code; //单独访问每个功能码
          
}

```



#### 2.功能码索引



```c
//获取当前参数地址

//将功能码传入，因为是功能码在内存中是顺序排列，使用功能码相对首地址偏移获取 功能码索引
#define GET_CODE_INDEX(code)     (  (unsigned short)( (&(code)) - (&(function.func[0]))  )

//传入功能码的 (组) 与 (组内偏移) 获取具体功能码的地址,组地址+偏移地址
#define GET_GREAD_INDEX(group,grade)      ( us_table_funcGroupAdrr[group] + (grade) )

const unsigned short us_table_funcGroupAdrr[ ] = { //各个功能码组首地址数组，便利次获取各组首地址索引
     GET_CODE_INDEX(function.group.f0[0]);
     GET_CODE_INDEX(function.group.f1[0])  
}

```



#### 3.功能码属性 - （待写）

通过Table表配置各个功能码属性

```

```



#### 4.功能码使用

```c
//常规引用
function.code.function1 = xxx ;   //精确使用具体功能码
function.groude.f1[xx] = xxx ; //遍历索引

//获取索引
GET_CODE_INDEX(function.groude.f1[0])   //获取具体功能码索引
 GET_GREAD_INDEX(1,1)  // 获取f1-1索引，常用于工装协议读取功能码等  
```





#### 5.功能码存储



##### 5.1智能电躯ARD处理

![image-20240831161411334](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20240831161411334.png)









# 2.产品





## 2.1ARD





### 2.2.1智能电躯



#### 1.充电

###### 小电流解锁电池

- 在电池过放后，电池本身保护板会自动保护，不对外进行放电。需要使用小电流浮充再次唤醒过放的电池。
- 原来充电的逻辑是：功能进入BUCK-充电，将此命令下发给性能，性能控制BUCK开始调压（以电池电压为目标值）。功能实时检测BUCK电压与电池端电压，当两端电压压差小于2.5V，闭合K8。性能此时以49V为目标电压（无缓启），以49V进行充电。调压差是防止充电时有冲击，以0V直接升到49V；当调压差后，以电池电压跳到49V
- 当检查到电池电压小于20V，报电池未接故障。当系统外部条件满足充电时，在有电池未接故障时尝试对电池进行一次激活。进入解锁状态，性能识别到此时为解锁状态，功能端同时下发充电指令和激活状态指令，等待100ms后下发K8闭合指令，性能接收到后缓启将电压台升到37V左右（缓启目的是，之前因为电池端电压为零，未进行调压当K8闭合指令下发，直接将目标电压定为49V，产生大概60的冲击电压。），电流为0.2A进行解锁。  功能持续此状态700ms,判断此时电流大小（小于0.5A电池未接，大于0.5A电池激活成功），之前考虑使用电池未接进行判断是否激活成功，电池未接时，闭合K8电池端电压直接和BUCK电压一样，此时电压大于20V电池未接故障消失。功能认为电池激活成功开始充电，所以使用真实物理量来判断是否激活成功。









